{
  "name": "dogfood-mobile-real-cli-app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Dogfood app v1.54-2 — Mobile v0.5 child_process.spawn stub workflow backed by @kiwa-lab/mobile v0.5。 6 CLI stub (expo build / metro bundle / codegen run / react-native start / pod install / gradle build) 呼出契約 + env-gate 3 pattern + fail-closed 検証 dogfood、 pair 深度 5 段拡張 1 例目 candidate。",
  "scripts": {
    "test": "node ../../scripts/build-deps.mjs @kiwa-lab/core @kiwa-lab/mobile && node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "typecheck": "tsc --noEmit",
    "test:perf": "vitest run -c vitest.perf.config.ts --reporter=verbose"
  },
  "dependencies": {
    "@kiwa-lab/core": "workspace:*",
    "@kiwa-lab/mobile": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.6"
  }
}
