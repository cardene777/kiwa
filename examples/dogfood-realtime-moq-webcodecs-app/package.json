{
  "name": "dogfood-realtime-moq-webcodecs-app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Dogfood app v1.45-2 — MoQ + WebCodecs encoder + Simulcast/SVC observability harness backed by @kiwa-lab/realtime v0.3 moq-fetch + webcodecs-encoder + simulcast-svc semantics.",
  "scripts": {
    "test": "pnpm -F @kiwa-lab/core -F @kiwa-lab/realtime build && node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kiwa-lab/core": "workspace:*",
    "@kiwa-lab/realtime": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
