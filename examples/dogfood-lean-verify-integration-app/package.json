{
  "name": "dogfood-lean-verify-integration-app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Dogfood app v2.15-2 — @kiwa-lab/lean v0.2 verifyLeanSpec 統合 dogfood。 depth-5 pattern 5 lifecycle-orchestrator の Lean spec を verifyLeanSpec 経由で lean --check 経路 or skip 経路の 2 mode 実験、 kiwa の runtime testing + 静的形式検証 2 軸融合の第 2 段。",
  "scripts": {
    "test": "node ../../scripts/build-deps.mjs @kiwa-lab/core @kiwa-lab/lean && node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "typecheck": "tsc --noEmit",
    "test:perf": "vitest run -c vitest.perf.config.ts --reporter=verbose"
  },
  "dependencies": {
    "@kiwa-lab/core": "workspace:*",
    "@kiwa-lab/lean": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.6"
  }
}
