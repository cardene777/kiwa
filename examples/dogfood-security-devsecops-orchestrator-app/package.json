{
  "name": "dogfood-security-devsecops-orchestrator-app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Dogfood app v1.48-2 — runSecurityAudit single entry + 4 preset (audit-all / supply-chain / specialty / threat-model) + summary workflow backed by @kiwa/security-devsecops v0.3 orchestrator。 skill 4 種を library single entry で置換する reference。",
  "scripts": {
    "test": "pnpm -F @kiwa/core -F @kiwa/security-devsecops build && node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kiwa/core": "workspace:*",
    "@kiwa/security-devsecops": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
