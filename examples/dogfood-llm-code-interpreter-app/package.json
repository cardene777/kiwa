{
  "name": "dogfood-llm-code-interpreter-app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Dogfood app v1.40-3 — a sandboxed code interpreter + Python REPL + tool use + rollback state-machine service that drives @kiwa-lab/ai-llm v0.5 code-interpreter axis (sandbox lifecycle + execution history + tool call ledger + memory snapshot rollback) backed by both KIWA_MODE=real (Vercel AI SDK + Anthropic Messages API when ANTHROPIC_API_KEY + KIWA_LLM_BUDGET_USD are set) and KIWA_MODE=mock (in-memory sandbox + deterministic execution + memory snapshot stack) so behavioural fidelity feeds @kiwa-lab/quality-metrics 11-axis release gate.",
  "scripts": {
    "test": "node ../../scripts/build-deps.mjs @kiwa-lab/ai-llm @kiwa-lab/core @kiwa-lab/quality-metrics && vitest run tests --exclude '**/.vitest-dist/**' --environment node",
    "typecheck": "tsc --noEmit",
    "test:perf": "vitest run -c vitest.perf.config.ts --reporter=verbose"
  },
  "dependencies": {
    "@kiwa-lab/ai-llm": "workspace:*",
    "@kiwa-lab/core": "workspace:*",
    "@kiwa-lab/quality-metrics": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.6"
  }
}
