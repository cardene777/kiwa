{
  "name": "dogfood-oauth21-provider",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Dogfood app v1.21-3 — a Hono + Cloudflare Workers OAuth 2.1 Authorization Server (AS) that exercises the RFC 9700 endpoint surface (/authorize, /token, /revoke, /introspect, /.well-known/openid-configuration) with PKCE always mandatory, DPoP-bound tokens, refresh token rotation and revocation cascade. Drivable in both KIWA_MODE=real (spawns oauth2-mock-server through testcontainers, env-skip via KIWA_OAUTH21_ENV_MISSING) and KIWA_MODE=mock (@kiwa-test/auth setupOAuth21Env). Behavioural fidelity between the two drivers feeds the release gate.",
  "scripts": {
    "test": "pnpm -F @kiwa-test/auth -F @kiwa-test/core build && node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kiwa-test/auth": "workspace:*",
    "@kiwa-test/core": "workspace:*",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
