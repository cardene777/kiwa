{
  "name": "dogfood-storybook-design-system",
  "version": "0.0.2",
  "private": true,
  "type": "module",
  "description": "Dogfood app (v1.16-2) — a React design-system Storybook 8 harness that registers 12 components (Button / Input / Card / Modal / Dropdown / Tabs / Toast / Table / Tooltip / Badge / Avatar / Icon) as StoryObj (CSF3) + play function + a11y parameters, drivable in both KIWA_MODE=real (spawns @storybook/react preview with a real story loader via env-skip when STORYBOOK_URL is not set) and KIWA_MODE=mock (@kiwa-test/component createStoryRegistry + play + a11y). Behavioural fidelity feeds @kiwa-test/quality-metrics 7-axis release gate.",
  "scripts": {
    "test": "pnpm -F @kiwa-test/component -F @kiwa-test/core -F @kiwa-test/quality-metrics build && node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "pretest:perf": "pnpm -F @kiwa-test/component -F @kiwa-test/core -F @kiwa-test/quality-metrics -F @kiwa-test/perf-harness build",
    "test:perf": "vitest run -c vitest.perf.config.ts --reporter=verbose",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kiwa-test/component": "workspace:*",
    "@kiwa-test/core": "workspace:*",
    "@kiwa-test/quality-metrics": "workspace:*"
  },
  "devDependencies": {
    "@kiwa-test/perf-harness": "workspace:*",
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
