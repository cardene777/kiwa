{
  "name": "dogfood-desktop-native-invoke-app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Dogfood app v1.64-2 — Desktop v0.9 実 native binding 呼出 workflow backed by @kiwa/desktop v0.9。 probeAndInvoke 統合経路で probe availability 判定後の 実 spawn 呼出、 4 InvokeStatus (invoked / cli-unavailable / axis-skipped / no-cli-mapping) 全経路の pattern reference。 v1.62 real behavior + v1.63 probe + v1.64 実 invoke の 3 layer separation 完全 pay off dogfood。",
  "scripts": {
    "test": "pnpm -F @kiwa/core -F @kiwa/desktop build && node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kiwa/core": "workspace:*",
    "@kiwa/desktop": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
