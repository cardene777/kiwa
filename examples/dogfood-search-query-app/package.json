{
  "name": "dogfood-search-query-app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Dogfood app v2.6-2 — @kiwa/search v2.1 query-orchestrator 4 pattern workflow reference。 depth-5 pattern 7 例目発生 dogfood。",
  "scripts": {
    "test": "pnpm -F @kiwa/core -F @kiwa/search build && node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kiwa/core": "workspace:*",
    "@kiwa/search": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
