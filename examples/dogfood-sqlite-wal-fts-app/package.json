{
  "name": "dogfood-sqlite-wal-fts-app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Dogfood app (v1.32-4) — a SQLite WAL + FTS5 full-text search notebook backed by Bun-style edge deployment semantics driven by @kiwa-test/orm v0.10 in KIWA_MODE=mock and libsql/turso edge driver in KIWA_MODE=real (env-skip SQLITE_KEY). Behavioural fidelity feeds @kiwa-test/quality-metrics 13-axis release gate + Playwright e2e for wal-checkpoint / fts5-query / edge-latency / testcontainers-probe flows.",
  "scripts": {
    "test": "pnpm -F @kiwa-test/orm -F @kiwa-test/core -F @kiwa-test/quality-metrics build && node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "test:e2e": "playwright test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kiwa-test/core": "workspace:*",
    "@kiwa-test/orm": "workspace:*",
    "@kiwa-test/quality-metrics": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
