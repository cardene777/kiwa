{
  "name": "dogfood-cloudflare-workers-durable-object-app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Dogfood app v1.24-2 — a Cloudflare Workers Durable Object realtime chat room app that exercises the durable-object axis (createDurableObject / requestDurableObject / fireAlarm / writeStorage) + websocket-edge axis (upgrade-requested / accepted / message / closed) + Hibernation API + storage transactional + 24-h alarm-based purge. Drivable in both KIWA_MODE=real (Miniflare + wrangler dev + WRANGLER_KEY env-gate) and KIWA_MODE=mock (@kiwa-lab/edge v0.2 8 axis semantics) so behavioural fidelity feeds the release gate 7 axis.",
  "scripts": {
    "test": "node ../../scripts/build-deps.mjs @kiwa-lab/edge @kiwa-lab/core @kiwa-lab/quality-metrics && vitest run tests --exclude '**/.vitest-dist/**' --environment node",
    "typecheck": "tsc --noEmit",
    "test:perf": "vitest run -c vitest.perf.config.ts --reporter=verbose"
  },
  "dependencies": {
    "@kiwa-lab/core": "workspace:*",
    "@kiwa-lab/edge": "workspace:*",
    "@kiwa-lab/quality-metrics": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.6"
  }
}
