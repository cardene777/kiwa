{
  "name": "dogfood-security-csp-headers-app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Dogfood app v1.37-2 — a Next.js 15.4 + React 19.1 App Router app that drives CSP nonce + hash + strict-dynamic + trusted-types + report-only + advanced security headers (HSTS + X-Frame-Options + X-Content-Type-Options + Referrer-Policy + Permissions-Policy) across a provider-neutral SecurityAdapter surface backed by @kiwa-lab/security v0.1 csp + security-headers semantics, runnable in both KIWA_MODE=real (Playwright + Chromium headless when CSP_BROWSER_READY=1) and KIWA_MODE=mock (in-memory violation harness) so behavioural fidelity feeds @kiwa-lab/quality-metrics.",
  "scripts": {
    "test": "node ../../scripts/build-deps.mjs @kiwa-lab/core @kiwa-lab/security @kiwa-lab/quality-metrics && node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node --hookTimeout 30000",
    "test:e2e": "playwright test",
    "typecheck": "tsc --noEmit",
    "test:perf": "vitest run -c vitest.perf.config.ts --reporter=verbose --hookTimeout 30000"
  },
  "dependencies": {
    "@kiwa-lab/core": "workspace:*",
    "@kiwa-lab/quality-metrics": "workspace:*",
    "@kiwa-lab/security": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
