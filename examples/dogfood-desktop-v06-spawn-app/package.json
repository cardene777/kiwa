{
  "name": "dogfood-desktop-v06-spawn-app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Dogfood app v1.61-2 — Desktop v0.6 実 child_process.spawn workflow backed by @kiwa/desktop v0.6。 dry-run 経路で shape 契約 検証 + SpawnFn DI で決定的挙動 + env sanitize + timeout / buffer 上限 pattern reference。 Mobile v1.55 rhythm 再現、 depth-5 pattern 2 例目確定 + depth-6 pattern 新設 candidate dogfood。",
  "scripts": {
    "test": "pnpm -F @kiwa/core -F @kiwa/desktop build && node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kiwa/core": "workspace:*",
    "@kiwa/desktop": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
