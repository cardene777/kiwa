{
  "name": "dogfood-desktop-v06-spawn-app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Dogfood app v1.61-2 — Desktop v0.6 実 child_process.spawn workflow backed by @kiwa-lab/desktop v0.6。 dry-run 経路で shape 契約 検証 + SpawnFn DI で決定的挙動 + env sanitize + timeout / buffer 上限 pattern reference。 Mobile v1.55 rhythm 再現、 depth-5 pattern 2 例目確定 + depth-6 pattern 新設 candidate dogfood。",
  "scripts": {
    "test": "node ../../scripts/build-deps.mjs @kiwa-lab/core @kiwa-lab/desktop && node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "typecheck": "tsc --noEmit",
    "test:perf": "vitest run -c vitest.perf.config.ts --reporter=verbose"
  },
  "dependencies": {
    "@kiwa-lab/core": "workspace:*",
    "@kiwa-lab/desktop": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.6"
  }
}
