{
  "name": "dogfood-lemon-squeezy-app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Dogfood app v1.23-4 — a SvelteKit + Lemon Squeezy Merchant-of-Record RP that exercises hosted checkout + license key issue / activation / revoke + refund (full + partial) + chargeback dispute (opened + evidence + won / lost) + webhook signature verify (X-Signature HMAC-SHA256 over the raw body). Drivable in both KIWA_MODE=real (Lemon Squeezy sandbox + LEMONSQUEEZY_KEY env-gate) and KIWA_MODE=mock (@kiwa-test/payment createLemonSqueezyMock + 8 axis semantics) so behavioural fidelity feeds the release gate.",
  "scripts": {
    "test": "pnpm -F @kiwa-test/payment -F @kiwa-test/core build && node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "test:e2e": "playwright test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kiwa-test/core": "workspace:*",
    "@kiwa-test/payment": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
