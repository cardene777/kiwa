{
  "name": "examples-defi-swap",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "pnpm -F @kiwa/core build && playwright test",
    "typecheck": "tsc --noEmit",
    "forge:build": "forge build"
  },
  "dependencies": {
    "@kiwa/core": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "viem": "^2.21.0"
  }
}
