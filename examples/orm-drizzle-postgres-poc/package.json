{
  "name": "examples-orm-drizzle-postgres-poc",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "pnpm -F @kiwa-lab/orm -F @kiwa-lab/core build && node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kiwa-lab/core": "workspace:*",
    "@kiwa-lab/orm": "workspace:*",
    "@testcontainers/postgresql": "^10.16.0",
    "drizzle-orm": "^0.36.0",
    "postgres": "^3.4.0",
    "testcontainers": "^10.16.0"
  },
  "devDependencies": {
    "@types/dockerode": "^3.3.0",
    "@types/node": "^22.10.0",
    "dockerode": "^4.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
