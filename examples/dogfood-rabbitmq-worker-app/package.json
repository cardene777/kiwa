{
  "name": "examples-dogfood-rabbitmq-worker-app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Dogfood app 2 (v1.11-3) — order-processing worker driven by @kiwa-lab/queue (RabbitMQ basic + advanced) in `KIWA_MODE=mock` and by amqplib against a live rabbitmq:3-management broker in `KIWA_MODE=real`; behavioural fidelity feeds @kiwa-lab/quality-metrics.",
  "scripts": {
    "test": "node ../../scripts/build-deps.mjs @kiwa-lab/queue @kiwa-lab/core @kiwa-lab/quality-metrics && vitest run tests --exclude '**/.vitest-dist/**' --environment node",
    "typecheck": "tsc --noEmit",
    "test:perf": "vitest run -c vitest.perf.config.ts --reporter=verbose"
  },
  "dependencies": {
    "@kiwa-lab/queue": "workspace:*",
    "@kiwa-lab/core": "workspace:*",
    "@kiwa-lab/quality-metrics": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.6"
  }
}
