{
  "name": "dogfood-observability-dashboard",
  "version": "0.0.2",
  "private": true,
  "type": "module",
  "description": "Dogfood app (v1.17-2) — Next.js App Router style Grafana dashboard driven behind a provider-neutral surface. 5 panels (line / bar / gauge / stat / table) execute PromQL-style queries against @kiwa/observability DashboardMock (KIWA_MODE=mock, default) or a real Prometheus HTTP API (KIWA_MODE=real, PROMETHEUS_URL set). The fidelity harness diffs metric query results + panel renders + refresh timing across mock vs real, feeding @kiwa/quality-metrics 7-axis release gate.",
  "scripts": {
    "test": "pnpm -F @kiwa/observability -F @kiwa/core -F @kiwa/quality-metrics build && node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "pretest:perf": "pnpm -F @kiwa/observability -F @kiwa/core -F @kiwa/quality-metrics -F @kiwa/perf-harness build",
    "test:perf": "vitest run -c vitest.perf.config.ts --reporter=verbose",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kiwa/core": "workspace:*",
    "@kiwa/observability": "workspace:*",
    "@kiwa/quality-metrics": "workspace:*"
  },
  "devDependencies": {
    "@kiwa/perf-harness": "workspace:*",
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
