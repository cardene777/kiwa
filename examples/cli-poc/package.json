{
  "name": "examples-cli-poc",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node ../../scripts/build-deps.mjs @kiwa-lab/cli-test @kiwa-lab/cli @kiwa-lab/core && node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node --testTimeout 15000",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kiwa-lab/cli": "workspace:*",
    "@kiwa-lab/cli-test": "workspace:*",
    "@kiwa-lab/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.6"
  }
}
