{
  "name": "examples-cli-poc",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "pnpm -F @kiwa-lab/cli-test -F @kiwa-lab/cli -F @kiwa-lab/core build && node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node --testTimeout 15000",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kiwa-lab/cli": "workspace:*",
    "@kiwa-lab/cli-test": "workspace:*",
    "@kiwa-lab/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
