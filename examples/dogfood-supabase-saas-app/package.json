{
  "name": "examples-dogfood-supabase-saas-app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Dogfood app 1 (v1.11-2) — SaaS onboarding + protected doc list + MFA + SSO SAML + Web3 SIWE, drivable in both KIWA_MODE=real (Supabase Local) and KIWA_MODE=mock (@kiwa-lab/auth) so behavioural fidelity feeds @kiwa-lab/quality-metrics.",
  "scripts": {
    "test": "node ../../scripts/build-deps.mjs @kiwa-lab/auth @kiwa-lab/core @kiwa-lab/quality-metrics && node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "typecheck": "tsc --noEmit",
    "test:perf": "vitest run -c vitest.perf.config.ts --reporter=verbose"
  },
  "dependencies": {
    "@kiwa-lab/auth": "workspace:*",
    "@kiwa-lab/core": "workspace:*",
    "@kiwa-lab/quality-metrics": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.6"
  }
}
