{
  "name": "dogfood-quality-metrics-adaptive-threshold-app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Dogfood app v2.1-2 — quality-metrics v2.1 adaptive drift threshold learning workflow backed by @kiwa-lab/quality-metrics v2.1。 collectRolling + learnFromHistory + evaluateWithLearnedThreshold + explainLearnedGate の 4 pattern workflow reference。 v0.6 drift-gate dogfood を adaptive learning 経路に拡張、 depth-5 実運用継続 pattern 3 例目 の compound 深化 dogfood。",
  "scripts": {
    "test": "node ../../scripts/build-deps.mjs @kiwa-lab/core @kiwa-lab/quality-metrics && vitest run tests --exclude '**/.vitest-dist/**' --environment node",
    "typecheck": "tsc --noEmit",
    "test:perf": "vitest run -c vitest.perf.config.ts --reporter=verbose"
  },
  "dependencies": {
    "@kiwa-lab/core": "workspace:*",
    "@kiwa-lab/quality-metrics": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.6"
  }
}
