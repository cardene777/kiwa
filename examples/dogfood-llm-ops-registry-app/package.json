{
  "name": "dogfood-llm-ops-registry-app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Dogfood app v1.40-4 — a model registry + version rollout + A/B testing + canary promotion + shadow comparison service that drives @kiwa-lab/ai-llm v0.5 llm-ops axis (registry updates + rollout advancement + A/B variant scoring + canary error-rate gating + shadow-vs-production delta) backed by both KIWA_MODE=real (Vercel AI SDK + Anthropic Messages API when ANTHROPIC_API_KEY + KIWA_LLM_BUDGET_USD are set) and KIWA_MODE=mock (in-memory registry + deterministic rollout percentages + variant scoring + canary threshold check + shadow delta) so behavioural fidelity feeds @kiwa-lab/quality-metrics 11-axis release gate.",
  "scripts": {
    "test": "node ../../scripts/build-deps.mjs @kiwa-lab/ai-llm @kiwa-lab/core @kiwa-lab/quality-metrics && vitest run tests --exclude '**/.vitest-dist/**' --environment node",
    "typecheck": "tsc --noEmit",
    "test:perf": "vitest run -c vitest.perf.config.ts --reporter=verbose"
  },
  "dependencies": {
    "@kiwa-lab/ai-llm": "workspace:*",
    "@kiwa-lab/core": "workspace:*",
    "@kiwa-lab/quality-metrics": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.6"
  }
}
