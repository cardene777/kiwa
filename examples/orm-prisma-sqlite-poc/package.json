{
  "name": "examples-orm-prisma-sqlite-poc",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "prisma:generate": "prisma generate --schema=prisma/schema.prisma",
    "test": "node ../../scripts/build-deps.mjs @kiwa-lab/orm @kiwa-lab/core && pnpm prisma:generate && node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "typecheck": "pnpm prisma:generate && tsc --noEmit"
  },
  "dependencies": {
    "@kiwa-lab/core": "workspace:*",
    "@kiwa-lab/orm": "workspace:*",
    "@prisma/client": "^5.22.0",
    "prisma": "^5.22.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
