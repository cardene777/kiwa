{
  "name": "examples-nuxt-server-routes-full",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -F @kiwa-test/nuxt -F @kiwa-test/core build",
    "dev": "nuxt dev",
    "test": "pnpm build && node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "test:e2e": "playwright test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kiwa-test/core": "workspace:*",
    "@kiwa-test/nuxt": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.0",
    "nuxt": "^3.14.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
