# Foundry project descriptor for the v1.11-4 dogfood dApp. Kept minimal so
# `forge test` runs against the checked-in contracts without extra tooling.
[profile.default]
src = "contracts"
out = "out"
libs = ["lib"]
test = "test"
solc = "0.8.20"
optimizer = true
optimizer_runs = 200
