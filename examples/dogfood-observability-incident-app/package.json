{
  "name": "dogfood-observability-incident-app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Dogfood app v2.7-2 — @kiwa-lab/observability v2.1 incident-orchestrator 4 pattern workflow reference。 depth-5 pattern 8 例目 dogfood。",
  "scripts": {
    "test": "pnpm -F @kiwa-lab/core -F @kiwa-lab/observability build && node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kiwa-lab/core": "workspace:*",
    "@kiwa-lab/observability": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
