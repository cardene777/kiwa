{
  "name": "dogfood-security-siem-incident-app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Dogfood app v1.39-3 — a Splunk + PagerDuty style SIEM + incident response orchestrator (CIM-formatted structured audit log + tamper-evident hash-chain seal + hot/warm/cold retention + correlation rule + playbook trigger + sev1-5 severity classification + escalation + forensics capture + post-mortem + fused SIEM-to-incident orchestrator) that drives a SIEM + incident response surface backed by @kiwa-lab/security v0.2 siem-audit + incident-response semantics, runnable in both KIWA_MODE=real (KIWA_SIEM_ENDPOINT + KIWA_PAGERDUTY_URL + KIWA_LOKI_URL provider endpoints) and KIWA_MODE=mock (in-memory state machine) so behavioural fidelity feeds @kiwa-lab/quality-metrics.",
  "scripts": {
    "test": "node ../../scripts/build-deps.mjs @kiwa-lab/core @kiwa-lab/security @kiwa-lab/quality-metrics && node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node --hookTimeout 30000",
    "test:e2e": "playwright test",
    "typecheck": "tsc --noEmit",
    "test:perf": "vitest run -c vitest.perf.config.ts --reporter=verbose --hookTimeout 30000"
  },
  "dependencies": {
    "@kiwa-lab/core": "workspace:*",
    "@kiwa-lab/quality-metrics": "workspace:*",
    "@kiwa-lab/security": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
