<h1>kiwa SvelteKit PoC</h1>

<p>
  This example demonstrates <code>@kiwa-test/sveltekit</code> v1.0.x 全 3 helper
  (<code>invokeLoad</code> / <code>invokeAction</code> / <code>invokeHandle</code>)
  on a real SvelteKit project.
</p>

<ul>
  <li><a href="/items">/items</a> — load + actions + hooks 全部</li>
  <li><a href="/login">/login</a> — login page (session cookie 設定)</li>
</ul>
