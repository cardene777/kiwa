{
  "name": "dogfood-solidjs-signal-app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Dogfood app (v1.19-2) — a SolidJS Signal-based reactivity harness that exercises 3 store patterns (counter Signal / todos Signal / userProfile createResource) inside Counter / TodoList / UserProfile components with a Suspense boundary + error boundary. Drivable in both KIWA_MODE=real (spawns real solid-js + solid-testing-library through env-skip when SOLID_LIVE=1) and KIWA_MODE=mock (@kiwa-test/solidjs mockSignal / mockEffect / createResourceStub / renderWithSuspense). Behavioural fidelity feeds @kiwa-test/quality-metrics 7-axis release gate.",
  "scripts": {
    "test": "pnpm -F @kiwa-test/solidjs -F @kiwa-test/core -F @kiwa-test/quality-metrics build && node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "pretest:perf": "pnpm -F @kiwa-test/solidjs -F @kiwa-test/core -F @kiwa-test/quality-metrics -F @kiwa-test/perf-harness build",
    "test:perf": "vitest run -c vitest.perf.config.ts --reporter=verbose",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kiwa-test/core": "workspace:*",
    "@kiwa-test/quality-metrics": "workspace:*",
    "@kiwa-test/solidjs": "workspace:*"
  },
  "devDependencies": {
    "@kiwa-test/perf-harness": "workspace:*",
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
