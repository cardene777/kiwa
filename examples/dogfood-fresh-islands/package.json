{
  "name": "dogfood-fresh-islands",
  "version": "0.0.2",
  "private": true,
  "type": "module",
  "description": "Dogfood app (v1.19-3) — a Deno Fresh Islands + Route Handler + partial hydration + edge runtime harness that exercises 4 flows (route Handler dispatch / defineRoute page tree / island mount + interaction / Head merge + edge env) inside GreetRoute / Counter Island / TodoList Island / SiteHead components. Drivable in both KIWA_MODE=real (spawns real Deno Fresh + fresh-testing-library through env-skip when DENO_INSTALLED=1) and KIWA_MODE=mock (@kiwa-lab/fresh invokeFreshHandler / invokeDefineRoute / hydrateIslands / mergeHead). Behavioural fidelity feeds @kiwa-lab/quality-metrics 7-axis release gate.",
  "scripts": {
    "test": "pnpm -F @kiwa-lab/fresh -F @kiwa-lab/core -F @kiwa-lab/quality-metrics build && node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "pretest:perf": "pnpm -F @kiwa-lab/fresh -F @kiwa-lab/core -F @kiwa-lab/quality-metrics -F @kiwa-lab/perf-harness build",
    "test:perf": "vitest run -c vitest.perf.config.ts --reporter=verbose",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kiwa-lab/core": "workspace:*",
    "@kiwa-lab/fresh": "workspace:*",
    "@kiwa-lab/quality-metrics": "workspace:*"
  },
  "devDependencies": {
    "@kiwa-lab/perf-harness": "workspace:*",
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
