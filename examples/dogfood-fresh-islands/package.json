{
  "name": "dogfood-fresh-islands",
  "version": "0.0.2",
  "private": true,
  "type": "module",
  "description": "Dogfood app (v1.19-3) — a Deno Fresh Islands + Route Handler + partial hydration + edge runtime harness that exercises 4 flows (route Handler dispatch / defineRoute page tree / island mount + interaction / Head merge + edge env) inside GreetRoute / Counter Island / TodoList Island / SiteHead components. Drivable in both KIWA_MODE=real (spawns real Deno Fresh + fresh-testing-library through env-skip when DENO_INSTALLED=1) and KIWA_MODE=mock (@kiwa-test/fresh invokeFreshHandler / invokeDefineRoute / hydrateIslands / mergeHead). Behavioural fidelity feeds @kiwa-test/quality-metrics 7-axis release gate.",
  "scripts": {
    "test": "pnpm -F @kiwa-test/fresh -F @kiwa-test/core -F @kiwa-test/quality-metrics build && node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "pretest:perf": "pnpm -F @kiwa-test/fresh -F @kiwa-test/core -F @kiwa-test/quality-metrics -F @kiwa-test/perf-harness build",
    "test:perf": "vitest run -c vitest.perf.config.ts --reporter=verbose",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kiwa-test/core": "workspace:*",
    "@kiwa-test/fresh": "workspace:*",
    "@kiwa-test/quality-metrics": "workspace:*"
  },
  "devDependencies": {
    "@kiwa-test/perf-harness": "workspace:*",
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
