{
  "name": "dogfood-trace-flame-graph",
  "version": "0.0.2",
  "private": true,
  "type": "module",
  "description": "Dogfood app (v1.17-4) — trace flame graph explorer (React-style component tree modelled in pure TS for headless test) driven behind a provider-neutral surface. 10 canonical traces (nested span shapes; 100 spans + 34 logs across the set) build into span trees + collapsed flame graphs; drill-down extracts a subtree; log correlation joins logs to spans and back via traceId + spanId. @kiwa-lab/observability buildSpanTree + renderFlameGraph + LogCorrelationIndex back the mock adapter (KIWA_MODE=mock, default); the real adapter fetches traces from a live Jaeger HTTP API (KIWA_MODE=real, JAEGER_URL set). The fidelity harness diffs trace exploration events (loadTrace / renderFlame / drillDown / joinLogs / filterByName) across mock vs real, feeding @kiwa-lab/quality-metrics 7-axis release gate.",
  "scripts": {
    "test": "node ../../scripts/build-deps.mjs @kiwa-lab/observability @kiwa-lab/core @kiwa-lab/quality-metrics && node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "pretest:perf": "pnpm -F @kiwa-lab/observability -F @kiwa-lab/core -F @kiwa-lab/quality-metrics -F @kiwa-lab/perf-harness build",
    "test:perf": "vitest run -c vitest.perf.config.ts --reporter=verbose",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kiwa-lab/core": "workspace:*",
    "@kiwa-lab/observability": "workspace:*",
    "@kiwa-lab/quality-metrics": "workspace:*"
  },
  "devDependencies": {
    "@kiwa-lab/perf-harness": "workspace:*",
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
