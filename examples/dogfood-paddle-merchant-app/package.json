{
  "name": "dogfood-paddle-merchant-app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Dogfood app v1.23-3 — a Paddle Billing v2 merchant-of-record RP for Nuxt 3 that exercises inline checkout + subscription tier upgrade/downgrade + VAT/GST/sales-tax auto-calc + webhook signature verify (Paddle-Signature ts=..;h1=..). Drivable in both KIWA_MODE=real (Paddle sandbox + PADDLE_KEY env-gate) and KIWA_MODE=mock (@kiwa-lab/payment createPaddleMock + 9 axis semantics) so behavioural fidelity feeds the release gate.",
  "scripts": {
    "test": "node ../../scripts/build-deps.mjs @kiwa-lab/payment @kiwa-lab/core && node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "typecheck": "tsc --noEmit",
    "test:perf": "vitest run -c vitest.perf.config.ts --reporter=verbose"
  },
  "dependencies": {
    "@kiwa-lab/core": "workspace:*",
    "@kiwa-lab/payment": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
