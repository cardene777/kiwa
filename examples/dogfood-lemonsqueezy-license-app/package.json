{
  "name": "dogfood-lemonsqueezy-license-app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Dogfood app v1.33-4 — Lemon Squeezy Merchant-of-Record RP focused on license key (issue + activation + per-seat / per-machine bind) + affiliate program (referral link + commission + tier) + refund window (30 day money-back guarantee + partial refund) + webhook (order created / license activated / refund issued). Drives @kiwa-lab/payment v0.4 refund-advanced + payment-method-vault axes through a mock adapter, with real driver env-gate (KIWA_MODE=real + LEMONSQUEEZY_API_KEY) surfacing KIWA_LEMONSQUEEZY_ENV_MISSING until a sandbox fixture is wired.",
  "scripts": {
    "test": "node ../../scripts/build-deps.mjs @kiwa-lab/payment @kiwa-lab/core && node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "typecheck": "tsc --noEmit",
    "test:perf": "vitest run -c vitest.perf.config.ts --reporter=verbose"
  },
  "dependencies": {
    "@kiwa-lab/core": "workspace:*",
    "@kiwa-lab/payment": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.6"
  }
}
