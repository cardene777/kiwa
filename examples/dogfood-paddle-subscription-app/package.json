{
  "name": "dogfood-paddle-subscription-app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Dogfood app v2 — a Paddle Billing v2 subscription RP for Next.js 15 App Router that exercises subscription lifecycle + trial extension + retention offers + proration + coupon stacking + refund window in both KIWA_MODE=mock and env-gated KIWA_MODE=real.",
  "scripts": {
    "test": "node ../../scripts/build-deps.mjs @kiwa-lab/payment @kiwa-lab/core && node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "typecheck": "pnpm -F @kiwa-lab/payment -F @kiwa-lab/core build && tsc --noEmit",
    "test:perf": "vitest run -c vitest.perf.config.ts --reporter=verbose"
  },
  "dependencies": {
    "@kiwa-lab/core": "workspace:*",
    "@kiwa-lab/payment": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.6"
  }
}
