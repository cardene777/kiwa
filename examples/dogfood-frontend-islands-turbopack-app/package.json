{
  "name": "dogfood-frontend-islands-turbopack-app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Dogfood app v1.49-4 — Islands architecture + Turbopack HMR + progressive enhancement 3 axis × 3 target × fidelity harness backed by @kiwa-test/component v0.4 + @kiwa-test/nextjs v1.3。",
  "scripts": {
    "test": "pnpm -F @kiwa-test/core -F @kiwa-test/component -F @kiwa-test/nextjs build && node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kiwa-test/core": "workspace:*",
    "@kiwa-test/component": "workspace:*",
    "@kiwa-test/nextjs": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
