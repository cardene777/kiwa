import{_ as t,o,c as a,a2 as r}from"./chunks/framework.CyZTVJbV.js";const p=JSON.parse('{"title":"kiwa v1.22 released — Auth 深化 II (real driver + a11y + Passkey caBLE + Federation e2e)","description":"","frontmatter":{},"headers":[],"relativePath":"announcements/v1.22/gh-discussions-announcement.md","filePath":"announcements/v1.22/gh-discussions-announcement.md"}'),i={name:"announcements/v1.22/gh-discussions-announcement.md"};function s(n,e,d,c,l,h){return o(),a("div",null,[...e[0]||(e[0]=[r('<h1 id="kiwa-v1-22-released-—-auth-深化-ii-real-driver-a11y-passkey-cable-federation-e2e" tabindex="-1">kiwa v1.22 released — Auth 深化 II (real driver + a11y + Passkey caBLE + Federation e2e) <a class="header-anchor" href="#kiwa-v1-22-released-—-auth-深化-ii-real-driver-a11y-passkey-cable-federation-e2e" aria-label="Permalink to &quot;kiwa v1.22 released — Auth 深化 II (real driver + a11y + Passkey caBLE + Federation e2e)&quot;">​</a></h1><p>v1.22 is out. After v1.21&#39;s Auth 深化 milestone landed 4 protocol adapters as pure mocks (WebAuthn L3 + Passkey + OAuth 2.1 + OIDC), v1.22 layers <strong>real drivers</strong> on top — Keycloak testcontainers (OIDC + Federation), oauth2-mock-server testcontainers (OAuth 2.1), Chrome caBLE hybrid transport (Passkey) — plus a Nuxt 3 RP full journey + a11y axe-core gate + Federation JWKS rotation real end-to-end chain. Mocks stay the first-line contract; real drivers become the second-line fidelity check that catches drift before it silently ships.</p><h2 id="what-shipped" tabindex="-1">What shipped <a class="header-anchor" href="#what-shipped" aria-label="Permalink to &quot;What shipped&quot;">​</a></h2><ul><li><strong><code>@kiwa-lab/auth</code> v0.5.0</strong> (real driver adapters + caBLE hybrid transport). The v1.21 4 protocol adapters (<code>setupWebAuthnEnv</code> / <code>setupPasskeyEnv</code> / <code>setupOAuth21Env</code> / <code>setupOidcEnv</code>) keep every prior signature. v1.22 adds a <code>realDriver</code> option per adapter that, when combined with <code>KIWA_MODE=real</code>, drives the real Keycloak REST admin API / oauth2-mock-server HTTP surface / Chrome caBLE hybrid transport rather than the pure mock. When <code>KIWA_MODE</code> is unset, the mock branch runs — the exact v1.21 behaviour, unchanged. When <code>KIWA_MODE=real-optional</code>, the env tries the real driver and falls back to the mock with a warning if the container URL is missing. Passkey gains 5 new caBLE methods (<code>generateCableQr</code> + <code>broadcastBleAdvertisement</code> + <code>matchBleHandshake</code> + <code>openCableTunnel</code> + <code>migrateCredentialOverTunnel</code> + <code>signOverTunnel</code>) covering the CTAP2 hybrid transport 5-step handshake. 40 new caBLE behaviour tests + real driver behaviour tests across all 4 protocols.</li><li><strong><code>examples/dogfood-oidc-federation</code></strong> — Nuxt 3 Relying Party full journey (login → OP redirect → callback → userinfo panel → logout) with Playwright + <code>@axe-core/playwright</code> a11y gate. Real-mode driver hits a Keycloak testcontainer (Docker Keycloak 26 + realm import + client registration + JWKS endpoint + Federation trust anchor); mock-mode driver hits <code>@kiwa-lab/auth</code> <code>setupOidcEnv</code>. The v1.21-4 fidelity harness 16 axes now record live coverage; axes 1 (Discovery metadata field parity) + 3 (JWKS rotation with kid rollover) match one-to-one between mock and Keycloak. Release gate 7-axis a11y branch flips from <code>N/A</code> (v1.21 skeleton) to <code>PASS</code> (v1.22 full interactive surface).</li><li><strong><code>examples/dogfood-oauth21-provider</code></strong> — oauth2-mock-server testcontainers real driver on the Hono + Cloudflare Workers custom AS. 5 endpoint (<code>/authorize</code> + <code>/token</code> + <code>/revoke</code> + <code>/userinfo</code> + <code>/.well-known/oauth-authorization-server</code>) get live coverage against a real oauth2-mock-server container. Bug fix landed: v1.21-3a review had flagged the <code>/authorize</code> post-adapter error redirect (RFC 6749 §4.1.2.1) as returning inconsistent <code>state</code> parameters in edge cases; v1.22-2 aligns the mock to the RFC + real oauth2-mock-server behaviour. Discovery + PKCE + DPoP + refresh rotation + revocation cascade all record <code>PASS</code> on the fidelity axis.</li><li><strong><code>examples/dogfood-webauthn-passkey-app</code></strong> — CTAP2 hybrid transport (caBLE) real device flow. 5-axis fidelity harness (QR generation / BLE advertisement handshake / WebSocket tunnel establishment / credential migration payload / signature roundtrip). Playwright + real Chrome with <code>--enable-features=WebAuthenticationRemoteDesktopSupport</code> drives the real caBLE surface; <code>@kiwa-lab/auth</code> <code>setupPasskeyEnv</code> new caBLE methods drive the mock. Both branches record the same 5-axis behaviour — 40 new tests + fidelity report matrix. Release gate PASS.</li><li><strong>Federation JWKS rotation real e2e</strong> — a single end-to-end chain spanning real Keycloak OP + Nuxt 3 RP + real JWKS endpoint (public URL + HTTP cache invalidation). <code>kid</code> rotation on the OP → RP-side JWKS refresh (respecting cache-control) → <code>id_token</code> verify continues without dropping in-flight sessions. Axes 4a-4d (inside window / past retention / multi-rotation retention / fresh active key after rotation) all record real coverage.</li><li><strong>docs</strong> — 2 new tutorials (37 real driver testing with Keycloak + oauth2-mock-server testcontainers / 38 Passkey caBLE hybrid transport 5-axis harness) + additive migration guide v1.21 → v1.22 + concept doc <code>real-driver-testing.md</code> — SSOT for the 3 execution modes (<code>mock only</code> / <code>real-optional</code> / <code>real-required</code>) + the fidelity axis catalog across all 3 v1.22 dogfood apps. VitePress sidebar gains a new <code>Real driver (v1.22)</code> tutorial section; gh-pages published via <code>/docs-publish-kiwa</code>.</li></ul><h2 id="numbers" tabindex="-1">Numbers <a class="header-anchor" href="#numbers" aria-label="Permalink to &quot;Numbers&quot;">​</a></h2><ul><li><strong>6 sub-Issues resolved</strong> (#891-#896)</li><li><strong>6 PRs merged</strong> (v1.22-1 + v1.22-2 + v1.22-3 + v1.22-4 + v1.22-5 + this publish PR)</li><li><strong>1 npm minor bump</strong> (<code>@kiwa-lab/auth</code> v0.4.0 → v0.5.0) — kiwa runtime fixture count stays 34</li><li><strong>3 dogfood apps upgraded</strong> with fidelity reports feeding the 7-axis release gate</li><li><strong>~120 new tests</strong> across the 3 real driver adapters + caBLE hybrid transport + a11y gate + Federation JWKS rotation e2e</li></ul><h2 id="why-3-execution-modes-not-2" tabindex="-1">Why 3 execution modes (not 2) <a class="header-anchor" href="#why-3-execution-modes-not-2" aria-label="Permalink to &quot;Why 3 execution modes (not 2)&quot;">​</a></h2><p>A binary &quot;mock vs real&quot; split forces every contributor to install Docker + Chrome + Keycloak just to run tests. That kills PR velocity — the mock&#39;s whole point is to keep the fast path fast. But a pure-mock world lets the mock drift from real Keycloak silently.</p><p>The <code>real-optional</code> middle mode threads the needle. A contributor with Docker up gets the fidelity check for free (their local run detects drift before pushing). A contributor without Docker sees a warning but still passes the mock branch (their PR keeps flowing). CI runs <code>mock only</code> on every PR and <code>real-required</code> on nightly — the fast path stays fast, the drift catcher stays honest.</p><p>Concrete latency budgets.</p><table tabindex="0"><thead><tr><th>Mode</th><th>Latency budget</th><th>When to use</th></tr></thead><tbody><tr><td><code>mock only</code></td><td>&lt; 5 ms per test</td><td>Every PR, every push, every local run (default)</td></tr><tr><td><code>real-optional</code></td><td>&lt; 5 ms mock / &lt; 60 s real (best-effort)</td><td>Laptop exploration with Docker up</td></tr><tr><td><code>real-required</code></td><td>&lt; 90 s per test</td><td>Nightly CI + pre-release smoke</td></tr></tbody></table><h2 id="_12-milestone-streak" tabindex="-1">12-milestone streak <a class="header-anchor" href="#_12-milestone-streak" aria-label="Permalink to &quot;12-milestone streak&quot;">​</a></h2><p>v1.11 (release gate) → v1.12 (non-determinism) → v1.13 (time-axis) → v1.14 (horizontal expansion) → v1.15 (AI-LLM depth) → v1.16 (component depth) → v1.17 (Observability v2) → v1.18 (Blockchain depth) → v1.19 (Framework 深化) → v1.20 (Streaming 深化) → v1.21 (Auth 深化) → <strong>v1.22 (Auth 深化 II)</strong>. Every milestone since v1.11 has landed 6 sub-Issues in full.</p><h2 id="v2-0-candidates" tabindex="-1">v2.0 candidates <a class="header-anchor" href="#v2-0-candidates" aria-label="Permalink to &quot;v2.0 candidates&quot;">​</a></h2><ul><li>Multi-version Vitest matrix (Vitest 1.x vs 2.x vs 3.x parity)</li><li>Desktop (Electron / Tauri) + mobile (React Native / Expo) adapters</li><li>Coverage 100% milestone</li><li>Cache / Data depth (Dragonfly / Materialize / Neon)</li><li>L2 depth (Base / Arbitrum / Optimism / Scroll block-space fidelity)</li><li>ZK depth (Noir / Circom / RISC Zero test harness)</li><li>IoT depth (MQTT / CoAP / LWM2M)</li><li>DB depth (SurrealDB / EdgeDB / Turso)</li></ul><p>Feedback welcome on which of these should land next.</p>',16)])])}const v=t(i,[["render",s]]);export{p as __pageData,v as default};
