import{_ as a,o as s,c as i,a2 as t}from"./chunks/framework.CyZTVJbV.js";const k=JSON.parse('{"title":"Releasing kiwa (日本語)","description":"","frontmatter":{},"headers":[],"relativePath":"RELEASING.ja.md","filePath":"RELEASING.ja.md"}'),o={name:"RELEASING.ja.md"};function n(r,e,l,p,c,h){return s(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="releasing-kiwa-日本語" tabindex="-1">Releasing kiwa (日本語) <a class="header-anchor" href="#releasing-kiwa-日本語" aria-label="Permalink to &quot;Releasing kiwa (日本語)&quot;">​</a></h1><blockquote><p><a href="./RELEASING">🇬🇧 English</a> • <a href="./RELEASING.ja">🇯🇵 日本語</a></p></blockquote><p>kiwa の release は Changesets と GitHub Actions で管理します。 このリポジトリでは version 更新、CHANGELOG 更新、npm publish を main merge 後の workflow に寄せています。 ローカルでは changeset file の追加までを担当し、version bump と publish 実行は <code>release.yml</code> に委譲します。</p><h2 id="changesets-フロー" tabindex="-1">Changesets フロー <a class="header-anchor" href="#changesets-フロー" aria-label="Permalink to &quot;Changesets フロー&quot;">​</a></h2><ol><li>変更を加えた PR で <code>pnpm changeset</code> を実行し、<code>.changeset/{name}.md</code> を追加します。</li><li>PR を <code>main</code> に merge します。</li><li><code>release.yml</code> が起動し、Changesets bot が <code>chore(release): version packages</code> PR を作成します。</li><li>version PR には pending changeset を集計した package version bump と <code>CHANGELOG.md</code> 更新が含まれます。</li><li>version PR を merge すると <code>pnpm release</code> が走り、<code>pnpm publish -r --access public --provenance</code> で npm に公開されます。</li></ol><p>PR 作成前に、changeset が必要な変更では <code>pnpm changeset</code> を実行します。 そのうえでローカル検証として次の 3 コマンドを必ず通します。</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> typecheck</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span></code></pre></div><p><code>pnpm version-packages</code> は通常ローカルでは使わず、Changesets bot が作る version PR で差分を確認します。 手動で version bump を進めるのは、release 運用を一時的に切り替える場合だけにしてください。</p><h2 id="npm-token-設定" tabindex="-1">NPM_TOKEN 設定 <a class="header-anchor" href="#npm-token-設定" aria-label="Permalink to &quot;NPM_TOKEN 設定&quot;">​</a></h2><p>npm 側で Automation Token を発行し、GitHub の <code>Settings &gt; Secrets and variables &gt; Actions</code> に <code>NPM_TOKEN</code> という名前で登録します。release workflow はこの secret を使って publish するため、公開前に必ず設定してください。 トークンは publish 専用の Automation Token を使い、個人アカウントの通常 token は使わない方針です。</p><h2 id="provenance-について" tabindex="-1">provenance について <a class="header-anchor" href="#provenance-について" aria-label="Permalink to &quot;provenance について&quot;">​</a></h2><p><code>--provenance</code> を付けて publish すると、GitHub Actions の OIDC token を使って sigstore provenance が生成されます。npmjs.com の package ページでは、source repository や build workflow への紐付け情報を確認できます。 この設定を有効にするため、release workflow では <code>permissions.id-token: write</code> を必ず付与しています。</p><h2 id="失敗時の見方" tabindex="-1">失敗時の見方 <a class="header-anchor" href="#失敗時の見方" aria-label="Permalink to &quot;失敗時の見方&quot;">​</a></h2><ul><li>version PR が作成されない場合は、<code>main</code> に pending changeset が存在するかを確認します。</li><li>publish が止まる場合は、<code>NPM_TOKEN</code> secret と workflow 権限を確認します。</li><li>tarball 内容を確認したい場合は、各 package で <code>npm pack --dry-run</code> を実行します。</li></ul><h2 id="sha-pin-運用" tabindex="-1">SHA pin 運用 <a class="header-anchor" href="#sha-pin-運用" aria-label="Permalink to &quot;SHA pin 運用&quot;">​</a></h2><p>dependabot は github-actions ecosystem を daily 監視し、<code>groups</code> 設定で全 action を 1 PR にまとめて提案します。PR は週 1 回まとめて merge する運用で、特に <code>changesets/action</code> の minor version up は内部 behavior 変化リスクがあるため手動 review を推奨します。 SHA pin により mutable major tag (例 <code>@v4</code>) 経由の supply chain 攻撃を防ぎ、各 action の更新を意図的に行えます。</p><p>routine group (<code>actions/*</code> + <code>pnpm/*</code>) も major up を手動レビュー運用に揃え、<code>dependabot.yml</code> の <code>ignore</code> で <code>version-update:semver-major</code> を弾く設定としています。 自動 PR は minor / patch のみに限定し、major up が必要な場合は手動レビューのうえで SHA pin 更新 PR を起票します (SHA 検証手順は <code>gh api repos/{owner}/{repo}/git/refs/tags/v{version}</code> → <code>gh api commits/{SHA}</code> の 2 段確認です)。 これは PR #19 で actions/checkout v4 → v6 を含む 3 アクション 2 段 major bump が自動 PR で来た経験から、routine group も <code>changesets/action</code> と同じ手動レビュー運用に揃える必要があると分かったためです。</p><h2 id="release-yml-の-ci-gate" tabindex="-1">release.yml の CI gate <a class="header-anchor" href="#release-yml-の-ci-gate" aria-label="Permalink to &quot;release.yml の CI gate&quot;">​</a></h2><p><code>release.yml</code> は main push 直接トリガで起動しますが、<code>changesets/action</code> 起動前に install → typecheck → test (core + cli) → build → consumer typecheck の 5 step を実行する CI gate を持ちます。test fail で <code>changesets/action</code> は起動せず、publish も行われません。 ローカルテスト主体運用のため、本リポジトリでは GitHub Actions の PR 用 CI workflow は保持しません。PR 提出前に開発者がローカルで <code>pnpm typecheck &amp;&amp; pnpm test &amp;&amp; pnpm build</code> を実行し、結果を PR 本文に記載する運用とします。 publish 経路の test gate は <code>release.yml</code> 内部の test 再走で担保され、複数 Node version の同時検証は廃止しますが、release.yml が publish 前に typecheck / test / build を走らせるため動作確認は維持されます。 main branch protection の required status check は本リポジトリでは設定しません (ci.yml 廃止により <code>test (20)</code> <code>test (22)</code> の status が存在しないため required にできず、設定すると PR merge が deadlock します)。代わりに「PR 経由必須」のみを branch protection で強制し、必要に応じて signed commits など review 経路の物理強制を追加します。test gate は開発者ローカル + <code>release.yml</code> 内 5 step の二段で担保する運用とします。GitGuardian Security Checks や CodeRabbit などの third-party app status は表示されますが、required check には含めません。</p><h2 id="v0-1-0-初回-publish-手順" tabindex="-1">v0.1.0 初回 publish 手順 <a class="header-anchor" href="#v0-1-0-初回-publish-手順" aria-label="Permalink to &quot;v0.1.0 初回 publish 手順&quot;">​</a></h2><p>初回 publish (まだ npmjs.com に package が存在しない状態) で実行する手順を整理します。 2 回目以降は通常の Changesets フローのみで自動化されますが、初回は NPM_TOKEN 配布と Trusted Publisher 設定がユーザー手作業として必要です。</p><h3 id="phase-1-—-npm-側準備-ユーザー作業" tabindex="-1">Phase 1 — npm 側準備 (ユーザー作業) <a class="header-anchor" href="#phase-1-—-npm-側準備-ユーザー作業" aria-label="Permalink to &quot;Phase 1 — npm 側準備 (ユーザー作業)&quot;">​</a></h3><ol><li><strong>npm account の 2FA 設定</strong> — <a href="https://www.npmjs.com/settings/%7Buser%7D/profile" target="_blank" rel="noreferrer">https://www.npmjs.com/settings/{user}/profile</a> で <strong>Auth only</strong> または <strong>Auth and writes</strong> を選択。Automation Token を使う場合は Auth only でも publish 可能です。</li><li><strong>scope の確保 (任意)</strong> — <code>@kiwa-lab/dapp</code> <code>@kiwa-lab/cli</code> は <code>@kiwa-test</code> npm organization で公開済 (<a href="https://www.npmjs.com/org/kiwa-test" target="_blank" rel="noreferrer">https://www.npmjs.com/org/kiwa-test</a>)。 fork して別 scope で公開したい場合は <code>https://www.npmjs.com/org/create</code> で独自 org を作成してください。</li><li><strong>Granular Access Token を発行</strong> — Settings &gt; Access Tokens &gt; Generate New Token &gt; Granular Access Token。 <ul><li>name: <code>kiwa-publish</code></li><li>expiration: 1 year (推奨)</li><li>packages: <code>@kiwa-lab/*</code></li><li>permissions: <strong>Read and write</strong> (publish 用)</li><li><strong>Bypass two-factor authentication (2FA): ON</strong> (CI publish 用に必須)</li><li>発行直後の token は 1 度だけ表示されるためコピー必須</li></ul></li></ol><h3 id="phase-2-—-github-側準備-ユーザー作業" tabindex="-1">Phase 2 — GitHub 側準備 (ユーザー作業) <a class="header-anchor" href="#phase-2-—-github-側準備-ユーザー作業" aria-label="Permalink to &quot;Phase 2 — GitHub 側準備 (ユーザー作業)&quot;">​</a></h3><ol><li><strong>NPM_TOKEN secret 登録</strong> — <a href="https://github.com/cardene777/kiwa/settings/secrets/actions" target="_blank" rel="noreferrer">https://github.com/cardene777/kiwa/settings/secrets/actions</a> で <code>NPM_TOKEN</code> という名前で Phase 1.3 のトークンを登録。</li><li><strong>Workflow permissions</strong> — Settings &gt; Actions &gt; General &gt; <strong>Read and write permissions</strong> + Allow GitHub Actions to create and approve pull requests を有効化。これにより <code>release.yml</code> が version PR を自動作成可能になります。</li><li><strong>Trusted Publishers (provenance 強化)</strong> — npm の Trusted Publisher を設定すると Token 不要で publish 可能になります。Settings &gt; Packages &gt; Trusted Publishers で repo + workflow を指定。<code>release.yml</code> で <code>permissions.id-token: write</code> を維持してください。</li></ol><h3 id="phase-3-—-release-workflow-起動-自動経路" tabindex="-1">Phase 3 — release workflow 起動 (自動経路) <a class="header-anchor" href="#phase-3-—-release-workflow-起動-自動経路" aria-label="Permalink to &quot;Phase 3 — release workflow 起動 (自動経路)&quot;">​</a></h3><ol><li><code>.changeset/</code> 配下に pending changeset を作成 (<code>pnpm changeset</code>)。</li><li>main に merge すると <code>release.yml</code> が起動し <code>chore(release): version packages</code> PR が自動作成される。</li><li>version PR を merge → <code>release.yml</code> が再起動 → <code>pnpm release</code> で npm publish 実行。</li><li>provenance が有効なため npmjs.com の package page に source repository / build workflow へのリンクが表示される。</li></ol><h3 id="phase-4-—-publish-後確認" tabindex="-1">Phase 4 — publish 後確認 <a class="header-anchor" href="#phase-4-—-publish-後確認" aria-label="Permalink to &quot;Phase 4 — publish 後確認&quot;">​</a></h3><ol><li><p><strong>npmjs.com で表示確認</strong> — <code>https://www.npmjs.com/package/@kiwa-lab/dapp</code> / <code>@kiwa-lab/cli</code> で README / provenance badge / version <code>0.1.0</code> を確認。</p></li><li><p><strong>smoke test</strong> — 別の dApp project で <code>pnpm dlx @kiwa-lab/cli init</code> を実行し、生成された <code>e2e/connect.spec.ts</code> が PASS することを確認:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mkdir</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /tmp/kiwa-smoke</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /tmp/kiwa-smoke</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> init</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dlx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @kiwa-lab/cli</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> init</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> exec</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> playwright</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> chromium</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> exec</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> playwright</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span></span></code></pre></div></li><li><p><strong>完了報告</strong> — roadmap Issue へ trace を残す。</p></li></ol><h3 id="既知のリスク" tabindex="-1">既知のリスク <a class="header-anchor" href="#既知のリスク" aria-label="Permalink to &quot;既知のリスク&quot;">​</a></h3><table tabindex="0"><thead><tr><th>リスク</th><th>対処</th></tr></thead><tbody><tr><td>初回 publish で package name が npm 上で reject</td><td>scope 変更 Issue を別途起票</td></tr><tr><td>NPM_TOKEN が Read-only で publish が auth fail</td><td>Token 再発行 (Phase 1.3 を Read and write に修正)</td></tr><tr><td>GitHub Actions workflow permissions が Read-only で version PR 作成 fail</td><td>Phase 2.2 を再確認</td></tr><tr><td>provenance が sigstore 側で fail</td><td>Trusted Publisher 設定不在、Phase 2.3 を追加</td></tr><tr><td>changesets/action が pnpm v10 lockfile format で互換性 issue</td><td><code>release.yml</code> の pnpm version を 10.33.2 pin で対応済</td></tr></tbody></table><h2 id="関連リンク" tabindex="-1">関連リンク <a class="header-anchor" href="#関連リンク" aria-label="Permalink to &quot;関連リンク&quot;">​</a></h2><ul><li><a href="https://github.com/changesets/changesets" target="_blank" rel="noreferrer">Changesets 公式</a></li><li><a href="https://github.com/changesets/action" target="_blank" rel="noreferrer">changesets/action</a></li><li><a href="https://docs.npmjs.com/generating-provenance-statements" target="_blank" rel="noreferrer">npm provenance docs</a></li><li><a href="https://docs.npmjs.com/trusted-publishers" target="_blank" rel="noreferrer">npm Trusted Publishers</a></li><li><a href="https://docs.npmjs.com/about-access-tokens#about-granular-access-tokens" target="_blank" rel="noreferrer">npm Granular Access Tokens</a></li></ul>`,33)])])}const g=a(o,[["render",n]]);export{k as __pageData,g as default};
