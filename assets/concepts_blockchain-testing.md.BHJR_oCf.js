import{_ as s,o as t,c as a,a2 as i}from"./chunks/framework.CyZTVJbV.js";const k=JSON.parse('{"title":"Blockchain testing — chain state / EL client integration / fuzz shrinker / reorg semantics (SSOT)","description":"","frontmatter":{},"headers":[],"relativePath":"concepts/blockchain-testing.md","filePath":"concepts/blockchain-testing.md"}'),n={name:"concepts/blockchain-testing.md"};function r(o,e,h,d,l,c){return t(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="blockchain-testing-—-chain-state-el-client-integration-fuzz-shrinker-reorg-semantics-ssot" tabindex="-1">Blockchain testing — chain state / EL client integration / fuzz shrinker / reorg semantics (SSOT) <a class="header-anchor" href="#blockchain-testing-—-chain-state-el-client-integration-fuzz-shrinker-reorg-semantics-ssot" aria-label="Permalink to &quot;Blockchain testing — chain state / EL client integration / fuzz shrinker / reorg semantics (SSOT)&quot;">​</a></h1><p>kiwa&#39;s v1.10 <code>kiwa::contract::foundry</code> + <code>kiwa::contract::alloy</code> covered the &quot;compile a contract, spawn anvil, drive JSON-RPC&quot; case. v1.18 adds four axes on top of that base — the ones production dApp teams hit once their <code>forge test</code> suite is green but production still breaks. This concept doc is the SSOT for those four axes; the tutorials and dogfood apps are the concrete implementations.</p><h2 id="axis-1-—-chain-state-simulation" tabindex="-1">Axis 1 — Chain state simulation <a class="header-anchor" href="#axis-1-—-chain-state-simulation" aria-label="Permalink to &quot;Axis 1 — Chain state simulation&quot;">​</a></h2><p>Real chains are stateful. Every test that mutates the chain leaks state into the next test unless the harness explicitly rewinds. anvil ships <code>evm_snapshot</code> / <code>evm_revert</code> for exactly this reason, and reth&#39;s <code>debug_setHead</code> covers the same shape with block-depth granularity.</p><p>kiwa surfaces the pattern in three places.</p><ul><li><code>@kiwa-test/dapp</code> — <code>snapshotChain(client)</code> / <code>revertChain(client, snapshotId)</code> wrap the raw JSON-RPC calls through the viem <code>PublicClient</code> request path, returning the typed <code>Hex</code> snapshot id and anvil&#39;s boolean revert response.</li><li><code>kiwa::contract::foundry</code> — <code>Anvil::spawn(port)</code> returns a <code>Drop</code>-terminated handle so shell panics do not leak anvil processes.</li><li><code>kiwa::contract::reth</code> — <code>RethNode::spawn_dev(port)</code> mirrors the <code>Anvil</code> handle. <code>reth_reorg(endpoint, blocks)</code> drives <code>debug_setHead</code> for depth-N rewinds.</li></ul><p>The <strong>contract</strong> each harness enforces is symmetric — every <code>snapshot</code> returns an id, every <code>revert</code> consumes one, and downstream operations observe the pre-snapshot state after revert. Mock adapters (<code>MockChainState</code>) implement the same shape so a fidelity harness can diff mock vs real on the same 6-op trace.</p><h3 id="why-chain-state-matters-more-than-fresh-anvil" tabindex="-1">Why chain state matters more than fresh anvil <a class="header-anchor" href="#why-chain-state-matters-more-than-fresh-anvil" aria-label="Permalink to &quot;Why chain state matters more than fresh anvil&quot;">​</a></h3><p>The classic pattern &quot;<code>beforeEach</code> restarts anvil&quot; costs 500 ms per test and hides one important class of bug — state accumulated by test N + 1 that only reproduces because test N left it behind. <code>snapshot</code> / <code>revert</code> isolates state without paying the boot cost, and — critically — makes leaked state observable when a downstream test starts asserting on a value the previous test wrote.</p><h2 id="axis-2-—-execution-layer-client-integration" tabindex="-1">Axis 2 — Execution-layer client integration <a class="header-anchor" href="#axis-2-—-execution-layer-client-integration" aria-label="Permalink to &quot;Axis 2 — Execution-layer client integration&quot;">​</a></h2><p>Ethereum has multiple production EL clients — geth, nethermind, besu, erigon, reth. kiwa consumers who want release-gate confidence run the same JSON-RPC calls against two clients and diff. The v0.4 baseline covered anvil (Foundry&#39;s Rust dev EL). v0.5 adds reth (Paradigm&#39;s Rust production EL, dev mode).</p><p>The <strong><code>FidelityCase</code> matrix</strong> is how kiwa records the divergence contract.</p><div class="language-rust vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">rust</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">pub</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> struct</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> FidelityCase</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    pub</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> method</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> String</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,           </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// eth_blockNumber / eth_gasPrice / …</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    pub</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> params_json</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> String</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// &quot;[]&quot; or serialized JSON</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    pub</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> expected_agreement</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> bool</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// true = clients must agree, false = divergence-by-design</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    pub</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> rationale</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> String</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// human-readable — surfaces in test failures</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The <code>fidelity_matrix()</code> function in <code>kiwa::contract::reth</code> ships 7 canonical rows.</p><table tabindex="0"><thead><tr><th>Method</th><th>Agreement</th><th>Rationale</th></tr></thead><tbody><tr><td><code>eth_blockNumber</code></td><td>agree</td><td>Both dev modes emit 1 block/s</td></tr><tr><td><code>eth_chainId</code></td><td>agree</td><td>Fixed defaults per client</td></tr><tr><td><code>eth_getBalance</code></td><td>agree</td><td>Zero for non-existent accounts (Yellow Paper §6)</td></tr><tr><td><code>eth_call</code></td><td>agree</td><td>Empty call to zero address returns 0x</td></tr><tr><td><code>eth_gasPrice</code></td><td>diverge</td><td>anvil defaults to 1 gwei, reth follows EIP-1559 base fee</td></tr><tr><td><code>net_version</code></td><td>diverge</td><td>anvil returns 31337, reth dev returns 1337</td></tr><tr><td><code>web3_clientVersion</code></td><td>diverge</td><td>Banner strings differ by design</td></tr></tbody></table><p>When a downstream harness catches a <em>new</em> divergence (a row that used to agree stops agreeing), the test failure surfaces the <code>rationale</code> so reviewers know why the row was on the matrix in the first place. This is the same shape the observability v2 axes use — the assertion carries its own explanation.</p><h2 id="axis-3-—-fuzz-shrinker-semantics" tabindex="-1">Axis 3 — Fuzz shrinker semantics <a class="header-anchor" href="#axis-3-—-fuzz-shrinker-semantics" aria-label="Permalink to &quot;Axis 3 — Fuzz shrinker semantics&quot;">​</a></h2><p><code>forge test</code> runs unit tests that pick primitive inputs deterministically. <code>forge test --match-contract Invariant*</code> runs Foundry&#39;s state-explorer that walks the target contract set with random handlers until it finds a counter-example or the run budget elapses.</p><p>The <strong>shrinker</strong> is what makes invariant tests useful. When the state-explorer finds a break at run 4 823 with a 47-step sequence, Foundry shrinks the sequence until it cannot remove another step without losing the counter-example. The <code>[FAIL. Reason: …]</code> header + <code>sequence:</code> stanza is what teams paste into a bug report.</p><p>kiwa&#39;s <code>kiwa::contract::foundry::invariant</code> surfaces the shrink as a structured <code>ShrinkResult</code>.</p><div class="language-rust vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">rust</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">pub</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> struct</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ShrinkResult</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    pub</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> test_name</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> String</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,                </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// invariant_totalSupplyEqSumOfBalances</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    pub</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> reason</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> String</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,                   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// &quot;Assertion failed&quot;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    pub</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sequence</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Vec</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ShrinkStep</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;,        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ordered (target, sig, calldata)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">pub</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> struct</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ShrinkStep</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    pub</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> target</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> String</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// contract address</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    pub</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> signature</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> String</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// transfer(address,uint256)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    pub</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> calldata</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> String</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 0x-prefixed ABI-encoded blob</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Three properties are load-bearing.</p><ul><li><strong>Determinism via <code>FOUNDRY_INVARIANT_SEED</code> + <code>FOUNDRY_FUZZ_SEED</code>.</strong> Every run visits the same state graph. Same seed → same shrink. Different seed → different shrink but both are valid counter-examples.</li><li><strong>Env plumbing beats config mutation.</strong> <code>foundry.toml</code> <code>[invariant]</code> fields would race under parallel test invocations. Env vars are per-process, so parallel <code>cargo test</code> workers stay isolated.</li><li><strong>Structured output beats stdout scrape.</strong> <code>parse_invariant_shrink(stdout)</code> is exposed as a pure function so tests can exercise the parser without spawning <code>forge</code>, and downstream tools can format the shrink for a code-review comment without re-parsing.</li></ul><h3 id="_256-vs-10-000-runs-—-when-to-bump-the-budget" tabindex="-1">256 vs 10 000 runs — when to bump the budget <a class="header-anchor" href="#_256-vs-10-000-runs-—-when-to-bump-the-budget" aria-label="Permalink to &quot;256 vs 10 000 runs — when to bump the budget&quot;">​</a></h3><p>Foundry defaults to 256 runs per invariant. kiwa release-gate suites use 10 000. The empirical break-even is around 5 000 runs — below that, the state-explorer routinely misses depth-8 sequences that only surface once every 3 000 seeds. Above 10 000 the marginal cost per bug outweighs the compute budget on most CI runners.</p><p>For a scratch branch exploring a new invariant, 256 runs is fine. For a release-gate PR that promotes an invariant into the 11-axis fidelity report, 10 000 is the SSOT default — that budget is what <code>InvariantOptions::default()</code> returns.</p><h2 id="axis-4-—-reorg-semantics" tabindex="-1">Axis 4 — Reorg semantics <a class="header-anchor" href="#axis-4-—-reorg-semantics" aria-label="Permalink to &quot;Axis 4 — Reorg semantics&quot;">​</a></h2><p>Production Ethereum finality is not instant. <code>Transaction confirmed</code> is a lie the wagmi + viem UI tells you at 1 confirmation; a 3-block reorg can un-confirm it. Four failure modes cover the bulk of production bugs.</p><table tabindex="0"><thead><tr><th>Scenario</th><th>Trigger</th><th>Bug it catches</th></tr></thead><tbody><tr><td><strong>Pending tx dropped</strong></td><td>Snapshot before submit → revert before mine</td><td>Tx status stuck on <code>pending</code> when the mempool entry is gone</td></tr><tr><td><strong>Confirmed tx balance rollback</strong></td><td>Snapshot before submit → mine → revert past confirmation</td><td>UI keeps showing the post-transfer balance</td></tr><tr><td><strong>Transfer event history disappears</strong></td><td>Mine N transfers → revert past all N</td><td>UI does not refetch and shows stale rows</td></tr><tr><td><strong>Nonce gap re-send</strong></td><td>Snapshot → mine → revert → re-submit at same nonce</td><td>Wallet reports <code>nonce too low</code> because the local cache leaks</td></tr></tbody></table><p>kiwa records each scenario as a <code>ReorgOp</code> in the <code>dogfood-dapp-e2e-reorg</code> interface.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ReorgOp</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;pendingTx&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;confirmedTx&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;transferEvent&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;nonceGap&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p><code>MockChainState</code> implements the same 4 ops through an in-process snapshot stack, so a test suite that walks all four ops against the mock produces the same trace shape as one walking them against a live anvil fork. The <strong>fidelity ratio</strong> the release gate reads is the fraction of ops where the two traces agree — 1.0 means the mock reproduces the real chain, 0.0 means the mock lies. Anything below 0.7 fails the release gate.</p><h3 id="evm-revert-vs-debug-sethead-—-same-semantics-two-surfaces" tabindex="-1"><code>evm_revert</code> vs <code>debug_setHead</code> — same semantics, two surfaces <a class="header-anchor" href="#evm-revert-vs-debug-sethead-—-same-semantics-two-surfaces" aria-label="Permalink to &quot;\`evm_revert\` vs \`debug_setHead\` — same semantics, two surfaces&quot;">​</a></h3><p>anvil ships <code>evm_snapshot</code> / <code>evm_revert</code> — a snapshot id acts as a bookmark. reth ships <code>debug_setHead</code> — a block hash argument names the target. Both roll back the chain state, but anvil&#39;s contract is <em>revert to a marker</em> while reth&#39;s is <em>rewind by N blocks</em>.</p><p>The <code>@kiwa-test/dapp</code> reorg helpers (<code>snapshotChain</code> / <code>revertChain</code>) target anvil first because Foundry ships the primitive as a first-class RPC method. The <code>kiwa::contract::reth::reth_reorg</code> helper covers <code>debug_setHead</code> for the Rust-side fidelity report. When a downstream consumer wants to walk both, the adapter contract stays open — the <code>ReorgAdapter</code> interface accepts either primitive as long as the 4-op observable behaviour matches.</p><h2 id="assertion-patterns" tabindex="-1">Assertion patterns <a class="header-anchor" href="#assertion-patterns" aria-label="Permalink to &quot;Assertion patterns&quot;">​</a></h2><p>The 4 axes produce three assertion patterns.</p><ul><li><strong>Trace assertions</strong> — every op emits a <code>TraceEvent</code> (<code>ok: boolean</code> + <code>errorKind: string</code>), and the assertion is <code>expect(traces.filter(e =&gt; e.op === &#39;confirmedTx&#39;)).toHaveLength(1)</code>. This catches &quot;the mock silently returned success instead of the divergence trace&quot;.</li><li><strong>Snapshot-symmetric assertions</strong> — every <code>snapshotChain</code> matches a <code>revertChain</code>. The assertion is <code>expect(await rpc.getBalance(RECIPIENT)).toBe(preSnapshotBalance)</code>. Missing revert → leaked state → downstream test fails on an unrelated value.</li><li><strong>Fidelity ratio assertions</strong> — the release gate reads <code>fidelity.ratio &gt;= 0.7</code>. Below the threshold the deploy is blocked. Above it the mock is trusted enough to run in unit-test frequency.</li></ul><p>All three patterns are pure — they add no runtime overhead beyond the RPC call. The test grows one function call per assertion and gains a machine-verifiable contract.</p><h2 id="test-count-baseline" tabindex="-1">Test count baseline <a class="header-anchor" href="#test-count-baseline" aria-label="Permalink to &quot;Test count baseline&quot;">​</a></h2><p>The v1.18 dogfood harness ships the following behaviour test counts per axis.</p><ul><li>Axis 1 (chain state) — <code>dogfood-reth-node-test/tests/scenarios_mock.rs</code> × 10 + <code>dogfood-dapp-e2e-reorg/tests/unit/mock-scenarios.test.ts</code> × 10 = <strong>20 tests</strong></li><li>Axis 2 (EL client) — <code>kiwa-rs/tests/contract_reth.rs</code> × 11 + <code>dogfood-reth-node-test/tests/fidelity_report.rs</code> × 5 = <strong>16 tests</strong></li><li>Axis 3 (fuzz shrinker) — <code>kiwa-rs/tests/contract_foundry_invariant.rs</code> × 10 + <code>dogfood-foundry-invariant-fuzz/tests/invariant_mock.rs</code> × 12 = <strong>22 tests</strong></li><li>Axis 4 (reorg) — <code>dogfood-dapp-e2e-reorg/tests/e2e/reorg-4scenario.spec.ts</code> × 5 (1 warmup + 4 scenarios) + <code>dogfood-dapp-e2e-reorg/tests/unit/fidelity-report.test.ts</code> × 9 = <strong>14 tests</strong></li></ul><p>Every count sits above the 10-test release-gate floor so the 11-axis check passes without special-casing the blockchain surfaces.</p><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><ul><li><a href="./../tutorials/25-reth-node-test">Tutorial 25 — Reth node test</a></li><li><a href="./../tutorials/26-foundry-invariant-fuzz">Tutorial 26 — Foundry invariant + fuzz runner</a></li><li><a href="./../tutorials/27-dapp-e2e-reorg">Tutorial 27 — dApp e2e reorg</a></li><li><a href="./../migrations/v1.17-to-v1.18">Migration v1.17 → v1.18</a></li><li>v1.10 baseline — <a href="./../tutorials/03-rust-contract-from-zero">Rust contract test from zero</a></li></ul>`,45)])])}const u=s(n,[["render",r]]);export{k as __pageData,u as default};
