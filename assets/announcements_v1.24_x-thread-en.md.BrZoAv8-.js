import{_ as t,o,c as r,a2 as a}from"./chunks/framework.CyZTVJbV.js";const u=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"announcements/v1.24/x-thread-en.md","filePath":"announcements/v1.24/x-thread-en.md"}'),s={name:"announcements/v1.24/x-thread-en.md"};function c(d,e,i,n,l,g){return o(),r("div",null,[...e[0]||(e[0]=[a('<p>1/ kiwa v1.24 is out — Edge / Serverless 深化 milestone. After v1.14&#39;s <code>@kiwa-test/edge</code> v0.1 landed the fetch handler + KV / R2 / D1 / DurableObject minimal mock baseline, v1.24 layers <strong>8 axes of advanced edge semantics</strong> on top — Durable Object hibernation + WebSocket edge + edge KV eventual consistency + geo-replicated + Cron trigger + subrequest limit + CPU time limit + streaming Response. Platform-neutral state machines + strict transition guards across Cloudflare Workers + Vercel Edge + Deno Deploy. 120 new semantics tests.</p><p>2/ Why 8 axes? Real edge has 3 failure modes a fetch-only mock can&#39;t catch — stateful drift (Durable Object real lifecycle is a 4-event trace with transactional storage + Hibernation wake-up, not <code>create → serve</code>), consistency envelope (Cloudflare KV + Vercel Edge Config + Deno KV replicas trade strong consistency for edge reads), cross-platform fidelity (Cloudflare fires cron from 3 sources; Vercel + Deno from 2; streaming backpressure measured in bytes vs chunks). Each axis is a pure module under <code>@kiwa-test/edge/semantics/*</code> that emits a strongly typed <code>AxisStep</code> sequence.</p><p>3/ v1.24-1 <code>@kiwa-test/edge</code> v1.1 lands the 8 semantics files (<code>durable-object.ts</code> / <code>websocket-edge.ts</code> / <code>edge-kv.ts</code> / <code>geo-replicated.ts</code> / <code>cron-trigger.ts</code> / <code>subrequest-limit.ts</code> / <code>cpu-time-limit.ts</code> / <code>streaming-response.ts</code>) + 3-platform neutral event routing table + fidelity harness <code>collectFidelityCoverage()</code> 3 × 8 = 24 rows for the release gate. 120/120 tests pass.</p><p>4/ v1.24-2 dogfood-cloudflare-workers-durable-object-app — Cloudflare Workers Durable Object + Hibernation API + storage transactional + WebSocket edge broadcast realtime chat room. 40 vitest exercising the full envelope through <code>createDurableObject</code> + <code>requestWebSocketUpgrade</code>. <code>KIWA_MODE=real</code> flips the same tests to hit real Miniflare without touching bodies.</p><p>5/ v1.24-3 dogfood-vercel-edge-function-app — Next.js 15 middleware + edge runtime + Vercel KV (Redis) + streaming Response (SSE) + geo-based routing (accept-language + geo IP → region) + edge cache invalidation. 45 vitest — the widest v1.24 dogfood app because SSE streaming + geo routing double as backpressure + region-failover signal sources.</p><p>6/ v1.24-4 dogfood-deno-deploy-geo-app — Fresh + Deno KV geo-replicated with strong consistency + Deno Deploy Cron + queue trigger + read-your-writes. 50 vitest exercising multi-region KV write + eventual consistency observation + cron trigger fire + queue trigger dispatch across 3 regions.</p><p>7/ v1.24-5 docs — 3 new tutorials (42 Cloudflare Durable Object + 43 Vercel Edge streaming + 44 Deno Deploy geo), 1 concept doc <code>edge-runtime-testing.md</code> (8-axis SSOT + platform-specific fidelity surface reference), migration guide v1.23 → v1.24 (additive-only), snippet validation test <code>docs-tutorial-v1.24.test.ts</code> (16 test) that re-runs every code snippet against the real <code>@kiwa-test/edge</code> API. Same drift-detection pattern as v1.21 / v1.22 / v1.23.</p><p>8/ 1 npm minor bump — <code>@kiwa-test/edge</code> v1.0.2 → v1.1.0 published to npm. Additive-only: v0.1 fetch handler + KV / R2 / D1 / DurableObject minimal mocks (<code>invokeEdgeHandler</code> / <code>createKvNamespace</code> / <code>EdgeFetchHandler</code> / <code>EdgeEnvBindings</code> / <code>SimulatedExecutionContext</code>) keep every prior signature. VitePress sidebar gains <code>Edge / Serverless 深化 (v1.24)</code> tutorial section + <code>edge-runtime-testing.md</code> under Concepts + <code>v1.23 → v1.24</code> under Migrations. gh-pages published. Roadmap: <a href="https://github.com/cardene777/kiwa#roadmap" target="_blank" rel="noreferrer">https://github.com/cardene777/kiwa#roadmap</a> — v1.11 → v1.12 → v1.13 → v1.14 → v1.15 → v1.16 → v1.17 → v1.18 → v1.19 → v1.20 → v1.21 → v1.22 → v1.23 → v1.24: 14 milestones in a row.</p>',8)])])}const m=t(s,[["render",c]]);export{u as __pageData,m as default};
