import{_ as s,o as i,c as t,a2 as a}from"./chunks/framework.CyZTVJbV.js";const c=JSON.parse('{"title":"Mutation testing SSOT — kill rate + 4-tier threshold + baseline persistence + regression detection SSOT for kiwa v1.27","description":"","frontmatter":{},"headers":[],"relativePath":"concepts/mutation-testing-ssot.md","filePath":"concepts/mutation-testing-ssot.md"}'),n={name:"concepts/mutation-testing-ssot.md"};function o(l,e,r,h,d,p){return i(),t("div",null,[...e[0]||(e[0]=[a(`<h1 id="mutation-testing-ssot-—-kill-rate-4-tier-threshold-baseline-persistence-regression-detection-ssot-for-kiwa-v1-27" tabindex="-1">Mutation testing SSOT — kill rate + 4-tier threshold + baseline persistence + regression detection SSOT for kiwa v1.27 <a class="header-anchor" href="#mutation-testing-ssot-—-kill-rate-4-tier-threshold-baseline-persistence-regression-detection-ssot-for-kiwa-v1-27" aria-label="Permalink to &quot;Mutation testing SSOT — kill rate + 4-tier threshold + baseline persistence + regression detection SSOT for kiwa v1.27&quot;">​</a></h1><p>Introduced in v1.11 as <code>@kiwa-test/quality-metrics</code> v0.1 (<code>mutationFromCounts</code> + a fixed 60 % kill-rate axis on the 7-axis release gate), extended in v1.27-4 as v0.3 (<code>DEFAULT_MUTATION_TIER_THRESHOLDS</code> + <code>resolveMutationTier</code> + <code>assertMutationTier</code> + tier-aware 12-axis release gate), and rolled out to all 33 kiwa packages in the v1.27 milestone. This document is the SSOT for <strong>what a kiwa mutation suite measures, how the kill rate is computed, and how a regression is decided</strong>. Every downstream Stryker config (<code>packages/*/stryker.config.mjs</code>) and every downstream baseline JSON (<code>.mutation-baseline/*.json</code>) reads these rules from here — do not re-derive them locally.</p><h2 id="why-a-mutation-ssot" tabindex="-1">Why a mutation SSOT <a class="header-anchor" href="#why-a-mutation-ssot" aria-label="Permalink to &quot;Why a mutation SSOT&quot;">​</a></h2><p>Mutation tests without a shared standard fail three ways.</p><ul><li><strong>Threshold drift</strong>. One package uses 90 / 80 / 80 as the gate, another uses 80 / 60 / 50, a third uses 65 / 55 / 50. When a mutation regression fires on package A but not on B, the reader cannot tell whether B is genuinely fine or whether B&#39;s gate is looser than A&#39;s. The 4-tier SSOT names the shape of the code Stryker runs over (<code>Core</code> / <code>Framework</code> / <code>SaaS</code> / <code>Test type</code>), and every downstream config picks the tier that fits its shape — not the tier that flatters its historic score.</li><li><strong>Kill-rate formula drift</strong>. Stryker&#39;s HTML report exposes multiple numbers: <code>total MSI</code>, <code>covered MSI</code>, <code>killed / mutations</code>, <code>killed / (killed + survived)</code>. The SSOT pins the kill rate at <strong><code>killed / (killed + survived + timeout + error)</code></strong> — the &quot;% Mutation score / covered&quot; column — so a package with a lot of <code>no-coverage</code> mutants is not penalised for lines the test suite never touches, and a package with <code>timeout</code> / <code>error</code> mutants is not silently rewarded.</li><li><strong>Regression detection drift</strong>. Rebuilding the baseline on every run misses regressions entirely. Comparing today&#39;s kill rate against a hard-coded number misses improvements. The SSOT pins baseline persistence at <code>.mutation-baseline/{package}.json</code>, tracked in git, so a mutation regression on a future PR shows up as a <strong>diff on the baseline JSON alongside the code diff</strong>.</li></ul><p>The 4 rules below are the smallest set that make kiwa mutation suites comparable across packages, milestones, and forks.</p><h2 id="rule-1-—-kill-rate-killed-killed-survived-timeout-error" tabindex="-1">Rule 1 — kill rate = <code>killed / (killed + survived + timeout + error)</code> <a class="header-anchor" href="#rule-1-—-kill-rate-killed-killed-survived-timeout-error" aria-label="Permalink to &quot;Rule 1 — kill rate = \`killed / (killed + survived + timeout + error)\`&quot;">​</a></h2><p>Every <code>mutationFromCounts</code> call in the kiwa monorepo derives kill rate from the same denominator. <code>no-coverage</code> mutants (mutated lines that no test exercises) are <strong>excluded</strong> from the denominator — they inflate the &quot;% Mutation score / total&quot; column but say nothing about test suite quality.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { mutationFromCounts } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@kiwa-test/quality-metrics&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> metric</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> mutationFromCounts</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  mutations: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">200</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// killed + survived (no timeout or error in this suite)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  killed: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">130</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// metric.killRate === 65</span></span></code></pre></div><p>The SSOT matches Stryker&#39;s &quot;% Mutation score / covered&quot; column. The v0.3 <code>mutationFromCounts</code> helper takes <code>{ mutations, killed }</code> and derives <code>survived + killRate</code> from the two inputs; downstream code should not compute the ratio by hand.</p><h3 id="exceptions" tabindex="-1">Exceptions <a class="header-anchor" href="#exceptions" aria-label="Permalink to &quot;Exceptions&quot;">​</a></h3><ul><li><strong>Provider-specific <code>testcontainers-*.js</code></strong> — when a file only exercises against a live container, its mutants land at <code>no-coverage</code> under the unit suite. Exclude those files from Stryker&#39;s <code>mutate</code> glob (see <code>@kiwa-test/queue</code> / <code>cache</code> / <code>realtime</code> <code>stryker.config.mjs</code>) and record the exclusion in the baseline JSON <code>note</code> field so a reviewer sees why. Live-container mutation testing is out of scope for the unit gate.</li><li><strong>Effectful bridges</strong> — thin re-exports of downstream helpers (<code>@kiwa-test/realtime/report.js</code> re-exports <code>@kiwa-test/quality-metrics</code>) are excluded from <code>mutate</code> because the mutation coverage belongs to the source module.</li></ul><h2 id="rule-2-—-4-tier-threshold-rationale" tabindex="-1">Rule 2 — 4-tier threshold rationale <a class="header-anchor" href="#rule-2-—-4-tier-threshold-rationale" aria-label="Permalink to &quot;Rule 2 — 4-tier threshold rationale&quot;">​</a></h2><p>The v1.27 milestone pins every kiwa package to one of four rationale tiers, named after the shape of the code Stryker runs over — not the package name. The tiers and their <code>high</code> / <code>low</code> / <code>break</code> thresholds live in <code>docs/quality/mutation-thresholds.md</code>. The <code>high</code> value is the SSOT floor.</p><table tabindex="0"><thead><tr><th>Tier</th><th><code>high</code> (SSOT floor)</th><th><code>low</code></th><th><code>break</code></th><th>Rationale</th></tr></thead><tbody><tr><td>Core</td><td>80</td><td>60</td><td>50</td><td>Pure logic packages with fully deterministic tests and no external protocol drift.</td></tr><tr><td>Framework</td><td>70</td><td>60</td><td>50</td><td>SSR / hydration / RSC / adapter-wrapper layers where framework internals + client / server dual code paths lower the maximum practical kill-rate.</td></tr><tr><td>SaaS</td><td>65</td><td>55</td><td>50</td><td>Provider-specific adapters (Stripe / Paddle / Anthropic / Ably / Redis / Prisma / …) where mocks approximate a live external API and drift is expected.</td></tr><tr><td>Test type</td><td>60</td><td>50</td><td>40</td><td>Test harness packages (component / visual / a11y / e2e) where DOM / measurement noise + browser dependence inflates false-negative mutants.</td></tr></tbody></table><p>Kill rate ≥ <code>high</code> colours the Stryker HTML report green. Kill rate ≥ <code>low</code> colours it yellow. Kill rate &lt; <code>break</code> fails the mutation run. The 4-tier table is the SSOT for the <code>high</code> column — Stryker configs, baseline JSON, <code>PACKAGE_TIER</code> in <code>scripts/check-mutation-gates.mjs</code>, and <code>DEFAULT_MUTATION_TIER_THRESHOLDS</code> in <code>@kiwa-test/quality-metrics</code> all agree by construction.</p><h3 id="stricter-and-looser-overrides" tabindex="-1">Stricter and looser overrides <a class="header-anchor" href="#stricter-and-looser-overrides" aria-label="Permalink to &quot;Stricter and looser overrides&quot;">​</a></h3><ul><li><strong>Stricter override</strong> — a package may raise its floor above the tier default (e.g. <code>@kiwa-test/api</code> = Core-strict 90 / 80 / 80 because its historical bar already met it). Stricter overrides do not need approval — they raise the floor. The <code>stryker.config.mjs</code> header comment records the raised value.</li><li><strong>Looser override</strong> — a package may sit one point below tier <code>low</code> when the baseline sweep lands below the tier default and a follow-up PR is scoped to bring it back. Looser overrides require a one-line justification pinned to the follow-up work, and must <strong>not</strong> drop below the tier&#39;s <code>break</code> threshold. <code>@kiwa-test/auth</code> at 65 / <code>@kiwa-test/cache</code> / <code>realtime</code> / <code>orm</code> at 60 are the four Framework / SaaS packages that currently sit on a documented looser override.</li></ul><h3 id="the-mutationtier-enum" tabindex="-1">The <code>MutationTier</code> enum <a class="header-anchor" href="#the-mutationtier-enum" aria-label="Permalink to &quot;The \`MutationTier\` enum&quot;">​</a></h3><p>The runtime enum is <code>type MutationTier = &#39;core&#39; | &#39;framework&#39; | &#39;saas&#39; | &#39;test-type&#39;</code>. The verbal labels written in <code>.mutation-baseline/*.json</code> (<code>Core</code> / <code>Framework</code> / <code>SaaS</code> / <code>Test type</code>) resolve through <code>resolveMutationTier</code>.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { resolveMutationTier } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@kiwa-test/quality-metrics&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">resolveMutationTier</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Core&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// → &#39;core&#39;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">resolveMutationTier</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Framework&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// → &#39;framework&#39;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">resolveMutationTier</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39; SaaS &#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// → &#39;saas&#39;   (trim + lowercase)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">resolveMutationTier</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Test type&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// → &#39;test-type&#39;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">resolveMutationTier</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;unknown&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// throws — silent drift refused</span></span></code></pre></div><p>The verbal label and the machine enum agree by construction — the SSOT and the runtime read the same tier without a lookup table per call site.</p><h2 id="rule-3-—-baseline-persistence-is-json-path-is-mutation-baseline-package-json" tabindex="-1">Rule 3 — baseline persistence is JSON, path is <code>.mutation-baseline/{package}.json</code> <a class="header-anchor" href="#rule-3-—-baseline-persistence-is-json-path-is-mutation-baseline-package-json" aria-label="Permalink to &quot;Rule 3 — baseline persistence is JSON, path is \`.mutation-baseline/{package}.json\`&quot;">​</a></h2><p>Every <code>pnpm test:mutation</code> invocation writes a Stryker report to <code>packages/{package}/mutation-report/mutation.json</code>. That report is transient — regenerated on every run. The <strong>persistent baseline</strong> lives at <code>.mutation-baseline/{package}.json</code> (repo root, tracked in git), and it is the last known green mutation snapshot.</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;package&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;@kiwa-test/auth&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;tier&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Framework&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;thresholds&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;high&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">70</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;low&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">60</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;break&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">50</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;killRate&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">68.86</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;totalMsi&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">68.86</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;coveredMsi&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">68.86</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;killed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">115</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;survived&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">52</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;timeout&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;noCoverage&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;error&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;mutants&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;file&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;.vitest-dist/src/session.js&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;line&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">35</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;column&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">15</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;mutator&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ConditionalExpression&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;replacement&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;false&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;capturedAt&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;2026-07-05T12:00:19Z&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;note&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Total MSI 68.86% / Covered MSI 68.86% (threshold high=70 / low=60 / break=50).&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Commit the baseline JSON so a mutation regression on a future PR shows up as a diff on the JSON alongside the code diff. Reviewers see the exact number that moved and the list of surviving mutants without opening the HTML report. Baseline refresh happens in-PR when kill rate improves, and is written by the same PR that raises test coverage — never as a standalone commit.</p><p>The 4-tier <code>tier</code> field is verbal (<code>Core</code> / <code>Framework</code> / <code>SaaS</code> / <code>Test type</code>) to keep the baseline JSON human-readable; the runtime helpers normalise it to the machine enum. The <code>note</code> field records the SSOT formula and any exclusions applied.</p><h2 id="rule-4-—-12-axis-release-gate-integration" tabindex="-1">Rule 4 — 12-axis release gate integration <a class="header-anchor" href="#rule-4-—-12-axis-release-gate-integration" aria-label="Permalink to &quot;Rule 4 — 12-axis release gate integration&quot;">​</a></h2><p>v1.27-4 promotes the mutation kill rate to a first-class 12th axis in the release gate. <code>evaluateReleaseGate</code> gains an optional third parameter <code>context</code>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  evaluateReleaseGate,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  resolveMutationTier,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@kiwa-test/quality-metrics&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> verdict</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> evaluateReleaseGate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(report, {}, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  mutationTier: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">resolveMutationTier</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;SaaS&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  mutationTierThreshold: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">60</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// optional per-package looser override</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// verdict.axesEvaluated === 8  (non-AI-LLM + tier)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// verdict.axesEvaluated === 12 (AI-LLM + tier)</span></span></code></pre></div><p>When <code>mutationTier</code> is omitted the verdict stays at 7 (non-AI-LLM) or 11 (AI-LLM) axes for backward compatibility. When present the verdict count grows by 1 and any threshold miss surfaces as a <code>mutation.tier</code> blocker.</p><p>The tier axis surfaces <strong>alongside</strong> the legacy <code>mutation.killRate</code> axis — v1.11 consumers keep the old blocker shape, and 12-axis consumers additionally see the tier-aware bar. Both axes coexist so an SSOT change on the tier default does not silently invalidate a v1.11 consumer&#39;s overrides.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// A report with mutation kill rate at 50 % on a Core-tier package with a</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// legacy override of 55 % sees both blockers:</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> verdict</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> evaluateReleaseGate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  report,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  { mutationKillRate: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">55</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// v1.11 override on the legacy axis</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  { mutationTier: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;core&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// v1.27-4 opt-in on the 12th axis</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// verdict.blockers -&gt; [</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">//   { axis: &#39;mutation.killRate&#39;, threshold: 55, actual: 50, op: &#39;&gt;=&#39; },</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">//   { axis: &#39;mutation.tier&#39;,     threshold: 80, actual: 50, op: &#39;&gt;=&#39; },</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ]</span></span></code></pre></div><p>The <code>assertMutationTier</code> helper is available for callers that want to enforce the tier floor without going through the full release-gate report — useful in per-package test suites that gate their own PR.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  assertMutationTier,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  mutationFromCounts,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  resolveMutationTier,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@kiwa-test/quality-metrics&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> metric</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> mutationFromCounts</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ mutations: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">167</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, killed: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">115</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">assertMutationTier</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  metric,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  tier: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">resolveMutationTier</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Framework&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  threshold: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">65</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// .mutation-baseline/auth.json override</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>The zero-mutation guard is deliberate. <code>assertMutationTier</code> throws when <code>metric.mutations === 0</code> with the message <code>no mutation signal</code> — an empty test suite would otherwise register as 0/0 = 0 % kill rate and slip past a naïve <code>&gt;= threshold</code> check.</p><h2 id="the-3-layer-harness-alignment" tabindex="-1">The 3-layer harness alignment <a class="header-anchor" href="#the-3-layer-harness-alignment" aria-label="Permalink to &quot;The 3-layer harness alignment&quot;">​</a></h2><p><code>@kiwa-test/perf-harness</code> runs <code>serial + concurrent + memory</code> in one <code>runPerf3Layer</code> call. The mutation harness is single-layer by design — Stryker mutates one operator per run and re-executes the full vitest suite. That is expensive (a 200-mutant run over 30 tests is 6 000 vitest invocations), so kiwa runs mutation once per PR at the branch tip, not on every commit. The perf harness is 3-layer because contention + memory leaks slip past a serial p95; the mutation harness is 1-layer because test-suite quality already covers the concurrency + memory shapes through the same behavior tests Stryker mutates.</p><p>The kiwa release gate treats them as parallel axes.</p><table tabindex="0"><thead><tr><th>Axis</th><th>Layer</th><th>Cost</th><th>Runs when</th></tr></thead><tbody><tr><td><code>perf.p95Ms</code> (v1.13)</td><td>serial</td><td>~90 s across 33 packages</td><td>every <code>pnpm test:perf</code></td></tr><tr><td>perf 3-layer (v1.14)</td><td>serial + concurrent + memory</td><td>~120 s per package</td><td>every <code>pnpm test:perf</code></td></tr><tr><td><code>mutation.killRate</code> (v1.11)</td><td>single</td><td>~200 s per package</td><td>every <code>pnpm test:mutation</code></td></tr><tr><td><code>mutation.tier</code> (v1.27-4)</td><td>single</td><td>(piggybacks on above)</td><td>every <code>pnpm test:mutation</code> + <code>evaluateReleaseGate({ mutationTier })</code></td></tr></tbody></table><p>Mutation runs are excluded from the default <code>pnpm test</code> sweep because a 33-package × ~200 s per-package run is ~110 minutes. <code>pnpm test:mutation</code> is the explicit opt-in — run it once per PR at the branch tip, commit the baseline JSON when kill rate improves, and let the diff surface any regression.</p><h2 id="package-coverage-v1-27" tabindex="-1">Package coverage (v1.27) <a class="header-anchor" href="#package-coverage-v1-27" aria-label="Permalink to &quot;Package coverage (v1.27)&quot;">​</a></h2><p>The v1.27 milestone applied the tier gate to every kiwa package.</p><table tabindex="0"><thead><tr><th>Layer</th><th>Packages</th></tr></thead><tbody><tr><td>Core (6)</td><td><code>@kiwa-test/core</code> / <code>api</code> / <code>data</code> / <code>cli-test</code> / <code>cli</code> / <code>observability</code></td></tr><tr><td>Framework (12)</td><td><code>@kiwa-test/nextjs</code> / <code>nuxt</code> / <code>sveltekit</code> / <code>remix</code> / <code>astro</code> / <code>solidstart</code> / <code>qwikcity</code> / <code>edge</code> / <code>solidjs</code> / <code>fresh</code> / <code>hono</code> / <code>auth</code></td></tr><tr><td>SaaS (11)</td><td><code>@kiwa-test/ai-llm</code> / <code>payment</code> / <code>queue</code> / <code>cache</code> / <code>streaming</code> / <code>realtime</code> / <code>mcp</code> / <code>agent</code> / <code>search</code> / <code>orm</code> / <code>dapp</code></td></tr><tr><td>Test type (5)</td><td><code>@kiwa-test/ui</code> / <code>a11y</code> / <code>visual</code> / <code>component</code> / <code>e2e</code></td></tr></tbody></table><p>Every package writes a per-package baseline JSON to <code>.mutation-baseline/{package}.json</code>, runs Stryker on every <code>pnpm test:mutation</code> invocation, and gates on the tier floor via <code>assertMutationTier</code> (in package tests) or <code>evaluateReleaseGate({ mutationTier })</code> (at release time). The <code>PACKAGE_TIER</code> map in <code>scripts/check-mutation-gates.mjs</code> is the runtime SSOT for the tier assignment; the doc table above is the human-readable SSOT.</p><h2 id="where-each-axis-lands-in-the-release-gate" tabindex="-1">Where each axis lands in the release gate <a class="header-anchor" href="#where-each-axis-lands-in-the-release-gate" aria-label="Permalink to &quot;Where each axis lands in the release gate&quot;">​</a></h2><p>The 12-axis release gate exposes two mutation axes.</p><table tabindex="0"><thead><tr><th>Axis</th><th>Kind</th><th>Introduced</th><th>Threshold source</th></tr></thead><tbody><tr><td>mutation — killRate</td><td>floor</td><td>v1.11 (7-axis)</td><td><code>ReleaseGateThresholds.mutationKillRate</code> (default 60 %, overrideable)</td></tr><tr><td>mutation — tier</td><td>floor</td><td>v1.27-4 (12-axis)</td><td><code>DEFAULT_MUTATION_TIER_THRESHOLDS[tier]</code> + optional <code>context.mutationTierThreshold</code></td></tr></tbody></table><p>Both axes coexist. The legacy <code>mutation.killRate</code> axis stays at its v1.11 default (60 %) so downstream consumers that never adopt the 12-axis path keep the same shape. The <code>mutation.tier</code> axis is opt-in via <code>context.mutationTier</code> and gates on the 4-tier SSOT.</p><h2 id="related" tabindex="-1">Related <a class="header-anchor" href="#related" aria-label="Permalink to &quot;Related&quot;">​</a></h2><ul><li><a href="./../tutorials/50-mutation-testing-baseline">Tutorial 50 — Mutation testing baseline (Stryker + tier gate walkthrough)</a></li><li><a href="./../tutorials/51-mutation-baseline-migration">Tutorial 51 — Mutation baseline migration (22 → 33 package sweep methodology)</a></li><li><a href="./../migrations/v1.26-to-v1.27">Migration guide v1.26 → v1.27</a></li><li><a href="./../quality/mutation-thresholds">Mutation thresholds SSOT (4-tier rationale + package assignment matrix)</a></li><li><a href="./../quality/release-gate">Release gate SSOT (12-axis)</a></li><li><a href="./perf-testing-ssot">Perf-testing SSOT (parallel 3-layer harness)</a></li></ul>`,51)])])}const u=s(n,[["render",o]]);export{c as __pageData,u as default};
