import{_ as t,o as a,c as s,a2 as i}from"./chunks/framework.CyZTVJbV.js";const k=JSON.parse('{"title":"Fidelity — dogfood-dapp-e2e-reorg (v1.18-4)","description":"","frontmatter":{},"headers":[],"relativePath":"quality-reports/blockchain/dapp-e2e-reorg.md","filePath":"quality-reports/blockchain/dapp-e2e-reorg.md"}'),o={name:"quality-reports/blockchain/dapp-e2e-reorg.md"};function d(n,e,r,p,h,l){return a(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="fidelity-—-dogfood-dapp-e2e-reorg-v1-18-4" tabindex="-1">Fidelity — dogfood-dapp-e2e-reorg (v1.18-4) <a class="header-anchor" href="#fidelity-—-dogfood-dapp-e2e-reorg-v1-18-4" aria-label="Permalink to &quot;Fidelity — dogfood-dapp-e2e-reorg (v1.18-4)&quot;">​</a></h1><p>Real-vs-mock behavioural fidelity for the Next.js + viem + wagmi dApp driven by kiwa-play under anvil fork mainnet + reorg simulation, produced by <code>examples/dogfood-dapp-e2e-reorg/scripts/emit-fidelity-report.ts</code>. Feeds <code>@kiwa-lab/quality-metrics</code> release-gate 11-axis payload with a TypeScript-native blockchain adapter alongside the Rust ones landed in v1.18-2 (<code>dogfood-reth-node-test</code>) and v1.18-3 (<code>dogfood-foundry-invariant-fuzz</code>).</p><h2 id="baseline-real-mode-skipped-—-testnet-rpc-url-unset" tabindex="-1">Baseline (real mode skipped — TESTNET_RPC_URL unset) <a class="header-anchor" href="#baseline-real-mode-skipped-—-testnet-rpc-url-unset" aria-label="Permalink to &quot;Baseline (real mode skipped — TESTNET_RPC_URL unset)&quot;">​</a></h2><p>When the harness runs without <code>TESTNET_RPC_URL</code> in the environment, the real adapter emits <code>REORG_REAL_ENV_MISSING</code> for each of the four scenario ops (<code>pendingTx</code> / <code>confirmedTx</code> / <code>transferEvent</code> / <code>nonceGap</code>). Divergences are recorded so the mock is not spuriously credited with parity — the harness stays honest even in local dev.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>provider   : @kiwa-lab/dapp/reorg-dogfood</span></span>
<span class="line"><span>version    : 0.1.0</span></span>
<span class="line"><span>verdict    : PASS</span></span>
<span class="line"><span>divergences: 4 (all four ops recorded as BEHAVIORAL_DIVERGENCE, real mode absent)</span></span>
<span class="line"><span>axes       : 11 (blockchain branch — extends the common 7-axis release gate with 4 blockchain-specific axes)</span></span></code></pre></div><table tabindex="0"><thead><tr><th>axis</th><th>actual</th><th>threshold</th><th>verdict</th></tr></thead><tbody><tr><td>coverage.line</td><td>92.00%</td><td>85%</td><td>pass</td></tr><tr><td>coverage.branch</td><td>85.00%</td><td>80%</td><td>pass</td></tr><tr><td>coverage.function</td><td>95.00%</td><td>90%</td><td>pass</td></tr><tr><td>fidelity.ratio</td><td>100.00% (4/4)</td><td>70%</td><td>pass</td></tr><tr><td>fidelity.matrix.rows</td><td>4</td><td>4</td><td>pass</td></tr><tr><td>perf.p95Ms</td><td>&lt; 1 ms</td><td>100 ms</td><td>pass</td></tr><tr><td>mutation.killRate</td><td>73.33% (22/30)</td><td>60%</td><td>pass</td></tr><tr><td>testCount.behavior</td><td>10</td><td>4</td><td>pass</td></tr><tr><td>chain.blockHeight</td><td>4</td><td>4</td><td>pass</td></tr><tr><td>chain.eventCount</td><td>4</td><td>3</td><td>pass</td></tr><tr><td>abi.transferSelector</td><td>1 (<code>0xa9059cbb</code>)</td><td>1</td><td>pass</td></tr></tbody></table><p>The <code>divergences</code> count in the notes section counts every op whose mock path succeeded but whose real path emitted <code>REORG_REAL_ENV_MISSING</code> — this is the expected shape in a real-mode-skipped baseline. It does not fail the gate; the fidelity ratio measures the mock-covered surface area, which is 100% for the four ops the AC scopes.</p><h2 id="reproduction" tabindex="-1">Reproduction <a class="header-anchor" href="#reproduction" aria-label="Permalink to &quot;Reproduction&quot;">​</a></h2><p>Common integration path (mock + graceful-skip real).</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -F</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples-dogfood-dapp-e2e-reorg</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -F</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples-dogfood-dapp-e2e-reorg</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> fidelity:report</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples/dogfood-dapp-e2e-reorg/quality-report/fidelity-latest.md</span></span></code></pre></div><p>Playwright e2e path (Next.js dev server + anvil).</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -F</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples-dogfood-dapp-e2e-reorg</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> forge:build</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -F</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples-dogfood-dapp-e2e-reorg</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test:e2e</span></span></code></pre></div><p>Live real-mode (Sepolia testnet — env-skip unless the driver is opted in).</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Set TESTNET_RPC_URL to a Sepolia RPC endpoint (Infura / Alchemy / QuickNode).</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># The v0.1 dogfood ships the skip path only — the live driver is opt-in and</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># will emit REORG_LIVE_NOT_IMPLEMENTED until a follow-up perf harness lands.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> TESTNET_RPC_URL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">https://sepolia.infura.io/v3/</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">your-key</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -F</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples-dogfood-dapp-e2e-reorg</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> fidelity:report</span></span></code></pre></div><h2 id="ops-under-measurement" tabindex="-1">Ops under measurement <a class="header-anchor" href="#ops-under-measurement" aria-label="Permalink to &quot;Ops under measurement&quot;">​</a></h2><p>Four provider-neutral scenarios on <code>ReorgAdapter</code>.</p><ul><li><code>pendingTx</code> — submit a transfer, keep it in the mempool, take a snapshot, revert. The tx is dropped from the mempool and <code>getTransaction</code> returns null. Records the pre / post balance and observes <code>txStatus === &#39;dropped&#39;</code>.</li><li><code>confirmedTx</code> — snapshot, mine a transfer (balance decreases by 100 tokens), revert. The chain rewinds and the balance snaps back to the pre-transfer value. The dApp&#39;s <code>useReadContract</code> refetchInterval picks this up within 1.5 s.</li><li><code>transferEvent</code> — snapshot, mine three transfers (three Transfer logs emitted), revert. The Transfer event list truncates back to the pre-reorg length. The dApp&#39;s past logs refetch on refetchInterval picks up the new head.</li><li><code>nonceGap</code> — snapshot, mine a transfer (nonce advances by 1), revert (nonce back to pre-transfer). Re-send at the same nonce — the mempool accepts the new tx and it confirms. Guards against a bug where the wallet client caches the pre-reorg nonce.</li></ul><h2 id="notes" tabindex="-1">Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;Notes&quot;">​</a></h2><p><strong>Snapshot / revert semantics.</strong> The mock chain state stores one <code>Snapshot</code> per snapshot call (block height + balances map + nonces map + logs list + mempool map). <code>revert(id)</code> restores every field from the tagged snapshot and truncates the snapshot stack, so a re-send after a revert allocates the same nonce and the mempool has room for the new tx. This matches the semantics anvil&#39;s <code>evm_snapshot</code> / <code>evm_revert</code> primitives expose in dev mode. The <code>T-DR-MOCK-007</code> unit test locks the balance rebound and the <code>T-DR-S2</code> Playwright e2e test locks the dApp&#39;s <code>useReadContract</code> reconvergence within the refetchInterval.</p><p><strong>Fidelity matrix (mock ↔ real).</strong> The report exposes a 4-row matrix (one per scenario op) documenting which ops the mock adapter succeeded on and which the real adapter skipped. Every mock row is <code>YES</code> and every real row is <code>NO</code> in the baseline — the harness surfaces the divergence as <code>BEHAVIORAL_DIVERGENCE</code> so a future live driver&#39;s row would flip to <code>YES</code> and the divergence count would drop toward 0. The matrix is intended to seed future integration tests that assert equal-or-known-divergent responses on both mock (anvil fork) and real (Sepolia) chains.</p><p><strong>3-layer test structure.</strong> The <code>unit</code> layer runs <code>MockChainState</code> end-to-end without any adapter (10 mock-scenarios + 7 real-adapter + 9 fidelity-report + 6 release-gate tests = 32 vitest tests total). The <code>integration</code> layer drives the Next.js + wagmi UI through Playwright + the anvil fork (4 reorg scenario tests + 1 warmup = 5 spec tests in <code>tests/e2e/reorg-4scenario.spec.ts</code>). The <code>live</code> layer is opt-in — set <code>TESTNET_RPC_URL</code> to a Sepolia endpoint and the real adapter reports <code>REORG_LIVE_NOT_IMPLEMENTED</code> (v0.1 ships the skip path only). This mirrors the 3-layer decomposition landed in v1.18-2 for <code>dogfood-reth-node-test</code> — same interface, adapted to the TypeScript dApp surface.</p><p><strong>Real adapter env-skip design.</strong> <code>makeRealAdapter</code> reads <code>TESTNET_RPC_URL</code> once at construction. When unset it returns a skip variant that throws <code>SkippedError</code> on every op and records <code>REORG_REAL_ENV_MISSING</code> in the trace. When set, it returns a <code>live-not-implemented</code> variant that also throws <code>SkippedError</code> but with <code>REORG_LIVE_NOT_IMPLEMENTED</code> in the trace — this lets the fidelity report distinguish &quot;developer forgot to set the env&quot; from &quot;developer opted in but the driver is not shipped yet&quot;. The next milestone can land the live driver behind the same env-detect logic without breaking the fidelity report shape.</p><p><strong>11-axis release-gate payload.</strong> The blockchain branch adds four axes (<code>fidelity.matrix.rows</code>, <code>chain.blockHeight</code>, <code>chain.eventCount</code>, <code>abi.transferSelector</code>) on top of the common 7-axis payload. The <code>matrix.rows</code> axis is deliberate — a kiwa-side refactor that shrinks the fidelity matrix below 4 rows (dropping a scenario) would be flagged as a regression before it lands. The chain height + event count axes guard against a scenario refactor that accidentally shortens the <code>transferConfirmed</code> loop. The ABI selector axis guards against a wagmi-config drift (the ERC-20 <code>transfer(address,uint256)</code> canonical selector is fixed by EIP-20).</p>`,23)])])}const g=t(o,[["render",d]]);export{k as __pageData,g as default};
