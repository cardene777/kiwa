import{_ as o,o as t,c as a,a2 as d}from"./chunks/framework.CyZTVJbV.js";const u=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"announcements/v1.17/x-thread-en.md","filePath":"announcements/v1.17/x-thread-en.md"}'),r={name:"announcements/v1.17/x-thread-en.md"};function c(s,e,n,i,l,p){return t(),a("div",null,[...e[0]||(e[0]=[d('<p>1/ kiwa v1.17 is out — Observability v2 milestone. After v1.16&#39;s component test vertical (Storybook 8 + Playwright CT + Chromatic), v1.17 lands the SaaS observability stack every production team already ships: Grafana dashboards + Prometheus AlertManager routing + trace flame graphs + log ↔ span correlation.</p><p>2/ <code>@kiwa/observability</code> v2.0 (major bump) — 4 additional axes on top of the v1.1 telemetry mock foundation (v1.14-4 OpenTelemetry / Datadog / Sentry). Zero breaking changes to v1.0 or v1.1 surfaces; 11 new public exports + 13 fixture builders under the &quot;Observability v2&quot; module signal.</p><p>3/ <code>DashboardMock</code> — bind a dashboard to a <code>TelemetryCollector</code>, evaluate N panel queries (<code>sum</code> / <code>avg</code> / <code>max</code> / <code>min</code> / <code>count</code> / <code>last</code>) with optional tag filter + time window, attach <code>PanelThreshold[]</code> for <code>ok</code> / <code>warn</code> / <code>critical</code> badge. <code>refresh()</code> returns <code>PanelResult[]</code> + increments <code>refreshCount</code>.</p><p>4/ <code>AlertRouter</code> — <code>pending → firing</code> transitions under a <code>forSamples</code> gate, <code>setRoute</code> walks a nested <code>RouteEntry</code> tree (deepest match wins), <code>addSilence</code> with literal + regex label match, <code>tickEscalation()</code> state machine so kiwa tests assert &quot;reached tier 2 at t=15min&quot; without any wall clock.</p><p>5/ <code>buildSpanTree</code> + <code>renderFlameGraph</code> + <code>drillDown</code> + <code>flattenFlame</code> — pure transforms over <code>SpanRecord[]</code> compute <code>totalMs</code> / <code>selfMs</code>, collapse siblings by name into <code>FlameNode</code> (samples counted), extract subtree with depth normalized to 0. Orphan spans become roots.</p><p>6/ <code>LogCorrelationIndex</code> + <code>correlateLogsAndSpans</code> — bidirectional index over <code>LogRecord[]</code> + <code>SpanRecord[]</code>. Configurable <code>CorrelationKeys</code> cover OpenTelemetry (<code>trace_id</code> / <code>span_id</code>) + Datadog (<code>dd.trace_id</code>) + Sentry (<code>sentry-trace</code>) in one API. 40+ new behavior tests, 145 total pass.</p><p>7/ dogfood apps — dashboard (Next.js App Router + Grafana 5 panel × 22 test) / alert-orchestrator (Node.js + AlertManager 10 rule + 3 level route + escalation × 27 test) / trace-flame-graph (React SPA + Jaeger 10 fixture (100 spans + 34 logs) × 29 test). All 3 hit release gate PASS with mock vs real fidelity harness.</p><p>8/ docs — 3 tutorials (22 dashboard / 23 alert / 24 flame graph) + additive migration v1.16 → v1.17 + concept doc <code>observability-v2-testing.md</code> (4 additional axes × 6 semantic axis SSOT). VitePress + gh-pages published. Roadmap: <a href="https://github.com/cardene777/kiwa/issues/777" target="_blank" rel="noreferrer">https://github.com/cardene777/kiwa/issues/777</a> — v1.11 → v1.12 → v1.13 → v1.14 → v1.15 → v1.16 → v1.17: 7 milestones in a row.</p>',8)])])}const h=o(r,[["render",c]]);export{u as __pageData,h as default};
