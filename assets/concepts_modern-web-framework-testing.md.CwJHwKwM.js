import{_ as t,o as a,c as s,a2 as r}from"./chunks/framework.CyZTVJbV.js";const p=JSON.parse('{"title":"Modern web framework testing — Signal reactivity / Islands architecture / edge runtime + RPC type-safety (SSOT)","description":"","frontmatter":{},"headers":[],"relativePath":"concepts/modern-web-framework-testing.md","filePath":"concepts/modern-web-framework-testing.md"}'),o={name:"concepts/modern-web-framework-testing.md"};function i(d,e,n,c,l,h){return a(),s("div",null,[...e[0]||(e[0]=[r('<h1 id="modern-web-framework-testing-—-signal-reactivity-islands-architecture-edge-runtime-rpc-type-safety-ssot" tabindex="-1">Modern web framework testing — Signal reactivity / Islands architecture / edge runtime + RPC type-safety (SSOT) <a class="header-anchor" href="#modern-web-framework-testing-—-signal-reactivity-islands-architecture-edge-runtime-rpc-type-safety-ssot" aria-label="Permalink to &quot;Modern web framework testing — Signal reactivity / Islands architecture / edge runtime + RPC type-safety (SSOT)&quot;">​</a></h1><p>kiwa&#39;s v1.14 horizontal framework expansion (Next.js / Nuxt / Remix / Astro / Qwik / SvelteKit / SolidStart) covered the request/response mock case for 7 web frameworks. v1.19 adds <strong>four axes on top of that base</strong> — the ones production teams hit once their <code>Next.js + wagmi</code> or <code>Express + fetch</code> suite is green but the modern web framework introduces a runtime characteristic the existing mocks do not capture. This concept doc is the SSOT for those four axes; the tutorials and dogfood apps are the concrete implementations.</p><h2 id="axis-1-—-signal-based-fine-grained-reactivity-solidjs" tabindex="-1">Axis 1 — Signal-based fine-grained reactivity (SolidJS) <a class="header-anchor" href="#axis-1-—-signal-based-fine-grained-reactivity-solidjs" aria-label="Permalink to &quot;Axis 1 — Signal-based fine-grained reactivity (SolidJS)&quot;">​</a></h2><p>React and Solid share the same JSX syntax and the same &quot;component returns a tree&quot; mental model. Everything below that is different — React&#39;s Virtual DOM re-renders the whole component on every state change and diffs the tree, while Solid&#39;s Signal-based reactivity re-runs <strong>only the closures that read the signal that wrote</strong>.</p><p>That divergence matters for tests because Solid bugs look like:</p><ul><li>&quot;The effect body captured a stale signal read&quot; — the effect subscribed at mount but the signal getter it read went out of scope</li><li>&quot;Two writes fired the effect twice instead of once&quot; — <code>batch()</code> was missing, or the batch scope leaked outside the write group</li><li>&quot;The Suspense boundary showed <code>fallback</code> forever&quot; — the resource state stayed on <code>pending</code> because the fetcher promise was replaced without a <code>refetch()</code></li></ul><p>kiwa surfaces the pattern in three places.</p><ul><li><code>mockSignal(initial)</code> — returns a <code>[getter, setter]</code> pair. The setter is <code>Object.is</code>-guarded so writing the same value is a no-op, and NaN writes are treated as equal to NaN (matches Solid&#39;s real behaviour).</li><li><code>mockEffect(fn)</code> — runs <code>fn</code> synchronously on creation and re-runs it whenever a signal read inside the closure writes. Returns an <code>EffectHandle</code> with <code>.trace()</code> for observability, <code>.runCount()</code> for assertion, and <code>.dispose()</code> for cleanup.</li><li><code>createResourceStub(fetcher)</code> — awaits the fetcher, exposes a <code>resource()</code> accessor + <code>resource.state</code> + <code>refetch()</code> + <code>mutate()</code>. The five-state lifecycle (<code>unresolved / pending / ready / refreshing / errored</code>) matches real Solid, so a test asserting <code>expect(accessor.state).toBe(&#39;refreshing&#39;)</code> mid-refetch catches a stuck state machine.</li></ul><p>The <strong>contract</strong> each helper enforces is symmetric — every <code>signal</code> write triggers exactly one <code>effect</code> re-run per subscribed closure, every <code>batch</code> scope flushes exactly once regardless of nesting, and every <code>resource</code> refetch transitions <code>ready → refreshing → ready</code> on success or <code>ready → refreshing → errored</code> on throw. Mock components implement the same shape so a fidelity harness can diff mock vs a real Solid runtime run on the same 4-op trace.</p><h3 id="why-fine-grained-matters-more-than-virtual-dom-diff" tabindex="-1">Why fine-grained matters more than Virtual DOM diff <a class="header-anchor" href="#why-fine-grained-matters-more-than-virtual-dom-diff" aria-label="Permalink to &quot;Why fine-grained matters more than Virtual DOM diff&quot;">​</a></h3><p>The classic React pattern &quot;the component re-renders on every state change and React diffs the output&quot; costs ~200 μs per re-render even with <code>memo</code>. Solid&#39;s fine-grained rebuild is closer to 5 μs — but only if the effect subscribed exactly the signals it needed. Over-subscribe (touch every signal in the store) and Solid becomes React with extra syntax. Under-subscribe (miss a signal that should have triggered a re-run) and the UI silently shows stale data.</p><p><code>mockEffect(fn).trace()</code> records the <code>readValues</code> array on every re-run. That means the test asserts on both dimensions — the exact re-run count via <code>handle.runCount()</code> and the exact values read via <code>trace().map((e) =&gt; e.readValues)</code>. Under-subscription surfaces as a missing re-run; over-subscription surfaces as an unexpected re-run row.</p><h2 id="axis-2-—-islands-architecture-partial-hydration-fresh" tabindex="-1">Axis 2 — Islands architecture + partial hydration (Fresh) <a class="header-anchor" href="#axis-2-—-islands-architecture-partial-hydration-fresh" aria-label="Permalink to &quot;Axis 2 — Islands architecture + partial hydration (Fresh)&quot;">​</a></h2><p>Deno Fresh (Deno.land&#39;s SSR framework) diverges from Next.js on one axis that shows up in every non-trivial test — routes render <strong>entirely</strong> on the server and only components explicitly marked as islands ship JavaScript to the client. Next.js hydrates the whole tree; Fresh hydrates only the islands, and each island receives its props through a <code>data-props</code> blob serialized into the SSR HTML.</p><p>That divergence matters for tests because Fresh bugs look like:</p><ul><li>&quot;The island did not hydrate on the client because its <code>data-island</code> marker was missing&quot;</li><li>&quot;The client-side island received stale props because the <code>data-props</code> blob was serialized before the loader ran&quot;</li><li>&quot;The page rendered but the interactive island never mounted&quot; — the island was registered in the layout but the placeholder was never emitted</li></ul><p>kiwa surfaces the pattern in four places.</p><ul><li><code>invokeFreshHandler({ handlers, req, page? })</code> — dispatches <code>GET</code> / <code>POST</code> / <code>PATCH</code> / <code>PUT</code> / <code>DELETE</code> handlers, surfaces thrown <code>redirect()</code> / <code>notFound()</code> signals through <code>result.redirect</code> / <code>result.notFound</code>, captures <code>ctx.render(data)</code> into <code>result.renderData</code>, and returns 405 + <code>Allow</code> header for unhandled methods.</li><li><code>defineIsland({ name, component, defaultProps? })</code> — brands the island definition. <code>islandPlaceholder(Island, props)</code> emits a <code>&lt;div data-island=&quot;Name&quot; data-props=&quot;...&quot;&gt;</code> marker that the server sends and the client hydrator picks up.</li><li><code>hydrateIslands({ ssrTree, islands })</code> — walks the SSR tree, decodes each <code>data-props</code> blob, and returns <code>{ hydrated, missing, unregistered, html }</code>. <code>missing</code> catches islands registered but never placed; <code>unregistered</code> catches placeholders referencing an unregistered island.</li><li><code>defineHead({ title, meta, link, script, base })</code> + <code>mergeHead(fragments[])</code> + <code>renderHead(fragment)</code> — Head fragment normalize. Route title wins over layout title, meta rows dedup by <code>name</code> / <code>property</code> (or singleton for <code>charset</code>), links dedup by <code>rel + href</code>, external scripts dedup by <code>src</code>.</li></ul><p>The <strong>contract</strong> each helper enforces is symmetric — every island marker emitted server-side matches a hydration mount client-side, every Head fragment merges into a canonical HTML order, and every route handler either returns a Response, throws a redirect / notFound signal, or falls through to <code>ctx.render(data)</code>.</p><h3 id="why-islands-need-dedicated-hydration-assertions" tabindex="-1">Why Islands need dedicated hydration assertions <a class="header-anchor" href="#why-islands-need-dedicated-hydration-assertions" aria-label="Permalink to &quot;Why Islands need dedicated hydration assertions&quot;">​</a></h3><p>The Next.js pattern &quot;hydrate everything&quot; costs ~200 KB of JavaScript per page. Fresh&#39;s Islands pattern is closer to 5 KB per page — but only if the developer marks exactly the components that need interactivity. Over-mark (turn every static block into an island) and Fresh becomes Next.js with extra syntax. Under-mark (miss a component that should have been interactive) and the button silently does nothing.</p><p><code>hydrateIslands({ ssrTree, islands }).missing</code> catches under-marking (an island registered in the layout but never placed on a route). <code>hydrateIslands({ ssrTree, islands }).unregistered</code> catches over-marking (a placeholder that references an island the client-side hydrator does not know about). Both classes surface at test time with the exact island name that drifted.</p><h2 id="axis-3-—-edge-runtime-hc-rpc-type-safety-honojs" tabindex="-1">Axis 3 — Edge runtime + hc RPC type-safety (HonoJS) <a class="header-anchor" href="#axis-3-—-edge-runtime-hc-rpc-type-safety-honojs" aria-label="Permalink to &quot;Axis 3 — Edge runtime + hc RPC type-safety (HonoJS)&quot;">​</a></h2><p>HonoJS is the standard on Cloudflare Workers. <code>hc</code> is Hono&#39;s typed RPC client — a Proxy that reflects the app&#39;s route tree back at the caller as a TypeScript object graph. Instead of <code>fetch(&#39;/users/42&#39;)</code> with a stringly typed URL, the caller writes <code>client.users[&#39;:id&#39;].$get({ param: { id: &#39;42&#39; } })</code> and the TypeScript compiler catches param name typos, missing body fields, and method mismatches at compile time.</p><p>That divergence matters for tests because Hono + Workers bugs look like:</p><ul><li>&quot;The RPC client sent an unexpected header because the client contract diverged from the server contract&quot;</li><li>&quot;The middleware trace missed the auth entry because a <code>use()</code> pattern did not match the request path&quot;</li><li>&quot;The handler returned 500 because a Workers env binding threw at runtime&quot;</li></ul><p>kiwa surfaces the pattern in three places.</p><ul><li><code>createHonoApp()</code> — chainable app with every HTTP method + <code>use()</code> for middleware. <code>invokeRoute({ app, method, path, headers?, body? })</code> dispatches through the middleware chain and returns <code>{ matched, response, trace, error }</code>.</li><li><code>createRpcClient(app, { baseUrl? })</code> — Proxy client that mirrors the app&#39;s route tree. Terminals are <code>$get</code> / <code>$post</code> / <code>$put</code> / <code>$delete</code> / <code>$patch</code>, and every response carries <code>res.status</code> / <code>res.matched</code> / <code>res.error</code> / <code>res.trace</code> alongside <code>res.json()</code> / <code>res.text()</code>.</li><li><code>mockKVNamespace()</code> / <code>mockD1Database()</code> / <code>mockR2Bucket()</code> — Workers env binding mocks. <code>mockKVNamespace</code> honours <code>expirationTtl</code> by reading <code>Date.now</code>. <code>mockD1Database.__setResponse(sql, rows)</code> seeds canned rows for prepared statements. All three expose <code>__snapshot()</code> / <code>__log()</code> escape hatches.</li></ul><p>The <strong>contract</strong> each helper enforces is symmetric — every middleware that matches records a trace entry, every RPC terminal call generates exactly the request shape a real <code>fetch(URL)</code> would send, and every env binding round-trips through the same put / get / delete / list / expire / snapshot surface as the real Workers runtime.</p><h3 id="why-edge-runtime-typed-rpc-beats-fetch-mocks" tabindex="-1">Why edge runtime + typed RPC beats fetch mocks <a class="header-anchor" href="#why-edge-runtime-typed-rpc-beats-fetch-mocks" aria-label="Permalink to &quot;Why edge runtime + typed RPC beats fetch mocks&quot;">​</a></h3><p>The classic pattern &quot;mock <code>fetch</code> and assert on the URL&quot; costs ~10 lines per endpoint and breaks the moment the URL scheme changes. The RPC client keeps the caller code in sync with the server code — a route renamed on the server surfaces as a TypeScript error at the call site, before any test runs.</p><p><code>client.trace</code> surfaces every middleware hop plus the terminal handler. When a downstream harness catches a regression the test names the exact hop that changed (e.g., <code>trace[0].pattern === &#39;/api/*&#39;</code> instead of the expected <code>/api/auth/*</code>).</p><p>The Workers env mocks share the same 6-op surface as the real Workers runtime, so a Layer 1 unit test asserting on the mock behaviour maps 1:1 onto a Layer 3 fidelity harness driving the same operations against <code>wrangler dev</code>. The <code>__snapshot()</code> escape hatch on every mock returns the whole store as a plain object — a test can assert on the exact key set after a batch of puts, catching leaked keys that a real KV would silently accept.</p><h2 id="axis-4-—-fidelity-vs-cost-trade-off-release-gate-axis" tabindex="-1">Axis 4 — Fidelity vs cost trade-off (release gate axis) <a class="header-anchor" href="#axis-4-—-fidelity-vs-cost-trade-off-release-gate-axis" aria-label="Permalink to &quot;Axis 4 — Fidelity vs cost trade-off (release gate axis)&quot;">​</a></h2><p>The 3 dogfood apps (<code>dogfood-solidjs-signal-app</code> + <code>dogfood-fresh-islands</code> + <code>dogfood-hono-workers-rpc</code>) each produce a <strong>fidelity report</strong> that measures the mock behaviour against the real runtime. The report walks the same trace shape through both surfaces and computes a fidelity ratio in <code>[0, 1]</code>.</p><p>Three properties are load-bearing.</p><ul><li><strong>Fidelity ≥ 0.7 is the release-gate floor.</strong> Below that the mock is lying to the caller — a test that passes against the mock but fails against a real runtime tells the reviewer the mock needs work.</li><li><strong>Fidelity 1.0 is a warning sign, not a goal.</strong> A mock that reproduces the real runtime byte-for-byte is either a real runtime in disguise (slow) or a mock that tracks every irrelevant timing detail (brittle). The target is 0.85–0.95 with intentional divergence documented per axis.</li><li><strong>The fidelity harness runs Layer 3, not Layer 1.</strong> Layer 1 (unit tests) drives the mock. Layer 3 (fidelity harness) drives both mock and real, diffs traces, and emits the fidelity ratio. Layer 2 (integration) rides on the mock — the fidelity harness is what tells the reviewer the mock is worth riding on.</li></ul><p>The <code>evaluateReleaseGate</code> 11-axis contract reads the fidelity ratio through the common 7-axis branch, alongside coverage / test count / perf p95 / mutation kill rate. The AI-LLM 4 axes (cost / latency / token / accuracy) do not apply to modern web framework surfaces — there is no token pricing to measure.</p><h2 id="assertion-patterns" tabindex="-1">Assertion patterns <a class="header-anchor" href="#assertion-patterns" aria-label="Permalink to &quot;Assertion patterns&quot;">​</a></h2><p>The 4 axes produce four assertion patterns.</p><ul><li><strong>Signal + Effect trace assertions</strong> — every effect re-run appends a <code>{ readValues }</code> row to <code>handle.trace()</code>, and the assertion is <code>expect(handle.trace().map((e) =&gt; e.readValues[0])).toEqual([1, 2, 3])</code>. This catches &quot;the effect body captured a stale signal read&quot;.</li><li><strong>Island hydration assertions</strong> — every <code>data-island</code> placeholder that ships from the server matches a hydration mount client-side, and <code>hydrateIslands</code> returns <code>{ hydrated, missing, unregistered }</code>. The assertion is <code>expect(missing).toEqual([])</code> + <code>expect(unregistered).toEqual([])</code>. Missing islands → under-mark; unregistered placeholders → over-mark.</li><li><strong>RPC middleware trace assertions</strong> — every middleware that matches records a <code>{ kind, pattern }</code> entry in <code>result.trace</code>. The assertion is <code>expect(trace.map((e) =&gt; e.kind)).toEqual([&#39;middleware&#39;, &#39;handler&#39;])</code>. This catches &quot;the auth middleware silently short-circuited without recording a reason&quot;.</li><li><strong>Fidelity ratio assertions</strong> — the release gate reads <code>fidelity.ratio &gt;= 0.7</code>. Below the threshold the deploy is blocked. Above it the mock is trusted enough to run in unit-test frequency.</li></ul><p>All four patterns are pure — they add no runtime overhead beyond the mock call. The test grows one function call per assertion and gains a machine-verifiable contract.</p><h2 id="test-count-baseline" tabindex="-1">Test count baseline <a class="header-anchor" href="#test-count-baseline" aria-label="Permalink to &quot;Test count baseline&quot;">​</a></h2><p>The v1.19 dogfood harness ships the following behaviour test counts per axis.</p><ul><li>Axis 1 (Signal reactivity) — <code>packages/solidjs/tests/signal.test.ts</code> × 18 + <code>packages/solidjs/tests/render.test.ts</code> × 14 + <code>packages/solidjs/tests/route.test.ts</code> × 12 = <strong>44 tests</strong></li><li>Axis 2 (Islands + partial hydration) — <code>packages/fresh/tests/route.test.ts</code> × 26 + <code>packages/fresh/tests/islands.test.ts</code> × 16 + <code>packages/fresh/tests/head.test.ts</code> × 10 = <strong>52 tests</strong></li><li>Axis 3 (edge runtime + hc RPC + Workers env) — <code>packages/hono/tests/app.test.ts</code> × 30 + <code>packages/hono/tests/rpc.test.ts</code> × 12 + <code>packages/hono/tests/workers.test.ts</code> × 32 = <strong>74 tests</strong></li><li>Axis 4 (fidelity ratio) — 3 dogfood apps × 3-5 scenarios each + Layer 3 fidelity walker = <strong>12–15 tests</strong></li></ul><p>Every count sits above the 10-test release-gate floor so the 11-axis check passes without special-casing the modern web framework surfaces.</p><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><ul><li><a href="./../tutorials/28-solidjs-signal-app">Tutorial 28 — SolidJS Signal + Effect + Resource + Suspense</a></li><li><a href="./../tutorials/29-fresh-islands">Tutorial 29 — Fresh Islands + Route Handler + Head normalize</a></li><li><a href="./../tutorials/30-hono-workers-rpc">Tutorial 30 — HonoJS + hc RPC type-safe client + Workers env (KV / D1 / R2)</a></li><li><a href="./../migrations/v1.18-to-v1.19">Migration v1.18 → v1.19</a></li><li>v1.14 baseline — <a href="./../tutorials/04-nextjs-server-actions">Testing Next.js Server Actions with @kiwa-lab/nextjs</a></li></ul>',48)])])}const m=t(o,[["render",i]]);export{p as __pageData,m as default};
