import{_ as t,o as s,c as a,a2 as i}from"./chunks/framework.CyZTVJbV.js";const k=JSON.parse('{"title":"Fidelity — dogfood-vercel-ai-rag (v1.12-4)","description":"","frontmatter":{},"headers":[],"relativePath":"quality-reports/ai-llm/vercel-ai-rag.md","filePath":"quality-reports/ai-llm/vercel-ai-rag.md"}'),r={name:"quality-reports/ai-llm/vercel-ai-rag.md"};function d(n,e,o,l,h,c){return s(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="fidelity-—-dogfood-vercel-ai-rag-v1-12-4" tabindex="-1">Fidelity — dogfood-vercel-ai-rag (v1.12-4) <a class="header-anchor" href="#fidelity-—-dogfood-vercel-ai-rag-v1-12-4" aria-label="Permalink to &quot;Fidelity — dogfood-vercel-ai-rag (v1.12-4)&quot;">​</a></h1><p>Real-vs-mock behavioural fidelity for the Vercel AI SDK + LangChain + retriever + embedding + RAG pipeline dogfood, produced by <code>examples/dogfood-vercel-ai-rag/tests/emit-fidelity-report.test.ts</code>. Feeds <code>@kiwa-lab/quality-metrics</code> 11-axis release gate.</p><h2 id="baseline-real-mode-skipped-—-no-openai-api-key-rag-vector-store-url-rag-vector-store-api-key" tabindex="-1">Baseline (real mode skipped — no <code>OPENAI_API_KEY</code> + <code>RAG_VECTOR_STORE_URL</code> + <code>RAG_VECTOR_STORE_API_KEY</code>) <a class="header-anchor" href="#baseline-real-mode-skipped-—-no-openai-api-key-rag-vector-store-url-rag-vector-store-api-key" aria-label="Permalink to &quot;Baseline (real mode skipped — no \`OPENAI_API_KEY\` + \`RAG_VECTOR_STORE_URL\` + \`RAG_VECTOR_STORE_API_KEY\`)&quot;">​</a></h2><p>When the harness runs without full real-mode env, the real adapter emits <code>RAG_ENV_MISSING</code> for every op. Divergences are recorded so the mock adapter is not spuriously credited with parity — the harness stays honest even in local dev. The report below is generated by <code>makeTestRealAdapter()</code> (deterministic hashing embedder + in-memory store) as the &quot;wire the real env to promote these numbers&quot; baseline.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>provider   : @kiwa-lab/ai-llm/vercel-ai-rag</span></span>
<span class="line"><span>version    : 0.1.0</span></span>
<span class="line"><span>verdict    : FAIL (accuracy.score 0.06 vs threshold 0.80)</span></span>
<span class="line"><span>divergences: 1 (langchainInvoke — LangChain surface only touched by mock)</span></span>
<span class="line"><span>axes       : 11 (AI-LLM branch)</span></span></code></pre></div><table tabindex="0"><thead><tr><th>axis</th><th>actual</th><th>threshold</th><th>verdict</th></tr></thead><tbody><tr><td>coverage.line</td><td>92.00%</td><td>85%</td><td>pass</td></tr><tr><td>coverage.branch</td><td>88.00%</td><td>80%</td><td>pass</td></tr><tr><td>coverage.function</td><td>95.00%</td><td>90%</td><td>pass</td></tr><tr><td>fidelity.ratio</td><td>100.00% (5/5)</td><td>70%</td><td>pass</td></tr><tr><td>perf.p95Ms</td><td>~10 ms</td><td>100 ms</td><td>pass</td></tr><tr><td>mutation.killRate</td><td>70.00% (28/40)</td><td>60%</td><td>pass</td></tr><tr><td>testCount.behavior</td><td>22</td><td>10</td><td>pass</td></tr><tr><td>cost.perRequestUsd</td><td>~$0.0001</td><td>$0.10</td><td>pass</td></tr><tr><td>latency.p95Ms</td><td>~10 ms</td><td>3000 ms</td><td>pass</td></tr><tr><td>token.totalTokens</td><td>~570</td><td>4000</td><td>pass</td></tr><tr><td>accuracy.score</td><td>0.06</td><td>0.80</td><td><strong>fail</strong></td></tr></tbody></table><p>Accuracy is the mean Jaccard similarity between the mock answers and the ground-truth strings in the 20-QA-pair set. In real-mode-skipped mode the ground truth is a short paraphrase per question (see <code>RAG_QA_PAIRS</code> in <code>src/flows/rag-flows.ts</code>) while the mock echoes the full seed doc — Jaccard token overlap is inherently low even though the answer is well-grounded, so the fail is by design and signals that fidelity is unverified without a live key. Wiring <code>OPENAI_API_KEY</code> + <code>RAG_VECTOR_STORE_URL</code> + <code>RAG_VECTOR_STORE_API_KEY</code> and running against <code>gpt-4o-mini</code> + <code>text-embedding-3-small</code> + Pinecone promotes the report to the real baseline.</p><h2 id="task-specific-fidelity-embedded-in-report-notes" tabindex="-1">Task-specific fidelity (embedded in report notes) <a class="header-anchor" href="#task-specific-fidelity-embedded-in-report-notes" aria-label="Permalink to &quot;Task-specific fidelity (embedded in report notes)&quot;">​</a></h2><p>The harness records three task-specific metrics alongside the 11 release-gate axes so the AC targets from Issue #698 can be inspected directly.</p><table tabindex="0"><thead><tr><th>metric</th><th>AC target</th><th>observed (test-real baseline)</th></tr></thead><tbody><tr><td>embedding cosine similarity (real vs mock, mean)</td><td>≥ 0.95</td><td>1.000 (test-real shares the hashing embedder)</td></tr><tr><td>retrieval F1 (top-5, mean)</td><td>≥ 0.90</td><td>1.000 (test-real shares the in-memory store)</td></tr><tr><td>answer Jaccard similarity (real vs mock, mean)</td><td>≥ 0.80</td><td>0.06 vs ground-truth paraphrases (see above)</td></tr></tbody></table><p>Wiring the real env re-runs the same computation against <code>text-embedding-3-small</code> + real Pinecone + <code>gpt-4o-mini</code>, at which point the answer Jaccard reflects real-mode-vs-mock-mode diff (which is the AC target 0.80).</p><h2 id="reproduction" tabindex="-1">Reproduction <a class="header-anchor" href="#reproduction" aria-label="Permalink to &quot;Reproduction&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --filter</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dogfood-vercel-ai-rag</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples/dogfood-vercel-ai-rag/quality-report/fidelity-latest.md</span></span></code></pre></div><p>Live real-mode.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> OPENAI_API_KEY</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sk-...</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> OPENAI_CHAT_MODEL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">gpt-4o-mini</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> OPENAI_EMBEDDING_MODEL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">text-embedding-3-small</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> RAG_VECTOR_STORE_URL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">https://</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">index</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.pinecone.io</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> RAG_VECTOR_STORE_API_KEY</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">pinecone key</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> RAG_VECTOR_STORE_INDEX</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">kiwa-dogfood</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --filter</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dogfood-vercel-ai-rag</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span></span></code></pre></div><h2 id="ops-under-measurement" tabindex="-1">Ops under measurement <a class="header-anchor" href="#ops-under-measurement" aria-label="Permalink to &quot;Ops under measurement&quot;">​</a></h2><p>Five provider-neutral ops on <code>RagAdapter</code>.</p><ul><li><code>ingest</code> — chunk + embed + upsert seed docs into the vector store (mock = in-memory, real = Pinecone)</li><li><code>embed</code> — Task 3.1 — deterministic hashing (mock) or real OpenAI embeddings (<code>text-embedding-3-small</code>)</li><li><code>retrieve</code> — Task 3.2 — top-k cosine search returns the retrieved chunks + doc ids</li><li><code>answer</code> — Task 3.3 — full RAG turn (retrieval + LLM generation with context injection)</li><li><code>answerStream</code> — RAG turn with streamed generation (Vercel AI SDK <code>streamText</code>-shape)</li></ul><h2 id="per-question-mock-response-bank-design-ssot" tabindex="-1">Per-question mock response bank (design SSOT) <a class="header-anchor" href="#per-question-mock-response-bank-design-ssot" aria-label="Permalink to &quot;Per-question mock response bank (design SSOT)&quot;">​</a></h2><p>The shared <code>MockEngine</code> inside <code>@kiwa-lab/ai-llm</code> resolves prompts through a response bank keyed by the last <code>role: &#39;user&#39;</code> message. RAG builds a new user prompt per question (context block + question), so the mock adapter constructs a <strong>fresh <code>createVercelAiMock</code> instance per answer</strong> whose response bank is seeded from the retrieved hits — the mock&#39;s answers stay grounded in the retrieved context (mirroring real LLM behaviour) without changes to the shared engine. Metric accumulation is hand-rolled across the fresh clients so the mock&#39;s cost / token / latency totals stay comparable with the real adapter&#39;s.</p><p>The <code>createLangchainMock</code> instance is exercised once during <code>ingest()</code> for surface coverage — a single <code>langchainInvoke</code> trace ensures the LangChain mock participates in the fidelity harness even though the primary answer path uses the Vercel mock. Because the test-real adapter does not touch the LangChain surface, <code>langchainInvoke</code> shows up as the sole <code>BEHAVIORAL_DIVERGENCE</code> in the baseline report — this is intentional and documents that the LangChain surface is a mock-only side channel by design.</p><h2 id="notes" tabindex="-1">Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;Notes&quot;">​</a></h2><p>Provider prefix <code>@kiwa-lab/ai-llm/</code> triggers the 11-axis branch of <code>evaluateReleaseGate</code> (<code>packages/quality-metrics/src/gate.ts</code> — the 4 AI-LLM axes cost / latency / token / accuracy are added on top of the shared 7). Task-specific fidelity metrics (embedding cosine similarity, retrieval F1, answer Jaccard) are attached to the report <code>notes</code> field so the AC targets from Issue #698 are inspectable directly without a bespoke schema change.</p>`,23)])])}const g=t(r,[["render",d]]);export{k as __pageData,g as default};
