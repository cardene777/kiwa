import{_ as t,o as a,c as d,a2 as o}from"./chunks/framework.CyZTVJbV.js";const v=JSON.parse('{"title":"Payment advanced III testing — v0.5 8 axis × 3 provider = 24 cell advanced III grid + 25-axis combined harness + pair 深度 4 段 2 例目 record (SSOT)","description":"","frontmatter":{},"headers":[],"relativePath":"concepts/payment-advanced-III-testing.md","filePath":"concepts/payment-advanced-III-testing.md"}'),i={name:"concepts/payment-advanced-III-testing.md"};function r(c,e,s,n,l,h){return a(),d("div",null,[...e[0]||(e[0]=[o(`<h1 id="payment-advanced-iii-testing-—-v0-5-8-axis-×-3-provider-24-cell-advanced-iii-grid-25-axis-combined-harness-pair-深度-4-段-2-例目-record-ssot" tabindex="-1">Payment advanced III testing — v0.5 8 axis × 3 provider = 24 cell advanced III grid + 25-axis combined harness + pair 深度 4 段 2 例目 record (SSOT) <a class="header-anchor" href="#payment-advanced-iii-testing-—-v0-5-8-axis-×-3-provider-24-cell-advanced-iii-grid-25-axis-combined-harness-pair-深度-4-段-2-例目-record-ssot" aria-label="Permalink to &quot;Payment advanced III testing — v0.5 8 axis × 3 provider = 24 cell advanced III grid + 25-axis combined harness + pair 深度 4 段 2 例目 record (SSOT)&quot;">​</a></h1><p>kiwa&#39;s v1.41-1 payment v0.5 package (<code>@kiwa/payment</code> v0.5.0) covers <strong>8 advanced III axes</strong> that model the deepening production payment posture of a real product stack beyond the v0.4 advanced II axes (payment orchestration + revenue recovery + refund advanced + dispute + webhook idempotency + tax localization + subscription state machine + payment-method vault) — embedded finance (Banking-as-a-Service + card issuance + KYC/KYB) + BNPL (installment plan + risk scoring + late fee) + crypto payment (stablecoin invoicing + on-chain confirmation + gas abstraction + wallet linking) + FX cross-border (multi-currency rate lock + SWIFT/SEPA settlement + rate expiration) + recurring revenue advanced (MRR/ARR + churn/expansion/contraction + NRR rollup) + payment orchestration II (smart routing + ML score gate + fallback ladder + cascade exhaustion) + fraud detection advanced (device fingerprint + behavioral biometrics + velocity + ML fusion) + regulatory reporting (PCI DSS + PSD2 SCA + DORA ICT + SAR + audit lock). This concept doc is the SSOT for those 8 advanced III axes; the tutorials (88-90) and dogfood apps (v1.41-2/3/4) are the concrete implementations.</p><p>The v0.5 grid is orthogonal to the v0.4 advanced II grid — the v0.4 grid covers the &quot;orchestrate a charge, recover a failure, refund a mistake, defend a dispute, deliver a webhook, localize a tax, drive a subscription state, vault a card&quot; primitive across 3 provider (<code>stripe</code> / <code>paddle</code> / <code>lemonsqueezy</code>), and the v0.5 advanced III grid extends the same 3 provider matrix with the &quot;onboard a BaaS account, split a purchase into installments, invoice in stablecoin, settle across borders, roll up NRR, ML-route a payment, ML-block fraud, file a regulatory report&quot; primitives. Read the <code>payment-real-driver-testing.md</code> concept doc first for the v0.3 base grid + <code>payment-testing.md</code> for the webhook signature primitive, then read this doc for the v0.5 advanced III grid.</p><h2 id="the-8-advanced-iii-axes-grid" tabindex="-1">The 8 advanced III axes grid <a class="header-anchor" href="#the-8-advanced-iii-axes-grid" aria-label="Permalink to &quot;The 8 advanced III axes grid&quot;">​</a></h2><p>The 8 axes are cover-oriented — each one names a real-world failure surface every non-trivial production payment stack hits after the v0.4 advanced II axes land.</p><table tabindex="0"><thead><tr><th>Axis</th><th>Real-world failure it catches</th><th>v0.5 API</th></tr></thead><tbody><tr><td>Embedded finance</td><td>&quot;The card was issued to an un-verified account because <code>issueCard</code> did not gate on <code>session.kycStatus === &#39;verified&#39;</code>, and the KYB step silently no-op&#39;d because <code>verifyKyb</code> was called with <code>requireKyb: false</code> in config&quot; (no KYC-verified precondition on card issuance, no explicit <code>requireKyb</code> opt-in check on KYB step)</td><td><code>openAccount</code> / <code>verifyKyc</code> / <code>verifyKyb</code> / <code>issueCard</code> / <code>closeAccount</code></td></tr><tr><td>BNPL</td><td>&quot;The plan was activated on a <code>score: 30</code> customer because <code>scoreRisk</code> compared with <code>&gt;</code> instead of <code>&gt;=</code>, and the late fee accumulator double-counted because <code>chargeLateFee</code> was called twice for the same installment without an index guard&quot; (no <code>&gt;= minRiskScore</code> gate on plan activation, no per-installment idempotency on late fee)</td><td><code>createBnplPlan</code> / <code>scheduleInstallment</code> / <code>scoreRisk</code> / <code>chargeLateFee</code> / <code>markInstallmentPaid</code></td></tr><tr><td>Crypto payment</td><td>&quot;The invoice was marked confirmed on a 1-confirmation tx because the required-confirmations gate was off by one, and the gas abstraction ran on a <code>gasAbstractionEnabled: false</code> session because the config check was inverted&quot; (no <code>&gt;= requiredConfirmations</code> invariant, no explicit <code>gasAbstractionEnabled</code> opt-in check)</td><td><code>createCryptoInvoice</code> / <code>confirmTx</code> / <code>abstractGas</code> / <code>linkWallet</code></td></tr><tr><td>FX cross-border</td><td>&quot;The settlement ran against an expired rate lock because <code>initiateSettlement</code> did not check <code>lockExpiresAt</code>, and the settlement completed against a fresh quote without re-computing <code>amountToCents</code> because <code>completeSettlement</code> used the initial <code>amountFromCents</code> instead of the locked <code>amountToCents</code>&quot; (no <code>Date.now() &gt; lockExpiresAt</code> guard, no <code>session.quote.amountToCents</code> read on settlement)</td><td><code>startFxTransfer</code> / <code>lockRate</code> / <code>initiateSettlement</code> / <code>completeSettlement</code> / <code>expireRate</code></td></tr><tr><td>Recurring revenue advanced</td><td>&quot;The NRR came out negative because <code>recordChurn</code> decremented <code>mrrEnd</code> but <code>computeNrr</code> used <code>mrrStart</code> as the denominator without accounting for the churn in the numerator sign, and the ARR was 10x off because the multiplier was 120 instead of 12&quot; (no <code>(mrrStart - churn - contraction + expansion) / mrrStart * 100</code> invariant, no MRR × 12 = ARR guard)</td><td><code>startRecurringRevenue</code> / <code>computeMrr</code> / <code>recordChurn</code> / <code>recordExpansion</code> / <code>computeNrr</code> / <code>recordContraction</code></td></tr><tr><td>Payment orchestration II</td><td>&quot;The cascade never exhausted because <code>triggerFallback</code> walked past the last provider without moving to <code>&#39;cascade-exhausted&#39;</code>, and the ML score gate accepted a <code>score: -0.1</code> because the bound check was <code>&lt;= 1</code> instead of <code>&gt;= 0 &amp;&amp; &lt;= 1</code>&quot; (no explicit <code>currentIndex &gt;= providers.length</code> guard, no <code>0-1</code> bound assertion on ML score)</td><td><code>startOrchestrationII</code> / <code>smartRoute</code> / <code>scoreMl</code> / <code>triggerFallback</code></td></tr><tr><td>Fraud detection advanced</td><td>&quot;The ML fusion model returned <code>verdict: &#39;accept&#39;</code> on a <code>score: 0.9</code> because the threshold comparison was <code>&gt;</code> instead of <code>&gt;=</code>, and the velocity check flagged a legitimate customer because the window was 1 hour but the counter was reset to 0 on process restart without a persistent store&quot; (no <code>&gt;= mlBlockThreshold</code> invariant, no persistent velocity counter)</td><td><code>startFraudDetection</code> / <code>scoreDevice</code> / <code>verifyBiometric</code> / <code>flagVelocity</code> / <code>scoreMlBlock</code></td></tr><tr><td>Regulatory reporting</td><td>&quot;The SAR was filed twice on the same session because <code>fileSar</code> did not check <code>session.sarFiled</code>, and the audit lock was bypassed because <code>reportPci</code> did not gate on <code>state !== &#39;audit-locked&#39;</code>&quot; (no one-shot invariant on SAR, no terminal-state guard on report steps)</td><td><code>startRegulatoryReporting</code> / <code>reportPci</code> / <code>reportPsd2</code> / <code>reportDora</code> / <code>fileSar</code> / <code>lockForAudit</code></td></tr></tbody></table><p>Each axis has 3 shapes — a mock-only path (fast inner loop, ms scale), a real-driver path (<code>KIWA_MODE=real</code> + real Stripe Treasury / Unit / Column / Klarna / Affirm / Afterpay / Coinbase Commerce / BitPay / Wise / Airwallex, seconds scale), and a fidelity assertion that the two produce the same output. Tutorial 88 covers the embedded-finance + BNPL end-to-end chain (openAccount → KYC → KYB → issueCard → createBnplPlan → schedule → risk → lateFee → paid), tutorial 89 covers the crypto-payment + FX cross-border chain (createCryptoInvoice → confirmTx → abstractGas → linkWallet → startFxTransfer → lockRate → initiateSettlement → completeSettlement → expireRate), tutorial 90 covers the recurring-revenue + orchestration II + fraud-detection + regulatory-reporting chain (startRecurringRevenue → computeMrr → recordChurn → recordExpansion → computeNrr → startOrchestrationII → smartRoute → scoreMl → triggerFallback → startFraudDetection → scoreDevice → verifyBiometric → flagVelocity → scoreMlBlock → startRegulatoryReporting → reportPci → reportPsd2 → reportDora → fileSar → lockForAudit).</p><h2 id="the-3-provider-×-8-axis-24-cell-advanced-iii-grid" tabindex="-1">The 3-provider × 8-axis = 24 cell advanced III grid <a class="header-anchor" href="#the-3-provider-×-8-axis-24-cell-advanced-iii-grid" aria-label="Permalink to &quot;The 3-provider × 8-axis = 24 cell advanced III grid&quot;">​</a></h2><p>Every provider covers every axis. The mock shapes are provider-neutral (the API surface is the same across <code>stripe</code> + <code>paddle</code> + <code>lemonsqueezy</code>), the emitted event dialects are provider-specific (<code>stripe.embedded.account_opened</code> vs. <code>paddle.embedded.account_opened</code> vs. <code>lemonsqueezy.embedded.account_opened</code>), and the advanced III fidelity harness reports the coverage explicitly through <code>collectFidelityCoverage()</code> walking all 25 axes (v0.3 9 + v0.4 8 + v0.5 8).</p><table tabindex="0"><thead><tr><th>Provider</th><th>Embedded finance</th><th>BNPL</th><th>Crypto payment</th><th>FX cross-border</th><th>Recurring revenue adv</th><th>Orchestration II</th><th>Fraud detection adv</th><th>Regulatory reporting</th></tr></thead><tbody><tr><td>stripe</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td></tr><tr><td>paddle</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td></tr><tr><td>lemonsqueezy</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td></tr></tbody></table><p>The v0.5 advanced III grid is fully covered — every provider implements every axis because the semantics are runtime-agnostic. That is what makes cross-provider reuse (a BNPL plan that runs under Stripe + Paddle + LemonSqueezy without change) even possible. The neutral event names live in the v0.5 semantics <code>types.ts</code> SSOT and the per-provider dialect table is a static lookup — a new provider drops into the same shape.</p><h3 id="why-the-advanced-iii-grid-is-fully-covered" tabindex="-1">Why the advanced III grid is fully covered <a class="header-anchor" href="#why-the-advanced-iii-grid-is-fully-covered" aria-label="Permalink to &quot;Why the advanced III grid is fully covered&quot;">​</a></h3><p>stripe + paddle + lemonsqueezy converged on the same neutral events at the &quot;onboard a customer, split a purchase, invoice in stablecoin, settle across borders, roll up recurring metrics, route a payment, block fraud, file a regulatory report&quot; primitive — the 8 advanced III shapes are the same across all 3 providers, even though the wire encodings differ (Stripe Webhook Events vs. Paddle Notifications vs. Lemon Squeezy Webhook payloads). The <code>providerEventName(provider, neutralEvent)</code> mapping table is the single point where the 3 dialects diverge; everything upstream stays neutral. The v1.41 advanced III fidelity grid at 24/24 = 100 % implemented reflects that convergence at the &quot;advanced III production payment&quot; level. Combined with the v0.3 27-cell base grid (9 axis × 3 provider) + the v0.4 24-cell advanced II grid (8 axis × 3 provider) + the v0.5 24-cell advanced III grid, the total v0.3 + v0.4 + v0.5 fidelity harness now walks 75 cells (3 provider × 25 axis) in one <code>collectFidelityCoverage()</code> call.</p><h2 id="the-kiwa-mode-real-env-gate-contract-for-the-advanced-iii-grid" tabindex="-1">The <code>KIWA_MODE=real</code> env-gate contract for the advanced III grid <a class="header-anchor" href="#the-kiwa-mode-real-env-gate-contract-for-the-advanced-iii-grid" aria-label="Permalink to &quot;The \`KIWA_MODE=real\` env-gate contract for the advanced III grid&quot;">​</a></h2><p><code>skipUnlessReal(provider, env)</code> (from <code>payment-real-driver-testing.md</code> SSOT) returns <code>{ skip: false, reason: &#39;KIWA_MODE=real + required env present — real driver&#39; }</code> when <code>env.KIWA_MODE === &#39;real&#39;</code> and the required env for that provider is set, and <code>{ skip: true, reason: &#39;KIWA_MODE!=real (got &quot;unset&quot;) — mock driver&#39; }</code> otherwise. A test that respects the contract combines the gate with a required-env presence check — the dogfood apps use this at each <code>describe.skipIf(gate.skip)</code> block. The v0.5 advanced III axes reuse the same real-driver gate as the v0.3 / v0.4 axes — no new env variable is introduced. The per-axis dogfood app pins the required backend URL (<code>KIWA_STRIPE_TREASURY_URL</code> / <code>KIWA_KLARNA_URL</code> / <code>KIWA_CRYPTO_URL</code> / <code>KIWA_FX_URL</code>) on top of the shared <code>KIWA_MODE</code> gate.</p><p>Per-provider required-env mapping stays the same as v0.3 / v0.4.</p><ul><li><strong>stripe</strong> → <code>KIWA_MODE</code> + <code>KIWA_STRIPE_URL</code> (base Stripe API) + optional <code>KIWA_STRIPE_TREASURY_URL</code> (embedded finance)</li><li><strong>paddle</strong> → <code>KIWA_MODE</code> + <code>KIWA_PADDLE_URL</code></li><li><strong>lemonsqueezy</strong> → <code>KIWA_MODE</code> + <code>KIWA_LEMONSQUEEZY_URL</code></li></ul><p>Per-dogfood-app required backend URL (on top of the provider gate above).</p><ul><li><strong>dogfood-payment-embedded-finance-app</strong> → <code>KIWA_STRIPE_TREASURY_URL</code> + <code>KIWA_UNIT_URL</code> + <code>KIWA_COLUMN_URL</code> (Stripe Treasury + Unit + Column BaaS)</li><li><strong>dogfood-payment-bnpl-installment-app</strong> → <code>KIWA_KLARNA_URL</code> + <code>KIWA_AFFIRM_URL</code> + <code>KIWA_AFTERPAY_URL</code> (Klarna + Affirm + Afterpay BNPL sandboxes)</li><li><strong>dogfood-payment-crypto-fx-app</strong> → <code>KIWA_CRYPTO_URL</code> (Coinbase Commerce / BitPay) + <code>KIWA_FX_URL</code> (Wise / Airwallex)</li></ul><h2 id="the-25-axis-fidelity-harness-v0-3-9-v0-4-8-v0-5-8-combined" tabindex="-1">The 25-axis fidelity harness (v0.3 9 + v0.4 8 + v0.5 8 combined) <a class="header-anchor" href="#the-25-axis-fidelity-harness-v0-3-9-v0-4-8-v0-5-8-combined" aria-label="Permalink to &quot;The 25-axis fidelity harness (v0.3 9 + v0.4 8 + v0.5 8 combined)&quot;">​</a></h2><p><code>collectFidelityCoverage()</code> walks the full 25-axis grid and returns a per-cell <code>{ provider, axis, implemented, missingEvents }</code> record so a CI job can assert <code>rows.every(r =&gt; r.implemented)</code> in one line. The neutral events per axis floor (asserted in the fidelity test) is preserved for the 8 new v0.5 axes.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { collectFidelityCoverage } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@kiwa/payment&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> cov</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> collectFidelityCoverage</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">console.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">log</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(cov.rows.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">length</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">); </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 75 (3 provider × 25 axis)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">console.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">log</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(cov.rows.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">every</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">r</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> r.implemented)); </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// true after v0.5 lands</span></span></code></pre></div><p>The 75-row grid is the single point of truth for the &quot;is a provider × axis pair covered?&quot; question — a new dogfood app that adds a new provider walks the same table without touching the axis list.</p><h2 id="the-v1-41-pair-深度-4-段-2-例目-record" tabindex="-1">The v1.41 pair 深度 4 段 2 例目 record <a class="header-anchor" href="#the-v1-41-pair-深度-4-段-2-例目-record" aria-label="Permalink to &quot;The v1.41 pair 深度 4 段 2 例目 record&quot;">​</a></h2><p>v0.5 is the <strong>second pair in kiwa history to reach depth 4 (v1.14 → v1.19 → v1.33 → v1.41)</strong>. The first was the AI-LLM 縦深化 pair (v1.12 v0.1 base → v1.15 v0.2 multimodal → v1.38 v0.4 advanced → v1.40 v0.5 advanced III), recorded in the v1.39 → v1.40 migration guide. The v1.41 milestone establishes the pair 深度 4 段 pattern as <strong>repeatable</strong> — the same 3-provider × 8-axis fidelity harness template applies at each depth without change, and the 深化 process (v0.1 base → v0.2 advanced → v0.4 advanced II → v0.5 advanced III) is idempotent under scale.</p><p>Combined with the v1.14 v0.1 base grid (9 axis × 3 provider = 27 cell) + the v1.19 v0.2 advanced grid (subset of v0.4 later) + the v1.33 v0.4 advanced II grid (8 axis × 3 provider = 24 cell) + the v1.41 v0.5 advanced III grid (8 axis × 3 provider = 24 cell), the payment package now covers 25 axes × 3 provider = 75 cells of production payment shape in one package with one fidelity harness — matched only by the AI-LLM package&#39;s 64-cell breadth (16 axes × 4 provider).</p><p>The 19-milestone snippet validation streak (v1.23 → v1.41) reinforces the same repeatability signal for docs — every milestone since v1.23 has added a snippet-validation test that walks the tutorial&#39;s code blocks against the actual API surface, catching drift before readers hit &quot;the tutorial does not compile&quot; gaps. The Payment 深化 pair achieves depth 4 as the second axis after AI-LLM, validating that the methodology is not AI-LLM-specific but applies to any production shape (payment / observability / security / streaming / etc.) that has natural v0.1 → v0.5 evolution.</p>`,27)])])}const u=t(i,[["render",r]]);export{v as __pageData,u as default};
