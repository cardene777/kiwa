import{_ as t,o as a,c as o,a2 as r}from"./chunks/framework.CyZTVJbV.js";const v=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"announcements/v1.28/x-thread-en.md","filePath":"announcements/v1.28/x-thread-en.md"}'),i={name:"announcements/v1.28/x-thread-en.md"};function s(n,e,d,c,p,l){return a(),o("div",null,[...e[0]||(e[0]=[r('<p>1/ kiwa v1.28 is out — Realtime depth II milestone. After v1.13 landed <code>@kiwa-test/realtime</code> v0.1 with 4 provider (Supabase / Ably / Pusher / Socket.io) × 5 base semantics unified mock, v1.28 stacks <strong>advanced realtime production semantics 8 axis (WebRTC + WebTransport + HTTP/3 + QUIC multiplexing) + 3 dogfood apps + real driver env-gate</strong>. <code>@kiwa-test/realtime</code> v0.1.1 → v0.2.0 minor bump. 24-row 3×8 fidelity grid + 30 snippet tests + 6 sub-Issue merges.</p><p>2/ Why 8 axes? Wrapping WebRTC / WebTransport / HTTP/3 in one &quot;realtime&quot; abstraction hides 3 protocol-specific shapes — WebRTC&#39;s 4-step SDP offer/answer + ICE trickle, WebTransport&#39;s HTTP/3 CONNECT + uni/bi stream flag, HTTP/3&#39;s <code>PUSH_PROMISE</code> server-initiated frame gated by <code>SETTINGS_ENABLE_PUSH</code>. The 8-axis SSOT (<code>docs/concepts/webrtc-webtransport-testing.md</code>) names the shape each protocol enforces so a fidelity regression on <code>webrtc-data-channel</code> reliable mode surfaces as a single cell change on the 24-row grid — not a percentage that could shift for any of 24 reasons.</p><p>3/ v1.28-1 realtime v0.2 advanced semantics (Issue #977) — 8 axis × 3 protocol = 24 row fidelity grid: <code>webrtc-signaling</code> (offer/answer + SDP + ICE + renegotiation) / <code>webrtc-data-channel</code> (ordered + reliable + maxRetransmits + binaryType) / <code>webrtc-media-track</code> (getUserMedia + MediaStream + simulcast) / <code>webrtc-ice-stun-turn</code> (candidate gathering + TURN relay + trickle ICE) / <code>webtransport-unidirectional</code> (uni stream + Datagram + reset) / <code>webtransport-bidirectional</code> (bi stream + flow control + backpressure) / <code>http3-push</code> (server push + prioritization + <code>PUSH_PROMISE</code>) / <code>quic-multiplexing</code> (stream priority + HPACK + 0-RTT resumption). 60 semantics behavior test + real driver env-gate for v1.13 4 provider.</p><p>4/ v1.28-2 dogfood-nextjs-webrtc-video-app (Issue #978) — Next.js 15 App Router + mediasoup SFU + coturn TURN server (testcontainers) + WebRTC video/audio + P2P + simulcast + ICE restart. Realtime video call room + track add/remove + reconnect on network flap + 36 vitest all-PASS with <code>KIWA_MODE=real</code> env-gate for coturn opt-in.</p><p>5/ v1.28-3 dogfood-nuxt-webtransport-stream-app (Issue #979) — Nuxt 3 + WebTransport unidirectional + bidirectional stream + HTTP/3 + Datagram + connection migration + 0-RTT resumption. Edge streaming with backpressure + connection migration across network change + aioquic testcontainers fidelity harness + 30 vitest all-PASS.</p><p>6/ v1.28-4 dogfood-sveltekit-http3-multiplex-app (Issue #980, #982 follow-up) — SvelteKit + nginx-quic HTTP/3 + QUIC multiplex + stream priority + HPACK dynamic table + 0-RTT resumption. Multi-stream + priority scheduling + HPACK observation + 32 vitest + 4 adversarial-review findings (closeStream FIN + HPACK metrics teardown + 0-RTT origin isolation + priority range) addressed in follow-up PR #982.</p><p>7/ v1.28-5 docs (Issue #983) — tutorial 52 (WebRTC video signaling + SFU vs P2P) + tutorial 53 (WebTransport stream + HTTP/3 Datagram) + tutorial 54 (HTTP/3 multiplex + HPACK + 0-RTT) + concept doc <code>webrtc-webtransport-testing.md</code> 8-axis SSOT + migration <code>v1.27 → v1.28</code> (additive-only) + snippet validation <code>docs-tutorial-v1.28.test.ts</code> (30 test) re-runs every code snippet against the real <code>@kiwa-test/realtime</code> v0.2 API.</p><p>8/ v1.28-6 publish (Issue #976) — <code>@kiwa-test/realtime</code> v0.1.1 → v0.2.0 minor bump goes to npm. plugin.json 1.28.0 + new WebRTC / WebTransport / QUIC / HPACK keywords + Roadmap ✅ v1.28 row + announcement 4 file + release-smoke <code>v1-28-publish.test.ts</code> (7 axis invariant) + docs-e2e <code>V1_28_PAGES</code> (4 pages) + <strong>release script filter check</strong> — verify <code>@kiwa-test/realtime</code> present in both <code>-F</code> and <code>--filter</code> halves (v1.14 payment + v1.25 perf-harness + v1.27 quality-metrics lesson, 4th application of the systematic root-cause pattern). gh-pages published via <code>/docs-publish-kiwa</code>.</p><p>9/ 17-milestone streak — v1.11 (release gate) → v1.12 (non-determinism) → v1.13 (time-axis) → v1.14 (horizontal expansion) → v1.15 (AI-LLM depth) → v1.16 (component depth) → v1.17 (Observability v2) → v1.18 (Blockchain depth) → v1.19 (Framework depth) → v1.20 (Streaming depth) → v1.21 (Auth depth) → v1.22 (Auth depth II) → v1.23 (Payment depth) → v1.24 (Edge / Serverless depth) → v1.25 (Perf-harness sweep) → v1.26 (Database depth) → v1.27 (Mutation testing sweep) → v1.28 (Realtime depth II). Every milestone since v1.11 has landed 6 sub-Issues in full. Roadmap: <a href="https://github.com/cardene777/kiwa#roadmap" target="_blank" rel="noreferrer">https://github.com/cardene777/kiwa#roadmap</a></p>',9)])])}const u=t(i,[["render",s]]);export{v as __pageData,u as default};
