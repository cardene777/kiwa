import{_ as t,o as a,c as d,a2 as s}from"./chunks/framework.CyZTVJbV.js";const g=JSON.parse('{"title":"Fidelity — dogfood-vercel-edge-function-app (v1.24-3)","description":"","frontmatter":{},"headers":[],"relativePath":"quality-reports/edge/vercel-edge-function-app.md","filePath":"quality-reports/edge/vercel-edge-function-app.md"}'),i={name:"quality-reports/edge/vercel-edge-function-app.md"};function o(r,e,n,c,l,p){return a(),d("div",null,[...e[0]||(e[0]=[s(`<h1 id="fidelity-—-dogfood-vercel-edge-function-app-v1-24-3" tabindex="-1">Fidelity — dogfood-vercel-edge-function-app (v1.24-3) <a class="header-anchor" href="#fidelity-—-dogfood-vercel-edge-function-app-v1-24-3" aria-label="Permalink to &quot;Fidelity — dogfood-vercel-edge-function-app (v1.24-3)&quot;">​</a></h1><p>Real-vs-mock behavioural fidelity for the Next.js 15 middleware + edge runtime app driven by <code>@kiwa-test/edge</code> v0.2 (v1.24-1 land, PR #920) under <code>KIWA_MODE=real</code> (Vercel Edge sandbox + <code>vercel dev</code> + <code>VERCEL_KEY=1</code> env-gate) and <code>KIWA_MODE=mock</code> (<code>@kiwa-test/edge</code> v0.2 8 axis semantics), produced by <code>examples/dogfood-vercel-edge-function-app/tests/emit-fidelity-report.test.ts</code>. Feeds <code>@kiwa-test/quality-metrics</code> release-gate 7-axis payload as the second edge-platform dogfood in the v1.24 milestone.</p><h2 id="baseline-real-mode-skipped-—-kiwa-mode-real-vercel-key-1-unset" tabindex="-1">Baseline (real mode skipped — <code>KIWA_MODE=real</code> + <code>VERCEL_KEY=1</code> unset) <a class="header-anchor" href="#baseline-real-mode-skipped-—-kiwa-mode-real-vercel-key-1-unset" aria-label="Permalink to &quot;Baseline (real mode skipped — \`KIWA_MODE=real\` + \`VERCEL_KEY=1\` unset)&quot;">​</a></h2><p>When the harness runs without both <code>KIWA_MODE=real</code> and <code>VERCEL_KEY=1</code> in the environment, the real adapter emits <code>KIWA_VERCEL_EDGE_ENV_MISSING</code> for each of the 8 ops (<code>driveGeoRoute</code> / <code>driveGeoPrimaryWrite</code> / <code>driveGeoReplicaSync</code> / <code>driveKvRead</code> / <code>driveKvWrite</code> / <code>driveKvRangeQuery</code> / <code>driveSseOpen</code> / <code>driveSseBackpressure</code>). Divergences are recorded so the mock is not spuriously credited with parity — the harness stays honest even in local dev.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>provider   : @kiwa-test/edge/vercel-edge-function</span></span>
<span class="line"><span>version    : 0.1.0</span></span>
<span class="line"><span>verdict    : PASS</span></span>
<span class="line"><span>divergences: 8 (all eight ops recorded as BEHAVIORAL_DIVERGENCE, real mode absent)</span></span>
<span class="line"><span>axes       : 7 (framework branch — common 7-axis release gate)</span></span></code></pre></div><table tabindex="0"><thead><tr><th>axis</th><th>actual</th><th>threshold</th><th>verdict</th></tr></thead><tbody><tr><td>coverage.line</td><td>92.00%</td><td>85%</td><td>pass</td></tr><tr><td>coverage.branch</td><td>86.00%</td><td>80%</td><td>pass</td></tr><tr><td>coverage.function</td><td>95.00%</td><td>90%</td><td>pass</td></tr><tr><td>fidelity.ratio</td><td>100.00% (8/8)</td><td>70%</td><td>pass</td></tr><tr><td>perf.p95Ms</td><td>~0.10 ms</td><td>100 ms</td><td>pass</td></tr><tr><td>mutation.killRate</td><td>70.00% (28/40)</td><td>60%</td><td>pass</td></tr><tr><td>testCount.behavior</td><td>30</td><td>10</td><td>pass</td></tr></tbody></table><p>The <code>divergences</code> count in the notes section counts every op whose mock path succeeded but whose real path emitted <code>KIWA_VERCEL_EDGE_ENV_MISSING</code> — this is the expected shape in a real-mode-skipped baseline. It does not fail the gate; the fidelity ratio measures the mock-covered surface area, which is 100% for the eight ops the AC scopes.</p><h2 id="reproduction" tabindex="-1">Reproduction <a class="header-anchor" href="#reproduction" aria-label="Permalink to &quot;Reproduction&quot;">​</a></h2><p>Common integration path (mock + graceful-skip real).</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --filter</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dogfood-vercel-edge-function-app</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples/dogfood-vercel-edge-function-app/quality-report/fidelity-latest.md</span></span></code></pre></div><p>Live real-mode (real Vercel Edge sandbox or <code>vercel dev</code> — env-skip unless the driver is opted in).</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> KIWA_MODE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">real</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> VERCEL_KEY</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --filter</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dogfood-vercel-edge-function-app</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># The v1.24-3 dogfood ships the env-gate skip path only; a follow-up</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># milestone swaps in a real Vercel Edge sandbox driver behind the same env gate.</span></span></code></pre></div><h2 id="_8-op-surface-geo-replicated-3-edge-kv-3-streaming-response-2" tabindex="-1">8-op surface = geo-replicated 3 + edge-kv 3 + streaming-response 2 <a class="header-anchor" href="#_8-op-surface-geo-replicated-3-edge-kv-3-streaming-response-2" aria-label="Permalink to &quot;8-op surface = geo-replicated 3 + edge-kv 3 + streaming-response 2&quot;">​</a></h2><p>The 8 ops correspond directly to the 8 axis routing pattern inherited from v1.24-1 (<code>@kiwa-test/edge</code> v0.2 semantics). Each op emits neutral events on 1 axis:</p><table tabindex="0"><thead><tr><th>op</th><th>primary axis</th><th>neutral events emitted</th></tr></thead><tbody><tr><td><code>driveGeoRoute</code></td><td>geo-replicated</td><td>stateless region resolution from Accept-Language + <code>x-vercel-ip-country</code>, no session mutation</td></tr><tr><td><code>driveGeoPrimaryWrite</code></td><td>geo-replicated</td><td><code>geo.primary-write</code> — bumps version + marks every replica lagging</td></tr><tr><td><code>driveGeoReplicaSync</code></td><td>geo-replicated</td><td><code>geo.replica-lagged</code> + <code>geo.replica-synced</code> per replica + optional <code>geo.conflict-resolved</code></td></tr><tr><td><code>driveKvRead</code></td><td>edge-kv</td><td><code>kv.read</code> (read-through) / <code>kv.cache-hit</code> (warm) / <code>kv.cache-miss</code> (absent)</td></tr><tr><td><code>driveKvWrite</code></td><td>edge-kv</td><td><code>kv.write</code> — persists to store + invalidates cache entry</td></tr><tr><td><code>driveKvRangeQuery</code></td><td>edge-kv</td><td><code>kv.read</code> with a prefix + sorted match list</td></tr><tr><td><code>driveSseOpen</code></td><td>streaming-response</td><td><code>stream.opened</code> + <code>stream.chunk-sent</code> for the initial event</td></tr><tr><td><code>driveSseBackpressure</code></td><td>streaming-response</td><td><code>stream.chunk-sent</code> per follow-up + <code>stream.backpressure</code> on high-water breach + <code>stream.closed</code></td></tr></tbody></table><h2 id="_3-route-paths-dispatched-by-the-next-js-15-edge-runtime" tabindex="-1">3 route paths dispatched by the Next.js 15 edge runtime <a class="header-anchor" href="#_3-route-paths-dispatched-by-the-next-js-15-edge-runtime" aria-label="Permalink to &quot;3 route paths dispatched by the Next.js 15 edge runtime&quot;">​</a></h2><table tabindex="0"><thead><tr><th>route</th><th>HTTP method</th><th>handler kind</th></tr></thead><tbody><tr><td><code>/api/geo</code></td><td>GET</td><td>echoes middleware-resolved region + language + fellBack flag</td></tr><tr><td><code>/api/kv</code></td><td>GET / POST</td><td><code>handleKvGet</code> (read-through via KV) / <code>handleKvPost</code> (write + invalidate) / <code>handleKvRange</code> (prefix scan)</td></tr><tr><td><code>/api/stream</code></td><td>GET</td><td><code>handleSse</code> opens SSE stream + writes N chunks + closes cleanly</td></tr></tbody></table><p><code>middleware.ts</code> runs at every edge request to stamp <code>x-kiwa-region</code> / <code>x-kiwa-language</code> / <code>x-kiwa-fell-back</code> on the response so downstream handlers see the negotiated region.</p><h2 id="test-spec-map-41-tests-total" tabindex="-1">Test spec map (41 tests total) <a class="header-anchor" href="#test-spec-map-41-tests-total" aria-label="Permalink to &quot;Test spec map (41 tests total)&quot;">​</a></h2><ul><li><code>geo-routing-e2e.spec.ts</code> (12 tests) — geo-replicated axis: country-decisive routing, Accept-Language fallback, country-vs-language precedence, default region fallback, primary write + replica sync, partial sync stays lagging, routing statelessness, middleware layer, <code>/api/geo</code> route echo, metrics, real env-skip (2 ops).</li><li><code>cache-invalidation-e2e.spec.ts</code> (13 tests) — edge-kv axis: cache-miss on absent key, write invalidates cache, second read is cache-hit, unchanged rewrite still invalidates, range query sorted, empty range, <code>/api/kv</code> GET/POST/range, 404 on absent key, metrics, real env-skip (3 ops).</li><li><code>sse-stream-e2e.spec.ts</code> (11 tests) — streaming-response axis: initial chunk delivery, small run stays under high-water, overwhelming run hits backpressure, close idempotence, shared session across open + backpressure, Vercel KV + SSE integration, <code>serializeSseFrame</code> wire format, <code>/api/stream</code> route, metrics, real env-skip (2 ops).</li><li><code>fidelity-report.test.ts</code> (4 tests) — harness contract: 8-op mock coverage, mock failure propagation, divergence notes on skip, p95 latency surfaces.</li><li><code>emit-fidelity-report.test.ts</code> (1 test) — writes JSON snapshot + markdown report to <code>quality-report/</code>.</li></ul><h2 id="env-gate-escape-hatch" tabindex="-1">Env-gate escape hatch <a class="header-anchor" href="#env-gate-escape-hatch" aria-label="Permalink to &quot;Env-gate escape hatch&quot;">​</a></h2><p>The real adapter reads two environment variables at construction:</p><ul><li><code>KIWA_MODE=real</code> — enables the connected path; any other value returns the skip adapter.</li><li><code>VERCEL_KEY=1</code> — proxy for &quot;vercel is installed + authorized&quot; so tests do not spawn <code>vercel dev</code> unless the caller explicitly opts in.</li></ul><p>When either gate is unset the adapter records <code>KIWA_VERCEL_EDGE_ENV_MISSING</code> on every op call, throws <code>SkippedError</code>, and lets the harness compute divergences from the trace. This lets the same test suite run in local dev without a Vercel installation and in CI with a <code>vercel dev</code> subprocess behind the env gate.</p><h2 id="refs" tabindex="-1">Refs <a class="header-anchor" href="#refs" aria-label="Permalink to &quot;Refs&quot;">​</a></h2><ul><li>Parent — v1.24 (#913)</li><li>Sub-Issue — v1.24-3 (#916)</li><li>Depends on — v1.24-1 (@kiwa-test/edge v0.2 with 8 axis semantics, PR #920)</li><li>Sibling — v1.24-2 (dogfood-cloudflare-workers-durable-object-app, PR #921)</li></ul>`,26)])])}const u=t(i,[["render",o]]);export{g as __pageData,u as default};
