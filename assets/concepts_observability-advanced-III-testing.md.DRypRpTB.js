import{_ as t,o as a,c as o,a2 as i}from"./chunks/framework.CyZTVJbV.js";const v=JSON.parse('{"title":"Observability advanced III testing — v2.2 8 axis × 4 provider = 32 cell advanced III grid + 16-axis combined harness + pair 深度 4 段拡張 3 例目 record (SSOT)","description":"","frontmatter":{},"headers":[],"relativePath":"concepts/observability-advanced-III-testing.md","filePath":"concepts/observability-advanced-III-testing.md"}'),d={name:"concepts/observability-advanced-III-testing.md"};function r(s,e,c,n,l,h){return a(),o("div",null,[...e[0]||(e[0]=[i(`<h1 id="observability-advanced-iii-testing-—-v2-2-8-axis-×-4-provider-32-cell-advanced-iii-grid-16-axis-combined-harness-pair-深度-4-段拡張-3-例目-record-ssot" tabindex="-1">Observability advanced III testing — v2.2 8 axis × 4 provider = 32 cell advanced III grid + 16-axis combined harness + pair 深度 4 段拡張 3 例目 record (SSOT) <a class="header-anchor" href="#observability-advanced-iii-testing-—-v2-2-8-axis-×-4-provider-32-cell-advanced-iii-grid-16-axis-combined-harness-pair-深度-4-段拡張-3-例目-record-ssot" aria-label="Permalink to &quot;Observability advanced III testing — v2.2 8 axis × 4 provider = 32 cell advanced III grid + 16-axis combined harness + pair 深度 4 段拡張 3 例目 record (SSOT)&quot;">​</a></h1><p>kiwa&#39;s v1.42-1 observability v2.2 package (<code>@kiwa-lab/observability</code> v2.2.0) covers <strong>8 advanced III axes</strong> that model the deepening production observability posture of a real product stack beyond the v2.1 advanced axes (SLO + RED-USE + exemplar + OTel advanced + log correlation + alert routing + profiling + cardinality) — IaC observability (Terraform plan capture + drift detection + OPA policy evaluation + team cost attribution) + service mesh (Istio / Linkerd mTLS + sidecar injection + circuit breaker + traffic split) + eBPF profiling III (user-space uprobe + kernel kprobe / tracepoint / LSM + syscall counters + network flow) + LLM observability (token counting + prompt log + hallucination detection + budget check) + FinOps observability (cost per request + team attribution + rightsizing + spot optimization) + chaos engineering (fault injection + blast radius + auto-rollback + game day) + data-pipeline (OpenLineage capture + freshness SLA + schema drift + data quality) + AIOps (anomaly detection + auto-remediation + RCA + alert correlation). This concept doc is the SSOT for those 8 advanced III axes; the tutorials (91-93) and dogfood apps (v1.42-2/3/4) are the concrete implementations.</p><p>The v2.2 grid is orthogonal to the v2.1 advanced grid — the v2.1 grid covers the &quot;compute an SLO error budget, aggregate a RED-USE panel, attach an exemplar, batch an OTel export, join a log to a trace, silence an alert, sample a CPU profile, detect a high-cardinality label&quot; primitive across 4 provider (<code>grafana-oss</code> / <code>prometheus</code> / <code>loki</code> / <code>otel-collector</code>), and the v2.2 advanced III grid extends the same 4 provider matrix with the &quot;capture an IaC plan, handshake mTLS, probe a syscall, count LLM tokens, attribute FinOps cost, inject a chaos fault, evaluate pipeline freshness, correlate AIOps alerts&quot; primitives. Read the <code>observability-v2-testing.md</code> concept doc first for the v2.0 base grid (dashboard + alert + trace + correlation) + <code>observability-real-driver-testing.md</code> for the v2.1 real-driver pattern, then read this doc for the v2.2 advanced III grid.</p><h2 id="the-8-advanced-iii-axes-grid" tabindex="-1">The 8 advanced III axes grid <a class="header-anchor" href="#the-8-advanced-iii-axes-grid" aria-label="Permalink to &quot;The 8 advanced III axes grid&quot;">​</a></h2><p>The 8 axes are cover-oriented — each one names a real-world failure surface every non-trivial production observability stack hits after the v2.1 axes land.</p><table tabindex="0"><thead><tr><th>Axis</th><th>Real-world failure it catches</th><th>v2.2 API</th></tr></thead><tbody><tr><td>IaC</td><td>&quot;The drift detector missed a resource because <code>detectDrift</code> compared expected vs. actual with the wrong direction, and the OPA policy evaluation counted <code>passed: false</code> as <code>passedCount + 1</code> because the branch inverted&quot; (no set-diff invariant on drift, no boolean-inverse guard on policy count)</td><td><code>startIacSession</code> / <code>capturePlan</code> / <code>detectDrift</code> / <code>evaluatePolicy</code> / <code>attributeCost</code></td></tr><tr><td>Service mesh</td><td>&quot;The mTLS handshake accepted an un-scoped SPIFFE ID because <code>handshakeMtls</code> did not gate on <code>spiffe://</code> prefix, and the circuit breaker never tripped because <code>tripCircuitBreaker</code> compared <code>failures / total &gt; threshold</code> instead of <code>&gt;=</code>&quot; (no <code>spiffe://</code> prefix guard, no <code>&gt;= threshold</code> invariant)</td><td><code>startMeshSession</code> / <code>handshakeMtls</code> / <code>injectSidecar</code> / <code>tripCircuitBreaker</code> / <code>applyTrafficSplit</code></td></tr><tr><td>eBPF-III</td><td>&quot;The uprobe count double-fired because <code>probeUserspace</code> accepted a duplicate symbol without a set guard, and the kernel hook categoriser lumped every kprobe / tracepoint / LSM sample into a single bucket because the categoriser did not switch on <code>kind</code>&quot; (no dedup on probe symbols, no per-kind branch)</td><td><code>startEbpfIiiSession</code> / <code>probeUserspace</code> / <code>traceKernel</code> / <code>recordSyscall</code> / <code>captureNetworkFlow</code></td></tr><tr><td>LLM observability</td><td>&quot;The budget alert never fired because <code>checkBudget</code> compared <code>spentUsd &gt; limitUsd</code> instead of <code>&gt;=</code>, and the hallucination check missed the toxicity signal because <code>flagHallucination</code> used the same direction (below-threshold) for toxicity as faithfulness&quot; (no <code>&gt;=</code> invariant on budget, no per-signal direction switch)</td><td><code>startLlmObsSession</code> / <code>countTokens</code> / <code>logPrompt</code> / <code>flagHallucination</code> / <code>checkBudget</code></td></tr><tr><td>FinOps</td><td>&quot;The cost-per-request came out as <code>Infinity</code> because <code>recordCostPerRequest</code> accepted <code>requests: 0</code>, and the team-attribution remainder went negative because <code>attributeTeam</code> did not clamp to 0 when teams over-accounted&quot; (no divide-by-zero guard, no clamp-to-0 on unattributed)</td><td><code>startFinopsSession</code> / <code>recordCostPerRequest</code> / <code>attributeTeam</code> / <code>recommendRightsizing</code> / <code>optimizeSpot</code></td></tr><tr><td>Chaos</td><td>&quot;The rollback never fired because <code>triggerRollback</code> compared <code>errorRate &gt; threshold</code> instead of <code>&gt;=</code>, and the blast-radius ratio went negative because <code>computeBlastRadius</code> did not check <code>totalInstances &gt; 0</code>&quot; (no <code>&gt;=</code> invariant on rollback, no divide-by-zero guard on blast radius)</td><td><code>startChaosSession</code> / <code>injectFault</code> / <code>computeBlastRadius</code> / <code>triggerRollback</code> / <code>recordGameDay</code></td></tr><tr><td>Data pipeline</td><td>&quot;The freshness eval passed on a 60-minute stale pipeline because <code>evaluateFreshness</code> used <code>&lt;</code> instead of <code>&lt;=</code>, and the schema-drift detector missed a renamed column because <code>detectSchemaDrift</code> compared only names, not name + type pairs&quot; (no <code>&lt;= slaMinutes</code> invariant, no compound-key set-diff)</td><td><code>startPipelineSession</code> / <code>captureLineage</code> / <code>evaluateFreshness</code> / <code>detectSchemaDrift</code> / <code>scoreDataQuality</code></td></tr><tr><td>AIOps</td><td>&quot;The anomaly detector missed a negative-z-score outlier because <code>detectAnomaly</code> compared <code>zScore &gt; threshold</code> instead of <code>Math.abs(zScore) &gt;= threshold</code>, and the RCA returned the wrong root because <code>analyzeRootCause</code> walked outgoing edges instead of incoming edges&quot; (no absolute-value guard on z-score, no topological-root invariant)</td><td><code>startAiopsSession</code> / <code>detectAnomaly</code> / <code>executeRemediation</code> / <code>analyzeRootCause</code> / <code>correlateAlerts</code></td></tr></tbody></table><p>Each axis has 3 shapes — a mock-only path (fast inner loop, ms scale), a real-driver path (<code>KIWA_MODE=real</code> + real Terraform + OPA / Istio / Linkerd / bpftrace / Anthropic + OpenAI + Datadog LLM Obs / AWS Cost Explorer / Chaos Mesh + Litmus / Airflow + Dagster / Datadog Watchdog, seconds scale), and a fidelity assertion that the two produce the same neutral event. Tutorial 91 covers the IaC + service-mesh + eBPF-III end-to-end chain (capturePlan → detectDrift → evaluatePolicy → attributeCost + handshakeMtls → injectSidecar → tripCircuitBreaker → applyTrafficSplit + probeUserspace → traceKernel → recordSyscall → captureNetworkFlow), tutorial 92 covers the LLM observability + FinOps chain (countTokens → logPrompt → flagHallucination → checkBudget + recordCostPerRequest → attributeTeam → recommendRightsizing → optimizeSpot), tutorial 93 covers the chaos + data-pipeline + AIOps chain (injectFault → computeBlastRadius → triggerRollback → recordGameDay + captureLineage → evaluateFreshness → detectSchemaDrift → scoreDataQuality + detectAnomaly → executeRemediation → analyzeRootCause → correlateAlerts).</p><h2 id="the-4-provider-×-8-axis-32-cell-advanced-iii-grid" tabindex="-1">The 4-provider × 8-axis = 32 cell advanced III grid <a class="header-anchor" href="#the-4-provider-×-8-axis-32-cell-advanced-iii-grid" aria-label="Permalink to &quot;The 4-provider × 8-axis = 32 cell advanced III grid&quot;">​</a></h2><p>Every provider covers every axis. The mock shapes are provider-neutral (the API surface is the same across <code>grafana-oss</code> + <code>prometheus</code> + <code>loki</code> + <code>otel-collector</code>), the emitted event dialects are provider-specific (<code>grafana.iac.plan</code> vs. <code>prom.iac.plan.export</code> vs. <code>loki.iac.plan.log</code> vs. <code>otel.iac.plan.span</code>), and the advanced III fidelity harness reports the coverage explicitly through <code>collectFidelityCoverage()</code> walking all 16 axes (v2.1 8 + v2.2 8).</p><table tabindex="0"><thead><tr><th>Provider</th><th>IaC</th><th>Service mesh</th><th>eBPF-III</th><th>LLM obs</th><th>FinOps</th><th>Chaos</th><th>Data pipeline</th><th>AIOps</th></tr></thead><tbody><tr><td>grafana-oss</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td></tr><tr><td>prometheus</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td></tr><tr><td>loki</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td></tr><tr><td>otel-collector</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td></tr></tbody></table><p>The v2.2 advanced III grid is fully covered — every provider implements every axis because the semantics are runtime-agnostic. That is what makes cross-provider reuse (an IaC drift detector that runs against Grafana OSS Alerting + Prometheus rules + Loki log queries + OTel Collector processors without change) even possible. The neutral event names live in the v2.2 semantics <code>types.ts</code> SSOT and the per-provider dialect table is a static lookup — a new provider drops into the same shape.</p><h3 id="why-the-advanced-iii-grid-is-fully-covered" tabindex="-1">Why the advanced III grid is fully covered <a class="header-anchor" href="#why-the-advanced-iii-grid-is-fully-covered" aria-label="Permalink to &quot;Why the advanced III grid is fully covered&quot;">​</a></h3><p>grafana-oss + prometheus + loki + otel-collector converged on the same neutral events at the &quot;capture an IaC plan, handshake mTLS, probe a syscall, count LLM tokens, attribute FinOps cost, inject a chaos fault, evaluate pipeline freshness, correlate AIOps alerts&quot; primitive — the 8 advanced III shapes are the same across all 4 providers, even though the wire encodings differ (Grafana OSS Alerting event name vs. Prometheus metric family name vs. Loki log field name vs. OTel Collector processor event name). The <code>providerEventName(target, neutralEvent)</code> mapping table is the single point where the 4 dialects diverge; everything upstream stays neutral. The v1.42 advanced III fidelity grid at 32/32 = 100 % implemented reflects that convergence at the &quot;advanced III production observability&quot; level. Combined with the v2.1 32-cell advanced grid (8 axis × 4 provider) + the v2.2 32-cell advanced III grid, the total v2.1 + v2.2 fidelity harness now walks 64 cells (4 provider × 16 axis) in one <code>collectFidelityCoverage()</code> call.</p><h2 id="the-kiwa-mode-real-env-gate-contract-for-the-advanced-iii-grid" tabindex="-1">The <code>KIWA_MODE=real</code> env-gate contract for the advanced III grid <a class="header-anchor" href="#the-kiwa-mode-real-env-gate-contract-for-the-advanced-iii-grid" aria-label="Permalink to &quot;The \`KIWA_MODE=real\` env-gate contract for the advanced III grid&quot;">​</a></h2><p><code>resolveObservabilityRealDriver(provider, env)</code> (from <code>observability-real-driver-testing.md</code> SSOT) returns <code>{ mode: &#39;real&#39;, reason: &#39;...&#39; }</code> when <code>env.KIWA_MODE === &#39;real&#39;</code> and the required env for that provider is set, and <code>{ mode: &#39;mock&#39;, reason: &#39;KIWA_MODE!=real — mock driver&#39; }</code> otherwise. A test that respects the contract combines the gate with a required-env presence check — the dogfood apps use this at each <code>describe.skipIf(mode === &#39;mock&#39;)</code> block. The v2.2 advanced III axes reuse the same real-driver gate as the v2.1 axes — no new env variable is introduced. The per-axis dogfood app pins the required backend URL (<code>KIWA_TERRAFORM_URL</code> / <code>KIWA_ISTIO_URL</code> / <code>KIWA_BPFTRACE_URL</code> / <code>KIWA_ANTHROPIC_API_KEY</code> / <code>KIWA_AWS_COST_EXPLORER_URL</code> / <code>KIWA_CHAOSMESH_URL</code> / <code>KIWA_AIRFLOW_URL</code> / <code>KIWA_DATADOG_WATCHDOG_URL</code>) on top of the shared <code>KIWA_MODE</code> gate.</p><p>Per-provider required-env mapping stays the same as v2.1.</p><ul><li><strong>grafana-oss</strong> → <code>KIWA_MODE</code> + <code>KIWA_GRAFANA_URL</code></li><li><strong>prometheus</strong> → <code>KIWA_MODE</code> + <code>KIWA_PROMETHEUS_URL</code></li><li><strong>loki</strong> → <code>KIWA_MODE</code> + <code>KIWA_LOKI_URL</code></li><li><strong>otel-collector</strong> → <code>KIWA_MODE</code> + <code>KIWA_OTEL_COLLECTOR_URL</code></li></ul><p>Per-dogfood-app required backend URL (on top of the provider gate above).</p><ul><li><strong>dogfood-observability-iac-drift-app</strong> → <code>KIWA_TERRAFORM_URL</code> + <code>KIWA_OPA_URL</code> (Terraform state + OPA policy engine)</li><li><strong>dogfood-observability-llm-ops-app</strong> → <code>KIWA_ANTHROPIC_API_KEY</code> + <code>KIWA_OPENAI_API_KEY</code> + <code>KIWA_DATADOG_LLM_OBS_URL</code> + <code>KIWA_LLM_BUDGET_USD</code> (Anthropic + OpenAI + Datadog LLM Observability + budget guard)</li><li><strong>dogfood-observability-chaos-aiops-app</strong> → <code>KIWA_CHAOSMESH_URL</code> + <code>KIWA_LITMUS_URL</code> + <code>KIWA_AIRFLOW_URL</code> + <code>KIWA_DAGSTER_URL</code> + <code>KIWA_DATADOG_WATCHDOG_URL</code> (Chaos Mesh + Litmus + Airflow + Dagster + Datadog Watchdog)</li></ul><h2 id="the-16-axis-fidelity-harness-v2-1-8-v2-2-8-combined" tabindex="-1">The 16-axis fidelity harness (v2.1 8 + v2.2 8 combined) <a class="header-anchor" href="#the-16-axis-fidelity-harness-v2-1-8-v2-2-8-combined" aria-label="Permalink to &quot;The 16-axis fidelity harness (v2.1 8 + v2.2 8 combined)&quot;">​</a></h2><p><code>collectFidelityCoverage()</code> walks the full 16-axis grid and returns a per-cell <code>{ provider, axis, neutralEvents, providerEvents }</code> record so a CI job can assert <code>rows.every(r =&gt; r.providerEvents.length === r.neutralEvents.length)</code> in one line. The neutral events per axis floor (asserted in the fidelity test) is preserved for the 8 new v2.2 axes.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { collectFidelityCoverage } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@kiwa-lab/observability&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> cov</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> collectFidelityCoverage</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">console.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">log</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(cov.rows.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">length</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">); </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 64 (4 provider × 16 axis)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">console.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">log</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(cov.rows.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">every</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">r</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> r.providerEvents.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">length</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ===</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> r.neutralEvents.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">length</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)); </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// true after v2.2 lands</span></span></code></pre></div><p>The 64-row grid is the single point of truth for the &quot;is a provider × axis pair covered?&quot; question — a new dogfood app that adds a new provider walks the same table without touching the axis list.</p><h2 id="the-v1-42-pair-深度-4-段拡張-3-例目-record" tabindex="-1">The v1.42 pair 深度 4 段拡張 3 例目 record <a class="header-anchor" href="#the-v1-42-pair-深度-4-段拡張-3-例目-record" aria-label="Permalink to &quot;The v1.42 pair 深度 4 段拡張 3 例目 record&quot;">​</a></h2><p>v2.2 is the <strong>third pair in kiwa history to reach depth 4 (v1.14 → v1.17 → v1.35 → v1.42)</strong> on the observability axis. The first was the AI-LLM 縦深化 pair (v1.12 v0.1 base → v1.15 v0.2 multimodal → v1.38 v0.4 advanced → v1.40 v0.5 advanced III), recorded in the v1.39 → v1.40 migration guide. The second was the Payment 深化 pair (v1.14 v0.1 base → v1.19 v0.2 advanced → v1.33 v0.4 advanced II → v1.41 v0.5 advanced III), recorded in the v1.40 → v1.41 migration guide. The v1.42 milestone establishes the pair 深度 4 段 pattern as <strong>stabilised across 3 axes</strong> — the same 4-provider × 8-axis fidelity harness template applies at each depth without change, and the 深化 process (v0.1 base → v0.2 advanced → v0.4/v2.1 advanced II → v0.5/v2.2 advanced III) is idempotent under scale, no longer specific to a single production shape.</p><p>Combined with the v2.0 base grid (dashboard + alert + trace + correlation) + the v2.1 32-cell advanced grid (8 axis × 4 provider) + the v2.2 32-cell advanced III grid (8 axis × 4 provider), the observability package now covers 16 axes × 4 provider = 64 cells of production observability shape in one package with one fidelity harness — matched only by the AI-LLM package&#39;s 64-cell breadth (16 axes × 4 provider) and the Payment package&#39;s 75-cell breadth (25 axes × 3 provider).</p><p>The 20-milestone snippet validation streak (v1.23 → v1.42) reinforces the same repeatability signal for docs — every milestone since v1.23 has added a snippet-validation test that walks the tutorial&#39;s code blocks against the actual API surface, catching drift before readers hit &quot;the tutorial does not compile&quot; gaps. The Observability 深化 pair achieves depth 4 as the third axis after AI-LLM and Payment, validating that the methodology is not axis-specific but applies to any production shape (payment / observability / security / streaming / etc.) that has natural v0.1 / v2.0 → v0.5 / v2.2 evolution — the pattern is now record-stable across 3 axes.</p>`,27)])])}const u=t(d,[["render",r]]);export{v as __pageData,u as default};
