import{_ as t,o as s,c as a,a2 as i}from"./chunks/framework.CyZTVJbV.js";const u=JSON.parse('{"title":"Fidelity — dogfood-visual-regression (v1.16-4)","description":"","frontmatter":{},"headers":[],"relativePath":"quality-reports/component/visual-regression.md","filePath":"quality-reports/component/visual-regression.md"}'),d={name:"quality-reports/component/visual-regression.md"};function o(r,e,n,c,l,p){return s(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="fidelity-—-dogfood-visual-regression-v1-16-4" tabindex="-1">Fidelity — dogfood-visual-regression (v1.16-4) <a class="header-anchor" href="#fidelity-—-dogfood-visual-regression-v1-16-4" aria-label="Permalink to &quot;Fidelity — dogfood-visual-regression (v1.16-4)&quot;">​</a></h1><p>Real-vs-mock behavioural fidelity for the 10-scene Chromatic-style visual regression dogfood, produced by <code>examples/dogfood-visual-regression/tests/emit-fidelity-report.test.ts</code>. Feeds <code>@kiwa-test/quality-metrics</code> 7-axis release gate.</p><h2 id="baseline-real-mode-skipped-—-no-chromatic-project-token-and-no-live-chromatic-cli-install" tabindex="-1">Baseline (real mode skipped — no <code>CHROMATIC_PROJECT_TOKEN</code> and no live <code>chromatic-cli</code> install) <a class="header-anchor" href="#baseline-real-mode-skipped-—-no-chromatic-project-token-and-no-live-chromatic-cli-install" aria-label="Permalink to &quot;Baseline (real mode skipped — no \`CHROMATIC_PROJECT_TOKEN\` and no live \`chromatic-cli\` install)&quot;">​</a></h2><p>When the harness runs without a <code>CHROMATIC_PROJECT_TOKEN</code> env var, the real adapter emits <code>CHROMATIC_REAL_ENV_MISSING</code> for every op. Divergences are recorded so the mock adapter is not spuriously credited with parity — the harness stays honest even in local dev.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>provider   : @kiwa-test/component/visual-regression</span></span>
<span class="line"><span>version    : 0.1.0</span></span>
<span class="line"><span>verdict    : PASS (7-axis component branch — all axes clear the default gate)</span></span>
<span class="line"><span>divergences: 3 (seedBaselines / captureAll / review — real mode absent)</span></span>
<span class="line"><span>axes       : 7 (component branch — no AI-LLM cost / latency / token / accuracy overlay)</span></span></code></pre></div><table tabindex="0"><thead><tr><th>axis</th><th>actual</th><th>threshold</th><th>verdict</th></tr></thead><tbody><tr><td>coverage.line</td><td>92.00%</td><td>85%</td><td>pass</td></tr><tr><td>coverage.branch</td><td>88.00%</td><td>80%</td><td>pass</td></tr><tr><td>coverage.function</td><td>95.00%</td><td>90%</td><td>pass</td></tr><tr><td>fidelity.ratio</td><td>100.00% (3/3)</td><td>70%</td><td>pass</td></tr><tr><td>perf.p95Ms</td><td>~1.20 ms</td><td>100 ms</td><td>pass</td></tr><tr><td>mutation.killRate</td><td>73.33% (22/30)</td><td>60%</td><td>pass</td></tr><tr><td>testCount.behavior</td><td>44</td><td>10</td><td>pass</td></tr></tbody></table><p>Fidelity ratio counts mock-covered ops vs the real Chromatic adapter surface (3 total — <code>seedBaselines</code> / <code>captureAll</code> / <code>review</code>). Divergences count all 3 because the real adapter is skipped when <code>CHROMATIC_PROJECT_TOKEN</code> is absent — that is by design, wiring the env promotes the report to the real baseline.</p><h2 id="reproduction" tabindex="-1">Reproduction <a class="header-anchor" href="#reproduction" aria-label="Permalink to &quot;Reproduction&quot;">​</a></h2><p>Mock only.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --filter</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dogfood-visual-regression</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples/dogfood-visual-regression/quality-report/fidelity-latest.md</span></span></code></pre></div><p>Live real mode.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> CHROMATIC_PROJECT_TOKEN</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">chpt_xxxxxxxxxxxxxxxx</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># optional — pin the branch label so Chromatic groups the runs</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> CHROMATIC_BRANCH</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">feature/v1.16-4</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># optional — hand a running Storybook preview URL to the connected driver</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> CHROMATIC_STORYBOOK_URL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">http://localhost:6006</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --filter</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dogfood-visual-regression</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span></span></code></pre></div><p>The v0.1 real adapter emits <code>CHROMATIC_LIVE_NOT_IMPLEMENTED</code> when <code>CHROMATIC_PROJECT_TOKEN</code> is set — a follow-up milestone will swap in a real <code>chromatic-cli</code> driver that uploads a Storybook preview build, captures baseline + diff, and drives accept / reject through the Chromatic web app for every (scene × viewport) pair.</p><h2 id="ops-under-measurement" tabindex="-1">Ops under measurement <a class="header-anchor" href="#ops-under-measurement" aria-label="Permalink to &quot;Ops under measurement&quot;">​</a></h2><p>Three provider-neutral ops on <code>VisualRegressionAdapter</code>.</p><ul><li><code>seedBaselines</code> — walks the 10-scene registry, renders each with <code>seedArgs()</code>, hashes the pseudo-HTML through <code>@kiwa-test/component</code> <code>hashMarkup</code>, and seeds a <code>VisualBaseline</code> for every declared viewport (10 scenes × 2 viewports = 20 seed pairs per invocation)</li><li><code>captureAll</code> — walks the 10-scene registry, renders each with either <code>seedArgs()</code> or <code>changedArgs()</code>, captures the current markup, and returns 1 <code>VisualDiff</code> per viewport (<code>status=&#39;new&#39;</code> for the first capture, <code>status=&#39;passed&#39;</code> for a match, <code>status=&#39;failed&#39;</code> on hash mismatch). Every diff also carries the <code>baselineHash</code> and <code>currentHash</code> fields for downstream inspection</li><li><code>review</code> — records an accept or reject entry against a <code>(sceneId, viewport)</code> pair; accept swaps the baseline for the currently captured markup so the next matching capture reads passed, reject leaves the baseline intact so the next matching capture still fails</li></ul><p>Every op emits at least 1 trace event, so the fidelity harness can diff the mock vs the real Chromatic behaviour without needing a live upload to run.</p><h2 id="_10-scenes-ssot-for-saas-front-ends" tabindex="-1">10 scenes (SSOT for SaaS front ends) <a class="header-anchor" href="#_10-scenes-ssot-for-saas-front-ends" aria-label="Permalink to &quot;10 scenes (SSOT for SaaS front ends)&quot;">​</a></h2><p>The dogfood exercises the 5 UI primitives that show up in every SaaS design system, each in both light + dark themes. The intent-change branch swaps 1 visible bit (button label, cell text, heading rename) so the pseudo-HTML hash diverges and the mock reports <code>status=&#39;failed&#39;</code>.</p><table tabindex="0"><thead><tr><th>primitive</th><th>fields captured in the pseudo-HTML</th><th>intent-change diff</th><th>test</th></tr></thead><tbody><tr><td>card</td><td>heading + body + primary button + <code>data-theme</code></td><td>button label swap</td><td><code>diff-detect.test.ts</code></td></tr><tr><td>modal</td><td>overlay + dialog + header + body + confirm / cancel + <code>data-theme</code></td><td>header title rename</td><td><code>diff-detect.test.ts</code></td></tr><tr><td>table</td><td>caption + 3-column verdict table + <code>data-theme</code></td><td>first-row verdict swap</td><td><code>diff-detect.test.ts</code></td></tr><tr><td>toast</td><td>live-region container + icon + message + close + <code>data-theme</code></td><td>message rewrite</td><td><code>diff-detect.test.ts</code></td></tr><tr><td>form</td><td>sign-in form + email + password + submit + <code>data-theme</code></td><td>submit label swap</td><td><code>diff-detect.test.ts</code></td></tr></tbody></table><p>Each primitive renders identically across light + dark themes except for the <code>data-theme</code> attribute at the scene root — sufficient for the pseudo-HTML hash to diverge and the mock to score light + dark as distinct baselines.</p><h2 id="notes" tabindex="-1">Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;Notes&quot;">​</a></h2><p>Provider prefix <code>@kiwa-test/component/</code> does not match the AI-LLM branch of <code>evaluateReleaseGate</code> (<code>packages/quality-metrics/src/gate.ts</code> — 4 AI-LLM axes are appended only when the prefix is <code>@kiwa-test/ai-*</code>). Component test dogfoods stay on the 7-axis common track — Chromatic is a snapshot + diff surface, not a token-priced generative surface, so cost / latency / token / accuracy do not apply. Seed + capture + review round-trip latency still feeds <code>perf.p95Ms</code> so visual-regression performance stays visible in the report.</p>`,23)])])}const k=t(d,[["render",o]]);export{u as __pageData,k as default};
