import{_ as s,o as a,c as i,a2 as t}from"./chunks/framework.CyZTVJbV.js";const k=JSON.parse('{"title":"Observability v2 testing — dashboard, alert, trace flame graph, log correlation","description":"","frontmatter":{},"headers":[],"relativePath":"concepts/observability-v2-testing.md","filePath":"concepts/observability-v2-testing.md"}'),n={name:"concepts/observability-v2-testing.md"};function r(o,e,l,h,d,c){return a(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="observability-v2-testing-—-dashboard-alert-trace-flame-graph-log-correlation" tabindex="-1">Observability v2 testing — dashboard, alert, trace flame graph, log correlation <a class="header-anchor" href="#observability-v2-testing-—-dashboard-alert-trace-flame-graph-log-correlation" aria-label="Permalink to &quot;Observability v2 testing — dashboard, alert, trace flame graph, log correlation&quot;">​</a></h1><p><code>@kiwa/observability</code> v1.1 (v1.14-4) shipped provider-flavoured mocks for OpenTelemetry / Datadog / Sentry. Every mock wrote into the same <code>TelemetryCollector</code> so the assertion code was provider-neutral at the <strong>emit boundary</strong>. That covers instrumentation coverage. It does not cover the <strong>operational surface</strong> that lives on top of that emitted data — the Grafana dashboard that renders the metrics, the AlertManager that pages the on-call, the Jaeger flame graph that a debugger drills into after an incident, or the log line a support engineer joins to a trace ID.</p><p>v1.17 adds 4 new axes on top of that v1.1 base so the whole SaaS observability wall is testable in-process, deterministically, without a real Prometheus / AlertManager / Jaeger.</p><h2 id="why-v1-1-collectors-alone-are-not-enough" tabindex="-1">Why v1.1 collectors alone are not enough <a class="header-anchor" href="#why-v1-1-collectors-alone-are-not-enough" aria-label="Permalink to &quot;Why v1.1 collectors alone are not enough&quot;">​</a></h2><p>The v1.1 collector answers &quot;was this span emitted?&quot;, &quot;did the counter increment?&quot;, &quot;was this exception captured?&quot;. Those are unit-scoped questions about instrumentation. The real production observability wall raises 4 questions the collector cannot answer.</p><table tabindex="0"><thead><tr><th>Production question</th><th>Collector-only answer</th><th>What v2 adds</th></tr></thead><tbody><tr><td>&quot;The Grafana p99 latency panel went red — did the threshold badge fire correctly?&quot;</td><td>not modelled — badges live in Grafana, not in the SDK</td><td><code>DashboardMock</code> — panel + threshold + refresh cadence + badge assertions</td></tr><tr><td>&quot;AlertManager paged the platform team at 03:00 — did the routing tree match the label set?&quot;</td><td>not modelled — routing lives in AlertManager, not in the SDK</td><td><code>AlertRouter</code> — rule + route + silence + escalation state machine</td></tr><tr><td>&quot;Debugger drilled into the flame graph for span <code>db.query</code> — did the self-time look right?&quot;</td><td>not modelled — flame graphs aggregate spans across shape, not per-span</td><td><code>buildSpanTree</code> + <code>renderFlameGraph</code> + <code>drillDown</code> — tree + collapse + subtree</td></tr><tr><td>&quot;Support engineer joined logs to trace <code>abc123</code> — did both sides carry the correlation attributes?&quot;</td><td>not modelled — trace / log join is a query-time operation</td><td><code>LogCorrelationIndex</code> — bidirectional lookup keyed on <code>trace_id</code> / <code>span_id</code></td></tr></tbody></table><p>All 4 v2 axes read from the same <code>TelemetryCollector</code> sinks the v1.1 mocks populate. There is no v2-only writer — every v2 abstraction consumes the collector state that v1.1 already produces. That keeps the boundary consistent — instrumentation code stays untouched, only the assertion surface expands.</p><h2 id="the-4-v2-axes-on-one-collector" tabindex="-1">The 4 v2 axes on one collector <a class="header-anchor" href="#the-4-v2-axes-on-one-collector" aria-label="Permalink to &quot;The 4 v2 axes on one collector&quot;">​</a></h2><p>The whole v2 API surface is a set of pure readers over the <code>TelemetryCollector</code>. Nothing writes to the collector except v1.1 provider mocks (OpenTelemetry / Datadog / Sentry). A test that already uses v1.1 gets v2 assertions for free — the same collector instance passes through.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  AlertRouter,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  DashboardMock,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  LogCorrelationIndex,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  TelemetryCollector,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  buildSpanTree,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  createOtelMock,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  renderFlameGraph,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@kiwa/observability&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> collector</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> TelemetryCollector</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> otel</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createOtelMock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ collector });</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ... SUT emits spans / metrics / logs into \`collector\` ...</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// v2 axis 1 — dashboard</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> dashboard</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DashboardMock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ id: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;sre&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, title: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;SRE&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, panels: [</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* ... */</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] }, collector);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">dashboard.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">refresh</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// v2 axis 2 — alert</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> router</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> AlertRouter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(collector);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">router.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">registerRule</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* ... */</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">router.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">evaluate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// v2 axis 3 — trace flame graph</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> roots</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> buildSpanTree</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(collector.spans);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> flame</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> renderFlameGraph</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(roots);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// v2 axis 4 — log correlation</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> index</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> LogCorrelationIndex</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ logs: collector.logs, spans: collector.spans });</span></span></code></pre></div><h2 id="axis-1-—-dashboard-render-lifecycle" tabindex="-1">Axis 1 — dashboard render lifecycle <a class="header-anchor" href="#axis-1-—-dashboard-render-lifecycle" aria-label="Permalink to &quot;Axis 1 — dashboard render lifecycle&quot;">​</a></h2><p><code>DashboardMock</code> models the render lifecycle of a Grafana-style panel wall.</p><ul><li>A dashboard hosts N panels, each panel bound to one <code>MetricQuery</code> (<code>metricName</code> + <code>aggregation</code> + optional <code>tagFilter</code> + optional time window).</li><li><code>refresh()</code> re-evaluates every panel against the current collector state and returns a <code>PanelResult[]</code> with <code>value</code> / <code>matchedRecords</code> / <code>badge</code> / <code>refreshedAt</code>.</li><li><code>getRefreshCount()</code> exposes the refresh cadence so tests assert &quot;3 refresh cycles happened&quot; without wall-clock coupling.</li><li><code>panel(id)</code> looks up the most-recent result by id, so assertions read like <code>dashboard.panel(&#39;p99&#39;).badge === &#39;critical&#39;</code>.</li></ul><p>Threshold badges attach to numeric results deterministically — the first matching threshold in the panel&#39;s ordered <code>thresholds</code> array wins. That mirrors Grafana&#39;s &quot;first match wins&quot; semantics and makes the badge assertion stable across mock refreshes.</p><p>The mock does <strong>not</strong> model. rendering pixels (a UI concern that lives in the Grafana react tree, not the mock), the panel-plugin extension surface (a Grafana-plugin-manifest concern), templated variables in PromQL queries (a scripting concern that requires a real PromQL parser). The mock&#39;s SUT-agnostic contract stays on the query + aggregate + badge triangle — enough to catch the 3 most-common regressions (&quot;threshold changed and no test caught it&quot;, &quot;aggregation changed from <code>avg</code> to <code>p99</code> and no test caught it&quot;, &quot;panel bound to the wrong metric and no test caught it&quot;) without pulling in a full Grafana embedded runtime.</p><h2 id="axis-2-—-alert-lifecycle-state-machine" tabindex="-1">Axis 2 — alert lifecycle state machine <a class="header-anchor" href="#axis-2-—-alert-lifecycle-state-machine" aria-label="Permalink to &quot;Axis 2 — alert lifecycle state machine&quot;">​</a></h2><p><code>AlertRouter</code> models the AlertManager 4-state lifecycle end-to-end.</p><ul><li><strong>pending</strong> — a rule&#39;s predicate held on the last evaluation, but <code>forSamples</code> requires N holds in a row before firing. <code>AlertRouter</code> keeps a per-rule <code>pendingCount</code> that increments on hold and resets on flip.</li><li><strong>firing</strong> — <code>pendingCount &gt;= forSamples</code> triggers a <code>firing</code> state and the fire is routed through the tree. The deepest matching route wins (matches the AlertManager <code>routes[].match</code> semantics).</li><li><strong>escalated</strong> — an escalation ladder attached to the rule (<code>setEscalation</code>) walks the receiver chain at each <code>advanceEscalation()</code> call, emitting one <code>AlertReceiverEvent</code> per step whose <code>afterMs</code> has elapsed.</li><li><strong>resolved</strong> — the predicate flips back to <code>false</code>. <code>pendingCount</code> resets and the active fire transitions to <code>resolved</code>; no receiver events fire from <code>resolved</code> transitions.</li></ul><p>Silences suppress by label match with an <code>expiresAt</code> window. A pending fire whose labels match any active silence never advances to <code>firing</code>. Callers can add / remove silences at any time; the router re-checks the silence store on every <code>evaluate()</code>.</p><p>The mock reproduces the 3 AlertManager decision points that surface in real incidents. routing hierarchy (severity → team → channel), silence-vs-fire race (silence added between two evaluations), and escalation walk (L1 → L2 → L3 with configurable <code>afterMs</code>). It intentionally does <strong>not</strong> model. the AlertManager gossip protocol (a cluster concern), the notification adapter integrations (Slack / PagerDuty / OpsGenie payload shapes are integration concerns), or the notification retry / dedup window (a delivery concern that requires wall-clock coupling).</p><h2 id="axis-3-—-trace-aggregation-and-drill-down" tabindex="-1">Axis 3 — trace aggregation and drill-down <a class="header-anchor" href="#axis-3-—-trace-aggregation-and-drill-down" aria-label="Permalink to &quot;Axis 3 — trace aggregation and drill-down&quot;">​</a></h2><p><code>buildSpanTree</code> + <code>renderFlameGraph</code> + <code>drillDown</code> model the 3 flame graph operations a real debugger walks through during an incident.</p><ul><li><code>buildSpanTree(collector.spans)</code> — build a tree of <code>SpanNode</code>s from the flat span array. Parent lookups happen by <code>parentSpanName</code>; when the parent is null the span becomes a root. Children preserve insertion order (which matches call order in the SUT, so a <code>worker</code> span emitted 3 times shows up as 3 siblings under the parent).</li><li><code>renderFlameGraph(roots)</code> — collapse siblings with the same name at the same depth into one <code>FlameNode</code> whose <code>samples</code> counts contributions. That matches how real flame graphs summarise fan-out (100 parallel <code>worker</code> spans render as 1 wide flame node with <code>samples = 100</code>).</li><li><code>drillDown(flame, &#39;db.query&#39;)</code> — extract the subtree rooted at the first node whose name matches. Depths are normalised so the drilled-in root sits at depth 0. Returns <code>null</code> when no matching node exists.</li></ul><p><code>selfMs</code> = <code>totalMs - sum(child.totalMs)</code>, which mirrors the Jaeger UI &quot;self time&quot; column. Open spans (<code>endedAt = null</code>) count towards <code>samples</code> but contribute <code>0</code> to numeric aggregates. That keeps the aggregate stable even when a test asserts on a snapshot mid-run.</p><p>The mock does <strong>not</strong> model. cross-service parent references (real distributed traces use <code>spanContext</code> with <code>traceParent</code> headers; the mock uses <code>parentSpanName</code> for in-process shapes), sampling / probability decisions (a real-tracer runtime concern), or wire encoding (OTLP / Zipkin / Jaeger Thrift are exporter concerns).</p><h2 id="axis-4-—-bidirectional-log-↔-span-correlation" tabindex="-1">Axis 4 — bidirectional log ↔ span correlation <a class="header-anchor" href="#axis-4-—-bidirectional-log-↔-span-correlation" aria-label="Permalink to &quot;Axis 4 — bidirectional log ↔ span correlation&quot;">​</a></h2><p><code>LogCorrelationIndex</code> builds one index over the collector&#39;s <code>.logs</code> and <code>.spans</code> sinks and answers 4 questions.</p><ul><li><code>logsForSpan(spanId)</code> — logs whose <code>span_id</code> attribute equals the given id.</li><li><code>logsForTrace(traceId)</code> — logs whose <code>trace_id</code> attribute equals the given id.</li><li><code>spanById(spanId)</code> / <code>spansForTrace(traceId)</code> — the reverse direction.</li></ul><p>The correlation keys are configurable — real SUTs mix conventions during migrations (OpenTelemetry canonical is <code>trace_id</code> / <code>span_id</code>, Datadog is <code>dd.trace_id</code>, Sentry uses <code>sentry-trace</code>). Pass <code>altTraceIdKeys</code> to bridge the gap without asking the SUT to normalise:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> index</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> LogCorrelationIndex</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  { logs: collector.logs, spans: collector.spans },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  { traceIdKey: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;trace_id&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, altTraceIdKeys: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;dd.trace_id&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;sentry-trace&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p>The index is built once from the current collector state; callers that mutate the collector after building must rebuild. That is a deliberate design choice — it keeps every lookup <code>O(1)</code> and matches the &quot;build the join index at query time&quot; pattern real observability backends use for the same query surface.</p><p>The mock does <strong>not</strong> model. the retention policy (real backends drop old data), the fuzzy-time join for logs that lack correlation attributes (a heuristic that varies per backend), or the multi-line log parsing that turns stack traces into single events (a log-processor concern). It stays on the attribute-key join because that is the join real dashboards run 95% of the time.</p><h2 id="fixtures-—-one-call-scenario-bootstrapping" tabindex="-1">Fixtures — one-call scenario bootstrapping <a class="header-anchor" href="#fixtures-—-one-call-scenario-bootstrapping" aria-label="Permalink to &quot;Fixtures — one-call scenario bootstrapping&quot;">​</a></h2><p><code>fixtures.ts</code> ships 3 panel builders, 3 alert rules, 3 trace shapes, and 1 log-set matched to the http-handler trace, plus a default route + escalation + silence. A test that wants a realistic SaaS observability wall in one call writes.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  defaultRoute,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  panel_httpErrorRate,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  panel_p99Latency,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  panel_queueDepth,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  rule_errorRateCritical,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  rule_latencyDegraded,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  rule_queueBackpressure,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  trace_httpHandler,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  logs_forHttpTrace,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@kiwa/observability&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> dashboard</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DashboardMock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  { id: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;sre&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, title: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;SRE&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, panels: [</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">panel_httpErrorRate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(), </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">panel_p99Latency</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(), </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">panel_queueDepth</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()] },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  collector,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">router.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">registerRule</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rule_errorRateCritical</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">());</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">router.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">registerRule</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rule_latencyDegraded</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">());</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">router.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">registerRule</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rule_queueBackpressure</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">());</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">router.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setRoute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">defaultRoute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">());</span></span></code></pre></div><p>Every builder is pure — no side effects, no collector coupling. The test controls when the collector receives data (via v1.1 provider mocks) and when the v2 readers evaluate.</p><h2 id="fidelity-harness-—-mock-vs-real-per-axis" tabindex="-1">Fidelity harness — mock vs real, per axis <a class="header-anchor" href="#fidelity-harness-—-mock-vs-real-per-axis" aria-label="Permalink to &quot;Fidelity harness — mock vs real, per axis&quot;">​</a></h2><p>Each v1.17 dogfood app (<code>examples/dogfood-observability-dashboard/</code>, <code>examples/dogfood-alert-orchestrator/</code>, <code>examples/dogfood-trace-flame-graph/</code>) ships a <code>flows/fidelity.ts</code> module that.</p><ol><li>Runs the same 4-op / 5-op / 5-op surface through <code>makeMockAdapter()</code> — always available, driven by the v2 mock.</li><li>Runs the same surface through <code>makeRealAdapter()</code> — env-gated on <code>PROMETHEUS_URL</code> / <code>ALERTMANAGER_URL</code> / <code>JAEGER_URL</code>; falls back to <code>*_ENV_MISSING</code> traces when the connection env is absent.</li><li>Emits a <code>FidelityReport</code> with per-op traces, latency samples, and per-axis coverage summary that feeds <code>@kiwa/quality-metrics</code> release gate.</li><li>Writes the report as <code>quality-report/fidelity-latest.md</code> (git-ignored — CI reads this) and <code>quality-report/fidelity-latest.json</code> (machine-readable).</li></ol><p>The report is hand-promoted to <code>docs/quality-reports/observability/&lt;name&gt;.md</code> when a release cuts. <code>evaluateReleaseGate</code> consumes the JSON directly and gates the release on the 7-axis pass (<code>@kiwa/observability/*</code> prefix stays on the common 7-axis branch — no token pricing to measure).</p><h2 id="what-v2-does-not-model-—-ssot" tabindex="-1">What v2 does not model — SSOT <a class="header-anchor" href="#what-v2-does-not-model-—-ssot" aria-label="Permalink to &quot;What v2 does not model — SSOT&quot;">​</a></h2><ul><li><strong>UI pixels</strong> — dashboard panels have <code>value</code> + <code>badge</code> metadata; there is no <code>&lt;canvas&gt;</code> rendering. Real Grafana pixel differences (font, chart plugin, plot style) belong to a component-test harness (<code>@kiwa/component</code>), not observability.</li><li><strong>Delivery mechanics</strong> — <code>AlertReceiverEvent</code> records &quot;receiver X saw fire Y at time Z&quot;. It does not simulate a Slack API round-trip, PagerDuty ack window, or OpsGenie retry ladder.</li><li><strong>Wire encoding</strong> — v2 reads from <code>TelemetryCollector</code>. The v1.1 provider mocks own the SUT emit boundary; OTLP / Zipkin / Jaeger Thrift serialisation happens outside the mock surface.</li><li><strong>Distributed span context</strong> — parent references use <code>parentSpanName</code> for in-process traces. A production trace across services carries a <code>traceParent</code> header per W3C Trace Context; that is an integration concern the mock does not simulate.</li><li><strong>Retention</strong> — logs / spans stay in the collector until the SUT clears it. Real observability backends drop data by TTL; the mock has no TTL.</li></ul><p>The trade-off is explicit — v2 covers 4 axes that live above the SDK boundary at high fidelity, and stops at the network boundary. Wire, delivery, and pixel concerns route through other kiwa surfaces (integration tests, component tests, e2e tests) that own the right level of abstraction for each.</p><h2 id="related-reading" tabindex="-1">Related reading <a class="header-anchor" href="#related-reading" aria-label="Permalink to &quot;Related reading&quot;">​</a></h2><ul><li><a href="./../tutorials/22-observability-dashboard">Tutorial 22 — Observability dashboard (panel + refresh + badge)</a></li><li><a href="./../tutorials/23-alert-orchestrator">Tutorial 23 — Alert orchestrator (rule + route + silence + escalation)</a></li><li><a href="./../tutorials/24-trace-flame-graph">Tutorial 24 — Trace flame graph (span tree + drill-down + log correlation)</a></li><li><a href="./../migrations/v1.16-to-v1.17">Migration guide — v1.16 → v1.17</a></li><li><a href="./telemetry-testing">Concept — Telemetry testing (span + metric + log aggregation SSOT)</a></li><li>v1.17 milestone parent <a href="https://github.com/cardene777/kiwa/issues/777" target="_blank" rel="noreferrer">#777</a></li></ul>`,45)])])}const g=s(n,[["render",r]]);export{k as __pageData,g as default};
