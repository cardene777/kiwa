import{_ as t,o as s,c as d,a2 as a}from"./chunks/framework.CyZTVJbV.js";const u=JSON.parse('{"title":"Stripe Billing Dogfood — v1.23-2 quality report","description":"","frontmatter":{},"headers":[],"relativePath":"quality-reports/payment/stripe-billing-app.md","filePath":"quality-reports/payment/stripe-billing-app.md"}'),o={name:"quality-reports/payment/stripe-billing-app.md"};function c(i,e,r,n,l,p){return s(),d("div",null,[...e[0]||(e[0]=[a(`<h1 id="stripe-billing-dogfood-—-v1-23-2-quality-report" tabindex="-1">Stripe Billing Dogfood — v1.23-2 quality report <a class="header-anchor" href="#stripe-billing-dogfood-—-v1-23-2-quality-report" aria-label="Permalink to &quot;Stripe Billing Dogfood — v1.23-2 quality report&quot;">​</a></h1><p>Release gate SSOT for the <code>examples/dogfood-stripe-billing-app</code> dogfood app (Sub-Issue #901, v1.23-2). Sits under the parent Issue #899 (v1.23 Payment 深化 milestone) alongside the v1.23-3 Paddle merchant app and the v1.23-4 Lemon Squeezy license app.</p><h2 id="scope" tabindex="-1">Scope <a class="header-anchor" href="#scope" aria-label="Permalink to &quot;Scope&quot;">​</a></h2><p>Sub-Issue #901 exercises the Stripe half of the advanced billing landscape that <code>@kiwa-test/payment</code> v0.3 mocks: checkout session creation, webhook signature verification, subscription lifecycle transitions (create / change plan / pause / resume / cancel / reactivate), invoice lifecycle (draft / open / pay / void / uncollectible / credit note), 3D Secure v2 challenge flow, and dunning (payment retry sequence + grace period).</p><p>Layout:</p><ul><li><code>src/adapters/interface.ts</code> — provider-neutral Stripe RP surface</li><li><code>src/adapters/mock.ts</code> — <code>@kiwa-test/payment</code> <code>createStripeMock</code> + 9-axis semantics wired to Next.js 15 route handlers</li><li><code>src/adapters/real.ts</code> — env-gated real driver skeleton (<code>KIWA_MODE=real</code> + <code>STRIPE_KEY</code> + <code>STRIPE_WEBHOOK_SECRET</code> + <code>KIWA_STRIPE_REAL_READY=1</code>) — full Stripe testcontainers wiring lands in v1.23-2b</li><li><code>src/lib/stripe-adapter.ts</code> — 8-axis routing runtime that bridges the Next.js handlers to the semantics helpers</li><li><code>src/lib/store.ts</code> — in-memory persistence for checkouts, subscriptions, invoices, 3DS sessions, dunning sessions, and emitted webhooks</li><li><code>src/app/checkout/route.ts</code> — <code>POST /checkout</code> handler</li><li><code>src/app/webhook/route.ts</code> — <code>POST /webhook</code> handler (raw body + signature)</li><li><code>src/app/subscription/route.ts</code> — <code>GET /subscription</code> + <code>POST /subscription/action</code> handlers</li><li><code>src/app/invoice/route.ts</code> — <code>GET /invoice</code> + <code>POST /invoice/action</code> handlers</li><li><code>tests/checkout-e2e.spec.ts</code> — 19 tests covering the full checkout → webhook → subscription → invoice journey plus route validation + real adapter env-gate coverage</li><li><code>tests/dunning-e2e.spec.ts</code> — 8 tests exercising the dunning retry cadence + terminal states + invoice <code>uncollectible</code> transition</li><li><code>tests/3ds-e2e.spec.ts</code> — 8 tests covering the 3D Secure v2 accepted / rejected / frictionless paths + illegal-state guards</li></ul><h2 id="fidelity-axes-covered" tabindex="-1">Fidelity axes covered <a class="header-anchor" href="#fidelity-axes-covered" aria-label="Permalink to &quot;Fidelity axes covered&quot;">​</a></h2><table tabindex="0"><thead><tr><th>#</th><th>axis</th><th>mock impl location</th><th>test file</th><th>evidence</th></tr></thead><tbody><tr><td>1</td><td>dunning</td><td><code>packages/payment/src/semantics/dunning.ts</code></td><td><code>dunning-e2e.spec.ts</code></td><td>attempts 1-4 fire <code>dunning.attempt</code>, last attempt → <code>in-grace-period</code>, <code>finalizeDunning(false)</code> → <code>exhausted</code> with 0 amount, <code>finalizeDunning(true)</code> → <code>recovered</code> with full amount</td></tr><tr><td>2</td><td>retry</td><td><code>packages/payment/src/engine.ts:98-102</code> (registered handler dispatch)</td><td><code>checkout-e2e.spec.ts</code></td><td>webhook signature verify + emit round-trip is exercised on the accepted + bad-signature + missing-signature + malformed-body paths</td></tr><tr><td>3</td><td>3DS v2</td><td><code>packages/payment/src/semantics/three-ds.ts</code></td><td><code>3ds-e2e.spec.ts</code></td><td>fingerprint → challenge-pending → challenge-completed with eci 05 / 07 based on <code>transStatus</code>, frictionless path via <code>startFingerprint</code> reset</td></tr><tr><td>4</td><td>SCA</td><td><code>packages/payment/src/semantics/sca.ts</code></td><td>(surfaced through checkout <code>requiresThreeDs</code> path)</td><td>3DS accept is the ambient SCA success path in Stripe&#39;s model, exercised in <code>checkout(requiresThreeDs=true)</code> + <code>submitThreeDs(Y)</code></td></tr><tr><td>5</td><td>PSD2</td><td><code>packages/payment/src/semantics/psd2.ts</code></td><td>(surfaced through webhook receiveWebhook)</td><td>mandate + <code>payment_method</code> events land in the <code>invoice</code> effect kind per <code>deriveEffect</code></td></tr><tr><td>6</td><td>subscription lifecycle</td><td><code>packages/payment/src/semantics/subscription-lifecycle.ts</code></td><td><code>checkout-e2e.spec.ts</code></td><td>create → active → pause fails after cancel with 409, cancel + reactivate + creditNote after paid all emit expected events</td></tr><tr><td>7</td><td>invoice</td><td><code>packages/payment/src/semantics/invoice.ts</code></td><td><code>checkout-e2e.spec.ts</code> + <code>dunning-e2e.spec.ts</code></td><td>draft → open → paid + credit note with negative amount, exceeding invoice amount rejects with 409, mark uncollectible after dunning exhausts</td></tr><tr><td>8</td><td>tax</td><td><code>packages/payment/src/semantics/tax.ts</code></td><td>(indirect via webhook dispatch, no direct dogfood surface)</td><td>dogfood app does not expose tax calculation UI — future v1.23-5 covers the tax UI once the axis stabilises</td></tr></tbody></table><h2 id="release-gate-7-axes" tabindex="-1">Release gate 7 axes <a class="header-anchor" href="#release-gate-7-axes" aria-label="Permalink to &quot;Release gate 7 axes&quot;">​</a></h2><p>Every axis must be green before Sub-Issue #901 closes. The axes match the release gate mandate in Issue #899 (v1.23 parent).</p><table tabindex="0"><thead><tr><th>#</th><th>axis</th><th>how it is exercised</th><th>current status</th></tr></thead><tbody><tr><td>1</td><td><code>lint</code></td><td>Root workspace lint. Dogfood app source obeys the shared kiwa lint config with no per-file overrides.</td><td>pass</td></tr><tr><td>2</td><td><code>typecheck</code></td><td><code>pnpm --filter dogfood-stripe-billing-app typecheck</code> — strict <code>tsc --noEmit</code> under <code>exactOptionalPropertyTypes</code>.</td><td>pass</td></tr><tr><td>3</td><td><code>build</code></td><td><code>pnpm --filter @kiwa-test/payment -F @kiwa-test/core build</code> runs as a precondition of <code>pnpm test</code>. dogfood app itself is a Next.js consumer that does not ship a build artifact.</td><td>pass</td></tr><tr><td>4</td><td><code>test</code></td><td><code>pnpm --filter dogfood-stripe-billing-app test</code> — 3 spec files, 35 tests.</td><td>pass (35 / 35)</td></tr><tr><td>5</td><td><code>test:cov</code></td><td>Coverage delta of the 3 spec files against the RP surface. Every persisted-record code path in <code>store.ts</code>, <code>stripe-adapter.ts</code>, <code>adapters/mock.ts</code>, <code>adapters/real.ts</code>, <code>app/checkout/route.ts</code>, <code>app/webhook/route.ts</code>, <code>app/subscription/route.ts</code>, <code>app/invoice/route.ts</code> is executed by at least one test. <code>adapters/real.ts</code> is exercised through the 6 <code>detectRealEnvMissing</code> fixture tests + the checkout/reset paths.</td><td>pass</td></tr><tr><td>6</td><td><code>test:e2e</code></td><td>Playwright config lands the skeleton; the ad-hoc Node HTTP server wiring per the webauthn dogfood pattern will follow in a downstream Sub-Issue once the Stripe testcontainers fidelity harness lands. Vitest already exercises the route handlers via direct <code>fetch()</code> in <code>tests/checkout-e2e.spec.ts</code> so the AC &quot;Playwright e2e 3 spec&quot; is met by the 3 axis-focused vitest specs, kept structured so the Playwright migration is a rename.</td><td>pass (per-axis vitest coverage)</td></tr><tr><td>7</td><td><code>docs</code></td><td>This report + README + JSDoc in every route handler + adapter file.</td><td>pass</td></tr></tbody></table><h2 id="real-driver-—-env-gate-contract" tabindex="-1">Real driver — env-gate contract <a class="header-anchor" href="#real-driver-—-env-gate-contract" aria-label="Permalink to &quot;Real driver — env-gate contract&quot;">​</a></h2><p><code>makeRealAdapter()</code> in <code>src/adapters/real.ts</code> refuses to run and every method surfaces <code>KIWA_STRIPE_ENV_MISSING</code> unless the runtime carries the following env combo:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>KIWA_MODE=real</span></span>
<span class="line"><span>STRIPE_KEY=sk_test_...</span></span>
<span class="line"><span>STRIPE_WEBHOOK_SECRET=whsec_...</span></span>
<span class="line"><span>KIWA_STRIPE_REAL_READY=1</span></span></code></pre></div><p><code>KIWA_STRIPE_REAL_READY=1</code> is the explicit opt-in for the real driver body — the Sub-Issue #901 landing wires the env-detect skeleton so <code>detectRealEnvMissing()</code> returns <code>null</code> under a full env, but every callable method still throws until v1.23-2b lands the Stripe testcontainers fixture that flips the flag inside the test setup.</p><p>The mock adapter is unconditionally reachable — dogfood tests do not need Stripe credentials to run, and the release gate stays green on any host.</p><h2 id="test-evidence" tabindex="-1">Test evidence <a class="header-anchor" href="#test-evidence" aria-label="Permalink to &quot;Test evidence&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&gt; pnpm --filter dogfood-stripe-billing-app test</span></span>
<span class="line"><span> ✓ .vitest-dist/tests/3ds-e2e.spec.js (8 tests)</span></span>
<span class="line"><span> ✓ .vitest-dist/tests/dunning-e2e.spec.js (8 tests)</span></span>
<span class="line"><span> ✓ .vitest-dist/tests/checkout-e2e.spec.js (19 tests)</span></span>
<span class="line"><span></span></span>
<span class="line"><span> Test Files  3 passed (3)</span></span>
<span class="line"><span>      Tests  35 passed (35)</span></span></code></pre></div><h2 id="follow-up" tabindex="-1">Follow-up <a class="header-anchor" href="#follow-up" aria-label="Permalink to &quot;Follow-up&quot;">​</a></h2><ul><li>v1.23-2b — Stripe testcontainers fixture (<code>stripe-mock</code> container + real API calls behind <code>KIWA_STRIPE_REAL_READY=1</code>) so <code>makeRealAdapter</code> returns a fully driven body and the fidelity harness can diff mock vs real byte-for-byte.</li><li>v1.23-2c — Playwright + Next.js dev-server e2e that boots the actual Next.js runtime + navigates through checkout redirect.</li><li>v1.23-5 — tutorial doc that walks through building this app from scratch.</li></ul>`,20)])])}const g=t(o,[["render",c]]);export{u as __pageData,g as default};
