import{_ as o,o as a,c as t,a2 as r}from"./chunks/framework.CyZTVJbV.js";const h=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"announcements/v1.18/x-thread-en.md","filePath":"announcements/v1.18/x-thread-en.md"}'),d={name:"announcements/v1.18/x-thread-en.md"};function c(n,e,i,s,l,p){return a(),t("div",null,[...e[0]||(e[0]=[r('<p>1/ kiwa v1.18 is out — Blockchain 深化 milestone. After v1.17&#39;s Observability v2 (Grafana + AlertManager + trace flame + log correlation), v1.18 lands the Rust blockchain testing stack every dApp production team already ships: Reth (Rust EL client) + Foundry-rs invariant + Alloy encoder helpers + dApp e2e reorg.</p><p>2/ <code>kiwa-test-rs</code> v0.5 (minor bump from v0.4.2) — 3 additional axes on top of the v0.4 <code>kiwa::contract::foundry</code> + <code>kiwa::contract::alloy</code> foundation. Zero breaking changes; the v0.4 public re-export paths stay identical (<code>foundry.rs</code> moves to <code>foundry/mod.rs</code> etc, but every <code>use kiwa::contract::foundry::FoundryEnv;</code> caller does not touch a line).</p><p>3/ <code>kiwa::contract::reth</code> — new module behind feature <code>contract-reth</code> (default OFF). <code>RethBinary::detect</code> PATH check + <code>RethNode::spawn</code> Drop-based <code>reth node --dev</code> handle + <code>reth_reorg(endpoint, blocks)</code> for <code>debug_setHead</code> N-block reorg + <code>fidelity_matrix()</code> 7-row anvil ↔ reth JSON-RPC expectation table. Pure Rust; no alloy crate family dep.</p><p>4/ <code>kiwa::contract::foundry::invariant</code> — new submodule under <code>contract-foundry</code>. 10_000 forge run gate + fuzz seed determinism (<code>FOUNDRY_INVARIANT_SEED</code> env + Cargo.toml row) + counter-example shrink parser (<code>InvariantCounterExample { calls, revert }</code>) + <code>coverage_feed()</code> translates lcov into 7-axis release gate JSON.</p><p>5/ <code>kiwa::contract::alloy::helpers</code> — new submodule under <code>contract-alloy</code>. <code>Eip712TypedData::build(domain, primary_type, message)</code> 4-layer digest + <code>Multicall3::encode(calls)</code> <code>aggregate3(Call3[])</code> calldata + <code>Permit2::permit_witness_transfer_from(spec)</code> SignatureTransfer + witness verify. Pure Rust; no alloy crate dep — consumers plug encoded bytes into whichever Provider they build outside kiwa.</p><p>6/ dogfood apps — <code>dogfood-reth-node-test</code> (Reth NodeBuilder dev chain + alloy Provider ERC-20 drive + 3-block reorg × fidelity harness, 32 cargo test), <code>dogfood-foundry-invariant-fuzz</code> (ERC-20 + Vault + Router × invariant 10_000 run + fuzz + shrink parser + coverage feed, 21 cargo test), <code>dogfood-dapp-e2e-reorg</code> (Next.js 15 + viem + wagmi + <code>@kiwa/dapp</code> reorg fixture + anvil fork mainnet + 4 reorg scenario, 26 Playwright test). All 3 hit 7-axis release gate PASS.</p><p>7/ <code>@kiwa/dapp</code> reorg helpers — 2 new named exports <code>snapshotChain(client)</code> + <code>revertChain(client, snapshotId)</code> over <code>anvil_snapshot</code> + <code>anvil_revert</code>. Reused by the reorg dogfood app + tutorial 27 in one <code>import</code>. Package version unchanged.</p><p>8/ docs — 3 tutorials (25 Reth / 26 Foundry invariant / 27 dApp reorg) + additive migration v1.17 → v1.18 + concept doc <code>blockchain-testing.md</code> (chain state / EL client integration / fuzz shrinker / reorg semantics — 4 additional axes × 6 semantic axis SSOT). VitePress + gh-pages published. Roadmap: <a href="https://github.com/cardene777/kiwa#roadmap" target="_blank" rel="noreferrer">https://github.com/cardene777/kiwa#roadmap</a> — v1.11 → v1.12 → v1.13 → v1.14 → v1.15 → v1.16 → v1.17 → v1.18: 8 milestones in a row.</p>',8)])])}const v=o(d,[["render",c]]);export{h as __pageData,v as default};
