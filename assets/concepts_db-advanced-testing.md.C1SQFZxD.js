import{_ as t,o as a,c as s,a2 as i}from"./chunks/framework.CyZTVJbV.js";const k=JSON.parse('{"title":"Db advanced testing SSOT — 8 axis production semantics for kiwa v1.26","description":"","frontmatter":{},"headers":[],"relativePath":"concepts/db-advanced-testing.md","filePath":"concepts/db-advanced-testing.md"}'),d={name:"concepts/db-advanced-testing.md"};function o(r,e,n,c,l,p){return a(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="db-advanced-testing-ssot-—-8-axis-production-semantics-for-kiwa-v1-26" tabindex="-1">Db advanced testing SSOT — 8 axis production semantics for kiwa v1.26 <a class="header-anchor" href="#db-advanced-testing-ssot-—-8-axis-production-semantics-for-kiwa-v1-26" aria-label="Permalink to &quot;Db advanced testing SSOT — 8 axis production semantics for kiwa v1.26&quot;">​</a></h1><p>Introduced in v1.14 as <code>@kiwa-lab/orm</code> v0.8 (<code>setupOrmEnv</code> for 3 provider × 3 backend schema / migration / seed), extended in v1.26 as v0.9 (8 axis production semantics on the same 3 × 3 grid). This document is the SSOT for <strong>which advanced database axes kiwa mocks, what neutral events each axis emits, and how provider × backend fidelity is measured</strong>. Every downstream orm test in <code>packages/*/tests/**/*.test.ts</code> and every dogfood app in <code>examples/dogfood-{postgres-cdc-outbox,mysql-rls-tenant,vector-search}-app/</code> reads these rules from here — do not re-derive them locally.</p><h2 id="why-a-db-advanced-semantics-ssot" tabindex="-1">Why a db advanced semantics SSOT <a class="header-anchor" href="#why-a-db-advanced-semantics-ssot" aria-label="Permalink to &quot;Why a db advanced semantics SSOT&quot;">​</a></h2><p>Testing a production database against v0.8 <code>setupOrmEnv</code> alone leaves 8 axes of real-world behaviour unlocked.</p><ul><li><strong>v0.8 gap</strong>. <code>setupOrmEnv</code> gives you schema + migration + seed on 3 provider × 3 backend. What it does not give you is a way to assert that &quot;the outbox event ordering is preserved across a Postgres failover&quot;, &quot;the RLS policy refuses a cross-tenant read at the mock layer&quot;, or &quot;the pgvector HNSW index rejects a mismatched query dimension&quot;. Those assertions need per-axis state machines.</li><li><strong>Provider × backend dialect drift</strong>. Postgres <code>wal_sender.progress</code> vs MySQL <code>binlog.write</code> vs SQLite <code>mvcc.snapshot-taken</code> — the same neutral event fires under different backend names. Downstream tests that hard-code the backend name break when the provider swaps. A neutral event + backend-specific dialect map is the smallest surface that keeps the tests portable.</li><li><strong>Fidelity grid drift</strong>. Without a shared coverage grid, one package asserts on 6 axes and another on 7. The v1.26 SSOT pins the grid at <strong>3 provider × 3 backend × 8 axis = 72 rows</strong>, and <code>collectFidelityCoverage</code> builds it deterministically so release-gate reports show a stable dimension.</li></ul><p>The 4 rules below are the smallest set that make kiwa orm advanced semantics tests comparable across providers, backends, and forks.</p><h2 id="rule-1-—-8-axes-are-the-shared-floor" tabindex="-1">Rule 1 — 8 axes are the shared floor <a class="header-anchor" href="#rule-1-—-8-axes-are-the-shared-floor" aria-label="Permalink to &quot;Rule 1 — 8 axes are the shared floor&quot;">​</a></h2><p>Every <code>@kiwa-lab/orm</code> v0.9 axis session is one of the 8 axes below. New axes belong in a follow-up milestone; downstream tests should not roll a private axis alongside the 8.</p><table tabindex="0"><thead><tr><th>Axis</th><th>Session ctor</th><th>Neutral events</th></tr></thead><tbody><tr><td>replication</td><td><code>createReplicationSession</code></td><td><code>replication.primary-write</code> / <code>replication.replica-lagged</code> / <code>replication.failover-started</code> / <code>replication.promoted</code></td></tr><tr><td>cdc</td><td><code>createCdcSession</code></td><td><code>cdc.decoded</code> / <code>cdc.outbox-appended</code> / <code>cdc.event-ordered</code> / <code>cdc.at-least-once-delivered</code></td></tr><tr><td>logical-replication</td><td><code>createLogicalRepSession</code></td><td><code>logical.publication-created</code> / <code>logical.subscription-synced</code> / <code>logical.conflict-resolved</code> / <code>logical.heartbeat</code></td></tr><tr><td>mvcc</td><td><code>createMvccSession</code></td><td><code>mvcc.snapshot-taken</code> / <code>mvcc.serializable-aborted</code> / <code>mvcc.phantom-blocked</code> / <code>mvcc.deadlock-detected</code></td></tr><tr><td>rls</td><td><code>createRlsSession</code></td><td><code>rls.policy-installed</code> / <code>rls.tenant-isolated</code> / <code>rls.bypass-used</code> / <code>rls.audit-logged</code></td></tr><tr><td>connection-pool</td><td><code>createPoolSession</code></td><td><code>pool.acquired</code> / <code>pool.idle-timeout</code> / <code>pool.statement-timeout</code> / <code>pool.wait-queued</code></td></tr><tr><td>partitioning</td><td><code>createPartitioningSession</code></td><td><code>partition.declared</code> / <code>partition.pruned</code> / <code>partition.wise-joined</code> / <code>partition.route-selected</code></td></tr><tr><td>vector-store</td><td><code>createVectorStoreSession</code></td><td><code>vector.indexed</code> / <code>vector.knn-searched</code> / <code>vector.hybrid-searched</code> / <code>vector.distance-computed</code></td></tr></tbody></table><p>Every session ctor returns a plain object with a <code>state</code> field, a <code>history</code> array of <code>AxisStep&lt;TState&gt;</code> entries, and axis-specific bookkeeping. State machines are pure — no I/O, no timers, no adapters. Downstream tests drive the state machine with the exported step functions (<code>primaryWrite</code>, <code>decodeEvent</code>, <code>installPolicy</code>, …) and assert on the returned <code>AxisStep</code> envelope.</p><h2 id="rule-2-—-neutral-event-backend-dialect-optional-provider-overlay" tabindex="-1">Rule 2 — neutral event + backend dialect + optional provider overlay <a class="header-anchor" href="#rule-2-—-neutral-event-backend-dialect-optional-provider-overlay" aria-label="Permalink to &quot;Rule 2 — neutral event + backend dialect + optional provider overlay&quot;">​</a></h2><p>Every step returns an <code>AxisStep&lt;TState&gt;</code> with both <code>neutralEvent</code> (portable across backends) and <code>backendEvent</code> (backend dialect). The <code>backendEventName(backend, neutral, provider?)</code> helper resolves the dialect lookup.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { backendEventName } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@kiwa-lab/orm&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">backendEventName</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;postgres&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;cdc.decoded&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// -&gt; &#39;pg_logical_slot.decoded&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">backendEventName</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;mysql&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;replication.primary-write&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// -&gt; &#39;binlog.write&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">backendEventName</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;sqlite&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;replication.primary-write&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// -&gt; &#39;replication.primary-write&#39; (no server-side dialect, falls back to neutral)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">backendEventName</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;postgres&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;pool.acquired&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;prisma&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// -&gt; &#39;prisma.pool.acquired&#39; (provider overlay wins over backend dialect)</span></span></code></pre></div><p>Rules of dialect resolution.</p><ul><li><strong>Postgres + MySQL</strong> ship dialect entries for all 32 neutral events (8 axis × 4 events). Real backends surface distinct string ids that downstream telemetry key on (Postgres <code>wal_sender.progress</code>, MySQL <code>binlog.write</code>).</li><li><strong>SQLite</strong> ships dialect entries only for axes with a meaningful in-process analogue (mvcc / rls / connection-pool / partitioning / vector-store). Server-only axes (replication / cdc / logical-replication) fall back to the neutral name.</li><li><strong>Provider overlay</strong> currently only exists for <strong>Prisma</strong>. <code>drizzle</code> / <code>kysely</code> skip the overlay entirely and use the backend dialect. Downstream tests reading a <code>prisma.pool.acquired</code> string know the overlay fired; a raw <code>pgbouncer.client_acquired</code> string means the overlay skipped.</li></ul><p>The overlay is optional-safe. <code>backendEventName</code> falls back to the backend dialect when no overlay entry exists, and to the neutral name when no backend dialect exists — no silent typos, no undefined outputs.</p><h2 id="rule-3-—-state-transitions-are-strictly-guarded" tabindex="-1">Rule 3 — state transitions are strictly guarded <a class="header-anchor" href="#rule-3-—-state-transitions-are-strictly-guarded" aria-label="Permalink to &quot;Rule 3 — state transitions are strictly guarded&quot;">​</a></h2><p>Every step function throws on illegal state transitions. There is no silent state fall-through. This is what makes the mock trustworthy as a production semantics oracle — a bug in the caller (e.g. <code>confirmDelivery</code> before <code>markEventOrdered</code>) is caught at test time, not in production.</p><p>Examples of guarded transitions.</p><ul><li><code>appendOutbox</code> requires <code>decoding</code> / <code>buffered</code> / <code>ordered</code> — throwing when <code>idle</code> or <code>delivered</code>.</li><li><code>confirmDelivery</code> rejects <code>upToLsn</code> smaller than <code>confirmedLsn</code> (delivery pointer regression) and larger than the outbox high-water (acking an unappended LSN).</li><li><code>filterTenant</code> requires <code>policy-installed</code> state — throws when <code>bypassed</code> so tests assert the bypass &quot;sticks&quot; until the caller re-arms the policy.</li><li><code>takeSnapshot</code> refuses <code>phantom-blocked</code> state — a snapshot on a blocked txn masquerades as isolation.</li><li><code>partitionWiseJoin</code> requires <code>matchedBuckets === buckets.length</code> — Postgres partition-wise join is an all-or-nothing plan; partial matches fall back to a global join and are not partition-wise.</li><li><code>primaryWrite</code> refuses <code>promoted</code> state — the old primary is terminal after <code>promoteReplica</code>.</li></ul><p>These guards are asserted in <code>packages/orm/tests/semantics/*.test.ts</code> (v1.26-1) and are the reason the mock can serve as a state-machine oracle in dogfood app tests without a real database.</p><h2 id="rule-4-—-fidelity-coverage-grid-is-3-×-3-×-8-72-rows" tabindex="-1">Rule 4 — fidelity coverage grid is 3 × 3 × 8 = 72 rows <a class="header-anchor" href="#rule-4-—-fidelity-coverage-grid-is-3-×-3-×-8-72-rows" aria-label="Permalink to &quot;Rule 4 — fidelity coverage grid is 3 × 3 × 8 = 72 rows&quot;">​</a></h2><p><code>collectFidelityCoverage({ providers: [...], backends: [...] })</code> walks the requested providers × backends × axes and emits one <code>FidelityRow</code> per triple, with the 4 neutral events + 4 backend dialect strings for that combination.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { collectFidelityCoverage } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@kiwa-lab/orm&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> coverage</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> collectFidelityCoverage</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  providers: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;drizzle&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;prisma&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;kysely&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  backends: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;postgres&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;mysql&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;sqlite&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">coverage.rows.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">length</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// -&gt; 72 (3 × 3 × 8)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">coverage.axes; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// -&gt; 8 axes in declaration order</span></span></code></pre></div><p>Downstream release-gate reports assert the row count is stable (<code>72</code> for the default 3 × 3 grid) so a regression on the axis list surfaces as a dimension mismatch, not a silent field rename. The exposed fields are.</p><ul><li><code>rows[].provider</code> / <code>rows[].backend</code> / <code>rows[].axis</code> — the triple.</li><li><code>rows[].neutralEvents</code> — the 4 neutral event names for that axis.</li><li><code>rows[].backendEvents</code> — the 4 dialect-resolved strings for that provider × backend.</li></ul><p>The grid is deterministic — the axis order comes from <code>AXIS_TO_EVENTS</code> declaration order, so two runs against the same providers + backends produce byte-identical grids.</p><h2 id="provider-×-backend-fidelity-table-v1-26" tabindex="-1">Provider × backend fidelity table (v1.26) <a class="header-anchor" href="#provider-×-backend-fidelity-table-v1-26" aria-label="Permalink to &quot;Provider × backend fidelity table (v1.26)&quot;">​</a></h2><p>The v1.26 mock ships full dialect entries per (backend, axis) with a Prisma-only provider overlay on 3 events. \`\` means the backend has a named dialect; <code>–</code> means the backend falls back to the neutral name.</p><table tabindex="0"><thead><tr><th>Axis</th><th>Postgres</th><th>MySQL</th><th>SQLite</th><th>Prisma overlay</th></tr></thead><tbody><tr><td>replication (4 events)</td><td>✅ (<code>wal_sender.*</code> / <code>pg_stat_replication.*</code>)</td><td>✅ (<code>binlog.*</code> / <code>group_replication.*</code>)</td><td>– (no server-side replication)</td><td>–</td></tr><tr><td>cdc (4 events)</td><td>✅ (<code>pg_logical_slot.*</code> / <code>outbox.wal2json_*</code>)</td><td>✅ (<code>binlog.event_decoded</code> / <code>debezium.outbox_row</code>)</td><td>– (no server-side change log)</td><td>–</td></tr><tr><td>logical-replication (4 events)</td><td>✅ (<code>pg_publication.*</code> / <code>pg_subscription.*</code>)</td><td>✅ (<code>binlog.filter_registered</code> / <code>group_replication.*</code>)</td><td>–</td><td>–</td></tr><tr><td>mvcc (4 events)</td><td>✅ (<code>pg_snapshot.exported</code> / <code>pg_serializable.abort</code>)</td><td>✅ (<code>innodb.consistent_snapshot</code> / <code>innodb.deadlock</code>)</td><td>✅ (<code>sqlite.snapshot_open</code> / <code>sqlite.busy_rollback</code>)</td><td>✅ (<code>prisma.interactive_transaction</code> on snapshot)</td></tr><tr><td>rls (4 events)</td><td>✅ (<code>pg_policy.created</code> / <code>pg_rls.*</code>)</td><td>✅ (<code>view.tenant_filter</code> / <code>grant.super_bypass</code>)</td><td>✅ (<code>sqlite.view_created</code> / <code>sqlite.attach_bypass</code>)</td><td>–</td></tr><tr><td>connection-pool (4 events)</td><td>✅ (<code>pgbouncer.*</code> / <code>pg_stat_statements.timeout</code>)</td><td>✅ (<code>proxysql.*</code> / <code>max_execution_time</code>)</td><td>✅ (<code>sqlite.wal_writer_acquired</code> / <code>sqlite.busy_timeout_exceeded</code>)</td><td>✅ (<code>prisma.pool.acquired</code> / <code>prisma.pool.wait</code>)</td></tr><tr><td>partitioning (4 events)</td><td>✅ (<code>pg_partitioned_table.created</code> / <code>pg_plan.*</code>)</td><td>✅ (<code>partition.range_created</code> / <code>partition_prune</code>)</td><td>✅ (<code>sqlite.shard_attached</code> / <code>sqlite.shard_skipped</code>)</td><td>–</td></tr><tr><td>vector-store (4 events)</td><td>✅ (<code>pgvector.ivfflat_indexed</code> / <code>pgvector.knn</code>)</td><td>✅ (<code>heatwave.vector_indexed</code> / <code>heatwave.knn</code>)</td><td>✅ (<code>sqlite_vec.indexed</code> / <code>sqlite_vec.knn</code>)</td><td>–</td></tr></tbody></table><p>The full mapping lives in <code>packages/orm/src/semantics/types.ts</code> (<code>dialect</code> const + <code>providerOverlay</code> const). Downstream tests should read from <code>backendEventName</code> rather than hard-coding these strings — the map may add MySQL HeatWave / sqlite-vss / provider overlays without breaking neutral-event consumers.</p><h2 id="the-3-dogfood-apps-v1-26" tabindex="-1">The 3 dogfood apps (v1.26) <a class="header-anchor" href="#the-3-dogfood-apps-v1-26" aria-label="Permalink to &quot;The 3 dogfood apps (v1.26)&quot;">​</a></h2><p>The v1.26 milestone applied the 8 axis mocks to 3 dogfood apps that walk the provider × backend combinations end-to-end. Each app pairs one provider with one backend and exercises 1-3 axes as first-class flows.</p><table tabindex="0"><thead><tr><th>App</th><th>Framework</th><th>Provider</th><th>Backend</th><th>Primary axes</th></tr></thead><tbody><tr><td><code>examples/dogfood-postgres-cdc-outbox-app</code></td><td>Next.js 15</td><td>drizzle</td><td>postgres</td><td>cdc + logical-replication (outbox + Redis Streams downstream)</td></tr><tr><td><code>examples/dogfood-mysql-rls-tenant-app</code></td><td>Nuxt 3</td><td>prisma</td><td>mysql</td><td>rls + connection-pool (tenant isolation + audit log)</td></tr><tr><td><code>examples/dogfood-vector-search-app</code></td><td>SvelteKit</td><td>kysely</td><td>postgres</td><td>vector-store (pgvector IVFFlat / HNSW + hybrid)</td></tr></tbody></table><p>Each app ships a Playwright e2e that walks the axis end-to-end plus a fidelity harness (env-gated <code>KIWA_MODE=real</code>) that compares the mock verdict against a real testcontainers Postgres 16 / MySQL 8. The harness is opt-in — the app suite passes without Docker.</p><h2 id="where-each-axis-lands-in-the-release-gate" tabindex="-1">Where each axis lands in the release gate <a class="header-anchor" href="#where-each-axis-lands-in-the-release-gate" aria-label="Permalink to &quot;Where each axis lands in the release gate&quot;">​</a></h2><p>The 11-axis release gate ships one orm axis — <code>fidelity.ratio</code> — and one perf axis — <code>perf.p95Ms</code>. v1.26 does not add new release-gate axes; the 8 orm advanced axes fold into the same <code>fidelity.ratio</code> metric so the release-gate surface stays stable.</p><table tabindex="0"><thead><tr><th>Release-gate axis</th><th>v1.26 contribution</th><th>Source</th></tr></thead><tbody><tr><td>fidelity.ratio</td><td>8-axis mock vs real driver diff on <code>KIWA_MODE=real</code> runs</td><td><code>docs/quality/release-gate.md</code></td></tr><tr><td>perf.p95Ms</td><td>orm advanced axis suites feed p95 into the shared perf gate</td><td><code>docs/concepts/perf-testing-ssot.md</code></td></tr></tbody></table><p>Concurrent + memory gates are not in the 11-axis release gate — they are per-package gates enforced inside <code>runPerf3Layer</code> (v1.25). A concurrent or memory breach fails the local vitest run but does not cascade to the release-gate report, because the release gate is scoped to a single primary axis per category.</p><h2 id="related" tabindex="-1">Related <a class="header-anchor" href="#related" aria-label="Permalink to &quot;Related&quot;">​</a></h2><ul><li><a href="./../tutorials/47-postgres-cdc-outbox">Tutorial 47 — Postgres CDC + outbox (v1.26-2 walkthrough)</a></li><li><a href="./../tutorials/48-mysql-rls-tenant">Tutorial 48 — MySQL RLS + multi-tenant (v1.26-3 walkthrough)</a></li><li><a href="./../tutorials/49-vector-search-pgvector">Tutorial 49 — pgvector + hybrid search (v1.26-4 walkthrough)</a></li><li><a href="./../migrations/v1.25-to-v1.26">Migration guide v1.25 → v1.26</a></li><li><a href="./perf-testing-ssot">Perf-testing SSOT (p50 / p95 / p99 + baseline persistence)</a></li><li><a href="./../quality/release-gate">Release gate SSOT (11-axis)</a></li></ul>`,41)])])}const g=t(d,[["render",o]]);export{k as __pageData,g as default};
