import{_ as o,o as a,c as r,a2 as t}from"./chunks/framework.CyZTVJbV.js";const v=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"announcements/v1.18/x-thread-ja.md","filePath":"announcements/v1.18/x-thread-ja.md"}'),d={name:"announcements/v1.18/x-thread-ja.md"};function c(n,e,i,s,l,p){return a(),r("div",null,[...e[0]||(e[0]=[t('<p>1/ kiwa v1.18 released。 Blockchain 深化 milestone です。 v1.17 (Observability v2 縦軸、 Grafana + AlertManager + trace flame + log correlation) の後、 v1.18 は Rust blockchain testing stack (Reth (Rust EL client) + Foundry-rs invariant / fuzz + Alloy encoder helpers + dApp e2e reorg) を 1 統一 harness に land しました。</p><p>2/ <code>kiwa-test-rs</code> v0.5 (v0.4.2 からの minor bump) — v0.4 で land した <code>kiwa::contract::foundry</code> + <code>kiwa::contract::alloy</code> 基盤に、 3 追加軸を land。 v0.4 の source 互換性は 100% 維持 (<code>foundry.rs</code> → <code>foundry/mod.rs</code> / <code>alloy.rs</code> → <code>alloy/mod.rs</code> の module 再編は行うが、 全 public re-export path は不変、 <code>use kiwa::contract::foundry::FoundryEnv;</code> を触る必要なし)。</p><p>3/ <code>kiwa::contract::reth</code> — feature <code>contract-reth</code> (default OFF) 配下の new module。 <code>RethBinary::detect</code> で reth CLI の PATH 存在確認 + <code>RethNode::spawn</code> で <code>reth node --dev</code> の subprocess spawn + <code>Drop</code>-based tear-down + <code>reth_reorg(endpoint, blocks)</code> で <code>debug_setHead</code> JSON-RPC 経由の N-block reorg + <code>fidelity_matrix()</code> で anvil ↔ reth の 7 JSON-RPC method 期待突合。 pure Rust、 alloy crate family 依存なし。</p><p>4/ <code>kiwa::contract::foundry::invariant</code> — <code>contract-foundry</code> feature 配下の new submodule。 forge invariant runner の 10_000 run gate + fuzz seed 決定的化 (<code>FOUNDRY_INVARIANT_SEED</code> env + Cargo.toml row) + counter-example shrink parser (<code>InvariantCounterExample { calls, revert }</code>) + <code>coverage_feed()</code> で lcov を 7 軸 release gate JSON に変換。</p><p>5/ <code>kiwa::contract::alloy::helpers</code> — <code>contract-alloy</code> feature 配下の new submodule。 <code>Eip712TypedData::build(domain, primary_type, message)</code> で EIP-712 の 4 layer digest (domain separator + typeHash + structHash + digest) + <code>Multicall3::encode(calls)</code> で <code>aggregate3(Call3[])</code> calldata (N call を 1 tx に集約) + <code>Permit2::permit_witness_transfer_from(spec)</code> で SignatureTransfer + witness verify。 pure Rust、 alloy crate 依存なし。</p><p>6/ dogfood app 3 種 — <code>dogfood-reth-node-test</code> (Reth NodeBuilder dev chain + alloy Provider ERC-20 drive + 3-block reorg × fidelity harness、 32 cargo test)、 <code>dogfood-foundry-invariant-fuzz</code> (ERC-20 + Vault + Router × invariant 10_000 run + fuzz seed 決定的化 + shrink parser 検証 + coverage feed、 21 cargo test)、 <code>dogfood-dapp-e2e-reorg</code> (Next.js 15 + viem + wagmi + <code>@kiwa-test/dapp</code> reorg fixture + anvil fork mainnet + 4 reorg scenario、 26 Playwright test)。 全 3 app が 7 軸 release gate PASS。</p><p>7/ <code>@kiwa-test/dapp</code> reorg helper — 2 new named export <code>snapshotChain(client)</code> + <code>revertChain(client, snapshotId)</code> (<code>anvil_snapshot</code> + <code>anvil_revert</code> の JSON-RPC 経由 wrap)。 reorg dogfood app + tutorial 27 が 1 <code>import</code> で共有。 package version は変更なし。</p><p>8/ docs — tutorial 3 本 (25 Reth / 26 Foundry invariant / 27 dApp reorg) + additive migration v1.17 → v1.18 + concept doc <code>blockchain-testing.md</code> (chain state / EL client integration / fuzz shrinker / reorg semantics の 4 追加軸 × 6 semantic axis SSOT)。 VitePress sidebar + gh-pages 反映済。 Roadmap: <a href="https://github.com/cardene777/kiwa#roadmap" target="_blank" rel="noreferrer">https://github.com/cardene777/kiwa#roadmap</a> — v1.11 → v1.12 → v1.13 → v1.14 → v1.15 → v1.16 → v1.17 → v1.18 の 8 milestone 連続完遂。</p>',8)])])}const m=o(d,[["render",c]]);export{v as __pageData,m as default};
