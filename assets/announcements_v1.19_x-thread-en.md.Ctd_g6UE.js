import{_ as o,o as a,c as t,a2 as r}from"./chunks/framework.CyZTVJbV.js";const h=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"announcements/v1.19/x-thread-en.md","filePath":"announcements/v1.19/x-thread-en.md"}'),d={name:"announcements/v1.19/x-thread-en.md"};function n(s,e,c,i,l,p){return a(),t("div",null,[...e[0]||(e[0]=[r('<p>1/ kiwa v1.19 is out — Framework 深化 milestone. After v1.18&#39;s Blockchain 深化 vertical (Reth + Foundry invariant/fuzz + Alloy helpers + dApp reorg), v1.19 lands <strong>3 modern web framework testing adapters</strong> every SaaS/edge team already ships: SolidJS (Signal-based fine-grained reactivity) + Deno Fresh (Islands architecture + partial hydration) + HonoJS (Cloudflare Workers + hc RPC type-safe client + middleware chain).</p><p>2/ <code>@kiwa-lab/solidjs</code> v0.1.0 — Signal / Effect / createResource / Suspense boundary testing pattern for SolidJS. <code>createSignal</code> auto-tracks dependencies, <code>createEffect</code> runs against a deterministic <code>flushSync()</code> scheduler (no microtask races), <code>createResource</code> produces a Suspense-aware async signal, <code>SuspenseBoundary.captureFallback</code> + <code>resumeSuspense</code> transition the boundary state. 42 behavior test.</p><p>3/ <code>@kiwa-lab/fresh</code> v0.1.0 — Deno Fresh Islands architecture testing pattern. <code>defineRoute</code> normalizes handler + component shape, <code>Handler</code> union mock exposes <code>req</code> / <code>params</code> / <code>state</code> / <code>render</code> without a Deno runtime, <code>renderIsland</code> captures mount boundaries + partial hydration markup, <code>Head</code> fragment merger deduplicates <code>&lt;meta&gt;</code> / <code>&lt;title&gt;</code> / <code>&lt;link&gt;</code> across nested layouts. 58 behavior test.</p><p>4/ <code>@kiwa-lab/hono</code> v0.1.0 — HonoJS Cloudflare Workers testing pattern. <code>createApp()</code> returns a HonoLike stub matching <code>.get/.post/.use</code>, <code>app.request(path, init)</code> invokes handler chains without a Workers runtime, <code>hc&lt;AppType&gt;</code> produces a type-safe RPC client mock preserving the <code>AppType</code> inference chain end-to-end, <code>middlewareChain([cors, bearerAuth, logger])</code> traces execution order + context state, <code>mockEnv({ KV, D1, R2, ExecutionContext })</code> provides the Workers env binding surface. 89 behavior test.</p><p>5/ dogfood apps — <code>dogfood-solidjs-signal-app</code> (fine-grained reactivity + createResource + Suspense fidelity harness, 5 scenario) + <code>dogfood-fresh-islands</code> (SSR-first + island boundary + Head merge fidelity harness, 5 scenario) + <code>dogfood-hono-workers-rpc</code> (Workers env + hc RPC + middleware chain fidelity harness, 5 scenario). All 3 hit 7-axis release gate PASS.</p><p>6/ 3 new npm packages published — <code>@kiwa-lab/solidjs</code> v0.1.0 + <code>@kiwa-lab/fresh</code> v0.1.0 + <code>@kiwa-lab/hono</code> v0.1.0 to npm. kiwa runtime fixture count is now 33 packages. Web framework coverage 8 → 11 (Next.js / Nuxt / SvelteKit / Remix / Astro / SolidStart / Qwik City / Edge + SolidJS / Fresh / HonoJS).</p><p>7/ docs — 3 tutorials (28 SolidJS Signal / 29 Fresh Islands / 30 HonoJS + hc RPC + Workers env) + additive migration v1.18 → v1.19 + concept doc <code>modern-web-framework-testing.md</code> (Signal reactivity / Islands architecture / edge runtime + RPC type-safety — 3 additional axes × 6 semantic axis SSOT). VitePress + gh-pages published. Roadmap: <a href="https://github.com/cardene777/kiwa#roadmap" target="_blank" rel="noreferrer">https://github.com/cardene777/kiwa#roadmap</a> — v1.11 → v1.12 → v1.13 → v1.14 → v1.15 → v1.16 → v1.17 → v1.18 → v1.19: 9 milestones in a row.</p>',7)])])}const m=o(d,[["render",n]]);export{h as __pageData,m as default};
