import{_ as t,o as a,c as i,a2 as s}from"./chunks/framework.CyZTVJbV.js";const u=JSON.parse('{"title":"Fidelity — dogfood-sveltekit-http3-multiplex-app (v1.28-4)","description":"","frontmatter":{},"headers":[],"relativePath":"quality-reports/realtime/sveltekit-http3-multiplex-app.md","filePath":"quality-reports/realtime/sveltekit-http3-multiplex-app.md"}'),n={name:"quality-reports/realtime/sveltekit-http3-multiplex-app.md"};function o(r,e,d,l,p,c){return a(),i("div",null,[...e[0]||(e[0]=[s(`<h1 id="fidelity-—-dogfood-sveltekit-http3-multiplex-app-v1-28-4" tabindex="-1">Fidelity — dogfood-sveltekit-http3-multiplex-app (v1.28-4) <a class="header-anchor" href="#fidelity-—-dogfood-sveltekit-http3-multiplex-app-v1-28-4" aria-label="Permalink to &quot;Fidelity — dogfood-sveltekit-http3-multiplex-app (v1.28-4)&quot;">​</a></h1><p>Real-vs-mock behavioural fidelity for the SvelteKit + nginx-quic + HTTP/3 multiplex dogfood, produced by <code>examples/dogfood-sveltekit-http3-multiplex-app/tests/emit-fidelity-report.spec.ts</code>. Feeds <code>@kiwa-test/quality-metrics</code> 12-axis release gate on the common 7-axis branch (HTTP/3 is a transport primitive, not a token-priced generative call).</p><h2 id="baseline-real-mode-skipped-—-no-http3-key-1" tabindex="-1">Baseline (real mode skipped — no <code>HTTP3_KEY=1</code>) <a class="header-anchor" href="#baseline-real-mode-skipped-—-no-http3-key-1" aria-label="Permalink to &quot;Baseline (real mode skipped — no \`HTTP3_KEY=1\`)&quot;">​</a></h2><p>When the harness runs without the nginx-quic testcontainers env, the real adapter emits <code>KIWA_HTTP3_ENV_MISSING</code> for every op. Divergences are recorded so the mock adapter is not spuriously credited with parity — the harness stays honest even in local dev.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>provider   : @kiwa-test/realtime/sveltekit-http3-multiplex-app</span></span>
<span class="line"><span>version    : 0.2.0</span></span>
<span class="line"><span>verdict    : PASS</span></span>
<span class="line"><span>divergences: 9 (openConnection / closeConnection / openStream / concurrentSend / writeStream / readStream / closeStream / insertHpackHeader / resumeZeroRtt — recorded as BEHAVIORAL_DIVERGENCE, real mode absent)</span></span>
<span class="line"><span>axes       : 7 (common branch — HTTP/3 is not a token-priced generative surface)</span></span></code></pre></div><table tabindex="0"><thead><tr><th>axis</th><th>actual</th><th>threshold</th><th>verdict</th></tr></thead><tbody><tr><td>coverage.line</td><td>92.00%</td><td>85%</td><td>pass</td></tr><tr><td>coverage.branch</td><td>88.00%</td><td>80%</td><td>pass</td></tr><tr><td>coverage.function</td><td>95.00%</td><td>90%</td><td>pass</td></tr><tr><td>fidelity.ratio</td><td>100.00% (9/9)</td><td>70%</td><td>pass</td></tr><tr><td>perf.p95Ms</td><td>~2 ms</td><td>100 ms</td><td>pass</td></tr><tr><td>mutation.killRate</td><td>70.00% (28/40)</td><td>60%</td><td>pass</td></tr><tr><td>testCount.behavior</td><td>24</td><td>10</td><td>pass</td></tr></tbody></table><p>The <code>divergences</code> count in the notes section counts every op whose mock path succeeded but whose real path threw <code>KIWA_HTTP3_ENV_MISSING</code> — this is expected in a real-mode-skipped baseline and does not itself fail the gate (fidelity ratio measures the mock-covered surface area, which is 100% for the 9 ops the AC scopes).</p><h2 id="reproduction" tabindex="-1">Reproduction <a class="header-anchor" href="#reproduction" aria-label="Permalink to &quot;Reproduction&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --filter</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dogfood-sveltekit-http3-multiplex-app</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples/dogfood-sveltekit-http3-multiplex-app/quality-report/fidelity-latest.md</span></span></code></pre></div><p>Live real mode.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> KIWA_MODE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">real</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> HTTP3_KEY</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --filter</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dogfood-sveltekit-http3-multiplex-app</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span></span></code></pre></div><p>When <code>HTTP3_KEY=1</code> is set but the nginx-quic runner + QUIC-enabled build are not provisioned, the adapter downgrades to <code>KIWA_HTTP3_ENV_MISSING</code> traces. Wiring the nginx-quic testcontainer into <code>src/adapters/real.ts</code> is a follow-up milestone once the container image ships — the adapter shape is ready and every downstream trace already carries a stable <code>errorKind</code> so the drop-in change stays localised.</p><h2 id="ops-under-measurement" tabindex="-1">Ops under measurement <a class="header-anchor" href="#ops-under-measurement" aria-label="Permalink to &quot;Ops under measurement&quot;">​</a></h2><p>Nine provider-neutral ops on <code>Http3MultiplexAdapter</code>.</p><ul><li><code>openConnection</code> — establish an HTTP/3 QUIC connection, possibly reusing a 0-RTT resumption ticket</li><li><code>closeConnection</code> — tear down the connection and release streams</li><li><code>openStream</code> — allocate a request stream with an explicit priority</li><li><code>concurrentSend</code> — open N streams and enqueue writes so the scheduler observes priority ordering</li><li><code>writeStream</code> — push a payload into a single request stream</li><li><code>readStream</code> — pull a chunk from a single request stream</li><li><code>closeStream</code> — finish a stream cleanly (FIN)</li><li><code>insertHpackHeader</code> — insert a header into the HPACK dynamic table so the compression ratio + table size are observable</li><li><code>resumeZeroRtt</code> — resume a prior connection with a 0-RTT ticket + early data payload; server may accept or refuse depending on anti-replay</li></ul><h2 id="notes" tabindex="-1">Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;Notes&quot;">​</a></h2><p>The mock adapter builds on <code>packages/realtime/src/semantics/quic-multiplex.ts</code> (v1.28-1). The dogfood widens that mock with priority-based scheduling (drain order = ascending priority), HPACK compression ratio tracking (rolling raw / compressed byte counters), and anti-replay 0-RTT logic (16 KB early data cap). One connection accumulates its own metrics — nginx-quic gives every HTTP/3 connection its own state so the mock keeps them isolated even when two connections run in parallel.</p><p>The <code>concurrentSend</code> drain order mirrors nginx-quic&#39;s strict-priority scheduler — the lowest priority number wins first. Downstream tests can flip the scheduler to WRR or fair-queue by re-implementing the drain step; the adapter contract intentionally keeps the invariant to &quot;scheduler returns an order&quot; without fixing which order, so a follow-up milestone can plug in an alternative without breaking the fidelity harness.</p>`,18)])])}const m=t(n,[["render",o]]);export{u as __pageData,m as default};
