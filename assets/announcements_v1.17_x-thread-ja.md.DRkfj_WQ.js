import{_ as o,o as a,c as d,a2 as c}from"./chunks/framework.CyZTVJbV.js";const h=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"announcements/v1.17/x-thread-ja.md","filePath":"announcements/v1.17/x-thread-ja.md"}'),t={name:"announcements/v1.17/x-thread-ja.md"};function r(n,e,l,s,i,p){return a(),d("div",null,[...e[0]||(e[0]=[c('<p>1/ kiwa v1.17 released。 Observability v2 milestone です。 v1.16 (component test 縦軸、 Storybook 8 + Playwright CT + Chromatic) の後、 v1.17 は 2026 SaaS production team のほぼ全てが導入済の observability stack (Grafana dashboard + Prometheus AlertManager + trace flame graph + log correlation) を 1 統一 mock harness に land しました。</p><p>2/ <code>@kiwa-lab/observability</code> v2.0 (major bump) — v1.14-4 で land した v1.1 telemetry mock 3 provider (OpenTelemetry / Datadog / Sentry) を基盤に、 4 追加軸を land。 v1.0 (<code>renderDashboard</code> / <code>detectFlaky</code> / <code>analyzeSpecCoverage</code> / <code>checkThresholds</code>) + v1.1 (<code>TelemetryCollector</code> + 3 provider mock) surface は 100% backward compatible。 11 新 export + 13 fixture builder。</p><p>3/ <code>DashboardMock</code> — <code>TelemetryCollector</code> に bind して N panel query を 6 種 aggregate (<code>sum</code> / <code>avg</code> / <code>max</code> / <code>min</code> / <code>count</code> / <code>last</code>) で評価、 optional tag filter + time window + <code>PanelThreshold[]</code> で ok / warn / critical badge 選定。 <code>refresh()</code> は <code>PanelResult[]</code> 返却 + <code>refreshCount</code> tick。</p><p>4/ <code>AlertRouter</code> — <code>AlertRule[]</code> を collector に登録、 <code>pending → firing</code> を <code>forSamples</code> gate 経由で駆動、 <code>resolved</code> は metric 下降時に自動遷移。 <code>setRoute</code> は nested <code>RouteEntry</code> tree を deepest match で解決、 <code>addSilence</code> は literal + regex label match 対応、 <code>tickEscalation()</code> state machine で「t=15min で tier 2 到達」 を wall clock なしで assert 可能。</p><p>5/ <code>buildSpanTree</code> + <code>renderFlameGraph</code> + <code>drillDown</code> + <code>flattenFlame</code> — <code>SpanRecord[]</code> (v1.1 shape 無変更) の pure transform、 span parent chain 復元 + <code>totalMs</code> / <code>selfMs</code> 算出 + 同名 sibling を <code>FlameNode</code> に集約 (samples 数計上) + 部分木を depth 0 normalize で drill-down 抽出。 orphan span (parent 未検出) は root 昇格。</p><p>6/ <code>LogCorrelationIndex</code> + <code>correlateLogsAndSpans</code> — <code>LogRecord[]</code> + <code>SpanRecord[]</code> から双方向 index 構築、 <code>logsForSpan</code> / <code>logsForTrace</code> / <code>spansForTrace</code> / <code>linkAll</code> を提供。 configurable <code>CorrelationKeys</code> で OpenTelemetry (<code>trace_id</code> / <code>span_id</code>) + Datadog (<code>dd.trace_id</code>) + Sentry (<code>sentry-trace</code>) の 3 convention を統一 API で扱う。 40+ 新 behavior test / 145 total pass。</p><p>7/ dogfood app 3 種 — dashboard (Next.js App Router + Grafana 型 5 panel × 22 test)、 alert-orchestrator (Node.js + AlertManager 型 10 rule + 3 level route + escalation × 27 test)、 trace-flame-graph (React SPA + Jaeger 型 10 fixture (100 span + 34 log) × 29 test)。 全 3 app が mock vs real fidelity harness 経由で 7 軸 release gate PASS。</p><p>8/ docs — tutorial 3 本 (22 dashboard / 23 alert / 24 flame graph) + additive migration v1.16 → v1.17 + concept doc <code>observability-v2-testing.md</code> (4 追加軸 × 6 semantic axis SSOT)。 VitePress sidebar + gh-pages 反映済。 Roadmap: <a href="https://github.com/cardene777/kiwa/issues/777" target="_blank" rel="noreferrer">https://github.com/cardene777/kiwa/issues/777</a> — v1.11 → v1.12 → v1.13 → v1.14 → v1.15 → v1.16 → v1.17 の 7 milestone 連続完遂。</p>',8)])])}const g=o(t,[["render",r]]);export{h as __pageData,g as default};
