import{_ as o,o as t,c as a,a2 as c}from"./chunks/framework.CyZTVJbV.js";const v=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"announcements/v1.25/x-thread-en.md","filePath":"announcements/v1.25/x-thread-en.md"}'),s={name:"announcements/v1.25/x-thread-en.md"};function d(r,e,i,n,p,l){return t(),a("div",null,[...e[0]||(e[0]=[c('<p>1/ kiwa v1.25 is out — Perf-harness sweep milestone. After v1.13-1&#39;s <code>@kiwa-test/perf-harness</code> v0.1 landed the <code>measure</code> p50/p95/p99 + <code>saveBaseline</code> + <code>detectRegression</code> + <code>evaluatePerfGate</code> primitives and v1.14-post added <code>runPerf3Layer</code> (serial + concurrent + memory 3-layer harness), v1.25 rolls the same primitives out to <strong>all 33 kiwa packages</strong> under one shared SSOT (3 warmup + 100 iteration + 20 % p95 delta + Welch&#39;s t-test |t| &gt; 2). 33 package sweep + v0.2 minor bump.</p><p>2/ Why 33 package coverage? A 3-pilot rollout has 3 failure modes — threshold drift (p50 &lt; 5 ms vs p95 &lt; 10 ms vs mean &lt; 8 ms), iteration count drift (10 iter = warmup-noise, 10 000 iter = 10 s × 33 pkg CI hit), regression detection drift (5 % vs 10 % vs 30 % all called &quot;regression&quot;). The 4-rule SSOT (<code>docs/concepts/perf-testing-ssot.md</code>) — 3 warmup + 100 iter / Welch&#39;s t-test |t| &gt; 2 / 20 % p95 delta / baseline JSON schema — is the smallest set that makes kiwa perf suites comparable across packages, milestones, and forks.</p><p>3/ v1.25-1 core layer perf sweep — 9 package (<code>@kiwa-test/core</code> / <code>dapp</code> / <code>api</code> / <code>ui</code> / <code>data</code> / <code>cli-test</code> / <code>observability</code> / <code>e2e</code> / <code>cli</code>) each grew <code>tests/perf/{package}.perf.ts</code> + <code>test:perf</code> script + <code>.perf-baseline/{package}.json</code>. Every suite uses <code>runPerf3Layer</code> (serial concurrency=1 200 iter + concurrent concurrency=10 500 iter + memory heap sampling).</p><p>4/ v1.25-2 framework adapter perf sweep — 11 framework adapter (<code>a11y</code> / <code>visual</code> / <code>nextjs</code> / <code>nuxt</code> / <code>sveltekit</code> / <code>remix</code> / <code>astro</code> / <code>solidstart</code> / <code>qwikcity</code> / <code>edge</code> / <code>fresh</code> / <code>hono</code> / <code>solidjs</code>) same pattern. Framework baseline path separation prevents cross-adapter noise.</p><p>5/ v1.25-3 test type perf sweep — 3 test type package (<code>a11y</code> / <code>visual</code> / <code>component</code>) + 3-layer perf harness.</p><p>6/ v1.25-4 SaaS layer perf sweep — 10 SaaS layer package (<code>auth</code> / <code>queue</code> / <code>cache</code> / <code>orm</code> / <code>payment</code> / <code>streaming</code> / <code>search</code> / <code>mcp</code> / <code>agent</code> / <code>ai-llm</code>) + provider 別 baseline + <code>KIWA_MODE=real</code> opt-in for real sandbox measurement.</p><p>7/ v1.25-5 release-gate 統合 + docs — <code>perf.p95Ms</code> axis integrated with 11-axis release gate. 2 tutorials (45 perf-harness baseline + 46 perf baseline migration) + concept doc <code>perf-testing-ssot.md</code> 4-rule SSOT + migration guide v1.24 → v1.25 (additive-only) + snippet validation <code>docs-tutorial-v1.25.test.ts</code> (15 test).</p><p>8/ v1.25-6 publish — <code>@kiwa-test/perf-harness</code> v0.1.1 → v0.2.0 minor bump published to npm. Additive-only: v0.1 signature completely preserved. VitePress sidebar gains <code>Perf-harness sweep (v1.25)</code> tutorial section + <code>perf-testing-ssot.md</code> under Concepts + <code>v1.24 → v1.25</code> under Migrations. gh-pages published. release script filter に <code>@kiwa-test/perf-harness</code> 追加 (v1.14 payment 漏れ再発防止). Roadmap: <a href="https://github.com/cardene777/kiwa#roadmap" target="_blank" rel="noreferrer">https://github.com/cardene777/kiwa#roadmap</a> — v1.11 → v1.12 → v1.13 → v1.14 → v1.15 → v1.16 → v1.17 → v1.18 → v1.19 → v1.20 → v1.21 → v1.22 → v1.23 → v1.24 → v1.25: 15 milestones in a row.</p>',8)])])}const u=o(s,[["render",d]]);export{v as __pageData,u as default};
