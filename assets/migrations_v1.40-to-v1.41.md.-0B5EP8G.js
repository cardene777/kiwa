import{_ as a,o as s,c as i,a2 as t}from"./chunks/framework.CyZTVJbV.js";const k=JSON.parse('{"title":"Migration guide — v1.40 → v1.41","description":"","frontmatter":{},"headers":[],"relativePath":"migrations/v1.40-to-v1.41.md","filePath":"migrations/v1.40-to-v1.41.md"}'),n={name:"migrations/v1.40-to-v1.41.md"};function o(d,e,c,r,l,p){return s(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="migration-guide-—-v1-40-→-v1-41" tabindex="-1">Migration guide — v1.40 → v1.41 <a class="header-anchor" href="#migration-guide-—-v1-40-→-v1-41" aria-label="Permalink to &quot;Migration guide — v1.40 → v1.41&quot;">​</a></h1><p>v1.41 is <strong>additive</strong> — every <code>@kiwa-test/*</code> package public API that existed in v1.40 keeps its surface. Existing tests keep passing without touching a single line. The v1.41 changes are pure additions on the Payment 深化 axis pair depth 4 段拡張 (v1.14 v0.1 base → v1.19 v0.2 advanced → v1.33 v0.4 advanced II → v1.41 v0.5 advanced III — Embedded finance + BNPL + Crypto payment + FX cross-border + Recurring revenue advanced + Payment orchestration II + Fraud detection advanced + Regulatory reporting). This is the <strong>second pair in kiwa history to reach depth 4</strong> — the first was the AI-LLM 縦深化 pair (v1.12 → v1.15 → v1.38 → v1.40), documented in the v1.39 → v1.40 migration guide.</p><p>The 5 things v1.41 adds are.</p><ul><li><code>@kiwa-test/payment</code> v0.5 — an 8-axis advanced III upgrade on top of the existing v0.4 surface. New axes cover embedded finance (<code>openAccount</code> + <code>verifyKyc</code> + <code>verifyKyb</code> + <code>issueCard</code> + <code>closeAccount</code>), BNPL (<code>createBnplPlan</code> + <code>scheduleInstallment</code> + <code>scoreRisk</code> + <code>chargeLateFee</code> + <code>markInstallmentPaid</code>), crypto payment (<code>createCryptoInvoice</code> + <code>confirmTx</code> + <code>abstractGas</code> + <code>linkWallet</code>), FX cross-border (<code>startFxTransfer</code> + <code>lockRate</code> + <code>initiateSettlement</code> + <code>completeSettlement</code> + <code>expireRate</code>), recurring revenue advanced (<code>startRecurringRevenue</code> + <code>computeMrr</code> + <code>recordChurn</code> + <code>recordExpansion</code> + <code>computeNrr</code> + <code>recordContraction</code>), payment orchestration II (<code>startOrchestrationII</code> + <code>smartRoute</code> + <code>scoreMl</code> + <code>triggerFallback</code>), fraud detection advanced (<code>startFraudDetection</code> + <code>scoreDevice</code> + <code>verifyBiometric</code> + <code>flagVelocity</code> + <code>scoreMlBlock</code>), and regulatory reporting (<code>startRegulatoryReporting</code> + <code>reportPci</code> + <code>reportPsd2</code> + <code>reportDora</code> + <code>fileSar</code> + <code>lockForAudit</code>). A <code>providerEventName()</code> mapper describes the 3 provider (<code>stripe</code> / <code>paddle</code> / <code>lemonsqueezy</code>) × 25 axis (v0.3 9 + v0.4 8 + v0.5 8) = 75-row grid. The <code>KIWA_MODE=real</code> env-gate for the dogfood apps reuses the existing v0.3 / v0.4 pattern — no new env variable is introduced. 558 new test.</li><li>3 dogfood apps new (<code>examples/dogfood-payment-embedded-finance-app</code> + <code>examples/dogfood-payment-bnpl-installment-app</code> + <code>examples/dogfood-payment-crypto-fx-app</code>) — each pins to real Stripe Treasury / Unit / Column (BaaS + card + KYC/KYB) / Klarna / Affirm / Afterpay (BNPL + credit + late fee) / Coinbase Commerce / BitPay + Wise / Airwallex (crypto + FX) backends under <code>KIWA_MODE=real</code> + the corresponding <code>_URL</code> env, routes the fidelity-harness assertions through the real backend APIs, and keeps the mock-only path green for the fast inner loop. The v1.29 3-layer defensive structure (release-invariants + docs-e2e + release-smoke) auto-fires on merge.</li><li>3 new tutorials (88 Embedded finance + BNPL + 89 Crypto payment + FX cross-border + 90 Recurring revenue + Orchestration II + Fraud detection + Regulatory reporting) + 1 concept doc SSOT (<code>payment-advanced-III-testing.md</code> — v0.5 8 axis SSOT + 25-cell fidelity grid + pair 深度 4 段 2 例目 record documentation) + 1 migration guide (this file) + 1 snippet validation test (<code>packages/payment/tests/docs-tutorial-v1.41.test.ts</code>) — <strong>19-milestone snippet validation streak</strong> (v1.23 → v1.41). All wired through the sidebar (<code>docs/.vitepress/config.mts</code>).</li><li>No new release-gate axis — v1.41 stays at the v1.30 13-axis lineup (<code>a11y.tier</code> was the 13th). The 8 v0.5 payment advanced III axes are surface-level (they gate the payment package&#39;s own tests) but do not surface as a per-package release-gate axis, because the advanced III fidelity harness lives inside <code>@kiwa-test/payment</code> rather than at <code>@kiwa-test/quality-metrics</code>.</li></ul><h2 id="what-breaks-nothing" tabindex="-1">What breaks (nothing) <a class="header-anchor" href="#what-breaks-nothing" aria-label="Permalink to &quot;What breaks (nothing)&quot;">​</a></h2><p>Nothing. The v1.41 changes are pure additions.</p><ul><li><code>@kiwa-test/payment</code> v0.4 (v1.33 payment 深化 II 8 axis) — untouched. v0.5 layers the 8 new advanced III axes on top through <code>export * from &#39;./semantics/index.js&#39;</code>; every v0.4 export (<code>startOrchestration</code> / <code>routeCharge</code> / <code>probeCircuit</code> / <code>startRecovery</code> / <code>scheduleSmartRetry</code> / <code>advanceCascade</code> / <code>applyCardUpdate</code> / <code>applyNetworkToken</code> / <code>finalizeRecovery</code> / <code>startRefund</code> / <code>partialRefund</code> / <code>fullRefund</code> / <code>denyByPolicy</code> / <code>markWindowExpired</code> / <code>preventChargeback</code> / <code>openDispute</code> / <code>submitDisputeEvidence</code> / <code>representDispute</code> / <code>escalateArbitration</code> / <code>shiftLiability</code> / <code>finalizeDispute</code> / <code>startIdempotency</code> / <code>deliver</code> / <code>reportFailure</code> / <code>rotateSignature</code> / <code>calculateLocalizedTax</code> / <code>reportDac7</code> / <code>startSubscriptionMachine</code> / <code>enterGracePeriod</code> / <code>exitGracePeriod</code> / <code>applyProration</code> / <code>stackCoupon</code> / <code>startVault</code> / <code>tokenizeCard</code> / <code>revokeToken</code> / <code>migrateToken</code> / <code>verifyPciScope</code>) stays available at the same import path.</li><li><code>@kiwa-test/payment</code> v0.3 (9-axis base — dunning / retry / 3DS / SCA / PSD2 / subscription-lifecycle / invoice / tax / chargeback) — untouched.</li><li><code>@kiwa-test/ai-llm</code> v0.5 (v1.40 ai-llm 深化 III 8 axis) — untouched.</li><li><code>@kiwa-test/security</code> v0.2 (v1.39 security 深化 II 8 axis) — untouched.</li><li><code>@kiwa-test/search</code> v0.3 (v1.36 search 深化) — untouched.</li><li><code>@kiwa-test/observability</code> v2.1 (v1.35 observability 深化) — untouched.</li><li><code>@kiwa-test/component</code> v0.3 + <code>@kiwa-test/nextjs</code> v1.2 (v1.34 frontend 深化) — untouched.</li><li><code>@kiwa-test/orm</code> v0.10 (Postgres logical replication advanced + MySQL cluster + SQLite WAL + FTS5 from v1.32) — untouched.</li><li><code>@kiwa-test/streaming</code> v0.3 + <code>@kiwa-test/a11y</code> v1.1 + <code>@kiwa-test/quality-metrics</code> v0.4 — untouched.</li><li><code>@kiwa-test/release-invariants</code> v0.1 — untouched.</li><li>Every prior <code>@kiwa-test/*</code> package (38 total) — public API surface untouched. Existing tests keep passing.</li><li>Every prior tutorial 01-87, migration guide v1.9→v1.40, concept doc — unchanged. v1.41 only appends tutorials 88-90 + concept <code>payment-advanced-III-testing.md</code> + migration <code>v1.40-to-v1.41.md</code>.</li><li>Every prior dogfood app (v1.40 ai-llm 深化 III, v1.39 security 深化 II, v1.38 ai-llm 深化, v1.37 security base 深化, v1.36 search 深化, etc.) — untouched. v1.41 adds 3 new payment-focused dogfood apps but does not modify unrelated existing apps.</li><li><code>evaluateReleaseGate</code> signature — no change. The v1.30 13-axis path (<code>{ a11yTier }</code>) stays as-is.</li></ul><h2 id="what-s-new-opt-in" tabindex="-1">What&#39;s new (opt in) <a class="header-anchor" href="#what-s-new-opt-in" aria-label="Permalink to &quot;What&#39;s new (opt in)&quot;">​</a></h2><h3 id="kiwa-test-payment-v0-5-—-8-axis-3-provider-advanced-iii-fidelity-harness" tabindex="-1"><code>@kiwa-test/payment</code> v0.5 — 8 axis + 3 provider advanced III fidelity harness <a class="header-anchor" href="#kiwa-test-payment-v0-5-—-8-axis-3-provider-advanced-iii-fidelity-harness" aria-label="Permalink to &quot;\`@kiwa-test/payment\` v0.5 — 8 axis + 3 provider advanced III fidelity harness&quot;">​</a></h3><p>Install (or bump) the package.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -D</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @kiwa-test/payment@^0.5</span></span></code></pre></div><p>Import the axis you need directly through the top-level barrel — every v0.5 function is re-exported through the package root.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  abstractGas,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  chargeLateFee,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  closeAccount,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  completeSettlement,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  computeMrr,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  computeNrr,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  confirmTx,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  createBnplPlan,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  createCryptoInvoice,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  expireRate,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  fileSar,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  flagVelocity,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  initiateSettlement,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  issueCard,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  linkWallet,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  lockForAudit,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  lockRate,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  markInstallmentPaid,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  openAccount,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  recordChurn,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  recordContraction,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  recordExpansion,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  reportDora,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  reportPci,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  reportPsd2,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  scheduleInstallment,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  scoreDevice,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  scoreMl,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  scoreMlBlock,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  scoreRisk,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  smartRoute,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  startFraudDetection,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  startFxTransfer,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  startOrchestrationII,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  startRecurringRevenue,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  startRegulatoryReporting,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  triggerFallback,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  verifyBiometric,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  verifyKyb,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  verifyKyc,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@kiwa-test/payment&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p>Each axis has a factory (<code>openAccount</code> / <code>createBnplPlan</code> / <code>createCryptoInvoice</code> / <code>startFxTransfer</code> / <code>startRecurringRevenue</code> / <code>startOrchestrationII</code> / <code>startFraudDetection</code> / <code>startRegulatoryReporting</code>) that returns a <code>{ session, ... }</code> shape, and the axis functions gate on the session state (an <code>issueCard</code> call before <code>verifyKyc</code> throws immediately). The <code>state</code> transition graph is documented per axis in the tutorials (88 for embedded-finance / BNPL, 89 for crypto / FX, 90 for recurring / orchestration II / fraud / regulatory).</p><h3 id="_25-axis-combined-fidelity-harness" tabindex="-1">25-axis combined fidelity harness <a class="header-anchor" href="#_25-axis-combined-fidelity-harness" aria-label="Permalink to &quot;25-axis combined fidelity harness&quot;">​</a></h3><p>The v0.5 package extends the existing <code>collectFidelityCoverage()</code> grid to cover the full 3 provider × 25 axis = 75-row grid in one call.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { collectFidelityCoverage } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@kiwa-test/payment&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> cov</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> collectFidelityCoverage</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">console.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">log</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(cov.rows.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">length</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">); </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 75</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">console.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">log</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(cov.rows.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">every</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">r</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> r.implemented)); </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// true</span></span></code></pre></div><p>The rows are ordered by axis first (v0.3 axes 1-9 first, v0.4 axes 10-17 second, v0.5 axes 18-25 third) then by provider (<code>stripe</code> / <code>paddle</code> / <code>lemonsqueezy</code>). A CI job can pin the row count as a regression guard so a mis-configured axis registration is caught before the release cuts.</p><h3 id="_3-dogfood-apps" tabindex="-1">3 dogfood apps <a class="header-anchor" href="#_3-dogfood-apps" aria-label="Permalink to &quot;3 dogfood apps&quot;">​</a></h3><p>Each app pins one v0.5 axis pair against a real backend under <code>KIWA_MODE=real</code>.</p><ul><li><strong>dogfood-payment-embedded-finance-app</strong> — embedded-finance axis (BaaS + card + KYC/KYB) against real Stripe Treasury / Unit / Column backends under <code>KIWA_MODE=real</code> + <code>KIWA_STRIPE_TREASURY_URL</code> + <code>KIWA_UNIT_URL</code> + <code>KIWA_COLUMN_URL</code>. Runs the same <code>embedded.account_opened</code> + <code>embedded.kyc_verified</code> + <code>embedded.card_issued</code> assertions on the mock path (<code>KIWA_MODE</code> unset) and the real path.</li><li><strong>dogfood-payment-bnpl-installment-app</strong> — BNPL axis (installment + risk + late fee) against real Klarna / Affirm / Afterpay backends under <code>KIWA_MODE=real</code> + <code>KIWA_KLARNA_URL</code> + <code>KIWA_AFFIRM_URL</code> + <code>KIWA_AFTERPAY_URL</code>. Walks the plan-create + schedule + risk-score + late-fee + settlement chain against live sandboxes.</li><li><strong>dogfood-payment-crypto-fx-app</strong> — crypto-payment + FX cross-border axis against real Coinbase Commerce / BitPay + Wise / Airwallex backends under <code>KIWA_MODE=real</code> + <code>KIWA_CRYPTO_URL</code> + <code>KIWA_FX_URL</code>. Exercises stablecoin invoice + on-chain confirmation + gas abstraction + FX rate lock + SWIFT/SEPA settlement against live crypto + FX APIs.</li></ul><h3 id="_3-new-tutorials-1-concept-doc-snippet-validation-streak-extension" tabindex="-1">3 new tutorials + 1 concept doc + snippet validation streak extension <a class="header-anchor" href="#_3-new-tutorials-1-concept-doc-snippet-validation-streak-extension" aria-label="Permalink to &quot;3 new tutorials + 1 concept doc + snippet validation streak extension&quot;">​</a></h3><ul><li><code>docs/tutorials/88-embedded-finance-bnpl.md</code> (Embedded finance + BNPL walkthrough)</li><li><code>docs/tutorials/89-crypto-payment-fx.md</code> (Crypto payment + FX cross-border walkthrough)</li><li><code>docs/tutorials/90-recurring-orchestration-fraud-regulatory.md</code> (Recurring revenue + Orchestration II + Fraud detection + Regulatory reporting walkthrough)</li><li><code>docs/concepts/payment-advanced-III-testing.md</code> (v0.5 8 axis × 3 provider = 24 cell advanced III grid SSOT + 25-axis combined harness + pair 深度 4 段 2 例目 record documentation)</li></ul><p>Snippet validation streak extends <strong>v1.23 → v1.41 = 19 milestone consecutive</strong> (<code>packages/payment/tests/docs-tutorial-v1.41.test.ts</code> walks the tutorial 88-90 snippets against the actual v0.5 API surface, 35+ tests).</p><h2 id="recommended-migration-path" tabindex="-1">Recommended migration path <a class="header-anchor" href="#recommended-migration-path" aria-label="Permalink to &quot;Recommended migration path&quot;">​</a></h2><ol><li>Bump <code>@kiwa-test/payment</code> from <code>^0.4</code> to <code>^0.5</code> in your <code>package.json</code>.</li><li>Existing v0.4 imports keep working — no code change required.</li><li>Opt-in to a v0.5 axis by importing the new function (e.g. <code>openAccount</code> for embedded-finance) and adding a corresponding test.</li><li>Wire the real-driver path by setting <code>KIWA_MODE=real</code> + the axis-specific backend URL (<code>KIWA_STRIPE_TREASURY_URL</code> / <code>KIWA_KLARNA_URL</code> / <code>KIWA_CRYPTO_URL</code> / <code>KIWA_FX_URL</code>) in the CI job that runs the dogfood app.</li><li>Optionally add <code>collectFidelityCoverage()</code> as a regression guard on the 3 × 25 = 75 grid so a mis-configured axis registration is caught before the next release cuts.</li></ol><p>The 25-axis grid is a strict superset of the 17-axis v0.4 grid — a downstream consumer that was already asserting <code>rows.every(r =&gt; r.implemented)</code> on 51 rows now asserts on 75 rows without changing the assertion shape.</p><h2 id="pair-深度-4-段-2-例目-記録" tabindex="-1">pair 深度 4 段 2 例目 記録 <a class="header-anchor" href="#pair-深度-4-段-2-例目-記録" aria-label="Permalink to &quot;pair 深度 4 段 2 例目 記録&quot;">​</a></h2><p>v1.41 is the <strong>second pair in kiwa history to reach depth 4</strong> — the first was the AI-LLM 縦深化 pair (v1.12 v0.1 base → v1.15 v0.2 multimodal → v1.38 v0.4 advanced → v1.40 v0.5 advanced III), recorded in the v1.39 → v1.40 migration guide. The Payment 縦深化 pair now spans v1.14 (v0.1 base) → v1.19 (v0.2 advanced) → v1.33 (v0.4 advanced II) → v1.41 (v0.5 advanced III). The pair 深度 4 段 record at 2 examples validates the 縦深化 methodology as a repeatable pattern — the same 3-provider × N-axis fidelity harness template applies at every depth without change, and each depth is a strict additive superset of the previous.</p><p>The 19-milestone consecutive snippet validation streak (v1.23 → v1.41) reinforces the same pattern for docs — every milestone since v1.23 has added a snippet-validation test that walks the tutorial&#39;s code blocks against the actual API surface, catching drift before readers hit &quot;the tutorial does not compile&quot; gaps.</p>`,30)])])}const g=a(n,[["render",o]]);export{k as __pageData,g as default};
