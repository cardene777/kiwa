import{_ as t,o as a,c as r,a2 as i}from"./chunks/framework.CyZTVJbV.js";const m=JSON.parse('{"title":"kiwa v1.28 released — Realtime 深化 II (WebRTC + WebTransport + HTTP/3 + QUIC multiplexing + 3 dogfood app + real driver env-gate)","description":"","frontmatter":{},"headers":[],"relativePath":"announcements/v1.28/gh-discussions-announcement.md","filePath":"announcements/v1.28/gh-discussions-announcement.md"}'),o={name:"announcements/v1.28/gh-discussions-announcement.md"};function s(n,e,c,l,d,p){return a(),r("div",null,[...e[0]||(e[0]=[i('<h1 id="kiwa-v1-28-released-—-realtime-深化-ii-webrtc-webtransport-http-3-quic-multiplexing-3-dogfood-app-real-driver-env-gate" tabindex="-1">kiwa v1.28 released — Realtime 深化 II (WebRTC + WebTransport + HTTP/3 + QUIC multiplexing + 3 dogfood app + real driver env-gate) <a class="header-anchor" href="#kiwa-v1-28-released-—-realtime-深化-ii-webrtc-webtransport-http-3-quic-multiplexing-3-dogfood-app-real-driver-env-gate" aria-label="Permalink to &quot;kiwa v1.28 released — Realtime 深化 II (WebRTC + WebTransport + HTTP/3 + QUIC multiplexing + 3 dogfood app + real driver env-gate)&quot;">​</a></h1><p>v1.28 is out. After v1.13 landed <code>@kiwa-lab/realtime</code> v0.1 with 4 providers (Supabase / Ably / Pusher / Socket.io) and 5 base semantics (presence / broadcast / postgres_changes / room / reconnect), v1.28 stacks <strong>advanced realtime production semantics 8 axis across WebRTC / WebTransport / HTTP/3 / QUIC + 3 dogfood apps (Next.js WebRTC video + Nuxt WebTransport stream + SvelteKit HTTP/3 multiplex) + real driver env-gate</strong>. Every axis is a <code>packages/realtime/src/semantics/*</code> pure state-machine helper (no adapter, no browser, no signaling server) so tests / fixtures / release gates run deterministically. <code>@kiwa-lab/realtime</code> v0.1.1 → v0.2.0 minor bump reflects the advanced-transport surface addition — the v0.1 API (4 provider mock + 5 base semantics) stays completely intact.</p><h2 id="what-shipped" tabindex="-1">What shipped <a class="header-anchor" href="#what-shipped" aria-label="Permalink to &quot;What shipped&quot;">​</a></h2><ul><li><strong><code>@kiwa-lab/realtime</code> v0.2.0</strong> (advanced realtime semantics 8 axis + 24-row 3×8 grid harness + real driver env-gate minor bump). v0.1&#39;s 4 provider mock (<code>createSupabaseRealtimeMock</code> / <code>createAblyMock</code> / <code>createPusherMock</code> / <code>createSocketIoMock</code>) + 5 base semantics (<code>presence</code> / <code>broadcast</code> / <code>postgres_changes</code> / <code>room</code> / <code>reconnect</code>) keep every prior signature. v0.2 layers 8 axis on top: <code>webrtc-signaling</code> (offer/answer + SDP + ICE candidate + renegotiation) / <code>webrtc-data-channel</code> (ordered/unordered + reliable + maxRetransmits + binaryType) / <code>webrtc-media-track</code> (getUserMedia mock + MediaStream + simulcast + track add/remove) / <code>webrtc-ice-stun-turn</code> (candidate gathering + connectivity check + TURN relay + trickle ICE) / <code>webtransport-unidirectional</code> (uni stream + Datagram + reset) / <code>webtransport-bidirectional</code> (bi stream + flow control + backpressure) / <code>http3-push</code> (server push + prioritization + push_promise + cancellation) / <code>quic-multiplexing</code> (stream multiplex + stream priority + HPACK + 0-RTT resumption). Existing v0.1 caller paths stay unchanged — no v0.1 caller is asked to migrate.</li><li><strong>v1.28-1 realtime v0.2 advanced semantics</strong> (Issue #977). 8 axis × 3 protocol = 24 row fidelity grid + real-vs-mock fidelity harness + 60 semantics behavior test + real driver env-gate for v1.13 4 provider (Supabase / Ably / Pusher / Socket.io). Each axis is a pure state-machine helper with strict transition guard — deterministic, adapter-free, network-free.</li><li><strong>v1.28-2 dogfood-nextjs-webrtc-video-app</strong> (Issue #978). Next.js 15 App Router + mediasoup SFU + coturn TURN server (testcontainers) + WebRTC video/audio track + P2P peer connection + simulcast + ICE restart. Realtime video call room + track add/remove + reconnect on network flap + 36 vitest all-PASS with <code>KIWA_MODE=real</code> env-gate for coturn opt-in.</li><li><strong>v1.28-3 dogfood-nuxt-webtransport-stream-app</strong> (Issue #979). Nuxt 3 + WebTransport unidirectional + bidirectional stream + HTTP/3 + Datagram + connection migration + 0-RTT resumption. Edge streaming with backpressure + connection migration across network change + aioquic testcontainers fidelity harness + 30 vitest all-PASS.</li><li><strong>v1.28-4 dogfood-sveltekit-http3-multiplex-app</strong> (Issue #980, #982 follow-up). SvelteKit + nginx-quic HTTP/3 + QUIC multiplex + stream priority + HPACK dynamic table + 0-RTT resumption. Multi-stream + priority scheduling + HPACK observation + close-stream FIN + priority range validation + 0-RTT origin isolation + HPACK metrics teardown + 32 vitest + 4 adversarial-review findings addressed (closeStream FIN + HPACK metrics teardown + 0-RTT origin isolation + priority range).</li><li><strong>v1.28-5 docs 補強</strong> (Issue #983). tutorial 52 (WebRTC video signaling + SFU vs P2P) + tutorial 53 (WebTransport stream + HTTP/3 Datagram) + tutorial 54 (HTTP/3 multiplex + HPACK + 0-RTT) + concept doc <code>webrtc-webtransport-testing.md</code> (8 axis SSOT + P2P vs SFU 選択規範 + 3 protocol fidelity table) + migration guide <code>v1.27-to-v1.28.md</code> (additive-only, breaking change 0) + snippet validation <code>packages/realtime/tests/docs-tutorial-v1.28.test.ts</code> (30 test) re-runs every tutorial code snippet against the real <code>@kiwa-lab/realtime</code> v0.2 API so drift is structurally blocked.</li><li><strong>v1.28-6 publish</strong> (Issue #976, this PR). plugin.json 1.27.0 → 1.28.0 + description v1.27 → v1.28 marker + new WebRTC / WebTransport / QUIC / HPACK keywords + Roadmap ✅ v1.28 row + announcement 4 file + release-smoke <code>v1-28-publish.test.ts</code> (7 axis publish artefact invariant) + docs-e2e <code>V1_28_PAGES</code> (4 page render check) + <strong>release script filter check</strong> — verify <code>@kiwa-lab/realtime</code> present in both build (<code>-F</code>) and publish (<code>--filter</code>) halves (v1.14 payment omission + v1.25 perf-harness + v1.27 quality-metrics lesson, 4th application of the same systematic root-cause pattern).</li></ul><h2 id="numbers" tabindex="-1">Numbers <a class="header-anchor" href="#numbers" aria-label="Permalink to &quot;Numbers&quot;">​</a></h2><ul><li><strong>6 sub-Issues resolved</strong> (#977 / #978 / #979 / #980 / #983 / #976)</li><li><strong>6 PRs merged</strong> (v1.28-1 through v1.28-6 + follow-up #982 adversarial-review)</li><li><strong>1 npm minor bump</strong> (<code>@kiwa-lab/realtime</code> v0.1.1 → v0.2.0) — kiwa runtime fixture count stays 34</li><li><strong>8 axes</strong> on advanced realtime semantics (<code>webrtc-signaling</code> / <code>webrtc-data-channel</code> / <code>webrtc-media-track</code> / <code>webrtc-ice-stun-turn</code> / <code>webtransport-unidirectional</code> / <code>webtransport-bidirectional</code> / <code>http3-push</code> / <code>quic-multiplexing</code>)</li><li><strong>3 protocol</strong> (WebRTC / WebTransport / HTTP/3) × 8 axis = <strong>24 row fidelity grid</strong></li><li><strong>3 dogfood apps</strong> (Next.js + mediasoup SFU + coturn / Nuxt + WebTransport + aioquic / SvelteKit + nginx-quic HTTP/3) — 36 + 30 + 32 = <strong>98 dogfood vitest</strong> total</li><li><strong>30 snippet-validation tests</strong> in <code>packages/realtime/tests/docs-tutorial-v1.28.test.ts</code></li></ul><h2 id="why-8-axes-and-not-one-universal-realtime-abstraction" tabindex="-1">Why 8 axes (and not one universal realtime abstraction) <a class="header-anchor" href="#why-8-axes-and-not-one-universal-realtime-abstraction" aria-label="Permalink to &quot;Why 8 axes (and not one universal realtime abstraction)&quot;">​</a></h2><p>Realtime tests without a per-protocol split fail three ways.</p><ul><li><strong>Protocol-shape drift</strong>. WebRTC&#39;s SDP negotiation is a 4-step offer/answer roundtrip with ICE trickle interleaved; WebTransport&#39;s stream setup is a single-shot HTTP/3 CONNECT with unidirectional / bidirectional flag; HTTP/3 push is a server-initiated <code>PUSH_PROMISE</code> frame gated by client <code>SETTINGS_ENABLE_PUSH</code>. Wrapping all three in one abstraction hides the ordering guarantees each protocol makes — a test that &quot;works with WebRTC and WebTransport&quot; may pass by accident because both happen to converge on the same happy-path event order. The 8-axis SSOT names the <strong>shape</strong> each protocol enforces: <code>webrtc-signaling</code> for SDP offer/answer, <code>webtransport-unidirectional</code> for one-way streams, <code>http3-push</code> for server push.</li><li><strong>Fidelity grid drift</strong>. Without a per-protocol × per-axis grid, &quot;realtime coverage&quot; is a single vague number. The 24-row 3×8 grid pins exactly which protocol × axis pair a test exercises, so a fidelity regression on WebRTC data-channel <code>reliable</code> mode is visible as a single cell change — not a percentage that could shift for any of 24 reasons.</li><li><strong>Real driver drift</strong>. Pure-mock WebRTC tests can hide bugs that only surface with real ICE gathering + TURN relay + STUN binding. Real-driver testcontainers (coturn / aioquic / nginx-quic) run behind the same axis names + fidelity grid + strict transition guard as the mock path, so <code>KIWA_MODE=real</code> opts a test into real transport with zero code change to the assertions.</li></ul><p>The 4 rules in <code>docs/concepts/webrtc-webtransport-testing.md</code> — 8 axis SSOT / 3 protocol neutral state machine / 24-row fidelity grid / real driver env-gate — are the smallest set that make kiwa realtime suites comparable across protocols, milestones, and forks.</p><h2 id="_17-milestone-streak" tabindex="-1">17-milestone streak <a class="header-anchor" href="#_17-milestone-streak" aria-label="Permalink to &quot;17-milestone streak&quot;">​</a></h2><p>v1.11 (release gate) → v1.12 (non-determinism) → v1.13 (time-axis) → v1.14 (horizontal expansion) → v1.15 (AI-LLM depth) → v1.16 (component depth) → v1.17 (Observability v2) → v1.18 (Blockchain depth) → v1.19 (Framework depth) → v1.20 (Streaming depth) → v1.21 (Auth depth) → v1.22 (Auth depth II) → v1.23 (Payment depth) → v1.24 (Edge / Serverless depth) → v1.25 (Perf-harness sweep) → v1.26 (Database depth) → v1.27 (Mutation testing sweep) → <strong>v1.28 (Realtime depth II)</strong>. Every milestone since v1.11 has landed 6 sub-Issues in full.</p><h2 id="v2-0-candidates" tabindex="-1">v2.0 candidates <a class="header-anchor" href="#v2-0-candidates" aria-label="Permalink to &quot;v2.0 candidates&quot;">​</a></h2><ul><li>Multi-version Vitest matrix (Vitest 1.x vs 2.x vs 3.x parity)</li><li>Desktop (Electron / Tauri) + mobile (React Native / Expo) adapters</li><li>Coverage 100 % milestone</li><li>Cache / Data depth (Dragonfly / Materialize / Neon)</li><li>L2 depth (Base / Arbitrum / Optimism / Scroll block-space fidelity)</li><li>ZK depth (Noir / Circom / RISC Zero test harness)</li><li>IoT depth (MQTT / CoAP / LWM2M)</li><li>DB depth II (SurrealDB / EdgeDB / Turso / CockroachDB / TimescaleDB / QuestDB)</li><li>Perf-harness sweep II — real-machine baseline (macOS ARM64 + Linux x86_64 + Windows x86_64 3 hardware matrix + CI reproducibility harness)</li><li>Mutation sweep II — property-based mutation (Stryker + fast-check integration + shrink parser)</li><li>Realtime depth III — WebCodecs / WebGPU compute + AV1/VP9 hardware encoding + WHIP/WHEP ingest fidelity</li></ul><p>Feedback welcome on which of these should land next.</p>',15)])])}const h=t(o,[["render",s]]);export{m as __pageData,h as default};
