import{_ as t,o as a,c as i,a2 as s}from"./chunks/framework.CyZTVJbV.js";const k=JSON.parse('{"title":"WebRTC / WebTransport / HTTP/3 testing SSOT — 8-axis grid + P2P vs SFU + ICE trickle vs half-trickle + WebTransport vs WebSocket for kiwa v1.28","description":"","frontmatter":{},"headers":[],"relativePath":"concepts/webrtc-webtransport-testing.md","filePath":"concepts/webrtc-webtransport-testing.md"}'),r={name:"concepts/webrtc-webtransport-testing.md"};function o(d,e,n,c,l,h){return a(),i("div",null,[...e[0]||(e[0]=[s(`<h1 id="webrtc-webtransport-http-3-testing-ssot-—-8-axis-grid-p2p-vs-sfu-ice-trickle-vs-half-trickle-webtransport-vs-websocket-for-kiwa-v1-28" tabindex="-1">WebRTC / WebTransport / HTTP/3 testing SSOT — 8-axis grid + P2P vs SFU + ICE trickle vs half-trickle + WebTransport vs WebSocket for kiwa v1.28 <a class="header-anchor" href="#webrtc-webtransport-http-3-testing-ssot-—-8-axis-grid-p2p-vs-sfu-ice-trickle-vs-half-trickle-webtransport-vs-websocket-for-kiwa-v1-28" aria-label="Permalink to &quot;WebRTC / WebTransport / HTTP/3 testing SSOT — 8-axis grid + P2P vs SFU + ICE trickle vs half-trickle + WebTransport vs WebSocket for kiwa v1.28&quot;">​</a></h1><p>Introduced in v1.28 as <code>@kiwa/realtime</code> v0.2 — 3 protocol × 8 axis low-layer transport mocks (<code>createWebRtcSignalingMock</code> + <code>createWebRtcIceMock</code> + <code>createWebRtcTrackMock</code> + <code>createWebRtcDataChannelMock</code> + <code>createWebTransportUniMock</code> + <code>createWebTransportBiMock</code> + <code>createHttp3PushMock</code> + <code>createQuicMultiplexMock</code>) + 24-row <code>SEMANTICS_GRID</code> + <code>resolveRealtimeDriver</code> env-gate. This document is the SSOT for <strong>what an advanced-realtime kiwa suite measures, which protocol handles which axis, and how to pick between the two most common architectural choices (P2P vs SFU, WebTransport vs WebSocket)</strong>. Every downstream axis mock (<code>packages/realtime/src/semantics/*.ts</code>) and every dogfood app adapter (<code>examples/dogfood-*/src/adapters/interface.ts</code>) reads these rules from here — do not re-derive them locally.</p><h2 id="why-an-advanced-realtime-ssot" tabindex="-1">Why an advanced-realtime SSOT <a class="header-anchor" href="#why-an-advanced-realtime-ssot" aria-label="Permalink to &quot;Why an advanced-realtime SSOT&quot;">​</a></h2><p>Realtime tests without a shared standard fail three ways.</p><ul><li><strong>Protocol / axis conflation</strong>. WebRTC signaling and WebRTC ICE both surface in an <code>RTCPeerConnection</code> lifecycle, but they measure different failure modes. Signaling fidelity is about SDP negotiation shape (offer / answer ordering, media section count, BUNDLE flag); ICE fidelity is about candidate gathering + connectivity check state machine (<code>new → gathering → complete</code>, <code>new → checking → connected</code>). A single &quot;WebRTC test&quot; that conflates them cannot pin down which one regressed. The 8-axis SSOT names each measurable surface separately.</li><li><strong>Mock vs real drift</strong>. WebRTC + WebTransport + HTTP/3 all specify complex asynchronous state machines. A hand-written mock that gets a state transition wrong (e.g. ICE <code>checking → connected</code> without an active candidate pair) can pass thousands of tests while silently diverging from real Chrome + aioquic behavior. The <code>@kiwa/realtime</code> v0.2 mocks pin the state machine transitions to a shared spec so a fidelity harness diff catches the drift.</li><li><strong>Architecture choice masking bugs</strong>. A test that mocks the SFU signaling shape (mediasoup) does not exercise P2P <code>RTCDataChannel</code> negotiation, and vice versa. Choosing the wrong architecture at the test level masks the bugs the app hits in production. The concept doc records the P2P vs SFU + WebTransport vs WebSocket decision criteria so tests pick the shape their app actually ships.</li></ul><p>The 4 rules below are the smallest set that make advanced-realtime kiwa suites comparable across the 3 protocols, 8 axes, and 3 dogfood apps.</p><h2 id="rule-1-—-8-axes-one-measurable-surface-per-axis" tabindex="-1">Rule 1 — 8 axes, one measurable surface per axis <a class="header-anchor" href="#rule-1-—-8-axes-one-measurable-surface-per-axis" aria-label="Permalink to &quot;Rule 1 — 8 axes, one measurable surface per axis&quot;">​</a></h2><p>The v1.28 milestone pins the advanced transport surface to 8 axes. Each axis measures one specific transport behavior — signaling shape, ICE state machine, media track lifecycle, DataChannel semantics, uni stream lifecycle, bi stream flow control, HTTP/3 push priority, QUIC stream multiplex — and is exposed through one <code>SemanticsMock</code> type. The 8 mocks + their event kinds live in <code>packages/realtime/src/semantics/</code>.</p><table tabindex="0"><thead><tr><th>Axis</th><th>Protocol</th><th>Mock</th><th>Event kinds emitted</th><th>What it measures</th></tr></thead><tbody><tr><td><code>webrtc-signaling</code></td><td><code>webrtc</code></td><td><code>createWebRtcSignalingMock</code></td><td><code>offer</code> / <code>answer</code> / <code>ice-candidate</code> / <code>renegotiation</code></td><td>SDP offer / answer negotiation shape + trickle ICE emission from signaling + renegotiation on track add</td></tr><tr><td><code>webrtc-ice</code></td><td><code>webrtc</code></td><td><code>createWebRtcIceMock</code></td><td><code>ice-gathering</code> / <code>ice-checking</code> / <code>ice-connected</code> / <code>ice-relay-used</code></td><td>ICE gathering / connectivity check state machine + TURN relay fallback + trickle candidate emission</td></tr><tr><td><code>webrtc-track</code></td><td><code>webrtc</code></td><td><code>createWebRtcTrackMock</code></td><td><code>track-add</code> / <code>track-remove</code> / <code>track-mute</code> / <code>track-unmute</code></td><td><code>getUserMedia</code> shape + <code>MediaStream</code> lifecycle + simulcast layer (low / med / high with 100k / 300k / 900k default bitrates)</td></tr><tr><td><code>webrtc-data-channel</code></td><td><code>webrtc</code></td><td><code>createWebRtcDataChannelMock</code></td><td><code>data-open</code> / <code>data-message</code> / <code>data-close</code></td><td><code>RTCDataChannel</code> ordered / unordered + <code>maxRetransmits</code> + <code>binaryType</code> (arraybuffer / blob) semantics</td></tr><tr><td><code>webtransport-uni</code></td><td><code>webtransport</code></td><td><code>createWebTransportUniMock</code></td><td><code>uni-stream-open</code> / <code>uni-stream-write</code> / <code>uni-stream-reset</code> / <code>datagram-recv</code></td><td>Unidirectional stream lifecycle + Datagram send + writer.abort() reset</td></tr><tr><td><code>webtransport-bi</code></td><td><code>webtransport</code></td><td><code>createWebTransportBiMock</code></td><td><code>bi-stream-open</code> / <code>bi-stream-write</code> / <code>bi-stream-close</code> / <code>bi-backpressure</code></td><td>Bidirectional stream + flow-control window + backpressure emission at window boundary + reader.read()</td></tr><tr><td><code>http3-push</code></td><td><code>http3-quic</code></td><td><code>createHttp3PushMock</code></td><td><code>push-promise</code> / <code>push-headers</code> / <code>push-body</code> / <code>push-cancelled</code></td><td>Server push_promise + RFC 9218 priority (urgency 0-7 + incremental flag) + cancellation</td></tr><tr><td><code>quic-multiplex</code></td><td><code>http3-quic</code></td><td><code>createQuicMultiplexMock</code></td><td><code>stream-open</code> / <code>stream-close</code> / <code>hpack-insert</code> / <code>zero-rtt-used</code></td><td>QUIC stream multiplex + priority (0-255, low = high) + HPACK dynamic table growth + 0-RTT resumption</td></tr></tbody></table><p>The <code>SemanticsProtocol</code> enum is <code>&#39;webrtc&#39; | &#39;webtransport&#39; | &#39;http3-quic&#39;</code>, and the <code>SemanticsAxis</code> enum lists all 8 axis identifiers. A caller cannot introduce a 9th axis without extending both enums — the type system refuses.</p><h2 id="rule-2-—-24-row-fidelity-grid-applicable-non-applicable-per-pair" tabindex="-1">Rule 2 — 24-row fidelity grid, applicable / non-applicable per pair <a class="header-anchor" href="#rule-2-—-24-row-fidelity-grid-applicable-non-applicable-per-pair" aria-label="Permalink to &quot;Rule 2 — 24-row fidelity grid, applicable / non-applicable per pair&quot;">​</a></h2><p>The 3 × 8 = 24 pair matrix is captured in <code>SEMANTICS_GRID</code>. Every pair is one of:</p><ul><li><strong><code>applicable: true</code></strong> — the axis measures a real behavior of that protocol. WebRTC × signaling / ICE / track / data-channel = 4 applicable pairs. WebTransport × uni / bi = 2 applicable pairs. HTTP/3 + QUIC × push / multiplex = 2 applicable pairs. <strong>Total: 8 applicable pairs</strong>, matching the 8 axes above.</li><li><strong><code>applicable: false</code></strong> — the axis is meaningful for a different protocol. WebRTC × webtransport-uni = false (WebRTC has no <code>WebTransport.createUnidirectionalStream</code>). HTTP/3 + QUIC × webrtc-signaling = false. <strong>Total: 16 non-applicable pairs</strong>.</li></ul><p>The grid is not just documentation — <code>measureSemanticsGrid({ scenarios })</code> iterates it and returns exactly 24 result rows, with non-applicable rows emitting a placeholder <code>{ eventsEmitted: 0, streamsOpened: 0, ... }</code>. Downstream matrix rendering does not need to fill gaps; the runtime and the doc agree by construction.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { measureSemanticsGrid, SEMANTICS_GRID } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@kiwa/realtime&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// SEMANTICS_GRID.length === 24</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// SEMANTICS_GRID.filter(r =&gt; r.applicable).length === 8</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> rows</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> measureSemanticsGrid</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ scenarios: </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Map</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">([</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]) });</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// rows.length === 24</span></span></code></pre></div><h3 id="protocol-×-framework-fidelity-table" tabindex="-1">Protocol × framework fidelity table <a class="header-anchor" href="#protocol-×-framework-fidelity-table" aria-label="Permalink to &quot;Protocol × framework fidelity table&quot;">​</a></h3><p>The v1.28 milestone ships 3 dogfood apps that pair one framework with one protocol. The pairing is deliberate — each framework&#39;s runtime characteristics amplify a different axis, and the concept doc records the choice so a reader can see which framework hosts which axis in production.</p><table tabindex="0"><thead><tr><th>Framework</th><th>Protocol</th><th>Dogfood app</th><th>Axes exercised end-to-end</th><th>Real driver</th></tr></thead><tbody><tr><td>Next.js 15</td><td>WebRTC</td><td><code>dogfood-nextjs-webrtc-video-app</code></td><td><code>webrtc-signaling</code> + <code>webrtc-ice</code> + <code>webrtc-track</code> (simulcast)</td><td>mediasoup SFU + coturn TURN under <code>WEBRTC_MEDIASOUP_READY=1</code></td></tr><tr><td>Nuxt 3</td><td>WebTransport</td><td><code>dogfood-nuxt-webtransport-stream-app</code></td><td><code>webtransport-uni</code> + <code>webtransport-bi</code> (backpressure) + Datagram + connection migration</td><td>aioquic + Chrome experimental flag under <code>WEBTRANSPORT_KEY=1</code></td></tr><tr><td>SvelteKit</td><td>HTTP/3 + QUIC</td><td><code>dogfood-sveltekit-http3-multiplex-app</code></td><td><code>quic-multiplex</code> (priority scheduling) + HPACK + 0-RTT + <code>http3-push</code></td><td>nginx-quic testcontainers under <code>HTTP3_KEY=1</code></td></tr></tbody></table><p>The mapping is not exclusive — a Next.js app can perfectly well use WebTransport, and a SvelteKit app can host WebRTC. The dogfood pairing exists because <em>specific</em> combinations tend to co-occur in production (Next.js + mediasoup video call rooms, Nuxt 3 SSR + aioquic streaming feeds, SvelteKit + nginx-quic for edge-cached HTTP/3 delivery).</p><h2 id="rule-3-—-p2p-vs-sfu-selection-guide-webrtc" tabindex="-1">Rule 3 — P2P vs SFU selection guide (WebRTC) <a class="header-anchor" href="#rule-3-—-p2p-vs-sfu-selection-guide-webrtc" aria-label="Permalink to &quot;Rule 3 — P2P vs SFU selection guide (WebRTC)&quot;">​</a></h2><p>WebRTC has two dominant deployment shapes. The 8-axis grid is protocol-neutral, but the <em>scenario a test exercises</em> depends on which shape the app uses in production.</p><h3 id="p2p-peer-to-peer" tabindex="-1">P2P (peer-to-peer) <a class="header-anchor" href="#p2p-peer-to-peer" aria-label="Permalink to &quot;P2P (peer-to-peer)&quot;">​</a></h3><p>Two browsers negotiate directly through an STUN server, TURN relays only kick in when NAT traversal fails. Signaling is a lightweight WebSocket / long-polling channel (Socket.io / SSE / plain WS). No SFU sits in the middle — media flows peer-to-peer.</p><ul><li><strong>Use when</strong>: 2-participant calls (1:1 chat, Google Meet 1:1, Whereby small rooms, Discord voice DMs). Signaling volume is O(N) per room. Media bandwidth is O(N²) per participant so P2P does not scale past ~4-5 participants without hitting client bandwidth ceilings.</li><li><strong>Axes to exercise</strong>: <code>webrtc-signaling</code> (offer / answer exchange over the signaling channel) + <code>webrtc-ice</code> (candidate gathering + STUN + TURN relay fallback) + <code>webrtc-data-channel</code> (if the app uses <code>RTCDataChannel</code> for chat / file transfer alongside the media). Skip <code>webrtc-track</code> if the app is data-only.</li><li><strong>Fidelity anchor</strong>: SDP fingerprint per peer must be unique. The mock&#39;s per-peer seed offset (Knuth multiplicative hash 2654435761 × peerSeq) is the invariant that catches accidental identity collisions — real mediasoup transports never produce identical DTLS fingerprints per peer.</li></ul><h3 id="sfu-selective-forwarding-unit" tabindex="-1">SFU (Selective Forwarding Unit) <a class="header-anchor" href="#sfu-selective-forwarding-unit" aria-label="Permalink to &quot;SFU (Selective Forwarding Unit)&quot;">​</a></h3><p>An SFU (mediasoup, Janus, LiveKit, Jitsi) sits between the peers. Each peer maintains one connection to the SFU; the SFU forwards media to every other peer selectively. Signaling is more complex (per-transport producer / consumer allocation), but media bandwidth per peer is O(1) so the room scales.</p><ul><li><strong>Use when</strong>: 3+ participant conferences (Zoom, Google Meet 3+, Twitch live streams via SFU-relay, virtual events). Signaling volume is O(N × M) per room (M = producer count). Media bandwidth per peer is O(1) — the SFU absorbs the cross-multiplication.</li><li><strong>Axes to exercise</strong>: <code>webrtc-signaling</code> (per-transport SDP offer / answer + renegotiation on producer add / remove) + <code>webrtc-ice</code> (per-transport ICE + TURN relay for participants behind restrictive NATs) + <code>webrtc-track</code> (simulcast — the SFU picks a layer per consumer based on bandwidth). <code>webrtc-data-channel</code> is optional; some SFUs (LiveKit) support DataChannel forwarding, others (mediasoup default) do not.</li><li><strong>Fidelity anchor</strong>: simulcast layer preference must round-trip through <code>selectLayer(peer, track, layer)</code>. The layer decision is stored on the consumer, not the producer — the mock preserves the per-consumer preference map so a fidelity diff catches accidental producer-side layer state.</li></ul><p>The <code>dogfood-nextjs-webrtc-video-app</code> uses the SFU shape (mediasoup) because 3+ participants is the realistic case. Its adapter has <code>selectLayer</code> as a first-class op; a P2P adapter would drop <code>selectLayer</code> and add explicit <code>RTCDataChannel</code> ops.</p><h2 id="rule-4-—-ice-trickle-vs-half-trickle-webtransport-vs-websocket" tabindex="-1">Rule 4 — ICE trickle vs half-trickle + WebTransport vs WebSocket <a class="header-anchor" href="#rule-4-—-ice-trickle-vs-half-trickle-webtransport-vs-websocket" aria-label="Permalink to &quot;Rule 4 — ICE trickle vs half-trickle + WebTransport vs WebSocket&quot;">​</a></h2><p>Two low-level design decisions surface repeatedly in advanced-realtime test suites. The concept doc records the trade-off so tests pick the shape their production stack ships.</p><h3 id="ice-trickle-vs-half-trickle" tabindex="-1">ICE trickle vs half-trickle <a class="header-anchor" href="#ice-trickle-vs-half-trickle" aria-label="Permalink to &quot;ICE trickle vs half-trickle&quot;">​</a></h3><ul><li><strong>Trickle ICE (RFC 8838)</strong> — candidates are emitted as they are discovered and forwarded through the signaling channel to the peer. <code>pc.onicecandidate</code> fires N times before <code>iceGatheringState === &#39;complete&#39;</code>. The peer can start connectivity checks before the full candidate set has arrived — first-byte latency drops from ~1500 ms (wait for gathering) to ~300 ms (first host candidate).</li><li><strong>Half-trickle</strong> — the offerer trickles candidates but the answerer waits for gathering to complete before sending the answer. Common in older signaling gateways that assume the SDP answer contains all <code>a=candidate:</code> lines. Adds ~800-1200 ms to the answerer-side handshake.</li><li><strong>Non-trickle</strong> — both sides wait for gathering to complete. Signaling channels that cannot handle multi-message candidate flows (some SIP-over-WebSocket bridges) fall back here.</li></ul><p>The <code>@kiwa/realtime</code> v0.2 <code>createWebRtcSignalingMock</code> + <code>createWebRtcIceMock</code> emit candidates one-by-one via <code>emitIceCandidates(n)</code> / <code>startGathering(n)</code>. A trickle-ICE-aware app will assert on the number of <code>ice-candidate</code> events emitted; a half-trickle app will assert that the answer only sends after <code>ice-gathering</code> reaches state <code>complete</code>.</p><h3 id="webtransport-vs-websocket" tabindex="-1">WebTransport vs WebSocket <a class="header-anchor" href="#webtransport-vs-websocket" aria-label="Permalink to &quot;WebTransport vs WebSocket&quot;">​</a></h3><p>The two protocols look interchangeable from an application-code perspective — both are message-oriented client → server streams over TCP-like fabric. The differences show up under load, packet loss, and NAT rebinding.</p><table tabindex="0"><thead><tr><th>Concern</th><th>WebSocket (WS / WSS)</th><th>WebTransport (over HTTP/3 + QUIC)</th></tr></thead><tbody><tr><td>Transport</td><td>TCP + TLS</td><td>QUIC (UDP + TLS 1.3)</td></tr><tr><td>Head-of-line blocking</td><td>Yes — one lost TCP packet blocks all data</td><td>No — QUIC streams are independent; a lost packet on stream 1 does not stall stream 2</td></tr><tr><td>Multiple streams</td><td>One (message ordering guaranteed across all messages)</td><td>Many (bidirectional + unidirectional streams; independent flow control per stream)</td></tr><tr><td>Datagram support</td><td>No (application must fragment)</td><td>Yes (<code>transport.datagrams.writable</code> — unreliable, unordered)</td></tr><tr><td>Backpressure</td><td>Only via <code>bufferedAmount</code> polling</td><td>Per-stream <code>writer.ready</code> promise; per-datagram <code>datagrams.outgoingHighWaterMark</code></td></tr><tr><td>Connection migration</td><td>No (breaks on network change)</td><td>Yes (QUIC path migration; connection survives NAT rebinding + roaming)</td></tr><tr><td>Handshake round trips</td><td>2 RTT (TCP + TLS handshake)</td><td>1 RTT (QUIC handshake), 0 RTT (with resumption ticket)</td></tr><tr><td>Firewall / proxy support</td><td>Universal</td><td>Restricted (UDP blocked in some corporate networks; falls back to WebSocket)</td></tr></tbody></table><ul><li><strong>Use WebSocket when</strong>: the app runs behind restrictive corporate proxies (UDP often blocked), payload is small and infrequent (chat messages, presence pings), or the deployment target is a WebSocket-first framework (Socket.io / SignalR / SSE fallback).</li><li><strong>Use WebTransport when</strong>: the app streams large payloads with tight latency budgets (video / game state / financial ticks), the client roams (mobile app, Wi-Fi ↔ cellular), or the workload benefits from unreliable Datagrams (input events, telemetry samples). Falls back to WebSocket when UDP is blocked.</li></ul><p>The <code>dogfood-nuxt-webtransport-stream-app</code> exercises the WebTransport shape end-to-end because the axis that most differentiates it from WebSocket (per-stream backpressure + connection migration + Datagram) is what the fidelity harness measures. A WebSocket-only app would not need <code>openBiStream</code> or <code>sendDatagram</code> or <code>migrateConnection</code> — dropping those ops from the adapter would signal the architectural difference at code level.</p><h2 id="the-3-layer-harness-alignment" tabindex="-1">The 3-layer harness alignment <a class="header-anchor" href="#the-3-layer-harness-alignment" aria-label="Permalink to &quot;The 3-layer harness alignment&quot;">​</a></h2><p><code>@kiwa/perf-harness</code> runs <code>serial + concurrent + memory</code> in one <code>runPerf3Layer</code> call. The advanced-realtime semantics harness is single-layer by design — each <code>measureSemanticsAxis</code> call runs one scenario against one mock and collects the event stream. The perf harness is 3-layer because contention + memory leaks slip past a serial p95; the realtime harness is 1-layer because concurrent stream measurement is already the <em>scenario itself</em> (<code>concurrentSend</code> opens N streams and measures priority ordering).</p><p>The kiwa release gate treats them as parallel axes.</p><table tabindex="0"><thead><tr><th>Axis</th><th>Layer</th><th>Cost</th><th>Runs when</th></tr></thead><tbody><tr><td><code>perf.p95Ms</code> (v1.13)</td><td>serial</td><td>~90 s across 33 packages</td><td>every <code>pnpm test:perf</code></td></tr><tr><td>perf 3-layer (v1.14)</td><td>serial + concurrent + memory</td><td>~120 s per package</td><td>every <code>pnpm test:perf</code></td></tr><tr><td>realtime 5-semantics fidelity (v1.13)</td><td>single</td><td>~1 s per provider</td><td>every <code>pnpm test</code></td></tr><tr><td>realtime 8-axis semantics fidelity (v1.28)</td><td>single</td><td>~2 s per axis</td><td>every <code>pnpm test</code></td></tr><tr><td><code>mutation.tier</code> (v1.27-4)</td><td>single</td><td>~200 s per package</td><td>every <code>pnpm test:mutation</code></td></tr></tbody></table><p>The semantics fidelity axis is cheap (~2 s per axis, 16 s for all 8 in one <code>measureSemanticsGrid</code> call) so every kiwa suite runs it on every <code>pnpm test</code>. Mutation runs are still excluded from the default sweep because a 33-package × ~200 s run is ~110 minutes.</p><h2 id="real-driver-env-gate" tabindex="-1">Real driver env-gate <a class="header-anchor" href="#real-driver-env-gate" aria-label="Permalink to &quot;Real driver env-gate&quot;">​</a></h2><p>The <code>resolveRealtimeDriver({ provider, requiredKeys, createReal, createMock })</code> helper is the SSOT for opting into real drivers on the 4 v1.13 providers (Supabase / Ably / Pusher / Socket.io). It returns <code>createMock()</code> unless <code>KIWA_MODE=real</code> + every <code>requiredKeys</code> env variable is present — safe fallback by construction.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { resolveRealtimeDriverByProvider } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@kiwa/realtime&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">driver</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">isReal</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">reason</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">missingKeys</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> resolveRealtimeDriverByProvider</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &#39;supabase&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">env</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createRealSupabaseDriver</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ url: env.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">SUPABASE_URL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, key: env.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">SUPABASE_ANON_KEY</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createMockSupabaseDriver</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// isReal === false unless KIWA_MODE=real + SUPABASE_URL + SUPABASE_ANON_KEY are all set</span></span></code></pre></div><p>The 3 v1.28 dogfood apps use a per-adapter env gate (not <code>resolveRealtimeDriver</code> directly) because their real driver reads app-specific env variables (<code>WEBRTC_MEDIASOUP_READY=1</code> / <code>WEBTRANSPORT_KEY=1</code> / <code>HTTP3_KEY=1</code>). The pattern is the same — refuse the real op with a code (<code>KIWA_WEBRTC_ENV_MISSING</code> / <code>KIWA_WEBTRANSPORT_ENV_MISSING</code> / <code>KIWA_HTTP3_ENV_MISSING</code>) until the env variables land, and the mock adapter is the always-available default.</p><h2 id="package-coverage-v1-28" tabindex="-1">Package coverage (v1.28) <a class="header-anchor" href="#package-coverage-v1-28" aria-label="Permalink to &quot;Package coverage (v1.28)&quot;">​</a></h2><p>The v1.28 milestone applied the 8-axis grid to one package (<code>@kiwa/realtime</code>) and three dogfood apps.</p><table tabindex="0"><thead><tr><th>Layer</th><th>Packages / apps</th></tr></thead><tbody><tr><td>SaaS (adapter, 1)</td><td><code>@kiwa/realtime</code> v0.2</td></tr><tr><td>Dogfood apps (3)</td><td><code>dogfood-nextjs-webrtc-video-app</code> / <code>dogfood-nuxt-webtransport-stream-app</code> / <code>dogfood-sveltekit-http3-multiplex-app</code></td></tr></tbody></table><p>Every dogfood app writes a per-app baseline JSON to <code>.mutation-baseline/{app}.json</code> (SaaS tier default 65 %), runs Stryker on every <code>pnpm test:mutation</code> invocation, and gates on the tier floor via <code>evaluateReleaseGate({ mutationTier: &#39;saas&#39; })</code>.</p><h2 id="where-each-axis-lands-in-the-release-gate" tabindex="-1">Where each axis lands in the release gate <a class="header-anchor" href="#where-each-axis-lands-in-the-release-gate" aria-label="Permalink to &quot;Where each axis lands in the release gate&quot;">​</a></h2><p>The 12-axis release gate does not add a new axis in v1.28. The 8 realtime axes feed into the existing <code>fidelity.ratio</code> axis (v1.11) — one row of <code>SemanticsFidelityRow</code> per axis becomes one row of the mock vs real coverage matrix. A behavioral divergence between mock and real (event count mismatch, stream state divergence) reduces the fidelity ratio; a caller reading the release-gate report sees <code>mock covered / real total</code> per axis.</p><table tabindex="0"><thead><tr><th>Axis</th><th>Kind</th><th>Introduced</th><th>Threshold source</th></tr></thead><tbody><tr><td>fidelity — ratio</td><td>floor</td><td>v1.11 (7-axis)</td><td><code>ReleaseGateThresholds.fidelityRatio</code> (default 60 %, overrideable). v1.28 rows contribute per-axis coverage.</td></tr><tr><td>mutation — tier</td><td>floor</td><td>v1.27-4 (12-axis)</td><td><code>DEFAULT_MUTATION_TIER_THRESHOLDS[tier]</code> — SaaS default 65 % for the 3 dogfood apps + <code>@kiwa/realtime</code> v0.2</td></tr></tbody></table><p>The 8-axis grid is a <em>fidelity signal</em> — it improves the resolution of the <code>fidelity.ratio</code> axis without changing the axis count. Backward-compatible with every v1.11 – v1.27 consumer.</p><h2 id="related" tabindex="-1">Related <a class="header-anchor" href="#related" aria-label="Permalink to &quot;Related&quot;">​</a></h2><ul><li><a href="./../tutorials/52-webrtc-video-signaling">Tutorial 52 — WebRTC video call (signaling + ICE + simulcast + ICE restart walkthrough)</a></li><li><a href="./../tutorials/53-webtransport-stream">Tutorial 53 — WebTransport stream (uni / bi / Datagram / migration walkthrough)</a></li><li><a href="./../tutorials/54-http3-multiplex">Tutorial 54 — HTTP/3 multiplex (stream priority + HPACK + 0-RTT walkthrough)</a></li><li><a href="./../migrations/v1.27-to-v1.28">Migration guide v1.27 → v1.28</a></li><li><a href="./realtime-testing">Realtime testing (time-axis mock SSOT for the 5 v1.13 semantics)</a></li><li><a href="./../quality/release-gate">Release gate SSOT (12-axis)</a></li><li><a href="./mutation-testing-ssot">Mutation testing SSOT (kill rate + 4-tier threshold + 12-axis integration)</a></li></ul>`,57)])])}const g=t(r,[["render",o]]);export{k as __pageData,g as default};
