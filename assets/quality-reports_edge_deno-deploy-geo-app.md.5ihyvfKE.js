import{_ as t,o as d,c as o,a2 as a}from"./chunks/framework.CyZTVJbV.js";const g=JSON.parse('{"title":"Fidelity — dogfood-deno-deploy-geo-app (v1.24-4)","description":"","frontmatter":{},"headers":[],"relativePath":"quality-reports/edge/deno-deploy-geo-app.md","filePath":"quality-reports/edge/deno-deploy-geo-app.md"}'),s={name:"quality-reports/edge/deno-deploy-geo-app.md"};function i(r,e,n,c,l,p){return d(),o("div",null,[...e[0]||(e[0]=[a(`<h1 id="fidelity-—-dogfood-deno-deploy-geo-app-v1-24-4" tabindex="-1">Fidelity — dogfood-deno-deploy-geo-app (v1.24-4) <a class="header-anchor" href="#fidelity-—-dogfood-deno-deploy-geo-app-v1-24-4" aria-label="Permalink to &quot;Fidelity — dogfood-deno-deploy-geo-app (v1.24-4)&quot;">​</a></h1><p>Real-vs-mock behavioural fidelity for the Deno Fresh app on Deno Deploy driven by <code>@kiwa/edge</code> v0.2 (v1.24-1 land, PR #920) under <code>KIWA_MODE=real</code> (Deno Deploy sandbox + <code>deno task dev</code> + <code>DENO_DEPLOY_KEY=1</code> env-gate) and <code>KIWA_MODE=mock</code> (<code>@kiwa/edge</code> v0.2 8 axis semantics), produced by <code>examples/dogfood-deno-deploy-geo-app/tests/emit-fidelity-report.test.ts</code>. Feeds <code>@kiwa/quality-metrics</code> release-gate 7-axis payload as the third edge-platform dogfood in the v1.24 milestone.</p><h2 id="baseline-real-mode-skipped-—-kiwa-mode-real-deno-deploy-key-1-unset" tabindex="-1">Baseline (real mode skipped — <code>KIWA_MODE=real</code> + <code>DENO_DEPLOY_KEY=1</code> unset) <a class="header-anchor" href="#baseline-real-mode-skipped-—-kiwa-mode-real-deno-deploy-key-1-unset" aria-label="Permalink to &quot;Baseline (real mode skipped — \`KIWA_MODE=real\` + \`DENO_DEPLOY_KEY=1\` unset)&quot;">​</a></h2><p>When the harness runs without both <code>KIWA_MODE=real</code> and <code>DENO_DEPLOY_KEY=1</code> in the environment, the real adapter emits <code>KIWA_DENO_DEPLOY_ENV_MISSING</code> for each of the 8 ops (<code>driveGeoRoute</code> / <code>driveGeoPrimaryWrite</code> / <code>driveGeoReplicaSync</code> / <code>driveKvWrite</code> / <code>driveKvRangeQuery</code> / <code>driveReadYourWrites</code> / <code>driveCronSchedule</code> / <code>driveCronComplete</code>). Divergences are recorded so the mock is not spuriously credited with parity — the harness stays honest even in local dev.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>provider   : @kiwa/edge/deno-deploy-geo</span></span>
<span class="line"><span>version    : 0.1.0</span></span>
<span class="line"><span>verdict    : PASS</span></span>
<span class="line"><span>divergences: 8 (all eight ops recorded as BEHAVIORAL_DIVERGENCE, real mode absent)</span></span>
<span class="line"><span>axes       : 7 (framework branch — common 7-axis release gate)</span></span></code></pre></div><table tabindex="0"><thead><tr><th>axis</th><th>actual</th><th>threshold</th><th>verdict</th></tr></thead><tbody><tr><td>coverage.line</td><td>92.00%</td><td>85%</td><td>pass</td></tr><tr><td>coverage.branch</td><td>86.00%</td><td>80%</td><td>pass</td></tr><tr><td>coverage.function</td><td>95.00%</td><td>90%</td><td>pass</td></tr><tr><td>fidelity.ratio</td><td>100.00% (8/8)</td><td>70%</td><td>pass</td></tr><tr><td>perf.p95Ms</td><td>~0.10 ms</td><td>100 ms</td><td>pass</td></tr><tr><td>mutation.killRate</td><td>70.00% (28/40)</td><td>60%</td><td>pass</td></tr><tr><td>testCount.behavior</td><td>30</td><td>10</td><td>pass</td></tr></tbody></table><p>The <code>divergences</code> count in the notes section counts every op whose mock path succeeded but whose real path emitted <code>KIWA_DENO_DEPLOY_ENV_MISSING</code> — this is the expected shape in a real-mode-skipped baseline. It does not fail the gate; the fidelity ratio measures the mock-covered surface area, which is 100% for the eight ops the AC scopes.</p><h2 id="reproduction" tabindex="-1">Reproduction <a class="header-anchor" href="#reproduction" aria-label="Permalink to &quot;Reproduction&quot;">​</a></h2><p>Common integration path (mock + graceful-skip real).</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --filter</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dogfood-deno-deploy-geo-app</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples/dogfood-deno-deploy-geo-app/quality-report/fidelity-latest.md</span></span></code></pre></div><p>Live real-mode (real Deno Deploy sandbox or <code>deno task dev</code> — env-skip unless the driver is opted in).</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> KIWA_MODE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">real</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> DENO_DEPLOY_KEY</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --filter</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dogfood-deno-deploy-geo-app</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># The v1.24-4 dogfood ships the env-gate skip path only; a follow-up</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># milestone swaps in a real Deno Deploy sandbox driver behind the same env gate.</span></span></code></pre></div><h2 id="_8-op-surface-geo-replicated-3-edge-kv-2-cron-trigger-3-1-fused-read-your-writes" tabindex="-1">8-op surface = geo-replicated 3 + edge-kv 2 + cron-trigger 3 (+ 1 fused read-your-writes) <a class="header-anchor" href="#_8-op-surface-geo-replicated-3-edge-kv-2-cron-trigger-3-1-fused-read-your-writes" aria-label="Permalink to &quot;8-op surface = geo-replicated 3 + edge-kv 2 + cron-trigger 3 (+ 1 fused read-your-writes)&quot;">​</a></h2><p>The 8 ops correspond directly to the 8 axis routing pattern inherited from v1.24-1 (<code>@kiwa/edge</code> v0.2 semantics). Each op emits neutral events on 1 axis:</p><table tabindex="0"><thead><tr><th>op</th><th>primary axis</th><th>neutral events emitted</th></tr></thead><tbody><tr><td><code>driveGeoRoute</code></td><td>geo-replicated</td><td>stateless region resolution from Accept-Language + Deno Deploy geo header, no session mutation</td></tr><tr><td><code>driveGeoPrimaryWrite</code></td><td>geo-replicated</td><td><code>geo.primary-write</code> — bumps version + marks every replica lagging</td></tr><tr><td><code>driveGeoReplicaSync</code></td><td>geo-replicated</td><td><code>geo.replica-lagged</code> + <code>geo.replica-synced</code> per replica + optional <code>geo.conflict-resolved</code></td></tr><tr><td><code>driveKvWrite</code></td><td>edge-kv</td><td><code>kv.write</code> — persists to store + invalidates cache entry</td></tr><tr><td><code>driveKvRangeQuery</code></td><td>edge-kv</td><td><code>kv.read</code> with a prefix + sorted match list</td></tr><tr><td><code>driveReadYourWrites</code></td><td>edge-kv</td><td><code>kv.write</code> + <code>kv.read</code> fused into a single consistency-observation op — strong on primary, eventual on lagging replica</td></tr><tr><td><code>driveCronSchedule</code></td><td>cron-trigger</td><td><code>cron.scheduled</code> + <code>cron.started</code> (schedule + start fired on the same op boundary)</td></tr><tr><td><code>driveCronComplete</code></td><td>cron-trigger</td><td><code>cron.completed</code> (success) or <code>cron.failed</code> (failure with retry decision — <code>willRetry=true</code> → <code>nextState=scheduled</code>; exhausted → <code>failed</code>)</td></tr></tbody></table><h2 id="_3-route-paths-1-cron-entrypoint-dispatched-by-the-fresh-runtime" tabindex="-1">3 route paths + 1 cron entrypoint dispatched by the Fresh runtime <a class="header-anchor" href="#_3-route-paths-1-cron-entrypoint-dispatched-by-the-fresh-runtime" aria-label="Permalink to &quot;3 route paths + 1 cron entrypoint dispatched by the Fresh runtime&quot;">​</a></h2><table tabindex="0"><thead><tr><th>route</th><th>HTTP method</th><th>handler kind</th></tr></thead><tbody><tr><td><code>/api/kv</code></td><td>POST</td><td><code>handleKvWrite</code> (multi-region KV write + geo-replicated primary-write + lagging replicas payload)</td></tr><tr><td><code>/api/kv</code></td><td>GET</td><td><code>handleKvRange</code> (prefix scan over Deno KV)</td></tr><tr><td><code>/api/read-your-writes</code></td><td>POST</td><td><code>handleReadYourWrites</code> (strong on primary, eventual on lagging replica via <code>?replica=1</code>)</td></tr><tr><td><code>/api/cron</code></td><td>POST / DELETE</td><td><code>handleCronSchedule</code> (schedule + start) / <code>handleCronComplete</code> (success or failure with retry decision)</td></tr><tr><td><code>cron/purge-job.ts</code></td><td>—</td><td><code>runPurgeJob</code> — retention purge (<code>0 0 * * *</code>) driving cron-trigger + edge-kv axes together</td></tr></tbody></table><p>Fresh middleware would run at every Deno Deploy edge request to stamp the negotiated region + language; the mock&#39;s <code>resolveRegion</code> covers the same routing table so behavioural fidelity holds.</p><h2 id="test-spec-map-48-tests-total" tabindex="-1">Test spec map (48 tests total) <a class="header-anchor" href="#test-spec-map-48-tests-total" aria-label="Permalink to &quot;Test spec map (48 tests total)&quot;">​</a></h2><ul><li><code>multi-region-write-e2e.spec.ts</code> (16 tests) — geo-replicated + edge-kv axes: country-decisive routing, Accept-Language fallback, country-vs-language precedence, default region fallback, primary write bumps version + marks replicas lagging, full sync collapses to in-sync, partial sync stays lagging, range query sorted, empty range, <code>/api/kv</code> POST + GET, multiple writes accumulate version, metrics, real env-skip (4 ops).</li><li><code>read-your-writes-e2e.spec.ts</code> (12 tests) — edge-kv consistency guarantee: strong at primary, sequential reads, eventual at replica returns null, brand-new key on replica returns null, <code>consistency</code> field distinguishes strong from eventual, <code>consistent</code> never true on lagging path, <code>/api/read-your-writes</code> route strong + eventual, last-writer-wins, metrics, real env-skip (2 ops).</li><li><code>cron-trigger-e2e.spec.ts</code> (17 tests) — cron-trigger axis: scheduled + queue trigger paths, completion transitions, retry decision, retry exhaustion, duplicate id detection, missing id detection, invalid cron spec rejection, <code>/api/cron</code> POST + DELETE, purge job success + forced failure + queue variant, metrics, real env-skip (2 ops).</li><li><code>fidelity-report.test.ts</code> (4 tests) — harness contract: 8-op mock coverage, mock failure propagation, divergence notes on skip, p95 latency surfaces.</li><li><code>emit-fidelity-report.test.ts</code> (1 test) — writes JSON snapshot + markdown report to <code>quality-report/</code>.</li></ul><h2 id="env-gate-escape-hatch" tabindex="-1">Env-gate escape hatch <a class="header-anchor" href="#env-gate-escape-hatch" aria-label="Permalink to &quot;Env-gate escape hatch&quot;">​</a></h2><p>The real adapter reads two environment variables at construction:</p><ul><li><code>KIWA_MODE=real</code> — enables the connected path; any other value returns the skip adapter.</li><li><code>DENO_DEPLOY_KEY=1</code> — proxy for &quot;deno is installed + a Deno Deploy access token is available&quot; so tests do not spawn <code>deno task dev</code> unless the caller explicitly opts in.</li></ul><p>When either gate is unset the adapter records <code>KIWA_DENO_DEPLOY_ENV_MISSING</code> on every op call, throws <code>SkippedError</code>, and lets the harness compute divergences from the trace. This lets the same test suite run in local dev without a Deno installation and in CI with a <code>deno task dev</code> subprocess behind the env gate.</p><h2 id="refs" tabindex="-1">Refs <a class="header-anchor" href="#refs" aria-label="Permalink to &quot;Refs&quot;">​</a></h2><ul><li>Parent — v1.24 (#913)</li><li>Sub-Issue — v1.24-4 (#917)</li><li>Depends on — v1.24-1 (@kiwa/edge v0.2 with 8 axis semantics, PR #920)</li><li>Siblings — v1.24-2 (dogfood-cloudflare-workers-durable-object-app, PR #921) + v1.24-3 (dogfood-vercel-edge-function-app, PR #922)</li></ul>`,26)])])}const u=t(s,[["render",i]]);export{g as __pageData,u as default};
