import{_ as s,o as t,c as a,a2 as i}from"./chunks/framework.CyZTVJbV.js";const k=JSON.parse('{"title":"Realtime testing — time axis mocks, event ordering, drop, reconnect, backpressure","description":"","frontmatter":{},"headers":[],"relativePath":"concepts/realtime-testing.md","filePath":"concepts/realtime-testing.md"}'),n={name:"concepts/realtime-testing.md"};function o(r,e,l,d,c,h){return t(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="realtime-testing-—-time-axis-mocks-event-ordering-drop-reconnect-backpressure" tabindex="-1">Realtime testing — time axis mocks, event ordering, drop, reconnect, backpressure <a class="header-anchor" href="#realtime-testing-—-time-axis-mocks-event-ordering-drop-reconnect-backpressure" aria-label="Permalink to &quot;Realtime testing — time axis mocks, event ordering, drop, reconnect, backpressure&quot;">​</a></h1><p>kiwa&#39;s provider mocks up to v1.12 all shared a request/response shape — the caller issues one call, the mock returns one reply. Auth mocks answer <code>signIn</code> synchronously. AI-LLM mocks answer <code>messages.create</code> after a bounded delay. RAG mocks answer <code>embed(...)</code> and <code>generateText(...)</code> on demand. Even the AI-LLM streaming path is still a caller-initiated pull — <code>for await (const chunk of stream)</code> drains at the caller&#39;s pace.</p><p>Realtime providers (Supabase Realtime, Ably, Pusher, Socket.io / SSE) break this shape. The <strong>server pushes</strong>, the client can be <strong>disconnected</strong> while the server keeps pushing, the pushes are <strong>timed</strong> and their <strong>order matters</strong>, and buffered messages can <strong>overflow</strong>. v1.13 is designed to absorb this second axis of non-triviality — time.</p><h2 id="what-time-axis-mock-actually-means" tabindex="-1">What &quot;time axis mock&quot; actually means <a class="header-anchor" href="#what-time-axis-mock-actually-means" aria-label="Permalink to &quot;What &quot;time axis mock&quot; actually means&quot;">​</a></h2><p>The v1.11 gate cared about outputs — did the mock return the right value? The v1.12 gate cared about outputs + a scalar drift — did the mock&#39;s cost / latency / token / accuracy stay close enough to real? The v1.13 gate cares about outputs + a <strong>sequence</strong> — did the mock&#39;s <em>event stream</em> stay close enough to real&#39;s <em>event stream</em>, measured on 5 axes.</p><p>The v1.13 mocks do not open a real socket. Instead, they simulate the socket by <strong>emitting scenario events on a virtual timeline</strong>. <code>createSupabaseRealtimeMock</code> accepts <code>scenarios: { &#39;room:1&#39;: [{ kind: &#39;broadcast&#39;, event: &#39;chat&#39;, payload: {...}, delay: 10 }, ...] }</code> — a per-channel array of events keyed by a virtual <code>delay</code> field. Subscription attaches a handler; the engine walks the array; each event fires after its <code>delay</code> ms elapses (or immediately when <code>delay</code> is 0).</p><p>The 4 provider mocks (<code>createSupabaseRealtimeMock</code> / <code>createAblyMock</code> / <code>createPusherMock</code> / <code>createSocketioMock</code>) all sit on top of one <strong>engine</strong> (<code>packages/realtime/src/engine.ts</code>) that owns the timeline, per-channel handler chains, and the pending-event queue. Provider adapters translate SDK-shaped calls (<code>supabase.channel().on(&#39;broadcast&#39;, ...)</code>, <code>ably.channels.get().subscribe(...)</code>, <code>pusher.subscribeChannel(...)</code>, <code>io.of().to().emit(...)</code>) into engine-level primitives (<code>subscribe(channel, handler)</code>, <code>publish(channel, event, payload)</code>).</p><p>Because there is no real socket, tests can assert on:</p><ul><li><strong>The exact number of events delivered per channel</strong> — real Supabase / Ably / Pusher / Socket.io does not expose this cleanly.</li><li><strong>The exact ordering of events across channels</strong> — hard to observe with real sockets under jitter.</li><li><strong>The exact number of events dropped by backpressure</strong> — real servers surface this as vague <code>disconnect</code> events at best.</li><li><strong>The exact number of reconnect attempts</strong> — real backoff is opaque.</li></ul><p>The trade-off is that the mock&#39;s timeline is <strong>discrete + synchronous</strong> (event <code>n</code> fires exactly <code>sum(delay[0..n])</code> ms after subscribe, with no jitter) while real sockets have jitter, batching, and Nagle-style micro-delays. The fidelity harness measures how far the real stream drifts from the mock stream, per axis.</p><h2 id="why-v1-11-v1-12-gates-alone-are-not-enough" tabindex="-1">Why v1.11 + v1.12 gates alone are not enough <a class="header-anchor" href="#why-v1-11-v1-12-gates-alone-are-not-enough" aria-label="Permalink to &quot;Why v1.11 + v1.12 gates alone are not enough&quot;">​</a></h2><p>The v1.11 gate measures test suite properties (coverage / test count / fidelity ratio / perf p95 / mutation kill rate). The v1.12 gate adds AI-LLM properties (cost / latency / token / accuracy). All 11 axes are <strong>scalar</strong> — one number per axis per report.</p><p>Realtime failures are not scalar. A chat app can pass all 11 axes and still be broken in production because:</p><table tabindex="0"><thead><tr><th>Realtime failure</th><th>Passes 11-axis gate?</th><th>Why</th></tr></thead><tbody><tr><td>Messages arrive in wrong order under presence sync</td><td>Yes</td><td>Ordering is a property of the <em>sequence</em>, not a scalar</td></tr><tr><td>Presence roll-up loses a member after a reconnect</td><td>Yes</td><td>Presence-count assertions are point-in-time, not time-series</td></tr><tr><td>Server pushes 1k events while client is offline, 800 are silently dropped</td><td>Yes</td><td>Drop counts are provider-internal; the 11 axes never see them</td></tr><tr><td>Two rooms cross-leak (room A payload reaches room B subscriber)</td><td>Yes</td><td>Routing correctness is a sequence property</td></tr><tr><td>Reconnect happens but pending queue is never flushed</td><td>Yes</td><td>Queue flush is a control-plane event, not a data-plane count</td></tr></tbody></table><p>The v1.13 axes measure the <em>sequence</em> directly. Same-shape reports still feed <code>evaluateReleaseGate</code> — realtime providers stay on the common 7-axis branch — but the <strong>fidelity axis</strong> for realtime is computed by <code>runRealtimeFidelityCheck</code> on the sequence, not the surface method ratio, and the <strong>perf axis</strong> measures socket round-trip latency rather than one-shot API latency.</p><h2 id="the-5-primary-axes" tabindex="-1">The 5 primary axes <a class="header-anchor" href="#the-5-primary-axes" aria-label="Permalink to &quot;The 5 primary axes&quot;">​</a></h2><p>The v1.13 mocks and fidelity harness measure 5 time-axis semantics, tracked per scenario in <code>RealtimeFidelityRecord</code>.</p><table tabindex="0"><thead><tr><th>Axis</th><th>What it measures</th><th>Provider surface</th></tr></thead><tbody><tr><td><strong>Order</strong></td><td><code>kind</code> sequence Jaccard between real vs mock event streams</td><td>Every broadcast + presence + postgres_changes event, in the order emitted</td></tr><tr><td><strong>Timing</strong></td><td><code>totalDurationDiffMs</code> — cumulative wall-clock drift across the scenario</td><td>Real sockets have jitter; mock timelines are discrete — the drift number surfaces the gap</td></tr><tr><td><strong>Drop</strong></td><td><code>eventsDropped</code> from mock metrics + <code>droppedByBackpressure</code> from adapter reports</td><td>Server-side buffer overflow; client-side backpressure limit</td></tr><tr><td><strong>Reconnect</strong></td><td><code>reconnectCount</code> + pending-queue flush behaviour on reconnect</td><td>Exponential backoff, jitter, <code>maxAttempts</code>, pending-event replay</td></tr><tr><td><strong>Backpressure</strong></td><td><code>pendingByRoom.queuedCount</code> — how many events queued while offline before overflow</td><td>Per-room pending queue with configurable <code>backpressureLimit</code> (default <code>Infinity</code>, <code>8</code> in the socketio dogfood)</td></tr></tbody></table><p>The 5 axes are not thresholds on individual scalars. They are properties of the <em>event trace</em> the mock records — a <code>RealtimeMetrics</code> snapshot exposes 7 numbers per axis (<code>subscribeCount</code>, <code>publishCount</code>, <code>eventsDelivered</code>, <code>eventsDropped</code>, <code>reconnectCount</code>, <code>subscribeLatencyMs[]</code>, <code>publishLatencyMs[]</code>), and the fidelity harness diff produces <code>kindOrderMatch</code>, <code>payloadMatch</code>, and <code>accuracyScore</code> per scenario.</p><h2 id="concept-1-—-presence-broadcast-postgres-changes-as-one-union" tabindex="-1">Concept 1 — presence + broadcast + postgres_changes as one union <a class="header-anchor" href="#concept-1-—-presence-broadcast-postgres-changes-as-one-union" aria-label="Permalink to &quot;Concept 1 — presence + broadcast + postgres_changes as one union&quot;">​</a></h2><p>The 4 providers surface presence, broadcast, and DB CDC events with different names but the same underlying shape.</p><table tabindex="0"><thead><tr><th>Provider</th><th>Presence</th><th>Broadcast</th><th>Postgres-change</th></tr></thead><tbody><tr><td>Supabase Realtime</td><td><code>channel.on(&#39;presence&#39;, { event: &#39;sync&#39; | &#39;join&#39; | &#39;leave&#39; }, ...)</code></td><td><code>channel.on(&#39;broadcast&#39;, { event }, ...)</code> + <code>channel.send(...)</code></td><td><code>channel.on(&#39;postgres_changes&#39;, filter, ...)</code></td></tr><tr><td>Ably</td><td><code>channel.presence.enter / leave / subscribe</code></td><td><code>channel.publish(event, data)</code> / <code>channel.subscribe(event, ...)</code></td><td>(not native — broadcast + custom payload convention)</td></tr><tr><td>Pusher</td><td><code>presence-&lt;name&gt;</code> channels + <code>pusher:subscription_succeeded / member_added / member_removed</code></td><td><code>channel.bind(event, ...)</code> + server-side trigger</td><td>(not native — same as Ably)</td></tr><tr><td>Socket.io</td><td>Custom (per-room member Map)</td><td><code>io.of(ns).to(room).emit(event, payload)</code> + <code>socket.on(event, ...)</code></td><td>(not native)</td></tr></tbody></table><p>The v1.13 mock engine normalises the union into a <code>RealtimeAnyEvent</code> type:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> RealtimeAnyEvent</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ({ </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">kind</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;presence&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> PresenceEvent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ({ </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">kind</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;broadcast&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> BroadcastEvent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ({ </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">kind</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;postgres_changes&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> PostgresChangeEvent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">kind</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;connection&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">state</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ConnectionState</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">timestamp</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> };</span></span></code></pre></div><p>Provider adapters own the surface mapping — the mock engine owns the union. This is why one <code>RealtimeFidelityRecord</code> shape covers all 4 providers.</p><h2 id="concept-2-—-timeline-as-scenarios-not-real-clock" tabindex="-1">Concept 2 — timeline as scenarios (not real clock) <a class="header-anchor" href="#concept-2-—-timeline-as-scenarios-not-real-clock" aria-label="Permalink to &quot;Concept 2 — timeline as scenarios (not real clock)&quot;">​</a></h2><p>The mock does not use <code>Date.now()</code> for scenario emission. It uses a <strong>virtual timeline</strong> driven by <code>delay</code> fields inside <code>RealtimeMockConfig.scenarios</code>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> supabase</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createSupabaseRealtimeMock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  scenarios: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &#39;room:1&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      { kind: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;broadcast&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, event: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;chat&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, payload: { text: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;hello&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, delay: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      { kind: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;broadcast&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, event: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;chat&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, payload: { text: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;world&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, delay: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">20</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      { kind: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;presence&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;leave&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, members: [], delay: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">5</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>Subscribe → event 0 fires 10 ms later → event 1 fires 20 ms after event 0 → event 2 fires 5 ms after event 1. Total scenario duration is the sum of <code>delay</code> values (35 ms). Tests can <code>await</code> a fixed number of events or use <code>getMetrics().eventsDelivered</code> to synchronise.</p><p>Why virtual not real? Two reasons:</p><ul><li><strong>Determinism</strong> — <code>Date.now()</code> on CI can slew mid-test (NTP correction, GC pause). A virtual timeline is bit-exact across runs.</li><li><strong>Speed</strong> — the mock&#39;s <code>artificialLatencyMs</code> field (default <code>5</code> ms) is the <em>only</em> wall-clock delay per subscribe / publish. A 100-event scenario at 20 ms virtual gaps completes in ~500 ms wall-clock, not 2 s.</li></ul><p>The <code>perf.p95Ms</code> axis on the release gate uses <code>publishLatencyMs[]</code> samples (mock&#39;s wall-clock time from <code>publish</code> to subscriber-side receipt), not the virtual <code>delay</code>. The virtual timeline is a <em>test-time</em> construct — production would be measured by real socket round-trip.</p><h2 id="concept-3-—-reconnect-policy-as-a-first-class-field" tabindex="-1">Concept 3 — reconnect policy as a first-class field <a class="header-anchor" href="#concept-3-—-reconnect-policy-as-a-first-class-field" aria-label="Permalink to &quot;Concept 3 — reconnect policy as a first-class field&quot;">​</a></h2><p>Every real provider ships an exponential-backoff reconnect implementation. The 4 provider mocks share one <code>ReconnectPolicy</code> type — 5 optional fields with sensible defaults:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ReconnectPolicy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  maxAttempts</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;         </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// default 5</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  initialBackoffMs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// default 100</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  maxBackoffMs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// default 5000</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  backoffMultiplier</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// default 2</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  jitter</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;              </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// default 0.1 (0-1 fraction)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Tests can override the policy per mock to force fast reconnect (<code>initialBackoffMs: 1, maxAttempts: 2</code>) or realistic reconnect (<code>initialBackoffMs: 100, maxAttempts: 5</code>) without touching adapter code. The <code>disconnect()</code> + <code>reconnect()</code> methods on <code>RealtimeMock</code> are test-only handles — real adapters do not expose them because real disconnects are driven by the network layer.</p><p>The dogfood-socketio-notification app exposes an extra <code>disconnectClient()</code> method on the mock adapter (see <a href="https://github.com/cardene777/kiwa/blob/main/examples/dogfood-socketio-notification/src/adapters/mock.ts" target="_blank" rel="noreferrer"><code>examples/dogfood-socketio-notification/src/adapters/mock.ts:69</code></a>) so the fidelity harness can prime the offline queue without a real network drop. This is the pattern for any realtime dogfood — the mock adapter&#39;s surface is a <em>superset</em> of the real adapter&#39;s surface, and the extra methods are the ones the fidelity harness needs to reproduce time-axis scenarios.</p><h2 id="concept-4-—-backpressure-as-bounded-queue-overflow" tabindex="-1">Concept 4 — backpressure as bounded queue overflow <a class="header-anchor" href="#concept-4-—-backpressure-as-bounded-queue-overflow" aria-label="Permalink to &quot;Concept 4 — backpressure as bounded queue overflow&quot;">​</a></h2><p>Realtime servers cannot buffer forever. Every real provider drops the client (or messages) after a bounded queue overflows. The v1.13 mock engine expresses this as <code>backpressureLimit</code> on <code>RealtimeMockConfig</code>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> socketio</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createSocketioMock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ backpressureLimit: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// While disconnected, events 1..8 queue; event 9 is dropped and counted in</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// eventsDropped. On reconnect, events 1..8 replay in FIFO order; the drop</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// count stays.</span></span></code></pre></div><p>Default is <code>Infinity</code> (no drop). Provider adapters implement per-room pending queues (see <a href="https://github.com/cardene777/kiwa/blob/main/examples/dogfood-socketio-notification/src/adapters/mock.ts" target="_blank" rel="noreferrer"><code>examples/dogfood-socketio-notification/src/adapters/mock.ts:100-108</code></a>) so tests can assert the exact drop count deterministically.</p><p>Why not model this as a scalar threshold on the release gate? Because the <em>correct</em> drop count is scenario-specific. A chat app with 8-message backpressure limit expects 0 drops in a 5-message scenario and 4 drops in a 12-message burst — the same &quot;correct&quot; behaviour, two different scalar outcomes. The gate measures the <em>drift</em> between real vs mock drop counts (via the fidelity harness), not an absolute count.</p><h2 id="provider-difference-table" tabindex="-1">Provider difference table <a class="header-anchor" href="#provider-difference-table" aria-label="Permalink to &quot;Provider difference table&quot;">​</a></h2><p>Each provider has semantics the others do not. The v1.13 harness covers all four via provider-neutral scenarios; the table below documents where provider surfaces diverge so adapter authors know what to translate.</p><table tabindex="0"><thead><tr><th>Semantic</th><th>Supabase Realtime</th><th>Ably</th><th>Pusher</th><th>Socket.io</th></tr></thead><tbody><tr><td>Subscribe primitive</td><td><code>channel.subscribe()</code></td><td><code>channel.subscribe(event, handler)</code></td><td><code>channel.bind(event, handler)</code></td><td><code>socket.on(event, handler)</code></td></tr><tr><td>Broadcast primitive</td><td><code>channel.send({ type: &#39;broadcast&#39;, event, payload })</code></td><td><code>channel.publish(event, data)</code></td><td>Server-side <code>trigger(channel, event, data)</code></td><td><code>socket.emit(event, payload)</code> / <code>io.of(ns).to(room).emit(...)</code></td></tr><tr><td>Presence surface</td><td><code>channel.on(&#39;presence&#39;, ...)</code> + <code>channel.track()</code></td><td><code>channel.presence.enter / leave / subscribe</code></td><td><code>presence-&lt;name&gt;</code> prefix + <code>pusher:member_added / removed</code></td><td>Custom (per-room member Map)</td></tr><tr><td>History rewind</td><td>Not native (persisted separately)</td><td><code>channel.history({ limit })</code> — last N in reverse order</td><td>Not native</td><td>Not native</td></tr><tr><td>Postgres CDC</td><td><code>channel.on(&#39;postgres_changes&#39;, filter, ...)</code></td><td>Broadcast + custom payload</td><td>Broadcast + custom payload</td><td>Broadcast + custom payload</td></tr><tr><td>Namespace + room</td><td>Single channel string (<code>room:demo</code>)</td><td>Single channel string (<code>board-1</code>)</td><td>Single channel string (<code>chat-general</code>)</td><td>2-level (<code>/notify</code> + <code>alerts:userId</code>)</td></tr><tr><td>Reconnect backoff</td><td>Client SDK default</td><td>Client SDK default</td><td>Client SDK default</td><td>Configurable in <code>io()</code> opts</td></tr><tr><td>Client-side throttle</td><td>Not native (app responsibility)</td><td>Not native (app responsibility)</td><td>Not native (app responsibility)</td><td>Not native (app responsibility)</td></tr><tr><td>Client-side debounce</td><td>Not native (app responsibility)</td><td>Not native (app responsibility)</td><td>Not native (app responsibility)</td><td>Not native (app responsibility)</td></tr></tbody></table><p>Two rows repeat &quot;app responsibility&quot; — client-side throttle (60 fps mousemove) and client-side debounce (500 ms typing) are not built into any of the 4 providers. The v1.13 dogfood apps show the pattern for the app layer to own these:</p><ul><li>Chat (<code>dogfood-supabase-realtime-chat</code>) — 500 ms typing debounce in <code>sendTyping</code> (<a href="https://github.com/cardene777/kiwa/blob/main/examples/dogfood-supabase-realtime-chat/src/adapters/mock.ts" target="_blank" rel="noreferrer"><code>src/adapters/mock.ts:129-155</code></a>)</li><li>Cursor (<code>dogfood-ably-collab-cursor</code>) — 60 fps mousemove throttle in <code>moveCursor</code> (<a href="https://github.com/cardene777/kiwa/blob/main/examples/dogfood-ably-collab-cursor/src/adapters/mock.ts" target="_blank" rel="noreferrer"><code>src/adapters/mock.ts:92-135</code></a>)</li><li>Notification (<code>dogfood-socketio-notification</code>) — backpressure-aware buffering in <code>deliverNotification</code> (<a href="https://github.com/cardene777/kiwa/blob/main/examples/dogfood-socketio-notification/src/adapters/mock.ts" target="_blank" rel="noreferrer"><code>src/adapters/mock.ts:164-223</code></a>)</li></ul><h2 id="test-strategy-—-deterministic-mock-sequence-assertion" tabindex="-1">Test strategy — deterministic mock, sequence assertion <a class="header-anchor" href="#test-strategy-—-deterministic-mock-sequence-assertion" aria-label="Permalink to &quot;Test strategy — deterministic mock, sequence assertion&quot;">​</a></h2><p>Realtime tests do not assert &quot;mock returned X&quot; against real. They assert two things:</p><ul><li><strong>Shape-level, on the mock side</strong> — event counts, ordering, presence roll-up, backpressure drop counts are exact integers when driven by the mock. Every v1.13 dogfood test uses this — see <a href="https://github.com/cardene777/kiwa/blob/main/examples/dogfood-supabase-realtime-chat/tests/e2e-mock-mode.test.ts" target="_blank" rel="noreferrer"><code>examples/dogfood-supabase-realtime-chat/tests/e2e-mock-mode.test.ts:39-46</code></a> for the debounce assertion (<code>emittedCount === 2</code> for 11 keystrokes at 50 ms).</li><li><strong>Sequence-level, on the fidelity side</strong> — the mock&#39;s event trace and the real&#39;s event trace are diffed per scenario. <code>kindOrderMatch</code> and <code>payloadMatch</code> land in <code>RealtimeFidelityRecord</code>, <code>avgAccuracyScore</code> lands in <code>RealtimeFidelityReport.summary</code>, and <code>buildRealtimeReport</code> maps that onto the release gate&#39;s fidelity axis.</li></ul><p>The pattern is the same across the 3 dogfood apps:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Mock-side, deterministic</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">it</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;debounces 11 keystrokes at 50ms to 2 emitted events&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> adapter</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> makeMockAdapter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> result</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> burstTyping</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(adapter);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  expect</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(result.typing.emittedCount).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">toBe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  expect</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(result.typing.suppressedCount).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">toBe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">9</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Fidelity-side, real-vs-mock drift</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">it</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;emits fidelity report ready for the release gate&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> report</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> runRealtimeFidelityCheck</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    realDriver,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    mockDriver,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    scenarios: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;chat-message-broadcast&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;presence-join-leave&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;reconnect-with-pending&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  });</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  expect</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(report.summary.avgAccuracyScore).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">toBeGreaterThan</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.80</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>The 0.80 accuracy floor is the same &quot;meaningfully similar&quot; bar the v1.12 AI-LLM concept doc landed on — a sequence Jaccard of 0.80 means the mock&#39;s event kind trace shares 80% of positions with the real event kind trace. Below that, the mock is drifting far enough that a v1.13 test written against the mock will not tell you much about production behaviour.</p><h2 id="where-the-pieces-live-in-code" tabindex="-1">Where the pieces live in code <a class="header-anchor" href="#where-the-pieces-live-in-code" aria-label="Permalink to &quot;Where the pieces live in code&quot;">​</a></h2><ul><li><strong>Engine (timeline + queue)</strong> — <a href="https://github.com/cardene777/kiwa/blob/main/packages/realtime/src/engine.ts" target="_blank" rel="noreferrer"><code>packages/realtime/src/engine.ts</code></a></li><li><strong>Provider adapters</strong> — <a href="https://github.com/cardene777/kiwa/blob/main/packages/realtime/src/supabase.ts" target="_blank" rel="noreferrer"><code>packages/realtime/src/supabase.ts</code></a> + <code>ably.ts</code> + <code>pusher.ts</code> + <code>socketio.ts</code></li><li><strong>Union types (5 semantics SSOT)</strong> — <a href="https://github.com/cardene777/kiwa/blob/main/packages/realtime/src/types.ts" target="_blank" rel="noreferrer"><code>packages/realtime/src/types.ts</code></a></li><li><strong>Fidelity harness</strong> — <a href="https://github.com/cardene777/kiwa/blob/main/packages/realtime/src/fidelity.ts" target="_blank" rel="noreferrer"><code>packages/realtime/src/fidelity.ts</code></a> <code>runRealtimeFidelityCheck</code></li><li><strong>Quality-metrics adapter</strong> — <a href="https://github.com/cardene777/kiwa/blob/main/packages/realtime/src/report.ts" target="_blank" rel="noreferrer"><code>packages/realtime/src/report.ts</code></a> <code>buildRealtimeReport</code></li></ul><h2 id="when-to-use-realtime-tests" tabindex="-1">When to use realtime tests <a class="header-anchor" href="#when-to-use-realtime-tests" aria-label="Permalink to &quot;When to use realtime tests&quot;">​</a></h2><p>Rules of thumb.</p><ul><li><strong>Use realtime tests if</strong> your app opens a socket (Supabase Realtime, Ably, Pusher, Socket.io, SSE) and its correctness depends on event ordering, presence roll-up, reconnect replay, or backpressure. All 3 v1.13 dogfood apps fit this shape.</li><li><strong>Skip realtime tests if</strong> your app polls a REST endpoint on an interval. Polling is a scalar problem — the v1.11 fidelity axis (mock method coverage) is enough.</li><li><strong>Skip realtime tests for pure request/response WebSocket RPC</strong> — if you use a socket only as a transport for req/res, model it as an API mock (v1.11) with a socket-shaped surface. The realtime axes measure <em>event streams</em>, not <em>RPC transports</em>.</li></ul><h2 id="what-is-deliberately-not-in-scope-for-v1-13" tabindex="-1">What is deliberately not in scope for v1.13 <a class="header-anchor" href="#what-is-deliberately-not-in-scope-for-v1-13" aria-label="Permalink to &quot;What is deliberately not in scope for v1.13&quot;">​</a></h2><ul><li><strong>Persistence + rewind for all providers</strong> — only Ably ships a native <code>history({ limit })</code> primitive. Supabase / Pusher / Socket.io rewind is a broadcast + persistence layer combined — the v1.13 mocks do not simulate the persistence layer. If your app relies on rewind past a reconnect on Supabase, model it as an application-level concern (e.g. a <code>getLastN</code> REST endpoint) and test that separately.</li><li><strong>Cross-server (Redis adapter) semantics for Socket.io</strong> — the mock is single-process. If your production runs multi-instance Socket.io with the Redis adapter, the mock&#39;s ordering guarantees hold <em>per instance</em> only. Cross-instance ordering is an integration-test concern.</li><li><strong>Time-warp beyond scenario <code>delay</code></strong> — the mock does not expose a <code>vi.useFakeTimers()</code>-style time jump. Tests that need to simulate &quot;3 hours pass while offline&quot; should either use a real fake-timer library at the adapter layer or drive the scenario with a large <code>delay</code> value.</li><li><strong>End-to-end latency under real network jitter</strong> — the mock&#39;s timeline is discrete. Real-world latency percentiles need a real socket in a QA environment; the mock&#39;s <code>publishLatencyMs[]</code> samples measure only the mock&#39;s internal path.</li></ul><h2 id="reading-list" tabindex="-1">Reading list <a class="header-anchor" href="#reading-list" aria-label="Permalink to &quot;Reading list&quot;">​</a></h2><ul><li><a href="./../tutorials/09-supabase-realtime-chat">Realtime chat tutorial</a> — hands-on with <code>createSupabaseRealtimeMock</code></li><li><a href="./../tutorials/10-ably-collab-cursor">Collab cursor tutorial</a> — hands-on with <code>createAblyMock</code></li><li><a href="./../tutorials/11-socketio-notification">Notification tutorial</a> — hands-on with <code>createSocketioMock</code></li><li><a href="./../migrations/v1.12-to-v1.13">Migration v1.12 → v1.13</a> — how to adopt the realtime harness</li><li><a href="https://github.com/cardene777/kiwa/blob/main/packages/realtime/README.md" target="_blank" rel="noreferrer"><code>@kiwa-lab/realtime</code> README</a> — full API reference for the 4 mocks + fidelity harness</li><li><a href="./ai-llm-testing">AI-LLM testing concept guide</a> — sibling SSOT for the AI-LLM axes (v1.12)</li><li><a href="./../quality/release-gate">Release gate SSOT</a> — thresholds + overrides + how the gate is wired</li></ul><h2 id="takeaways" tabindex="-1">Takeaways <a class="header-anchor" href="#takeaways" aria-label="Permalink to &quot;Takeaways&quot;">​</a></h2><ul><li>Realtime is a time-axis mock problem. Request/response mocks (v1.11 + v1.12) do not model event streams; v1.13 does.</li><li>The v1.13 mocks share one engine (<code>packages/realtime/src/engine.ts</code>) with 4 provider adapters that translate SDK surfaces into engine primitives. The union type <code>RealtimeAnyEvent</code> is the SSOT for the 3 event kinds.</li><li>5 primary axes — order, timing, drop, reconnect, backpressure — capture what scalar gates miss. All 5 land in <code>RealtimeFidelityRecord</code> per scenario; <code>RealtimeFidelityReport.summary.avgAccuracyScore</code> feeds the release gate.</li><li>Client-side throttle + debounce is <em>not</em> in any of the 4 providers — the app layer owns them. The v1.13 dogfood apps show 3 canonical implementations (500 ms typing debounce, 60 fps mousemove throttle, backpressure-aware notification buffering).</li><li>v1.13 is the realtime infrastructure; v1.14+ will build on it (real socket integration, persistence layer mocks, multi-instance Socket.io + Redis adapter simulation).</li></ul>`,64)])])}const g=s(n,[["render",o]]);export{k as __pageData,g as default};
