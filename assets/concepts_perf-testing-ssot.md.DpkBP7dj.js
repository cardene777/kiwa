import{_ as e,o as a,c as i,a2 as t}from"./chunks/framework.CyZTVJbV.js";const c=JSON.parse('{"title":"Perf testing SSOT — p50 / p95 / p99 + baseline persistence + regression detection SSOT for kiwa v1.25","description":"","frontmatter":{},"headers":[],"relativePath":"concepts/perf-testing-ssot.md","filePath":"concepts/perf-testing-ssot.md"}'),n={name:"concepts/perf-testing-ssot.md"};function l(r,s,h,p,o,d){return a(),i("div",null,[...s[0]||(s[0]=[t(`<h1 id="perf-testing-ssot-—-p50-p95-p99-baseline-persistence-regression-detection-ssot-for-kiwa-v1-25" tabindex="-1">Perf testing SSOT — p50 / p95 / p99 + baseline persistence + regression detection SSOT for kiwa v1.25 <a class="header-anchor" href="#perf-testing-ssot-—-p50-p95-p99-baseline-persistence-regression-detection-ssot-for-kiwa-v1-25" aria-label="Permalink to &quot;Perf testing SSOT — p50 / p95 / p99 + baseline persistence + regression detection SSOT for kiwa v1.25&quot;">​</a></h1><p>Introduced in v1.13-1 as <code>@kiwa-lab/perf-harness</code> v0.1 (<code>measure</code> + <code>saveBaseline</code> + <code>detectRegression</code>), extended in v1.14-post as <code>runPerf3Layer</code> (serial + concurrent + memory 3-layer), and rolled out to all 33 kiwa packages in the v1.25 milestone. This document is the SSOT for <strong>what a kiwa perf suite measures, how the numbers are computed, and how a regression is decided</strong>. Every downstream perf suite in <code>packages/*/tests/perf/*.perf.ts</code> reads these rules from here — do not re-derive them locally.</p><h2 id="why-a-perf-ssot" tabindex="-1">Why a perf SSOT <a class="header-anchor" href="#why-a-perf-ssot" aria-label="Permalink to &quot;Why a perf SSOT&quot;">​</a></h2><p>Perf tests without a shared standard fail three ways.</p><ul><li><strong>Threshold drift</strong>. One package uses p50 &lt; 5 ms as the gate, another uses p95 &lt; 10 ms, a third uses mean &lt; 8 ms. When a regression fires on package A but not on B, the reader cannot tell whether B is genuinely fine or whether B&#39;s gate is looser than A&#39;s. The 11-axis release gate defines <code>perf.p95Ms</code> as the single perf axis; every downstream suite converges on that same metric.</li><li><strong>Iteration count drift</strong>. A suite that runs 10 iterations gives a p95 that is dominated by warmup noise. A suite that runs 10 000 iterations wastes 10 seconds per package × 33 packages. The SSOT pins <strong>3 warmup + 100 iteration</strong> as the shared floor — enough samples to make Welch&#39;s t-test meaningful, few enough that the full sweep completes in under 90 seconds.</li><li><strong>Regression detection drift</strong>. Deltas of 5 % / 10 % / 30 % all get called &quot;regression&quot; in different suites. The SSOT pins the delta threshold at <strong>20 %</strong> and the significance test at Welch&#39;s t-test (<code>|t| &gt; 2</code>). Both conditions must hold for a <code>regressed</code> verdict — a delta alone can be noise, and a t-test alone can flag a statistically-significant 1 % move that no user notices.</li></ul><p>The 4 rules below are the smallest set that make kiwa perf suites comparable across packages, milestones, and forks.</p><h2 id="rule-1-—-3-warmup-100-iteration-is-the-shared-floor" tabindex="-1">Rule 1 — 3 warmup + 100 iteration is the shared floor <a class="header-anchor" href="#rule-1-—-3-warmup-100-iteration-is-the-shared-floor" aria-label="Permalink to &quot;Rule 1 — 3 warmup + 100 iteration is the shared floor&quot;">​</a></h2><p>Every <code>measure</code> call in the kiwa monorepo passes <code>warmup: 3</code> and <code>iterations: 100</code> unless there is a written override in the package&#39;s <code>README.md</code>. Fewer iterations get too noisy for Welch&#39;s t-test to reject the null hypothesis; more iterations waste CI time.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { measure } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@kiwa-lab/perf-harness&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> result</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> measure</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;primaryApi&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  iterations: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">100</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  warmup: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  fn</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> primaryApi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ input: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;x&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>Warmup samples are discarded — they are executed but not recorded. This lets V8&#39;s inline caches settle so the recorded samples reflect the steady-state cost, not the JIT compilation cost.</p><h3 id="exceptions" tabindex="-1">Exceptions <a class="header-anchor" href="#exceptions" aria-label="Permalink to &quot;Exceptions&quot;">​</a></h3><ul><li><strong>Real driver mode</strong>. <code>KIWA_MODE=real</code> swaps the adapter for a real testcontainers process. Real driver perf typically runs at <code>iterations: 20</code> because each iteration is 100-500 ms and the goal is to catch order-of-magnitude divergence from the mock, not to fit a tight p95 envelope.</li><li><strong>AI-LLM perf</strong>. The 11-axis release gate defines a separate <code>latency.p95Ms</code> axis (≤ 3 000 ms) for AI-LLM providers. Those suites use <code>iterations: 20-50</code> because each LLM round-trip is 500-3 000 ms.</li></ul><h2 id="rule-2-—-p95-is-the-primary-axis-p50-p99-are-informational" tabindex="-1">Rule 2 — p95 is the primary axis, p50 + p99 are informational <a class="header-anchor" href="#rule-2-—-p95-is-the-primary-axis-p50-p99-are-informational" aria-label="Permalink to &quot;Rule 2 — p95 is the primary axis, p50 + p99 are informational&quot;">​</a></h2><p>The 11-axis release gate exposes <code>perf.p95Ms</code> as the single perf axis. <code>evaluatePerfGate</code> maps <code>MeasureResult.p95</code> onto that axis and every downstream suite gates on it.</p><ul><li><strong>p50</strong> — surfaces the &quot;typical&quot; latency. Useful for reasoning about UX, less useful as a gate because it hides tail regressions.</li><li><strong>p95</strong> — surfaces the &quot;bad-day&quot; latency. This is the gate. 100 ms is the shared default cap for adapter-scope work.</li><li><strong>p99</strong> — surfaces the &quot;worst-day&quot; latency. Reported for context; not gated because the 100-sample floor gives only ~1 sample at p99, so the reading is too noisy to gate on reliably.</li></ul><p>The percentile computation is a straightforward rank-based extraction (<code>Math.ceil(sorted.length * ratio) - 1</code>) — not a Hyndman-Fan interpolation, not a t-digest. Simple, deterministic, easy to reproduce by hand from the raw samples.</p><h2 id="rule-3-—-baseline-persistence-is-json-path-is-perf-baseline-module-json" tabindex="-1">Rule 3 — baseline persistence is JSON, path is <code>.perf-baseline/{module}.json</code> <a class="header-anchor" href="#rule-3-—-baseline-persistence-is-json-path-is-perf-baseline-module-json" aria-label="Permalink to &quot;Rule 3 — baseline persistence is JSON, path is \`.perf-baseline/{module}.json\`&quot;">​</a></h2><p>The <code>saveBaseline(path, result)</code> call writes <code>\${JSON.stringify(result, null, 2)}\\n</code> — the full <code>MeasureResult</code> object including every sample. This lets a downstream reader recompute p50 / p95 / p99 without re-running the measurement.</p><p><code>defaultBaselinePath(&#39;my-module&#39;)</code> resolves to <code>\${cwd}/.perf-baseline/my-module.json</code>. The cwd anchor means each kiwa package writes its own baseline at <code>packages/{package}/.perf-baseline/{package}.json</code> when vitest runs from the package root.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  defaultBaselinePath,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  loadBaseline,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  measure,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  saveBaseline,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@kiwa-lab/perf-harness&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> path</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defaultBaselinePath</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;my-module&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> current</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> measure</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;my-module&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, iterations: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">100</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, warmup: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, fn });</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> baseline</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> loadBaseline</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(path);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">baseline) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> saveBaseline</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(path, current); </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// First run — seed the baseline.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Commit the baseline JSON to lock the envelope into the repo. A regression on a future PR then shows up as a diff on the JSON file alongside the code diff — reviewers can see the exact number that moved.</p><h2 id="rule-4-—-regression-20-delta-welch-t-test-significant" tabindex="-1">Rule 4 — regression = 20 % delta + Welch t-test significant <a class="header-anchor" href="#rule-4-—-regression-20-delta-welch-t-test-significant" aria-label="Permalink to &quot;Rule 4 — regression = 20 % delta + Welch t-test significant&quot;">​</a></h2><p><code>detectRegression({ current, baseline, threshold: 0.2 })</code> returns four fields.</p><ul><li><code>deltaPct</code> — signed p95 change. Positive = current is slower than baseline.</li><li><code>welchT</code> — Welch&#39;s t-statistic across the raw sample arrays. <code>|welchT| &gt; 2</code> is the significance bar.</li><li><code>significant</code> — boolean derived from the t-test.</li><li><code>verdict</code> — <code>&#39;stable&#39;</code> / <code>&#39;improved&#39;</code> / <code>&#39;regressed&#39;</code>. <code>regressed</code> fires when <code>significant &amp;&amp; deltaPct &gt;= threshold</code>; <code>improved</code> fires when <code>significant &amp;&amp; deltaPct &lt;= -threshold</code>; everything else is <code>stable</code>.</li></ul><p>Dual-gating (delta + significance) is what suppresses false positives. A 25 % delta with <code>welchT = 1.2</code> (many overlapping samples) is <code>stable</code>, not <code>regressed</code>. A 5 % delta with <code>welchT = 3.0</code> (tight distributions) is <code>stable</code>, not <code>regressed</code>. Only &quot;large <strong>and</strong> tight&quot; fires the alarm.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { detectRegression, measure } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@kiwa-lab/perf-harness&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> baseline</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> loadBaseline</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">defaultBaselinePath</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;my-module&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">));</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> current</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> measure</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;my-module&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, iterations: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">100</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, warmup: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, fn });</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (baseline) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> verdict</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> detectRegression</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ current, baseline, threshold: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (verdict.verdict </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;regressed&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    throw</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Error</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`perf regression on my-module: deltaPct=\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">verdict</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">deltaPct</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">} welchT=\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">verdict</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">welchT</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h2 id="the-3-layer-harness-—-serial-concurrent-memory" tabindex="-1">The 3-layer harness — serial + concurrent + memory <a class="header-anchor" href="#the-3-layer-harness-—-serial-concurrent-memory" aria-label="Permalink to &quot;The 3-layer harness — serial + concurrent + memory&quot;">​</a></h2><p><code>measure</code> alone captures serial latency at concurrency 1. Real production traffic is not serial, and a p95 that looks fine at concurrency 1 can collapse under contention. v1.14-post introduced <code>runPerf3Layer</code> to gate three axes in one call.</p><ul><li><strong>serial</strong> — <code>measure</code> at concurrency 1, 200 iterations, 5 warmup. Baseline latency floor.</li><li><strong>concurrent</strong> — <code>measureConcurrent</code> at concurrency 10, 50 iterations per worker (500 total). Contention detector.</li><li><strong>memory</strong> — <code>measureMemory</code> at 200 iterations, gate on <code>arrayBuffersDelta</code> (not <code>heapUsedDelta</code>, which is dominated by GC timing). Leak detector.</li></ul><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { resolveKiwaRepoRoot, runPerf3Layer } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@kiwa-lab/perf-harness&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> path </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;node:path&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> result</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> runPerf3Layer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  moduleName: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;my-module&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  reportPath: path.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">join</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    resolveKiwaRepoRoot</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(process.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cwd</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()),</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &#39;docs/quality-reports/perf/my-module.md&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ops: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;primaryApi&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, serialP95CapMs: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">5</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">fn</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> primaryApi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>Each op is gated against a per-op serial cap (<code>serialP95CapMs</code>). The concurrent cap defaults to 2× the serial cap; override with <code>concurrentP95CapMs</code> when the op is expected to scale worse than linearly. The memory cap defaults to 100 KB <code>arrayBuffers</code> delta across 200 iterations; override with <code>memoryArrayBuffersCapBytes</code>.</p><h2 id="package-coverage-v1-25" tabindex="-1">Package coverage (v1.25) <a class="header-anchor" href="#package-coverage-v1-25" aria-label="Permalink to &quot;Package coverage (v1.25)&quot;">​</a></h2><p>The v1.25 milestone applied the 3-layer harness to every kiwa package.</p><table tabindex="0"><thead><tr><th>Layer</th><th>Packages</th></tr></thead><tbody><tr><td>core (9)</td><td><code>@kiwa-lab/core</code> + <code>@kiwa-lab/dapp</code> + <code>@kiwa-lab/api</code> + <code>@kiwa-lab/ui</code> + <code>@kiwa-lab/data</code> + <code>@kiwa-lab/cli-test</code> + <code>@kiwa-lab/observability</code> + <code>@kiwa-lab/e2e</code> + <code>@kiwa-lab/cli</code></td></tr><tr><td>framework adapter (11)</td><td><code>@kiwa-lab/nextjs</code> + <code>@kiwa-lab/nuxt</code> + <code>@kiwa-lab/sveltekit</code> + <code>@kiwa-lab/remix</code> + <code>@kiwa-lab/astro</code> + <code>@kiwa-lab/solidstart</code> + <code>@kiwa-lab/qwikcity</code> + <code>@kiwa-lab/edge</code> + <code>@kiwa-lab/fresh</code> + <code>@kiwa-lab/hono</code> + <code>@kiwa-lab/solidjs</code></td></tr><tr><td>test type (3)</td><td><code>@kiwa-lab/a11y</code> + <code>@kiwa-lab/visual</code> + <code>@kiwa-lab/component</code></td></tr><tr><td>SaaS layer (10)</td><td><code>@kiwa-lab/auth</code> + <code>@kiwa-lab/queue</code> + <code>@kiwa-lab/cache</code> + <code>@kiwa-lab/orm</code> + <code>@kiwa-lab/payment</code> + <code>@kiwa-lab/streaming</code> + <code>@kiwa-lab/search</code> + <code>@kiwa-lab/mcp</code> + <code>@kiwa-lab/agent</code></td></tr></tbody></table><p>Every package writes a per-op p95 baseline, runs the 3-layer gate on every <code>pnpm test:perf</code> invocation, and feeds the report into <code>docs/quality-reports/perf/{package}.md</code>. The release gate (<code>evaluateReleaseGate</code>) treats a <code>regressed</code> verdict on any package as a release blocker on the <code>perf.p95Ms</code> axis.</p><h2 id="where-each-axis-lands-in-the-release-gate" tabindex="-1">Where each axis lands in the release gate <a class="header-anchor" href="#where-each-axis-lands-in-the-release-gate" aria-label="Permalink to &quot;Where each axis lands in the release gate&quot;">​</a></h2><p>The 11-axis release gate exposes one perf axis — <code>perf.p95Ms</code> — with a default cap of 100 ms for the unit-scope adapter surface. The v1.25 sweep confirms that every kiwa package clears the 100 ms cap on the primary paths tested; regressions above the cap block the release.</p><table tabindex="0"><thead><tr><th>Axis</th><th>Default cap</th><th>Overrideable</th><th>Source</th></tr></thead><tbody><tr><td>perf.p95Ms</td><td>100 ms</td><td>yes (per-op <code>serialP95CapMs</code>)</td><td><code>docs/quality/release-gate.md</code></td></tr><tr><td>concurrent p95</td><td>2× serial cap</td><td>yes (per-op <code>concurrentP95CapMs</code>)</td><td><code>runPerf3Layer</code> default</td></tr><tr><td>memory arrayBuffers</td><td>100 KB / 200 iter</td><td>yes (per-op <code>memoryArrayBuffersCapBytes</code>)</td><td><code>runPerf3Layer</code> default</td></tr></tbody></table><p>Concurrent + memory gates are not in the 11-axis release gate — they are per-package gates enforced inside <code>runPerf3Layer</code>. A concurrent or memory breach fails the local vitest run but does not cascade to the release-gate report, because the release gate is scoped to a single primary axis per category (<code>perf.p95Ms</code>, <code>coverage.line</code>, <code>fidelity.ratio</code>, etc.).</p><h2 id="related" tabindex="-1">Related <a class="header-anchor" href="#related" aria-label="Permalink to &quot;Related&quot;">​</a></h2><ul><li><a href="./../tutorials/45-perf-harness-baseline">Tutorial 45 — Perf-harness baseline (p95 walkthrough)</a></li><li><a href="./../tutorials/46-perf-baseline-migration">Tutorial 46 — Perf baseline migration (existing 3 package → 33 package sweep)</a></li><li><a href="./../migrations/v1.24-to-v1.25">Migration guide v1.24 → v1.25</a></li><li><a href="./../quality/release-gate">Release gate SSOT (11-axis)</a></li></ul>`,41)])])}const g=e(n,[["render",l]]);export{c as __pageData,g as default};
