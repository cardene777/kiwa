import{_ as t,o as a,c as o,a2 as d}from"./chunks/framework.CyZTVJbV.js";const m=JSON.parse('{"title":"Streaming real-driver testing — 8 axis × 3 provider = 24 cell grid + testcontainers pattern (SSOT)","description":"","frontmatter":{},"headers":[],"relativePath":"concepts/streaming-real-driver-testing.md","filePath":"concepts/streaming-real-driver-testing.md"}'),i={name:"concepts/streaming-real-driver-testing.md"};function r(s,e,n,c,l,h){return a(),o("div",null,[...e[0]||(e[0]=[d('<h1 id="streaming-real-driver-testing-—-8-axis-×-3-provider-24-cell-grid-testcontainers-pattern-ssot" tabindex="-1">Streaming real-driver testing — 8 axis × 3 provider = 24 cell grid + testcontainers pattern (SSOT) <a class="header-anchor" href="#streaming-real-driver-testing-—-8-axis-×-3-provider-24-cell-grid-testcontainers-pattern-ssot" aria-label="Permalink to &quot;Streaming real-driver testing — 8 axis × 3 provider = 24 cell grid + testcontainers pattern (SSOT)&quot;">​</a></h1><p>kiwa&#39;s v1.20 streaming work covered the <strong>5 base semantics</strong> (producer / consumer / exactly-once / DLQ / schema-registry) as unified mocks for Kafka + Redpanda + NATS — the <code>docs/concepts/streaming-testing.md</code> doc is the SSOT for those five axes. v1.31 adds <strong>8 advanced axes on top of that base</strong> — the ones production teams hit once their mock-only suite is green but real broker behavior (KIP-98 fencing, txn coordinator races, JetStream backoff, schema evolution boundaries) starts showing up in incident reports. This concept doc is the SSOT for those 8 axes; the tutorials (58-60) and dogfood apps (v1.31-2/3/4) are the concrete implementations.</p><h2 id="the-8-axis-grid" tabindex="-1">The 8-axis grid <a class="header-anchor" href="#the-8-axis-grid" aria-label="Permalink to &quot;The 8-axis grid&quot;">​</a></h2><p>The 8 advanced axes are cover-oriented — each one names a real-world failure surface every non-trivial streaming pipeline hits within the first 3 months.</p><table tabindex="0"><thead><tr><th>Axis</th><th>Real-world failure it catches</th><th>v0.3 API</th></tr></thead><tbody><tr><td>Kafka raw protocol</td><td>&quot;Our idempotent producer duplicated a message after retry&quot; (fencing missed the older-epoch send)</td><td><code>createKafkaRawProtocol</code></td></tr><tr><td>Kafka consumer group</td><td>&quot;The second consumer joined but got 0 partitions&quot; (rebalance did not run, or a static-membership session id was stale)</td><td><code>createKafkaConsumerGroup</code></td></tr><tr><td>Redpanda schema evolution</td><td>&quot;The v3 schema broke old readers&quot; (BACKWARD check was disabled, or the compat mode was FORWARD only)</td><td><code>createRedpandaSchemaEvolution</code></td></tr><tr><td>Redpanda transactions</td><td>&quot;Two producers claimed the same TID and both got committed&quot; (epoch fencing on TxnCoordinator)</td><td><code>createRedpandaTransactions</code></td></tr><tr><td>NATS JetStream durable</td><td>&quot;The consumer keeps redelivering the same message forever&quot; (max_deliver was 0, or ack_wait was too short)</td><td><code>createNatsJetStreamDurable</code></td></tr><tr><td>NATS KV / Object</td><td>&quot;The KV update overwrote a newer revision&quot; (CAS check was absent, or the revision counter reset)</td><td><code>createNatsKvObject</code></td></tr><tr><td>Cross-provider exactly-once</td><td>&quot;Kafka and NATS see the same message twice&quot; (read-committed filter was not applied)</td><td><code>createExactlyOnceSemantics</code></td></tr><tr><td>Consumer lag telemetry</td><td>&quot;The alerting fired 5 minutes after the actual lag spike&quot; (offset-lag vs time-lag distinction was missed)</td><td><code>createConsumerLagTelemetry</code></td></tr></tbody></table><p>Each axis has 3 shapes — a mock-only path (fast inner loop, ms scale), a real-driver path (<code>KIWA_MODE=real</code> + provider env, testcontainers, seconds scale), and a fidelity assertion that the two produce the same output. Tutorial 58 covers axis 1 in depth, tutorial 59 covers axis 3, tutorial 60 covers axis 5.</p><h2 id="the-3-provider-×-8-axis-24-cell-grid" tabindex="-1">The 3-provider × 8-axis = 24 cell grid <a class="header-anchor" href="#the-3-provider-×-8-axis-24-cell-grid" aria-label="Permalink to &quot;The 3-provider × 8-axis = 24 cell grid&quot;">​</a></h2><p>Not every provider covers every axis — NATS has no Kafka wire protocol, Kafka has no <code>nats.jetstream.js</code> API, etc. The fidelity harness (<code>createFidelityHarness()</code>) surfaces the coverage explicitly so a test iterating the grid can distinguish &quot;not-implemented&quot; from &quot;not-applicable&quot;.</p><table tabindex="0"><thead><tr><th>Provider</th><th>1 Kafka raw</th><th>2 Consumer group</th><th>3 Schema evolution</th><th>4 Transactions</th><th>5 JetStream durable</th><th>6 KV / Object</th><th>7 Exactly-once</th><th>8 Consumer lag</th></tr></thead><tbody><tr><td>Kafka</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td><td>not-applicable</td><td>not-applicable</td><td>implemented</td><td>implemented</td></tr><tr><td>Redpanda</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td><td>not-applicable</td><td>not-applicable</td><td>implemented</td><td>implemented</td></tr><tr><td>NATS</td><td>not-applicable</td><td>not-applicable</td><td>not-applicable</td><td>not-applicable</td><td>implemented</td><td>implemented</td><td>implemented</td><td>implemented</td></tr></tbody></table><p><code>not-applicable</code> cells are labeled with a <code>note</code> explaining the mismatch (e.g., &quot;NATS has no Kafka wire protocol&quot;; &quot;NATS uses queue groups, modeled via JetStream durable&quot;). A <code>planned</code> cell is a &quot;not yet implemented&quot; flag — the fidelity harness lets tests fail on <code>planned</code> when the milestone should have shipped it.</p><h3 id="why-not-applicable-is-not-implemented-false" tabindex="-1">Why <code>not-applicable</code> is not <code>implemented: false</code> <a class="header-anchor" href="#why-not-applicable-is-not-implemented-false" aria-label="Permalink to &quot;Why `not-applicable` is not `implemented: false`&quot;">​</a></h3><p>A &quot;not implemented&quot; cell is a todo; a &quot;not applicable&quot; cell is a design decision. The distinction matters for grid iteration — a test that iterates all 24 cells and asserts &quot;every cell is either <code>implemented</code> or <code>not-applicable</code>&quot; is a compile-time check that the grid is complete. If NATS were <code>not-implemented</code> on <code>kafka-raw-protocol</code>, the test would flag it as a todo forever; <code>not-applicable</code> says &quot;this pairing is designed out&quot; and the iteration passes.</p><h2 id="the-testcontainers-pattern" tabindex="-1">The testcontainers pattern <a class="header-anchor" href="#the-testcontainers-pattern" aria-label="Permalink to &quot;The testcontainers pattern&quot;">​</a></h2><p>The 3 dogfood apps (v1.31-2/3/4) each expose a <code>pnpm test:real</code> command that flips <code>KIWA_MODE=real</code> and spins up the provider under testcontainers.</p><ul><li><code>examples/dogfood-kafka-event-pipeline</code> v2 — Confluent Platform 7.6 (Kafka 3.7 + Schema Registry) via <code>confluent-kafka-python</code> testcontainers + Playwright e2e that walks the KIP-98 idempotent + txn coordinator + ISR advance path against the real broker.</li><li><code>examples/dogfood-redpanda-schema-registry</code> v2 — Redpanda 23+ testcontainers + Redpanda Console API + 5 compatibility mode (BACKWARD / BACKWARD_TRANSITIVE / FORWARD / FORWARD_TRANSITIVE / FULL) check + Playwright e2e that registers a schema, evolves it, and verifies the compat gate against the real registry.</li><li><code>examples/dogfood-nats-jetstream</code> v2 — NATS 2.10+ testcontainers + JetStream durable consumer + KV bucket revision + Object Store chunking + Playwright e2e that publishes, nacks, waits for the backoff, and asserts on the quarantine list.</li></ul><p>The pattern each v2 app follows.</p><ol><li>Keep the v1 mock-only path (<code>pnpm test</code>) green — the fast inner loop stays sub-second.</li><li>Add a <code>pnpm test:real</code> command that requires the provider env (<code>KAFKA_KEY</code> / <code>REDPANDA_KEY</code> / <code>NATS_KEY</code>) and pulls the testcontainers image.</li><li>Run the same fidelity-harness assertions against the real driver; failure means &quot;the mock diverged from real behavior&quot; — the mock gets the fix.</li><li>Route the e2e through Playwright when the flow crosses UI boundaries (e.g., the schema-registry dogfood app shows a Console admin UI).</li></ol><h2 id="kiwa-mode-real-—-the-env-gate-contract" tabindex="-1"><code>KIWA_MODE=real</code> — the env-gate contract <a class="header-anchor" href="#kiwa-mode-real-—-the-env-gate-contract" aria-label="Permalink to &quot;`KIWA_MODE=real` — the env-gate contract&quot;">​</a></h2><p><code>isRealDriverMode(env)</code> returns <code>true</code> when <code>env.KIWA_MODE === &#39;real&#39;</code>. <code>requiredKeyFor(axis)</code> returns the env key that gates the axis&#39;s real-driver run:</p><ul><li><code>kafka-raw-protocol</code>, <code>kafka-consumer-group</code> → <code>KAFKA_KEY</code></li><li><code>redpanda-schema-evolution</code>, <code>redpanda-transactions</code> → <code>REDPANDA_KEY</code></li><li><code>nats-jetstream-durable</code>, <code>nats-kv-object</code> → <code>NATS_KEY</code></li><li><code>exactly-once</code>, <code>consumer-lag-telemetry</code> → cross-provider, no key required</li></ul><p>A test that respects the contract runs the mock path unconditionally and the real-driver path only when both <code>KIWA_MODE=real</code> and the required key are present. That means CI stays cheap by default (mock only), the nightly job flips both envs (real driver + testcontainers), and the fidelity harness ties the two together.</p><h2 id="the-not-implemented-failure-mode" tabindex="-1">The <code>not-implemented</code> failure mode <a class="header-anchor" href="#the-not-implemented-failure-mode" aria-label="Permalink to &quot;The `not-implemented` failure mode&quot;">​</a></h2><p>If the fidelity harness has a <code>planned</code> cell, the corresponding tutorial + dogfood + snippet-validation-test trio does not exist yet. The 24-cell grid at v1.31 has 0 <code>planned</code> cells — every intended cell is <code>implemented</code> or <code>not-applicable</code>. When a future milestone adds a 9th axis (e.g., <code>kafka-log-compaction</code>), it will start as <code>planned</code> for all 3 providers, then transition to <code>implemented</code> for the ones that cover it as the milestone lands its tutorial + dogfood + snippet test.</p><h2 id="how-this-ties-into-the-13-axis-release-gate" tabindex="-1">How this ties into the 13-axis release gate <a class="header-anchor" href="#how-this-ties-into-the-13-axis-release-gate" aria-label="Permalink to &quot;How this ties into the 13-axis release gate&quot;">​</a></h2><p>v1.31 does not add a 14th release-gate axis. The 8 streaming axes gate the streaming package&#39;s own tests (via <code>pnpm --filter @kiwa-lab/streaming test</code>) but do not surface as a per-package <code>@kiwa-lab/quality-metrics</code> axis. The reasoning — the fidelity harness is provider-shape-specific, and a package that does not use Kafka / Redpanda / NATS has nothing to assert on. When a future milestone adds a <code>provider.fidelity</code> axis that describes &quot;which streaming providers this package&#39;s tests hit,&quot; it will slot into the 13-axis release gate as the 14th; v1.31 keeps the axis count at 13.</p><h2 id="ssot-boundaries" tabindex="-1">SSOT boundaries <a class="header-anchor" href="#ssot-boundaries" aria-label="Permalink to &quot;SSOT boundaries&quot;">​</a></h2><ul><li>The 5 base semantics (producer / consumer / exactly-once / DLQ / schema-registry) live in <code>docs/concepts/streaming-testing.md</code>. v1.31 does not modify that doc.</li><li>The 8 advanced axes live in this doc. Tutorials 58-60 and the migration guide (v1.30 → v1.31) link back here for the axis SSOT.</li><li>The 3-provider × 8-axis grid is the harness&#39;s data structure. The <code>createFidelityHarness()</code> implementation in <code>packages/streaming/src/semantics/fidelity-harness.ts</code> is the code SSOT — this doc&#39;s grid table is derived from that code.</li><li>The <code>KIWA_MODE=real</code> env-gate contract is shared with the v1.22 real-driver testing tutorial (auth adapters + Keycloak). Both use the same pattern; the streaming axes just add provider-specific <code>_KEY</code> envs.</li></ul>',27)])])}const u=t(i,[["render",r]]);export{m as __pageData,u as default};
