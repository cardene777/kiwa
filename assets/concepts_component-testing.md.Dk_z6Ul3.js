import{_ as s,o as t,c as a,a2 as i}from"./chunks/framework.CyZTVJbV.js";const k=JSON.parse('{"title":"Component testing — story registration, CT mount, visual diff","description":"","frontmatter":{},"headers":[],"relativePath":"concepts/component-testing.md","filePath":"concepts/component-testing.md"}'),n={name:"concepts/component-testing.md"};function o(r,e,h,l,d,c){return t(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="component-testing-—-story-registration-ct-mount-visual-diff" tabindex="-1">Component testing — story registration, CT mount, visual diff <a class="header-anchor" href="#component-testing-—-story-registration-ct-mount-visual-diff" aria-label="Permalink to &quot;Component testing — story registration, CT mount, visual diff&quot;">​</a></h1><p>kiwa&#39;s provider mocks up to v1.15 all lived at the <strong>service boundary</strong> — auth mocks answered <code>signIn</code>, AI-LLM mocks answered <code>messages.create</code>, realtime mocks pushed events over a virtual timeline, MCP mocks brokered JSON-RPC 2.0 tool calls. Every fidelity axis measured a <strong>network-shaped surface</strong> (request shape / response shape / latency / cost / event ordering).</p><p>Component tests break this shape. There is no network call. There is no token cost. The unit under test is a <strong>rendered UI fragment</strong> — a Button component with a click handler, a Form component with validation, a Modal component with an accessible close button. The properties that matter are DOM structure, interaction wiring, and pixel-level visual identity, not request/response semantics. v1.16 is designed to absorb this third axis of non-triviality — the browser.</p><h2 id="what-component-test-actually-means" tabindex="-1">What &quot;component test&quot; actually means <a class="header-anchor" href="#what-component-test-actually-means" aria-label="Permalink to &quot;What &quot;component test&quot; actually means&quot;">​</a></h2><p>Modern SaaS frontend teams converged in 2024-2026 on 3 co-existing test surfaces, all installed simultaneously in a fresh app scaffold and all consumed by roughly the same code paths.</p><ul><li><strong>Storybook 8</strong> — the canonical component catalog. A <code>.stories.ts</code> file registers a <code>StoryMeta</code> (title + render + default args) plus one or more <code>StoryObj</code> (per-story args + optional <code>play</code> function). Storybook 8&#39;s CSF3 (Component Story Format 3) is a plain object schema; there is no JSX indirection.</li><li><strong>Playwright Component Testing</strong> — a &quot;small&quot; e2e for a single component. <code>mount(&lt;Button label=&quot;ok&quot; /&gt;)</code> gives back a <code>Locator</code>, and the same <code>page.getByRole(&#39;button&#39;, { name: &#39;ok&#39; }).click()</code> API you use in full-page Playwright tests drives the interaction.</li><li><strong>Chromatic</strong> — visual regression. A story is snapshotted per viewport, hashed, and compared against a baseline; a diff kicks off a review workflow with <code>accept</code> / <code>reject</code> branches.</li></ul><p>The 3 tools have overlapping concepts (a story is often the input to a CT mount, and a story is also the input to a Chromatic snapshot) but historically forced 3 separate test runners, 3 configuration surfaces, and 3 sets of mocks. kiwa v1.16 unifies them behind a <strong>single mock harness</strong> that shares one <code>MockNode</code> tree across all 3 surfaces so the same <code>render(args)</code> function feeds all 3 without adapters in between.</p><h2 id="why-v1-11-–-v1-15-gates-alone-are-not-enough" tabindex="-1">Why v1.11 – v1.15 gates alone are not enough <a class="header-anchor" href="#why-v1-11-–-v1-15-gates-alone-are-not-enough" aria-label="Permalink to &quot;Why v1.11 – v1.15 gates alone are not enough&quot;">​</a></h2><p>The v1.15 gate measures 11 axes — the common 7 (coverage / test count / fidelity ratio / perf p95 / mutation kill rate) plus the AI-LLM 4 (cost / latency / token / accuracy). Every axis is a <strong>scalar</strong> measured across a <strong>network-shaped mock</strong>. Component testing surfaces 3 failure modes that never touch the network.</p><table tabindex="0"><thead><tr><th>Component failure</th><th>Passes 11-axis gate?</th><th>Why</th></tr></thead><tbody><tr><td>Button <code>onClick</code> handler bound to wrong element after refactor</td><td>Yes</td><td><code>onClick</code> firing is a DOM-tree property, not a network property</td></tr><tr><td>Form <code>Submit</code> button label changes from <code>Submit</code> → <code>Continue</code> in a copy edit</td><td>Yes</td><td>Copy is a visual property, not a request/response property</td></tr><tr><td>Modal loses its <code>role=&quot;dialog&quot;</code> after a wrapper <code>&lt;div&gt;</code> swap</td><td>Yes</td><td>a11y role is a semantic-tree property, not a scalar</td></tr><tr><td>Dark-mode color regresses from <code>#1a1a1a</code> → <code>#2a2a2a</code> after a Tailwind config bump</td><td>Yes</td><td>Pixel diff is a snapshot property, not a metric</td></tr><tr><td>Play function runs the <code>click</code> step twice due to a <code>setState</code> batch bug</td><td>Yes</td><td>Interaction ordering is a trace property, not a count</td></tr></tbody></table><p>The v1.16 axes measure these 3 tree properties (DOM structure, interaction trace, snapshot hash) <strong>on the mock</strong>, and the fidelity harness measures how far real Storybook / real Playwright CT / real Chromatic drift from that mock. Same-shape reports still feed <code>evaluateReleaseGate</code> — component providers stay on the common 7-axis branch — but the <strong>fidelity axis</strong> for component testing counts covered ops on the 3 surfaces separately, and the <strong>perf axis</strong> measures mount + play + a11y round-trip latency rather than one-shot API latency.</p><h2 id="the-3-surfaces-1-mock" tabindex="-1">The 3 surfaces, 1 mock <a class="header-anchor" href="#the-3-surfaces-1-mock" aria-label="Permalink to &quot;The 3 surfaces, 1 mock&quot;">​</a></h2><p>The v1.16 mock does not open a real browser. Instead, it builds an in-memory <code>MockNode</code> tree that represents the rendered component (see <a href="https://github.com/cardene777/kiwa/blob/main/packages/component/src/dom.ts" target="_blank" rel="noreferrer"><code>packages/component/src/dom.ts</code></a>). Every framework — React / Vue / Svelte / Solid — flattens down to a <code>(args) =&gt; MockNode</code> render function; the mock never sees framework-specific virtual DOM.</p><p>The 3 factories share this tree.</p><ul><li><code>createStoryRegistry()</code> — Storybook 8&#39;s registry, keyed on <code>title--storyName</code> (kebab-case, matches the Storybook 8 URL scheme). <code>register(meta)</code> merges <code>meta.args</code> + <code>story.args</code> per CSF3 semantics and stores 1 <code>StoryEntry</code> per story. <code>mount(title, storyName)</code> returns a <code>CanvasElement</code> (the rendered root). <code>play(title, storyName, canvas, args)</code> runs the story&#39;s <code>play</code> function and records 1 trace step per <code>step()</code> call. <code>runA11y(title, storyName, canvas)</code> runs 3 heuristic checkers (button-name / image-alt / label) and returns a <code>{ violations: A11yViolation[] }</code> result.</li><li><code>createPlaywrightCTMock()</code> — <code>mount(render, args)</code> returns a <code>ComponentLocator</code> with <code>getByRole</code> + <code>getByText</code> locators, matching the Playwright API. <code>click()</code> fires the bound handler via <code>fireEvent</code>. <code>fill(value)</code> mutates <code>value</code> and fires the <code>input</code> event. <code>activeMounts()</code> + <code>unmountAll()</code> expose leak detection for teardown.</li><li><code>createChromaticVisualMock(config)</code> — <code>capture({ entry, canvas, viewport })</code> snapshots the rendered markup via <code>hashMarkup(renderMarkup(canvas.root))</code> and compares against the baseline. <code>captureAll({ entry, canvas })</code> iterates <code>parameters.chromatic.viewports</code> and returns 1 diff per viewport. <code>review({ storyId, viewport, action })</code> accepts or rejects a pending diff — <code>accept</code> swaps the baseline to the current capture.</li></ul><p>The three factories are independent constructors — a test suite can use just one, any two, or all three. The fidelity harness in each dogfood app wires them together against a real adapter (env-gated on the tool&#39;s connection env var) and produces one report that feeds the release gate.</p><h2 id="the-4-semantic-axes-measured-on-the-mock" tabindex="-1">The 4 semantic axes measured on the mock <a class="header-anchor" href="#the-4-semantic-axes-measured-on-the-mock" aria-label="Permalink to &quot;The 4 semantic axes measured on the mock&quot;">​</a></h2><p>The v1.16 mocks and fidelity harness measure 4 component test semantics, tracked per surface in the per-dogfood <code>FidelityRecord</code>.</p><table tabindex="0"><thead><tr><th>Axis</th><th>What it measures</th><th>Surface</th></tr></thead><tbody><tr><td><strong>Story registration</strong></td><td>did <code>register(meta)</code> build the expected <code>StoryEntry</code> records? (title + storyName + args + parameters)</td><td>Storybook 8 registry</td></tr><tr><td><strong>Args resolution</strong></td><td>did <code>meta.args + story.args</code> merge match CSF3 semantics?</td><td>Storybook 8 registry</td></tr><tr><td><strong>Interaction trace</strong></td><td>did <code>play(...)</code> fire the expected step / event sequence in the expected order?</td><td>Storybook 8 + Playwright CT</td></tr><tr><td><strong>a11y violations</strong></td><td>did the heuristic checker return 0 violations for accessible-name / image-alt / label rules?</td><td>Storybook 8 registry</td></tr><tr><td><strong>Snapshot hash</strong></td><td>did the SHA-256 hash of the rendered markup match the baseline?</td><td>Chromatic</td></tr><tr><td><strong>Review workflow</strong></td><td>did <code>accept</code> swap the baseline and <code>reject</code> leave it untouched?</td><td>Chromatic</td></tr></tbody></table><p>The trade-off is that the mock cannot observe <strong>rendered pixels</strong> (only the semantic DOM tree) and cannot execute <strong>real event-loop side effects</strong> (only the wired handlers). Real Storybook 8 renders through a browser preview channel; real Playwright CT drives Chromium / WebKit / Firefox; real Chromatic captures headless PNGs. The fidelity harness measures how far real drifts from the mock per axis and reports the gap.</p><h2 id="concept-1-—-csf3-as-the-ssot" tabindex="-1">Concept 1 — CSF3 as the SSOT <a class="header-anchor" href="#concept-1-—-csf3-as-the-ssot" aria-label="Permalink to &quot;Concept 1 — CSF3 as the SSOT&quot;">​</a></h2><p>Storybook 8&#39;s CSF3 defines the story surface as a plain object.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { StoryObj } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@kiwa/component&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { buildButton, createStoryRegistry, fireEvent } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@kiwa/component&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> meta</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  title: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Components/Button&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  render: buildButton,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  args: { label: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;ok&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, variant: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;primary&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  parameters: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    chromatic: { viewports: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;mobile&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;desktop&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    a11y: { disable: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  stories: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Default: {} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">satisfies</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> StoryObj</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Danger: { args: { variant: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;danger&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">satisfies</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> StoryObj</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Interactive: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      args: { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">onClick</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> console.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">log</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;click&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) },</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">      play</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ({ </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">canvasElement</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">step</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> step</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;click primary CTA&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">          const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> button</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> canvasElement.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getByRole</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;button&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, { name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;ok&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">          fireEvent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(button, { type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;click&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, target: button });</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        });</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">satisfies</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> StoryObj</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> registry</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createStoryRegistry</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">registry.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">register</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(meta);</span></span></code></pre></div><p>The <strong>same</strong> <code>meta</code> object feeds Storybook 8 (via <code>registry.mount</code>), Playwright CT (via <code>ct.mount(meta.render, resolvedArgs)</code>), and Chromatic (via <code>chromatic.captureAll({ entry, canvas })</code>). One SSOT, three consumers. When <code>story.args</code> overrides <code>meta.args</code>, the mock merges them shallow-per-key exactly as Storybook 8&#39;s <code>entries.js</code> does.</p><p>The <code>play</code> function receives a <code>PlayContext</code> with <code>canvasElement</code> (a handle that exposes <code>getByRole</code> / <code>getByText</code> / <code>querySelector</code>), the resolved <code>args</code>, and <code>step(label, fn)</code> for grouping interactions in the trace. Every <code>step()</code> call appends 1 entry to <code>StoryPlayResult.steps</code>; if a step throws, <code>ok</code> becomes <code>false</code> and the error is captured — the play never crashes the test runner. Real Storybook 8&#39;s play function has the same <code>step</code> semantic (via <code>@storybook/test</code>).</p><h2 id="concept-2-—-ct-mount-interact-assert-the-tri-tuple" tabindex="-1">Concept 2 — CT mount + interact + assert (the tri-tuple) <a class="header-anchor" href="#concept-2-—-ct-mount-interact-assert-the-tri-tuple" aria-label="Permalink to &quot;Concept 2 — CT mount + interact + assert (the tri-tuple)&quot;">​</a></h2><p>Playwright Component Testing lives in the same shape as full-page Playwright — a <code>Locator</code> that supports <code>.click()</code> / <code>.fill(text)</code> / <code>.textContent()</code>. The v1.16 mock reproduces the shape.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> ct</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createPlaywrightCTMock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// mount = render into an in-memory canvas, get back a locator</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> locator</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ct.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mount</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(buildButton, { label: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Submit&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">onClick</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> hits</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">++</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// interact = getByRole / getByText + click / fill</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> locator.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getByRole</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;button&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, { name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Submit&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">click</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// assert = textContent + count on the locator</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">expect</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> locator.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getByRole</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;button&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, { name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Submit&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">textContent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">toBe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Submit&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">expect</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(hits).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">toBe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// teardown = unmount clears handlers to prevent leaks</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">locator.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">unmount</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">expect</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ct.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">activeMounts</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">toBe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p>The three primitives (<code>mount</code> / <code>getByRole|getByText</code> / <code>click|fill</code>) map 1:1 to real Playwright CT. The mock never opens a browser, so it runs in Node.js without a display server — but the API surface is the same, so a follow-up milestone can swap in the real driver by changing the factory function without touching any test code.</p><p>Leak detection is the mock-specific value-add. Real Playwright CT teardown is implicit (Chromium closes the tab); the mock&#39;s <code>activeMounts()</code> counter catches the case where a test forgot to <code>unmount()</code> in <code>afterEach</code> — a real leak that surfaces as memory growth in CI over hundreds of tests.</p><h2 id="concept-3-—-visual-diff-markup-hash-not-pixel-diff" tabindex="-1">Concept 3 — visual diff = markup hash, not pixel diff <a class="header-anchor" href="#concept-3-—-visual-diff-markup-hash-not-pixel-diff" aria-label="Permalink to &quot;Concept 3 — visual diff = markup hash, not pixel diff&quot;">​</a></h2><p>Real Chromatic captures a rendered PNG per story per viewport, hashes it, and compares against the baseline PNG hash. The v1.16 mock reproduces the workflow — baseline seed / capture / diff / accept / reject — but hashes <strong>rendered markup</strong> (<code>hashMarkup(renderMarkup(canvas.root))</code>), not pixels.</p><p>The equivalence holds when <code>diffThreshold = 0</code> (hash-exact matching), which is the mock default. Every diff has 3 states.</p><ul><li><code>status = &#39;new&#39;</code> — no baseline exists; the current capture becomes the baseline</li><li><code>status = &#39;passed&#39;</code> — baseline hash matches current hash exactly</li><li><code>status = &#39;failed&#39;</code> — hashes differ; a <code>pending</code> review entry is created</li></ul><p>The review workflow surfaces <code>pending → accept → passed</code> (swap baseline with current) or <code>pending → reject → failed</code> (leave baseline untouched). The 4 AC axes tested in <code>dogfood-visual-regression</code> cover exactly this state machine — baseline seed, capture in the passed state, capture in the failed state after an intent change, and capture in the passed state again after <code>accept</code>.</p><p>The trade-off — the mock cannot observe <strong>anti-aliased edges</strong>, <strong>font hinting variance</strong>, or <strong>subpixel rendering differences</strong> that real Chromatic sees. A component that changes its border-radius from <code>4px</code> → <code>5px</code> shows up in the markup hash (both are attribute values); a component whose text renders with different font metrics on macOS vs Linux does not. Real Chromatic catches the second; the mock does not. The fidelity harness reports the gap so consumers know what the mock covers and what it does not.</p><h2 id="concept-4-—-a11y-as-a-first-class-axis" tabindex="-1">Concept 4 — a11y as a first-class axis <a class="header-anchor" href="#concept-4-—-a11y-as-a-first-class-axis" aria-label="Permalink to &quot;Concept 4 — a11y as a first-class axis&quot;">​</a></h2><p>The v1.16 mock ships a 3-rule heuristic a11y checker (see <code>runA11y</code> in <code>packages/component/src/storybook.ts</code>), matching the axe-core rules Storybook 8&#39;s a11y addon enables by default.</p><ul><li><code>button-name</code> — every <code>role=&quot;button&quot;</code> must have an accessible name (visible text or <code>aria-label</code>)</li><li><code>image-alt</code> — every <code>role=&quot;img&quot;</code> must have an <code>alt</code> attribute (blank strings intentionally allowed for decorative images per WCAG)</li><li><code>label</code> — every <code>role=&quot;textbox&quot;</code> / <code>role=&quot;checkbox&quot;</code> must be labelled by a <code>&lt;label for&gt;</code> or <code>aria-label</code></li></ul><p>The checker returns <code>{ violations: A11yViolation[] }</code> with <code>id</code> (rule name), <code>nodeId</code> (path in the tree), and <code>message</code> (human-readable). Zero violations = passed. Any violation = failed.</p><p>The checker is deliberately narrow — 3 rules cover ~40% of real axe-core findings in a component-scoped scan. The tradeoff — the mock does not check color contrast (a rendered-pixel property that the markup hash cannot see), keyboard focus traversal (a browser property that in-memory nodes do not model), or aria-live announcement timing (a live-region property that the mock treats as instant). The fidelity harness measures a11y-rule coverage against real axe-core in the dogfood, so consumers can see the gap.</p><h2 id="where-the-fidelity-harness-lives" tabindex="-1">Where the fidelity harness lives <a class="header-anchor" href="#where-the-fidelity-harness-lives" aria-label="Permalink to &quot;Where the fidelity harness lives&quot;">​</a></h2><p>Each dogfood app (<code>examples/dogfood-storybook-design-system/</code>, <code>examples/dogfood-form-ct/</code>, <code>examples/dogfood-visual-regression/</code>) ships a <code>flows/fidelity.ts</code> module that.</p><ol><li>Runs the same 4-op / 5-op / 3-op surface through the mock adapter (<code>makeMockAdapter</code>) — always available</li><li>Runs the same surface through the real adapter (<code>makeRealAdapter</code>) — env-gated; falls back to <code>*_REAL_ENV_MISSING</code> traces when the connection env is absent</li><li>Emits a <code>FidelityReport</code> with per-op traces, latency samples, and a per-axis coverage summary</li><li>Writes the report as <code>quality-report/fidelity-latest.md</code> (git-ignored — CI reads this) and <code>quality-report/fidelity-latest.json</code> (machine-readable)</li></ol><p>The report is then hand-promoted to <code>docs/quality-reports/component/&lt;name&gt;.md</code> when a release cuts. <code>@kiwa/quality-metrics</code>&#39;s <code>evaluateReleaseGate</code> consumes the JSON directly and gates the release on the 7-axis pass.</p><h2 id="related-reading" tabindex="-1">Related reading <a class="header-anchor" href="#related-reading" aria-label="Permalink to &quot;Related reading&quot;">​</a></h2><ul><li><a href="./../tutorials/19-storybook-design-system">Tutorial 19 — Storybook 8 design system in 12 min</a></li><li><a href="./../tutorials/20-playwright-ct">Tutorial 20 — Playwright CT for 5 form patterns in 12 min</a></li><li><a href="./../tutorials/21-visual-regression">Tutorial 21 — Visual regression baseline / diff / accept in 12 min</a></li><li><a href="./../migrations/v1.15-to-v1.16">Migration guide — v1.15 → v1.16</a></li><li>v1.16 milestone parent <a href="https://github.com/cardene777/kiwa/issues/762" target="_blank" rel="noreferrer">#762</a></li></ul>`,47)])])}const g=s(n,[["render",o]]);export{k as __pageData,g as default};
