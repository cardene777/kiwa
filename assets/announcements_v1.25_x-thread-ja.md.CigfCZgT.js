import{_ as o,o as c,c as a,a2 as t}from"./chunks/framework.CyZTVJbV.js";const v=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"announcements/v1.25/x-thread-ja.md","filePath":"announcements/v1.25/x-thread-ja.md"}'),d={name:"announcements/v1.25/x-thread-ja.md"};function r(s,e,p,i,n,l){return c(),a("div",null,[...e[0]||(e[0]=[t('<p>1/ kiwa v1.25 リリース — Perf-harness sweep milestone。 v1.13-1 で land した <code>@kiwa/perf-harness</code> v0.1 (<code>measure</code> p50/p95/p99 + <code>saveBaseline</code> + <code>detectRegression</code> + <code>evaluatePerfGate</code> primitive) + v1.14-post で land した <code>runPerf3Layer</code> (serial + concurrent + memory 3-layer harness) を基盤に、 v1.25 は同 primitive を <strong>33 全 kiwa package</strong> に rollout、 3 warmup + 100 iteration + 20 % p95 delta + Welch&#39;s t-test |t| &gt; 2 の SSOT で統一。 33 package sweep + v0.2 minor bump。</p><p>2/ なぜ 33 package coverage? 3-pilot rollout には 3 失敗 mode がある — threshold drift (package A = p50 &lt; 5 ms / package B = p95 &lt; 10 ms / package C = mean &lt; 8 ms、 regression 発火の意味が package で違う)、 iteration count drift (10 iter は warmup noise / 10 000 iter は 10 s × 33 pkg CI 負荷)、 regression detection drift (5 % / 10 % / 30 % が全部「regression」 と呼ばれる)。 4 rule SSOT (<code>docs/concepts/perf-testing-ssot.md</code>) = 3 warmup + 100 iter / Welch&#39;s t-test |t| &gt; 2 / 20 % p95 delta / baseline JSON schema は package 越し・ milestone 越し・ fork 越しで perf 比較可能にする最小 set。</p><p>3/ v1.25-1 core layer perf sweep — 9 package (<code>@kiwa/core</code> / <code>dapp</code> / <code>api</code> / <code>ui</code> / <code>data</code> / <code>cli-test</code> / <code>observability</code> / <code>e2e</code> / <code>cli</code>) に <code>tests/perf/{package}.perf.ts</code> + <code>test:perf</code> script + <code>.perf-baseline/{package}.json</code> 追加。 各 suite は <code>runPerf3Layer</code> (serial concurrency=1 200 iter + concurrent concurrency=10 500 iter + memory heap sampling) 経由。</p><p>4/ v1.25-2 framework adapter perf sweep — 11 framework adapter (<code>a11y</code> / <code>visual</code> / <code>nextjs</code> / <code>nuxt</code> / <code>sveltekit</code> / <code>remix</code> / <code>astro</code> / <code>solidstart</code> / <code>qwikcity</code> / <code>edge</code> / <code>fresh</code> / <code>hono</code> / <code>solidjs</code>) に同 pattern。 framework baseline path 分離で cross-adapter noise 抑制。</p><p>5/ v1.25-3 test type perf sweep — 3 test type package (<code>a11y</code> / <code>visual</code> / <code>component</code>) + 3-layer perf harness。</p><p>6/ v1.25-4 SaaS layer perf sweep — 10 SaaS layer package (<code>auth</code> / <code>queue</code> / <code>cache</code> / <code>orm</code> / <code>payment</code> / <code>streaming</code> / <code>search</code> / <code>mcp</code> / <code>agent</code> / <code>ai-llm</code>) + provider 別 baseline + <code>KIWA_MODE=real</code> opt-in で real sandbox 走査。</p><p>7/ v1.25-5 release-gate 統合 + docs — <code>perf.p95Ms</code> axis を 11-axis release gate に統合。 2 tutorial (45 perf-harness baseline + 46 perf baseline migration) + concept doc <code>perf-testing-ssot.md</code> 4 rule SSOT + migration guide v1.24 → v1.25 (additive-only) + snippet validation <code>docs-tutorial-v1.25.test.ts</code> (15 test)。</p><p>8/ v1.25-6 publish — <code>@kiwa/perf-harness</code> v0.1.1 → v0.2.0 minor bump を npm publish。 additive-only ... v0.1 signature 完全維持。 VitePress sidebar に <code>Perf-harness sweep (v1.25)</code> tutorial section + <code>perf-testing-ssot.md</code> (Concepts) + <code>v1.24 → v1.25</code> (Migrations) 追加。 gh-pages publish。 release script filter に <code>@kiwa/perf-harness</code> 追加 (v1.14 payment 漏れ再発防止)。 Roadmap ... <a href="https://github.com/cardene777/kiwa#roadmap" target="_blank" rel="noreferrer">https://github.com/cardene777/kiwa#roadmap</a> — v1.11 → v1.12 → v1.13 → v1.14 → v1.15 → v1.16 → v1.17 → v1.18 → v1.19 → v1.20 → v1.21 → v1.22 → v1.23 → v1.24 → v1.25 の 15 milestone 連続完遂。</p>',8)])])}const u=o(d,[["render",r]]);export{v as __pageData,u as default};
