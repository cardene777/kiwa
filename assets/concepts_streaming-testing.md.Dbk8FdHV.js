import{_ as t,o,c as a,a2 as s}from"./chunks/framework.CyZTVJbV.js";const p=JSON.parse('{"title":"Streaming testing — producer / consumer / exactly-once / DLQ / schema-registry (SSOT)","description":"","frontmatter":{},"headers":[],"relativePath":"concepts/streaming-testing.md","filePath":"concepts/streaming-testing.md"}'),r={name:"concepts/streaming-testing.md"};function i(n,e,c,d,l,h){return o(),a("div",null,[...e[0]||(e[0]=[s('<h1 id="streaming-testing-—-producer-consumer-exactly-once-dlq-schema-registry-ssot" tabindex="-1">Streaming testing — producer / consumer / exactly-once / DLQ / schema-registry (SSOT) <a class="header-anchor" href="#streaming-testing-—-producer-consumer-exactly-once-dlq-schema-registry-ssot" aria-label="Permalink to &quot;Streaming testing — producer / consumer / exactly-once / DLQ / schema-registry (SSOT)&quot;">​</a></h1><p>kiwa&#39;s v1.13 realtime work (Supabase Realtime + Ably + Socket.io) covered the <strong>time-axis</strong> mock case — order, timing, drop, reconnect, backpressure — for synchronous over-the-wire signals. v1.20 adds <strong>five axes on top of that base</strong> — the ones production teams hit once their WebSocket + SSE suite is green but the event-driven backbone (Kafka / Redpanda / NATS) introduces semantics the realtime mocks do not capture. This concept doc is the SSOT for those five axes; the tutorials and dogfood apps are the concrete implementations.</p><h2 id="axis-1-—-producer-semantics-partition-serialization-batch" tabindex="-1">Axis 1 — Producer semantics (partition + serialization + batch) <a class="header-anchor" href="#axis-1-—-producer-semantics-partition-serialization-batch" aria-label="Permalink to &quot;Axis 1 — Producer semantics (partition + serialization + batch)&quot;">​</a></h2><p>Kafka, Redpanda, and NATS JetStream all route messages through a partition (Kafka / Redpanda) or a subject (NATS). A producer picks the partition either by hashing a key or by passing an explicit partition number; a subject picks the consumer by matching the routing pattern.</p><p>That divergence matters for tests because producer bugs look like:</p><ul><li>&quot;The keyed producer wrote to a different partition than the consumer expected&quot; — the hash function changed, or the partition count was off</li><li>&quot;The producer sent an unserialized value&quot; — the <code>serializer</code> config was missing, or the schema evolved without a version bump</li><li>&quot;The batch send did not flatten correctly&quot; — <code>sendBatch</code> merged records across topics into one write instead of one per topic</li></ul><p>kiwa surfaces the pattern in three places.</p><ul><li><code>createKafkaMock({ defaultPartitionCount })</code> — creates a broker that hashes keys to partitions using a deterministic djb2-style hash. Two writes with the same key always land on the same partition. <code>result[0].partition</code> surfaces the choice.</li><li><code>producer.send({ topic, messages: [{ key, value, partition? }] })</code> — writes to the hashed partition unless <code>partition</code> is set explicitly. Out-of-range partition throws. <code>sendBatch(records[])</code> flattens results across multiple topic writes.</li><li><code>nats.publish(subject, value)</code> (core) or <code>js.publish(subject, value)</code> (JetStream) — routes on subject match. Wildcards <code>*</code> (single token) + <code>&gt;</code> (trailing multi-token) determine which subscriber receives the message. <code>compileSubject(pattern)</code> throws if <code>&gt;</code> is not the last token.</li></ul><p>The <strong>contract</strong> each helper enforces is symmetric — every producer write records <code>{ topic, partition, offset }</code> (Kafka) or <code>{ subject, sequence }</code> (NATS JetStream), and every read surfaces the same shape. That means a test can assert on the exact routing choice without a real broker.</p><h3 id="why-partition-ordering-matters-more-than-global-ordering" tabindex="-1">Why partition ordering matters more than global ordering <a class="header-anchor" href="#why-partition-ordering-matters-more-than-global-ordering" aria-label="Permalink to &quot;Why partition ordering matters more than global ordering&quot;">​</a></h3><p>The classic microservice pattern &quot;publish an event and hope the consumer sees them in order&quot; costs 2 layers of Redis or Postgres to enforce. Kafka&#39;s per-partition ordering is stronger — messages within a partition are strictly ordered, but no cross-partition ordering guarantee applies. Get the partitioner wrong (e.g., hash on the wrong key), and events for the same user land on different partitions, breaking the ordering contract silently.</p><p><code>producer.send</code> returns the <code>partition</code> field, so a test asserting on user-1 events lands on partition N + user-2 events lands on partition M validates the partitioner. <code>kafka.getTopicMessages(topic)</code> returns the flat log (partition 0 first, then partition 1, etc.), so a per-partition ordering assertion is <code>filter().map(m =&gt; m.value)</code> on the returned array.</p><h2 id="axis-2-—-consumer-semantics-group-offset-rebalance" tabindex="-1">Axis 2 — Consumer semantics (group + offset + rebalance) <a class="header-anchor" href="#axis-2-—-consumer-semantics-group-offset-rebalance" aria-label="Permalink to &quot;Axis 2 — Consumer semantics (group + offset + rebalance)&quot;">​</a></h2><p>A consumer group is Kafka&#39;s coordination primitive — N consumers join a group, the coordinator assigns partitions to members, and offsets track &quot;where each consumer is&quot;. Two group semantics show up in every non-trivial test — partition assignment (range vs round-robin) and offset commit (auto vs manual).</p><p>That divergence matters for tests because consumer bugs look like:</p><ul><li>&quot;The second consumer joined the group but did not receive any messages&quot; — the assigner did not rebalance, or all partitions stayed on the first consumer</li><li>&quot;The consumer restarted and re-read messages&quot; — the committed offset was 0 because <code>autoCommit=false</code> was set without an explicit commit</li><li>&quot;The consumer skipped messages after a restart&quot; — the seek origin diverged from the committed offset</li></ul><p>kiwa surfaces the pattern in three places.</p><ul><li><code>kafka.consumer({ groupId, partitionAssigner: &#39;range&#39; | &#39;round-robin&#39; })</code> — creates a group member. <code>consumer.assignments()</code> returns the current partition ownership as <code>Map&lt;topic, partitions[]&gt;</code>. Range gives all partitions to a single owner; round-robin distributes 4 partitions as 2 + 2 across two consumers.</li><li><code>consumer.run({ eachMessage, autoCommit })</code> — walks each assigned partition, calls <code>eachMessage(record)</code> for each message, and commits offsets when <code>autoCommit=true</code> (the default). <code>consumer.commitOffsets(offsets[])</code> records explicit offsets.</li><li><code>consumer.seek({ topic, partition, offset })</code> — overrides the read start position. <code>kafka.getCommittedOffset(groupId, topic, partition)</code> surfaces the current committed value; <code>undefined</code> means &quot;never committed&quot;.</li></ul><p>The <strong>contract</strong> each helper enforces is symmetric — every commit records <code>{ groupId, topic, partition, offset }</code>, every read starts at the greater of <code>seek</code> or <code>committedOffset</code>, and every rebalance recomputes assignments deterministically.</p><h3 id="why-offset-semantics-need-dedicated-assertions" tabindex="-1">Why offset semantics need dedicated assertions <a class="header-anchor" href="#why-offset-semantics-need-dedicated-assertions" aria-label="Permalink to &quot;Why offset semantics need dedicated assertions&quot;">​</a></h3><p>The classic pattern &quot;consume all messages and hope they don&#39;t repeat&quot; costs a full at-least-once retry loop in the handler. Kafka&#39;s committed offset is the durable &quot;we processed everything up to here&quot; marker — the assertion <code>expect(kafka.getCommittedOffset(group, topic, partition)).toBe(N)</code> catches &quot;the commit did not fire&quot; bugs before they hit production.</p><p><code>autoCommit=false</code> + explicit <code>commitOffsets</code> is the pattern for at-least-once with idempotent handlers. The test asserts on the exact committed value after a batch of <code>run()</code> calls to verify the commit boundary. Miss the assertion and a real deploy silently re-reads messages after every restart.</p><h2 id="axis-3-—-exactly-once-semantics-idempotent-transactional-read-committed" tabindex="-1">Axis 3 — Exactly-once semantics (idempotent + transactional + read-committed) <a class="header-anchor" href="#axis-3-—-exactly-once-semantics-idempotent-transactional-read-committed" aria-label="Permalink to &quot;Axis 3 — Exactly-once semantics (idempotent + transactional + read-committed)&quot;">​</a></h2><p>Kafka&#39;s &quot;exactly-once semantics&quot; (EOS) is a three-part contract — idempotent producer (dedup on sequence), transactional producer (atomic commit across multiple partitions + topics), and read-committed consumer (skip uncommitted records). NATS JetStream provides a similar guarantee through the durable consumer + ack model.</p><p>That divergence matters for tests because EOS bugs look like:</p><ul><li>&quot;A retried publish landed twice on the broker&quot; — the producer was not idempotent, or the sequence number was reset</li><li>&quot;A transaction committed some messages but not all&quot; — the transactional producer aborted mid-flight, or the isolation level was <code>read-uncommitted</code></li><li>&quot;The consumer saw a message from an aborted transaction&quot; — the read-committed filter was not applied, or the transaction marker was missed</li></ul><p>kiwa surfaces the pattern in three places.</p><ul><li><code>createIdempotentProducer({ kafka })</code> — wraps the base producer with a sequence-number dedup. <code>producer.send(record, sequence)</code> returns <code>[]</code> (dedup) when the sequence was already seen. <code>producer.isDuplicate(sequence)</code> surfaces the check.</li><li><code>createTransactionalProducer({ kafka, transactionalId })</code> — gates the broker view. Pre-commit, <code>kafka.getTopicMessages(topic)</code> returns <code>[]</code>. Post-commit, all records flip visible atomically. <code>producer.currentState()</code> returns <code>&#39;active&#39; | &#39;committed&#39; | &#39;aborted&#39;</code>.</li><li><code>createReadCommittedFilter()</code> — filters a message stream to skip records inside an active or aborted transaction. <code>filter.filter(messages)</code> returns the visible subset.</li></ul><p>The <strong>contract</strong> each helper enforces is symmetric — every idempotent send dedups on <code>(producerId, sequence)</code>, every transactional commit flushes atomically, and every read-committed filter drops uncommitted or aborted records.</p><h3 id="why-exactly-once-needs-dedicated-assertions" tabindex="-1">Why exactly-once needs dedicated assertions <a class="header-anchor" href="#why-exactly-once-needs-dedicated-assertions" aria-label="Permalink to &quot;Why exactly-once needs dedicated assertions&quot;">​</a></h3><p>The classic pattern &quot;retry on failure and hope the handler is idempotent&quot; costs a full deduplication layer in the consumer. Kafka&#39;s idempotent producer moves the dedup upstream — the broker rejects duplicate sequence numbers before they enter the log.</p><p><code>isDuplicate(sequence)</code> + <code>expect(kafka.getTopicMessages(topic)).toHaveLength(1)</code> after a duplicate send catches the dedup path. <code>currentState()</code> + <code>expect(kafka.getTopicMessages(topic)).toHaveLength(0)</code> before commit catches the pre-commit gate. Both patterns are pure — no timing dependence, no waiting for a real broker.</p><h2 id="axis-4-—-dlq-semantics-retry-quarantine-poison-isolation" tabindex="-1">Axis 4 — DLQ semantics (retry + quarantine + poison isolation) <a class="header-anchor" href="#axis-4-—-dlq-semantics-retry-quarantine-poison-isolation" aria-label="Permalink to &quot;Axis 4 — DLQ semantics (retry + quarantine + poison isolation)&quot;">​</a></h2><p>A dead-letter queue (DLQ) is the &quot;we can&#39;t process this, park it for a human&quot; primitive. Every message-driven system needs one — otherwise a single poison message blocks the entire consumer group by looping forever.</p><p>That divergence matters for tests because DLQ bugs look like:</p><ul><li>&quot;The consumer got stuck retrying the same message forever&quot; — no maxAttempts cap, or the DLQ hand-off was missing</li><li>&quot;The DLQ received messages that should have been retried&quot; — maxAttempts=1, or the retry policy was misconfigured</li><li>&quot;The DLQ callback did not fire&quot; — <code>onDeadLetter</code> was configured incorrectly, or the quarantine array was reset</li></ul><p>kiwa surfaces the pattern in one place.</p><ul><li><code>createDeadLetterQueue({ topic, handler, retryPolicy, onDeadLetter })</code> — walks the retry loop, and on exhaustion appends the message to the quarantine array. <code>dlq.handle(message)</code> returns <code>&#39;handled&#39;</code> (success) / <code>&#39;quarantined&#39;</code> (retries exhausted). <code>dlq.quarantined()</code> returns the array of <code>{ original, attempts, reason, quarantinedAt }</code> entries.</li></ul><p>The <strong>contract</strong> the DLQ enforces is symmetric — every retry increments <code>attempts</code>, every quarantine appends to the array, every <code>onDeadLetter</code> callback fires with the same shape as the quarantine entry.</p><h3 id="why-dlq-needs-dedicated-assertions" tabindex="-1">Why DLQ needs dedicated assertions <a class="header-anchor" href="#why-dlq-needs-dedicated-assertions" aria-label="Permalink to &quot;Why DLQ needs dedicated assertions&quot;">​</a></h3><p>The classic pattern &quot;log the error and continue&quot; costs the message. A DLQ preserves the record — the assertion <code>expect(dlq.quarantined()).toHaveLength(1)</code> + <code>expect(entries[0]?.reason).toBe(&#39;poison&#39;)</code> catches &quot;the poison message was lost&quot; bugs before they hit production.</p><p><code>retryPolicy: { maxAttempts, backoff: &#39;linear&#39; | &#39;exponential&#39;, baseDelayMs, maxDelayMs }</code> shapes the retry curve. <code>onDeadLetter: (entry) =&gt; alertPagerDuty(entry)</code> chains to alerting. The test asserts on both the retry count (<code>entries[0]?.attempts</code>) and the quarantine array size, so a regression surfaces at the exact retry boundary.</p><h2 id="axis-5-—-schema-registry-semantics-avro-protobuf-json-compat-modes" tabindex="-1">Axis 5 — Schema registry semantics (Avro / Protobuf / JSON compat modes) <a class="header-anchor" href="#axis-5-—-schema-registry-semantics-avro-protobuf-json-compat-modes" aria-label="Permalink to &quot;Axis 5 — Schema registry semantics (Avro / Protobuf / JSON compat modes)&quot;">​</a></h2><p>Schema evolution is the &quot;the producer changed the message shape, will the consumer still work?&quot; primitive. Every long-lived event stream needs schema versioning — otherwise a producer deploy silently breaks every consumer.</p><p>That divergence matters for tests because schema bugs look like:</p><ul><li>&quot;The consumer crashed after a producer deploy&quot; — the schema evolved BACKWARD-incompatibly (added a required field)</li><li>&quot;The producer deploy was blocked but should have passed&quot; — the compat mode was FULL when BACKWARD was intended</li><li>&quot;A duplicate schema registration bumped the version&quot; — the registry did not detect the schema hash equality</li></ul><p>kiwa surfaces the pattern in one place.</p><ul><li><code>createSchemaRegistry({ defaultCompatibility, subjectNamingStrategy })</code> — Confluent-shaped registry. <code>registry.register({ subject, kind, schema })</code> writes a versioned entry. <code>registry.checkCompatibility({ subject, kind, schema })</code> returns <code>{ compatible, mode, reasons[] }</code> without writing. <code>registry.setCompatibility(subject, mode)</code> overrides per-subject.</li></ul><p>The <strong>contract</strong> the registry enforces is symmetric — every registration increments the version (unless the schema hash equals an existing version), every compat check returns a machine-readable verdict + reasons, and every subject carries an independent compat mode.</p><h3 id="why-compat-modes-need-dedicated-assertions" tabindex="-1">Why compat modes need dedicated assertions <a class="header-anchor" href="#why-compat-modes-need-dedicated-assertions" aria-label="Permalink to &quot;Why compat modes need dedicated assertions&quot;">​</a></h3><p>The classic pattern &quot;deploy the producer and hope the consumers survive&quot; costs a rollback on every schema drift. Confluent&#39;s compat modes shift the check to registration time — BACKWARD (new schema reads old data), FORWARD (old schema reads new data), FULL (both).</p><p><code>checkCompatibility</code> is synchronous (no round-trip to the broker) and returns <code>{ compatible, mode, reasons }</code>. The pattern <code>if (!check.compatible) throw new Error(check.reasons.join(&#39;, &#39;))</code> gates every publish against schema drift — the test asserts on the gate without a real registry.</p><p>Three compatibility modes are supported.</p><ul><li><strong>BACKWARD</strong> (default) — new schema can read old data. Safe to add optional fields with defaults, safe to remove required fields with defaults. This is the mode most Kafka-Streams / KSQL / consumer-first teams pick.</li><li><strong>FORWARD</strong> — old schema can read new data. Safe to add required fields, safe to remove optional fields. This is the mode producer-first teams pick.</li><li><strong>FULL</strong> — both BACKWARD and FORWARD. Strictest mode; every evolution passes both checks. Use it for cross-team contracts where producer + consumer upgrade in lockstep.</li></ul><h2 id="assertion-patterns" tabindex="-1">Assertion patterns <a class="header-anchor" href="#assertion-patterns" aria-label="Permalink to &quot;Assertion patterns&quot;">​</a></h2><p>The 5 axes produce five assertion patterns.</p><ul><li><strong>Producer partition assertions</strong> — every keyed send lands on the hashed partition. The assertion is <code>expect(first[0]?.partition).toBe(second[0]?.partition)</code> for two writes with the same key. This catches &quot;the partitioner changed under a library upgrade&quot;.</li><li><strong>Consumer group rebalance assertions</strong> — every consumer joining a group triggers a reassignment. The assertion is <code>expect(a1.length + a2.length).toBe(numPartitions)</code> for two consumers on a 4-partition topic. This catches &quot;the second consumer never received a partition&quot;.</li><li><strong>Exactly-once state assertions</strong> — every transactional producer transitions <code>active → committed | aborted</code>. The assertion is <code>expect(kafka.getTopicMessages(topic)).toHaveLength(0)</code> before commit, <code>expect(producer.currentState()).toBe(&#39;committed&#39;)</code> after commit. This catches &quot;the commit did not flush&quot; or &quot;the abort did not discard&quot;.</li><li><strong>DLQ quarantine assertions</strong> — every exhausted retry appends to the quarantine array. The assertion is <code>expect(dlq.quarantined()).toHaveLength(1)</code> + <code>expect(entries[0]?.attempts).toBe(maxAttempts)</code>. This catches &quot;the poison message was retried forever&quot; or &quot;the DLQ was silently swallowed&quot;.</li><li><strong>Schema compat assertions</strong> — every evolution passes or fails the current mode. The assertion is <code>expect(check.compatible).toBe(true)</code> for a safe evolution, <code>expect(check.compatible).toBe(false)</code> + <code>expect(check.reasons.length).toBeGreaterThan(0)</code> for a rejected one. This catches &quot;the deploy broke the compat guarantee&quot;.</li></ul><p>All five patterns are pure — they add no runtime overhead beyond the mock call. The test grows one function call per assertion and gains a machine-verifiable contract.</p><h2 id="fidelity-vs-cost-trade-off-release-gate-axis" tabindex="-1">Fidelity vs cost trade-off (release gate axis) <a class="header-anchor" href="#fidelity-vs-cost-trade-off-release-gate-axis" aria-label="Permalink to &quot;Fidelity vs cost trade-off (release gate axis)&quot;">​</a></h2><p>The 3 dogfood apps (<code>dogfood-kafka-event-pipeline</code> + <code>dogfood-redpanda-schema-registry</code> + <code>dogfood-nats-jetstream</code>) each produce a <strong>fidelity report</strong> that measures the mock behaviour against the real runtime. The report walks the same 5-op trace shape through both surfaces and computes a fidelity ratio in <code>[0, 1]</code>.</p><p>Three properties are load-bearing.</p><ul><li><strong>Fidelity ≥ 0.7 is the release-gate floor.</strong> Below that the mock is lying to the caller — a test that passes against the mock but fails against a real broker tells the reviewer the mock needs work.</li><li><strong>Fidelity 1.0 is a warning sign, not a goal.</strong> A mock that reproduces Kafka byte-for-byte is either a real broker in disguise (slow) or a mock that tracks every irrelevant coordinator detail (brittle). The target is 0.85–0.95 with intentional divergence documented per axis.</li><li><strong>The fidelity harness runs Layer 3, not Layer 1.</strong> Layer 1 (unit tests) drives the mock. Layer 3 (fidelity harness) drives both mock and real, diffs traces, and emits the fidelity ratio. Layer 2 (integration) rides on the mock — the fidelity harness is what tells the reviewer the mock is worth riding on.</li></ul><p>The <code>evaluateReleaseGate</code> 11-axis contract reads the fidelity ratio through the common 7-axis branch, alongside coverage / test count / perf p95 / mutation kill rate. The AI-LLM 4 axes (cost / latency / token / accuracy) do not apply to streaming surfaces — there is no token pricing to measure.</p><h2 id="test-count-baseline" tabindex="-1">Test count baseline <a class="header-anchor" href="#test-count-baseline" aria-label="Permalink to &quot;Test count baseline&quot;">​</a></h2><p>The v1.20 streaming harness ships the following behaviour test counts per axis.</p><ul><li>Axis 1 (producer) — <code>packages/streaming/tests/kafka.test.ts</code> producer group × 7 + <code>packages/streaming/tests/redpanda.test.ts</code> × 6 + <code>packages/streaming/tests/nats.test.ts</code> core pub × 7 = <strong>20 tests</strong></li><li>Axis 2 (consumer) — <code>packages/streaming/tests/kafka.test.ts</code> consumer group × 7 + admin × 4 + topic accessors × 3 = <strong>14 tests</strong></li><li>Axis 3 (exactly-once) — <code>packages/streaming/tests/exactly-once.test.ts</code> × 11 = <strong>11 tests</strong></li><li>Axis 4 (DLQ) — <code>packages/streaming/tests/dlq.test.ts</code> × 9 = <strong>9 tests</strong></li><li>Axis 5 (schema registry) — <code>packages/streaming/tests/schema-registry.test.ts</code> × 15+ = <strong>15+ tests</strong></li></ul><p>Every count sits above the 10-test release-gate floor (Axis 4 is at 9 by design — DLQ semantics are narrow) so the 11-axis check passes without special-casing the streaming surfaces.</p><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><ul><li><a href="./../tutorials/31-kafka-event-pipeline">Tutorial 31 — Kafka event pipeline (producer + consumer group + exactly-once + DLQ)</a></li><li><a href="./../tutorials/32-redpanda-schema-registry">Tutorial 32 — Redpanda + schema registry (Avro schemas + evolution + compatibility)</a></li><li><a href="./../tutorials/33-nats-jetstream">Tutorial 33 — NATS JetStream (persistent streams + KV + Object store + subject routing)</a></li><li><a href="./../migrations/v1.19-to-v1.20">Migration v1.19 → v1.20</a></li><li>v1.13 baseline — <a href="./realtime-testing">Realtime testing (time-axis mock SSOT)</a></li></ul>',69)])])}const m=t(r,[["render",i]]);export{p as __pageData,m as default};
