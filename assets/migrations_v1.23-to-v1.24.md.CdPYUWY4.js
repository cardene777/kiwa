import{_ as a,o as s,c as t,a2 as i}from"./chunks/framework.CyZTVJbV.js";const g=JSON.parse('{"title":"Migration guide — v1.23 → v1.24","description":"","frontmatter":{},"headers":[],"relativePath":"migrations/v1.23-to-v1.24.md","filePath":"migrations/v1.23-to-v1.24.md"}'),o={name:"migrations/v1.23-to-v1.24.md"};function d(n,e,r,c,l,p){return s(),t("div",null,[...e[0]||(e[0]=[i(`<h1 id="migration-guide-—-v1-23-→-v1-24" tabindex="-1">Migration guide — v1.23 → v1.24 <a class="header-anchor" href="#migration-guide-—-v1-23-→-v1-24" aria-label="Permalink to &quot;Migration guide — v1.23 → v1.24&quot;">​</a></h1><p>v1.24 is <strong>additive</strong> — every <code>@kiwa-test/*</code> package public API that existed in v1.23 keeps its surface. Existing tests keep passing without touching a single line.</p><p>The 5 things v1.24 adds are.</p><ul><li><code>@kiwa-test/edge</code> v0.2 layers <strong>8 axes of advanced edge semantics</strong> on top of the v0.1 <code>fetch</code> handler + KV / R2 / D1 / DurableObject minimal mocks (Durable Object / WebSocket edge / edge KV / geo-replicated / Cron trigger / subrequest limit / CPU time limit / streaming Response). Platform-neutral state machines + strict transition guards land in <code>packages/edge/src/semantics/*</code>.</li><li>3 dogfood edge apps — <code>dogfood-cloudflare-workers-durable-object-app</code> (realtime chat + Hibernation API + storage transactional + WebSocket edge broadcast), <code>dogfood-vercel-edge-function-app</code> (Next.js 15 middleware + edge runtime + Vercel KV + SSE streaming), <code>dogfood-deno-deploy-geo-app</code> (Fresh + Deno KV geo-replicated + Deno Deploy Cron + queue trigger). All three share the same <code>KIWA_MODE=mock|real-optional|real</code> switch v1.22 established.</li><li>3 new tutorials (42 Cloudflare Durable Object + 43 Vercel Edge streaming + 44 Deno Deploy geo), 1 concept doc SSOT (<code>edge-runtime-testing.md</code> — 8-axis reference), 1 migration guide (this file). All wired through the sidebar.</li><li>1 tutorial snippet validation test (<code>packages/edge/tests/docs-tutorial-v1.24.test.ts</code>) that re-runs every code snippet in tutorials 42-44 against the real <code>@kiwa-test/edge</code> API — same pattern as <code>docs-tutorial-v1.21.test.ts</code> / <code>docs-tutorial-v1.22.test.ts</code> / <code>docs-tutorial-v1.23.test.ts</code>.</li><li>Sidebar navigation (<code>docs/.vitepress/config.mts</code>) — new &quot;Edge/Serverless 深化 (v1.24)&quot; section under Tutorials, <code>edge-runtime-testing.md</code> under Concepts, <code>v1.23 → v1.24</code> under Migrations.</li></ul><h2 id="what-breaks-nothing" tabindex="-1">What breaks (nothing) <a class="header-anchor" href="#what-breaks-nothing" aria-label="Permalink to &quot;What breaks (nothing)&quot;">​</a></h2><p>Nothing. The v1.24 changes are pure additions.</p><ul><li><code>@kiwa-test/edge</code> v0.1 surface (<code>invokeEdgeHandler</code> / <code>createKvNamespace</code> / <code>EdgeFetchHandler</code> / <code>EdgeEnvBindings</code> / <code>InvokeEdgeHandlerOptions</code> / <code>InvokeEdgeHandlerResult</code> / <code>SimulatedExecutionContext</code>) — unchanged. v0.2 adds the <code>semantics/*</code> module tree alongside the existing surface. Every prior signature keeps its shape.</li><li>Every prior <code>@kiwa-test/*</code> package (auth / streaming / solidjs / fresh / hono / observability v2 / component / realtime / ai-llm / mcp / agent / perf-harness / quality-metrics / search / dapp / payment) — untouched.</li><li><code>evaluateReleaseGate</code> — same 11-axis contract as v1.17-v1.23. v1.24 introduces no new release-gate axes.</li><li>Every prior tutorial 01-41, migration guide v1.9→v1.23, concept doc — unchanged. v1.24 only appends tutorial 42-44 + concept <code>edge-runtime-testing.md</code> + migration <code>v1.23-to-v1.24.md</code>.</li><li>Every prior dogfood app (v1.23 payment apps, v1.22 auth apps, v1.20 streaming apps, v1.19 framework apps, etc.) — untouched. v1.24 adds 3 new dogfood edge apps alongside; nothing is removed or renamed.</li></ul><h2 id="what-s-new-opt-in" tabindex="-1">What&#39;s new (opt in) <a class="header-anchor" href="#what-s-new-opt-in" aria-label="Permalink to &quot;What&#39;s new (opt in)&quot;">​</a></h2><h3 id="kiwa-test-edge-v0-2-—-8-axis-advanced-edge-semantics" tabindex="-1"><code>@kiwa-test/edge</code> v0.2 — 8-axis advanced edge semantics <a class="header-anchor" href="#kiwa-test-edge-v0-2-—-8-axis-advanced-edge-semantics" aria-label="Permalink to &quot;\`@kiwa-test/edge\` v0.2 — 8-axis advanced edge semantics&quot;">​</a></h3><p>The v0.1 <code>fetch</code> handler + KV / R2 / D1 mocks stay the first-line contract. v0.2 layers 8 axes of edge state on top so tests exercise the full envelope real platforms converge on. Every axis operates on the same neutral <code>EdgePlatform</code> union (<code>&#39;cloudflare&#39; | &#39;vercel&#39; | &#39;deno&#39;</code>) — swap the platform and the tests re-run against a different runtime without touching the axis bodies.</p><p>The 8 axes are.</p><ul><li><strong>Durable Object</strong> — Cloudflare Durable Objects (and their session-affine analogues). State machine <code>initialized</code> → <code>active</code> (loops on request / alarm / storage-write) with a transactional storage guarantee. Default fires 4 neutral events (<code>durable-object.created</code> / <code>durable-object.requested</code> / <code>durable-object.alarm-fired</code> / <code>durable-object.storage-written</code>).</li><li><strong>WebSocket edge</strong> — edge WebSocket lifecycle. <code>pending</code> → <code>open</code> → <code>closed</code>, 4 neutral events (<code>websocket.upgrade-requested</code> / <code>websocket.accepted</code> / <code>websocket.message</code> / <code>websocket.closed</code>).</li><li><strong>Edge KV</strong> — read-through per-POP cache. 4 neutral events (<code>kv.read</code> / <code>kv.write</code> / <code>kv.cache-hit</code> / <code>kv.cache-miss</code>). Consistency states = <code>consistent</code> (Deno KV primary) / <code>eventually-consistent</code> (Cloudflare KV, Vercel Edge Config, Deno KV replicas).</li><li><strong>Geo-replicated</strong> — primary + N replicas that catch up asynchronously. <code>in-sync</code> → <code>lagging</code> → <code>in-sync</code> (only when every replica lag returns to 0) / <code>conflict-detected</code> → <code>in-sync</code>. 4 neutral events (<code>geo.primary-write</code> / <code>geo.replica-lagged</code> / <code>geo.replica-synced</code> / <code>geo.conflict-resolved</code>).</li><li><strong>Cron trigger</strong> — scheduled invocation lifecycle. <code>scheduled</code> → <code>running</code> → <code>completed</code> / <code>failed</code> (with retry back to <code>scheduled</code> until <code>maxRetries</code> exhausted). <code>triggerType</code> = <code>scheduled</code> / <code>queue</code> / <code>email</code>. Default <code>maxRetries: 3</code>. 4 neutral events (<code>cron.scheduled</code> / <code>cron.started</code> / <code>cron.completed</code> / <code>cron.failed</code>).</li><li><strong>Subrequest limit</strong> — per-invocation outbound fetch budget. <code>ok</code> → <code>approaching-limit</code> → <code>limited</code>. Default <code>limit: 50</code> (Workers free plan) + <code>warningThreshold: 40</code>. 4 neutral events (<code>subrequest.started</code> / <code>subrequest.counted</code> / <code>subrequest.limited</code> / <code>subrequest.completed</code>).</li><li><strong>CPU time limit</strong> — per-invocation compute budget. <code>idle</code> → <code>running</code> → <code>warning</code> → <code>throttled</code> / <code>completed</code>. Default <code>budgetMs: 50</code> + <code>warningAtMs: 40</code>. 4 neutral events (<code>cpu.started</code> / <code>cpu.budget-warning</code> / <code>cpu.limited</code> / <code>cpu.completed</code>).</li><li><strong>Streaming Response</strong> — chunked / SSE / websocket body delivery with backpressure. <code>open</code> → <code>backpressure</code> (when <code>bytesSent &gt; highWaterMark</code>) → <code>open</code> (on <code>resumeStream</code>) → <code>closed</code>. Default <code>kind: chunked</code>, <code>highWaterMark: 65536</code>. 4 neutral events (<code>stream.opened</code> / <code>stream.chunk-sent</code> / <code>stream.backpressure</code> / <code>stream.closed</code>).</li></ul><p>Concrete usage.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// v0.2 durable-object axis — Cloudflare Durable Objects envelope</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  createDurableObject,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  requestDurableObject,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  fireAlarm,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  writeStorage,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@kiwa-test/edge&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> room</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createDurableObject</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ id: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;room-1&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, platform: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;cloudflare&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">requestDurableObject</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(room, { url: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;https://edge/room-1/join&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">writeStorage</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(room, { key: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;last-message&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, value: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;hello&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">fireAlarm</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(room);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// room.history has 4 AxisStep entries with neutralEvent + platformEvent + state.</span></span></code></pre></div><p>The full 8-axis catalogue lives in <a href="./../concepts/edge-runtime-testing"><code>docs/concepts/edge-runtime-testing.md</code></a>.</p><h3 id="_3-dogfood-edge-apps" tabindex="-1">3 dogfood edge apps <a class="header-anchor" href="#_3-dogfood-edge-apps" aria-label="Permalink to &quot;3 dogfood edge apps&quot;">​</a></h3><ul><li><strong><code>examples/dogfood-cloudflare-workers-durable-object-app</code></strong> (Cloudflare Workers) — realtime chat + Durable Object + Hibernation API + storage transactional + WebSocket edge broadcast. 8-op surface (4 <code>durable-object</code> + 4 <code>websocket-edge</code>), fidelity harness diffs mock-vs-real traces, <code>KIWA_MODE=real</code> swaps to real Miniflare + <code>wrangler dev</code> with <code>WRANGLER_KEY=1</code>.</li><li><strong><code>examples/dogfood-vercel-edge-function-app</code></strong> (Next.js 15 App Router) — middleware + edge runtime + Vercel KV + SSE streaming with backpressure. 8-op surface (3 <code>geo-replicated</code> + 3 <code>edge-kv</code> + 2 <code>streaming-response</code>), fidelity harness diffs mock-vs-real traces, <code>KIWA_MODE=real</code> swaps to real Vercel Edge sandbox with <code>VERCEL_KEY=1</code>.</li><li><strong><code>examples/dogfood-deno-deploy-geo-app</code></strong> (Deno Fresh) — multi-region write + read-your-writes + Deno Deploy Cron + queue trigger. 8-op surface (3 <code>geo-replicated</code> + 2 <code>edge-kv</code> + 3 <code>cron-trigger</code>), fidelity harness diffs mock-vs-real traces, <code>KIWA_MODE=real</code> swaps to real Deno Deploy sandbox with <code>DENO_DEPLOY_KEY=1</code>.</li></ul><p>All three follow the v1.22-established <code>KIWA_MODE=mock</code> (default) / <code>KIWA_MODE=real-optional</code> (try real, fall back) / <code>KIWA_MODE=real</code> (fail hard) contract.</p><h3 id="_3-tutorials-1-concept-doc-1-migration-guide" tabindex="-1">3 tutorials + 1 concept doc + 1 migration guide <a class="header-anchor" href="#_3-tutorials-1-concept-doc-1-migration-guide" aria-label="Permalink to &quot;3 tutorials + 1 concept doc + 1 migration guide&quot;">​</a></h3><ul><li><strong>Tutorial 42</strong> — Cloudflare Workers Durable Object (realtime chat + Hibernation + storage transactional + alarm-driven purge)</li><li><strong>Tutorial 43</strong> — Vercel Edge streaming (Next.js 15 middleware + geo routing + edge KV cache invalidation + SSE backpressure)</li><li><strong>Tutorial 44</strong> — Deno Deploy geo (multi-region KV write + read-your-writes + Cron trigger + queue trigger)</li><li><strong>Concept doc</strong> — <a href="./../concepts/edge-runtime-testing"><code>edge-runtime-testing.md</code></a> (8-axis SSOT reference + platform-specific fidelity table)</li><li><strong>Migration guide</strong> — this file</li></ul><h3 id="tutorial-snippet-validation" tabindex="-1">Tutorial snippet validation <a class="header-anchor" href="#tutorial-snippet-validation" aria-label="Permalink to &quot;Tutorial snippet validation&quot;">​</a></h3><p><code>packages/edge/tests/docs-tutorial-v1.24.test.ts</code> re-runs every code snippet in tutorials 42-44 against the real <code>@kiwa-test/edge</code> API. If a tutorial snippet drifts from the shipped API (rename, signature change, deleted export), the vitest run fails — same guard v1.17 / v1.19 / v1.20 / v1.21 / v1.22 / v1.23 established for their tutorials.</p><h2 id="upgrade-path" tabindex="-1">Upgrade path <a class="header-anchor" href="#upgrade-path" aria-label="Permalink to &quot;Upgrade path&quot;">​</a></h2><p>Bump the dependency and start using the axes.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -D</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @kiwa-test/edge@^0.2</span></span></code></pre></div><p>Existing v0.1 tests keep passing. New tests can import the 8-axis semantics from the same package.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// v0.1 — still works</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { invokeEdgeHandler, createKvNamespace } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@kiwa-test/edge&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// v0.2 — new axes</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  createDurableObject, requestDurableObject, fireAlarm, writeStorage,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  requestWebSocketUpgrade, acceptWebSocket, sendMessage, closeWebSocket,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  createEdgeKvSession, kvRead, kvWrite, kvRangeQuery,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  createGeoReplicatedSession, geoPrimaryWrite, markReplicaLagged, syncReplica, resolveConflict,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  scheduleCron, startCron, completeCron, failCron,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  startSubrequestBudget, startSubrequest, countSubrequest, completeSubrequest, remainingBudget,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  startCpuBudget, startCpu, tickCpu, completeCpu,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  openStream, sendChunk, resumeStream, closeStream,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@kiwa-test/edge&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><h2 id="release-gate" tabindex="-1">Release gate <a class="header-anchor" href="#release-gate" aria-label="Permalink to &quot;Release gate&quot;">​</a></h2><p><code>evaluateReleaseGate</code> axis surface is unchanged at v1.24 — same 11 axes as v1.17-v1.23. The 3 new dogfood edge apps emit <code>fidelity-report</code> payloads that feed the existing gate; no new axis is added.</p><h2 id="related" tabindex="-1">Related <a class="header-anchor" href="#related" aria-label="Permalink to &quot;Related&quot;">​</a></h2><ul><li><a href="./../concepts/edge-runtime-testing">Concept doc — Edge runtime testing (8-axis SSOT)</a></li><li><a href="./../tutorials/42-cloudflare-durable-object">Tutorial 42 — Cloudflare Workers Durable Object</a></li><li><a href="./../tutorials/43-vercel-edge-streaming">Tutorial 43 — Vercel Edge streaming</a></li><li><a href="./../tutorials/44-deno-deploy-geo">Tutorial 44 — Deno Deploy geo</a></li><li><a href="./v1.22-to-v1.23">v1.22 → v1.23 migration guide</a></li></ul>`,31)])])}const u=a(o,[["render",d]]);export{g as __pageData,u as default};
