import{_ as t,o as a,c as s,a2 as o}from"./chunks/framework.CyZTVJbV.js";const k=JSON.parse('{"title":"Fidelity — dogfood-mcp-tool-agent (v1.15-5)","description":"","frontmatter":{},"headers":[],"relativePath":"quality-reports/ai-llm/mcp-tool-agent.md","filePath":"quality-reports/ai-llm/mcp-tool-agent.md"}'),i={name:"quality-reports/ai-llm/mcp-tool-agent.md"};function n(l,e,d,r,c,p){return a(),s("div",null,[...e[0]||(e[0]=[o(`<h1 id="fidelity-—-dogfood-mcp-tool-agent-v1-15-5" tabindex="-1">Fidelity — dogfood-mcp-tool-agent (v1.15-5) <a class="header-anchor" href="#fidelity-—-dogfood-mcp-tool-agent-v1-15-5" aria-label="Permalink to &quot;Fidelity — dogfood-mcp-tool-agent (v1.15-5)&quot;">​</a></h1><p>Real-vs-mock behavioural fidelity for the Anthropic Claude MCP tool-use agent dogfood, produced by <code>examples/dogfood-mcp-tool-agent/tests/emit-fidelity-report.test.ts</code>. Feeds <code>@kiwa-lab/quality-metrics</code> 11-axis release gate.</p><h2 id="baseline-real-mode-skipped-—-no-anthropic-api-key-and-no-live-modelcontextprotocol-sdk-install" tabindex="-1">Baseline (real mode skipped — no <code>ANTHROPIC_API_KEY</code> and no live <code>@modelcontextprotocol/sdk</code> install) <a class="header-anchor" href="#baseline-real-mode-skipped-—-no-anthropic-api-key-and-no-live-modelcontextprotocol-sdk-install" aria-label="Permalink to &quot;Baseline (real mode skipped — no \`ANTHROPIC_API_KEY\` and no live \`@modelcontextprotocol/sdk\` install)&quot;">​</a></h2><p>When the harness runs without an Anthropic API key or a real MCP server binary, the real adapter emits <code>MCP_REAL_ENV_MISSING</code> for every op. Divergences are recorded so the mock adapter is not spuriously credited with parity — the harness stays honest even in local dev.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>provider   : @kiwa-lab/ai-llm/mcp-tool-agent</span></span>
<span class="line"><span>version    : 0.1.0</span></span>
<span class="line"><span>verdict    : FAIL (accuracy.score below the 0.80 gate — expected without a live pair)</span></span>
<span class="line"><span>divergences: 4 (handshake / listTools / callTool / runMcpToolLoop — real mode absent)</span></span>
<span class="line"><span>axes       : 11 (AI-LLM branch)</span></span></code></pre></div><table tabindex="0"><thead><tr><th>axis</th><th>actual</th><th>threshold</th><th>verdict</th></tr></thead><tbody><tr><td>coverage.line</td><td>92.00%</td><td>85%</td><td>pass</td></tr><tr><td>coverage.branch</td><td>88.00%</td><td>80%</td><td>pass</td></tr><tr><td>coverage.function</td><td>95.00%</td><td>90%</td><td>pass</td></tr><tr><td>fidelity.ratio</td><td>100.00% (4/4)</td><td>70%</td><td>pass</td></tr><tr><td>perf.p95Ms</td><td>~7 ms</td><td>100 ms</td><td>pass</td></tr><tr><td>mutation.killRate</td><td>70.00% (28/40)</td><td>60%</td><td>pass</td></tr><tr><td>testCount.behavior</td><td>18</td><td>10</td><td>pass</td></tr><tr><td>cost.perRequestUsd</td><td>~$0.0003</td><td>$0.10</td><td>pass</td></tr><tr><td>latency.p95Ms</td><td>~7 ms</td><td>3000 ms</td><td>pass</td></tr><tr><td>token.totalTokens</td><td>~200</td><td>4000</td><td>pass</td></tr><tr><td>accuracy.score</td><td>~0.45</td><td>0.80</td><td><strong>fail</strong></td></tr></tbody></table><p>Accuracy is the mean Jaccard similarity between the real and mock final texts. In real-mode-skipped mode the &quot;real&quot; strings are seeded from expected Claude outputs so the harness has something to diff — the fail is by design and signals that fidelity is unverified without a live key. Wiring <code>ANTHROPIC_API_KEY</code> + installing <code>@modelcontextprotocol/sdk</code> promotes the report to the real baseline.</p><h2 id="reproduction" tabindex="-1">Reproduction <a class="header-anchor" href="#reproduction" aria-label="Permalink to &quot;Reproduction&quot;">​</a></h2><p>Mock only.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --filter</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dogfood-mcp-tool-agent</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples/dogfood-mcp-tool-agent/quality-report/fidelity-latest.md</span></span></code></pre></div><p>Live real mode.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ANTHROPIC_API_KEY</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sk-ant-...</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ANTHROPIC_MODEL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">claude-3-5-sonnet-latest</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># optional — override the MCP server the real adapter spawns</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> MCP_SERVER_COMMAND</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">npx</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> MCP_SERVER_ARGS</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;-y @modelcontextprotocol/server-everything&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --filter</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dogfood-mcp-tool-agent</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span></span></code></pre></div><h2 id="ops-under-measurement" tabindex="-1">Ops under measurement <a class="header-anchor" href="#ops-under-measurement" aria-label="Permalink to &quot;Ops under measurement&quot;">​</a></h2><p>Four provider-neutral ops on <code>McpAgentAdapter</code>.</p><ul><li><code>handshake</code> — MCP <code>initialize</code> roundtrip (client capabilities + protocol version + serverInfo negotiation)</li><li><code>listTools</code> — MCP <code>tools/list</code> returns the 3 declared tools (weather / calculator / search) with valid JSONSchema</li><li><code>callTool</code> — MCP <code>tools/call</code> roundtrip (schema validate -&gt; handler -&gt; <code>ToolCallResult</code>, including <code>isError=true</code> + JSON-RPC error paths -32000 / -32001 / -32002 / -32003)</li><li><code>runMcpToolLoop</code> — Anthropic Claude drives a 3-tool orchestration (weather -&gt; calculator -&gt; search -&gt; finalisation) with every tool call forwarded through the MCP client</li></ul><h2 id="the-3-step-mcp-protocol-chain-design-ssot" tabindex="-1">The 3-step MCP protocol chain (design SSOT) <a class="header-anchor" href="#the-3-step-mcp-protocol-chain-design-ssot" aria-label="Permalink to &quot;The 3-step MCP protocol chain (design SSOT)&quot;">​</a></h2><ol><li><code>initialize</code> — client + server exchange protocol version + capabilities. Mock latches <code>initialized</code> on the server; real MCP client auto-handshakes on <code>Client.connect(transport)</code>.</li><li><code>tools/list</code> — client fetches the 3 tools the server advertises and hands them to Claude as <code>input_schema</code> blocks alongside the user prompt.</li><li><code>tools/call</code> — Claude emits <code>tool_use</code> blocks; each is forwarded via <code>McpClient.callTool</code> and the JSON-RPC result (text content block + <code>isError</code> flag) is fed back to Claude as a <code>tool_result</code> block.</li></ol><p>The mock enforces the same op order as the real MCP protocol spec — a <code>tools/list</code> before <code>initialize</code> returns <code>NotInitialized</code> (-32002). The fidelity harness cannot spuriously pass on a mock that skipped the handshake.</p><h2 id="per-turn-mock-response-bank-implementation-ssot" tabindex="-1">Per-turn mock response bank (implementation SSOT) <a class="header-anchor" href="#per-turn-mock-response-bank-implementation-ssot" aria-label="Permalink to &quot;Per-turn mock response bank (implementation SSOT)&quot;">​</a></h2><p>Anthropic Claude&#39;s tool-use loop delivers follow-up turns as user messages whose content is a list of <code>tool_result</code> blocks. <code>MockEngine.extractUserPrompt</code> looks up the last <code>role: &#39;user&#39;</code> message&#39;s text content, which for turn 2+ extracts to an empty string (tool_result blocks are not text blocks). To avoid infinite loops on the same bank entry, the mock adapter constructs a <strong>fresh <code>createAnthropicMock</code> instance per iteration</strong>, each with a response bank keyed on the original prompt AND the empty string, cycling through weather -&gt; calculator -&gt; search -&gt; finalisation entries. Metric accumulation is hand-rolled across iterations so the mock&#39;s cost / token / latency totals stay comparable with the real adapter&#39;s.</p><h2 id="notes" tabindex="-1">Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;Notes&quot;">​</a></h2><p>Provider prefix <code>@kiwa-lab/ai-llm/</code> triggers the 11-axis branch of <code>evaluateReleaseGate</code> (<code>packages/quality-metrics/src/gate.ts</code> — the 4 AI-LLM axes cost / latency / token / accuracy are added on top of the shared 7). MCP-specific fidelity (protocol version negotiation, <code>tools/list</code> shape, JSON-RPC error code parity) rolls up into the shared <code>fidelity.ratio</code> axis via the trace-diffing harness.</p>`,22)])])}const u=t(i,[["render",n]]);export{k as __pageData,u as default};
