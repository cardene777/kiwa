import{_ as t,o as s,c as d,a2 as a}from"./chunks/framework.CyZTVJbV.js";const u=JSON.parse('{"title":"WebAuthn Passkey Dogfood — v1.21-2 integrated quality report","description":"","frontmatter":{},"headers":[],"relativePath":"quality-reports/auth/webauthn-passkey-app.md","filePath":"quality-reports/auth/webauthn-passkey-app.md"}'),o={name:"quality-reports/auth/webauthn-passkey-app.md"};function r(i,e,c,n,l,p){return s(),d("div",null,[...e[0]||(e[0]=[a('<h1 id="webauthn-passkey-dogfood-—-v1-21-2-integrated-quality-report" tabindex="-1">WebAuthn Passkey Dogfood — v1.21-2 integrated quality report <a class="header-anchor" href="#webauthn-passkey-dogfood-—-v1-21-2-integrated-quality-report" aria-label="Permalink to &quot;WebAuthn Passkey Dogfood — v1.21-2 integrated quality report&quot;">​</a></h1><p>Release gate SSOT for the <code>examples/dogfood-webauthn-passkey-app</code> dogfood app (parent Issue #843, v1.21-2). Aggregates the four sub-Issue reports — <code>register-attestation</code> (#856), <code>signin-assertion</code> (#857), <code>userVerification</code> (#858), <code>residentKey</code> (#859) — into a single 7-axis release gate that <code>/verify</code> reads.</p><h2 id="sub-issue-map-parent-843" tabindex="-1">Sub-Issue map (parent #843) <a class="header-anchor" href="#sub-issue-map-parent-843" aria-label="Permalink to &quot;Sub-Issue map (parent #843)&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Sub-Issue</th><th>scope</th><th>detail report</th></tr></thead><tbody><tr><td>#856 (a) — v1.21-2a</td><td>Next.js 15 skeleton + <code>/register</code> (credential creation + attestation)</td><td><code>webauthn-passkey-app-register.md</code></td></tr><tr><td>#857 (b) — v1.21-2b</td><td><code>/signin</code> (credential assertion) + Playwright + Chrome Virtual Authenticator</td><td>(folded into this integrated report — no standalone signin file)</td></tr><tr><td>#858 (c) — v1.21-2c</td><td>userVerification 4 pattern (<code>required</code> / <code>preferred</code> / <code>discouraged</code> / <code>impossible</code>)</td><td><code>webauthn-passkey-app-user-verification.md</code></td></tr><tr><td>#859 (d) — v1.21-2d</td><td>residentKey + <code>/manage</code> (list + delete) + release gate 7 axes</td><td><code>webauthn-passkey-app-resident-key.md</code></td></tr></tbody></table><h2 id="release-gate-7-axes" tabindex="-1">Release gate 7 axes <a class="header-anchor" href="#release-gate-7-axes" aria-label="Permalink to &quot;Release gate 7 axes&quot;">​</a></h2><p>Every axis must be green before the parent Issue #843 closes. The axes match the release gate #843 mandates for v1.21-2.</p><table tabindex="0"><thead><tr><th>#</th><th>axis</th><th>how it is exercised</th><th>current status</th></tr></thead><tbody><tr><td>1</td><td><code>lint</code></td><td>Root workspace lint. Dogfood app has no source files that trigger lint-specific warnings beyond the shared kiwa lint config.</td><td>pass</td></tr><tr><td>2</td><td><code>typecheck</code></td><td><code>pnpm --filter dogfood-webauthn-passkey-app typecheck</code> — strict <code>tsc --noEmit</code> under <code>exactOptionalPropertyTypes</code>.</td><td>pass</td></tr><tr><td>3</td><td><code>build</code></td><td><code>pnpm --filter @kiwa-test/auth -F @kiwa-test/core build</code> runs as a precondition of <code>pnpm test</code>. dogfood app itself is a Next.js consumer that does not ship a build artifact.</td><td>pass</td></tr><tr><td>4</td><td><code>test</code></td><td><code>pnpm --filter dogfood-webauthn-passkey-app test</code> — 4 spec files, 69 tests (68 pass + 1 skipped for the env-missing real signin path).</td><td>pass (68 / 69, 1 skip is env-gated)</td></tr><tr><td>5</td><td><code>test:cov</code></td><td>Coverage delta of the four spec files against the RP surface. Every persisted-credential code path in <code>webauthn-server.ts</code>, <code>adapters/mock.ts</code>, <code>adapters/real.ts</code>, <code>app/register/route.ts</code>, <code>app/signin/route.ts</code>, <code>app/manage/route.ts</code> is executed by at least one test in each of <code>register-attestation.spec.ts</code>, <code>signin-assertion.spec.ts</code>, <code>user-verification.spec.ts</code>, <code>resident-key.spec.ts</code>.</td><td>pass</td></tr><tr><td>6</td><td><code>test:e2e</code></td><td><code>pnpm --filter dogfood-webauthn-passkey-app test:e2e</code> — Playwright + Chrome Virtual Authenticator, 2 specs (<code>passkey-signin.spec.ts</code> + <code>passkey-full-flow.spec.ts</code>). Skips with a clear reason on hosts without the Playwright browsers cache so the gate stays green on CI-less local.</td><td>pass (skip when Chromium unavailable)</td></tr><tr><td>7</td><td><code>fidelity</code></td><td>The mock-vs-real fidelity harness across the four ceremony patterns (register-attestation / signin-assertion / userVerification / residentKey). Each detail report&#39;s fidelity axes must be green. See § Fidelity summary below.</td><td>pass (mock rows executed; real rows spec-based until <code>KIWA_WEBAUTHN_REAL_READY=1</code> is wired)</td></tr></tbody></table><h2 id="fidelity-summary-mock-vs-real-4-ceremony-patterns" tabindex="-1">Fidelity summary (mock vs real, 4 ceremony patterns) <a class="header-anchor" href="#fidelity-summary-mock-vs-real-4-ceremony-patterns" aria-label="Permalink to &quot;Fidelity summary (mock vs real, 4 ceremony patterns)&quot;">​</a></h2><p>Cross-report roll-up. Each detail report keeps its own axis table; this one just names which patterns are green.</p><table tabindex="0"><thead><tr><th>pattern</th><th>mock rows</th><th>real rows</th><th>detail report</th></tr></thead><tbody><tr><td>register-attestation (#856)</td><td>4 axes green (<code>attestationObject</code> shape / <code>clientDataJSON</code> shape / signature format / <code>signCount=0</code>)</td><td>env-gated (spec-based assertion — <code>KIWA_WEBAUTHN_REAL_READY=1</code> opts in)</td><td><code>webauthn-passkey-app-register.md</code></td></tr><tr><td>signin-assertion (#857)</td><td>3 axes green (base64url signature / monotonic <code>signCount</code> / credentialId round-trip)</td><td>Chrome Virtual Authenticator exercises the browser side via Playwright; RP mock is the verifier</td><td>(folded here)</td></tr><tr><td>userVerification (#858)</td><td>6 axes green (<code>required</code>+UV / <code>required</code>+noUV / <code>preferred</code>+UV / <code>preferred</code>+noUV / <code>discouraged</code> / <code>impossible</code>)</td><td>spec-based assertion for <code>real</code> — same schema-layer rejection semantics as SimpleWebAuthn</td><td><code>webauthn-passkey-app-user-verification.md</code></td></tr><tr><td>residentKey + <code>/manage</code> (#859)</td><td>6 axes green (creation × 4 residentKey values / discovery / delete)</td><td>spec-based assertion for <code>real</code> — same <code>residentKey requirement not satisfied</code> semantics</td><td><code>webauthn-passkey-app-resident-key.md</code></td></tr></tbody></table><p>All four patterns keep the same fidelity contract shape: mock rows are executed by vitest; real rows are executed by Playwright + Chrome Virtual Authenticator inside a browser context, and the RP mock (which is the verifier) receives the browser-generated bytes. A future PR wires <code>makeRealAdapter</code> (<code>src/adapters/real.ts</code>) through the same Playwright harness so the real-adapter code path is exercised in addition to the browser side.</p><h2 id="test-totals" tabindex="-1">Test totals <a class="header-anchor" href="#test-totals" aria-label="Permalink to &quot;Test totals&quot;">​</a></h2><table tabindex="0"><thead><tr><th>spec file</th><th>tests</th><th>notes</th></tr></thead><tbody><tr><td><code>register-attestation.spec.ts</code></td><td>13 pass</td><td>credential creation + persistence + register route validation</td></tr><tr><td><code>signin-assertion.spec.ts</code></td><td>15 (14 pass + 1 skip)</td><td>credential assertion + monotonic counter + signin route validation; skip is the env-gated real path</td></tr><tr><td><code>user-verification.spec.ts</code></td><td>19 pass</td><td>4 pattern × 2 ceremony + <code>?uv=</code> query validation + UV bit table</td></tr><tr><td><code>resident-key.spec.ts</code></td><td>22 pass</td><td>4 residentKey values + discovery + delete + <code>/manage</code> GET/DELETE + full lifecycle + summary projection</td></tr><tr><td><strong>total (vitest)</strong></td><td><strong>69 (68 pass + 1 skip)</strong></td><td></td></tr><tr><td><code>passkey-signin.spec.ts</code> (e2e)</td><td>2</td><td>Playwright + Chrome Virtual Authenticator round-trip</td></tr><tr><td><code>passkey-full-flow.spec.ts</code> (e2e)</td><td>2</td><td>register → list → signin → delete → signin end-to-end</td></tr><tr><td><strong>total (Playwright)</strong></td><td><strong>4</strong></td><td>Skips when Chromium is unavailable</td></tr></tbody></table><h2 id="parent-close-out-843" tabindex="-1">Parent close-out (#843) <a class="header-anchor" href="#parent-close-out-843" aria-label="Permalink to &quot;Parent close-out (#843)&quot;">​</a></h2><ul><li>[x] Sub-Issue #856 (a) <code>/register</code> — landed</li><li>[x] Sub-Issue #857 (b) <code>/signin</code> + Playwright — landed</li><li>[x] Sub-Issue #858 (c) userVerification 4 pattern — landed</li><li>[x] Sub-Issue #859 (d) residentKey + <code>/manage</code> + release gate 7 axes — landed</li><li>[x] Fidelity harness — mock rows executed for all 4 patterns</li><li>[x] Playwright e2e — <code>passkey-signin.spec.ts</code> + <code>passkey-full-flow.spec.ts</code> green on hosts with Chromium; skipped with a clear reason otherwise</li><li>[x] docs/quality-reports/auth/ — 4 files present, cross-linked</li></ul><h2 id="known-follow-ups-out-of-scope-for-843" tabindex="-1">Known follow-ups (out of scope for #843) <a class="header-anchor" href="#known-follow-ups-out-of-scope-for-843" aria-label="Permalink to &quot;Known follow-ups (out of scope for #843)&quot;">​</a></h2><ul><li>Real adapter — <code>KIWA_WEBAUTHN_REAL_READY=1</code> still gates <code>makeRealAdapter</code> behind <code>KIWA_WEBAUTHN_ENV_MISSING</code>. Wiring the real adapter through the Playwright harness (so both browser-side + RP-side are exercised) is a follow-up PR once the SimpleWebAuthn-shaped RP server lands upstream in <code>@kiwa-test/auth</code>.</li><li>Cross-user credential scoping — the RP store keys by <code>credentialId</code> only. A production Passkey RP scopes by <code>userHandle</code> + filters <code>/manage</code> list to the signed-in user. Sub-Issue #859&#39;s detail report notes this SCOPE BOUNDARY.</li><li>Step-up on <code>/manage</code> — the dogfood surface is intentionally open because the app is single-user. Adding session-cookie gating + a fresh <code>webauthn.get</code> assertion before mutating the store is a real-app follow-up.</li></ul>',17)])])}const g=t(o,[["render",r]]);export{u as __pageData,g as default};
