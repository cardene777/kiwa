1/ kiwa v1.29 リリース — release script filter systematic root cause SSOT milestone。 4 milestone (v1.14 payment / v1.25 perf-harness / v1.27 quality-metrics / v1.28 realtime) で全く同じ「build filter or publish filter 半分から package が漏れる」 bug が繰返された retrospective を受け、 v1.29 は **4 layer 自動化** (release-smoke test axis + PostToolUse hook + SSOT docs + 新規 npm package) で release-gate final hole を閉じた。 runtime fixture 34 → 35 (`@kiwa-test/release-invariants` v0.1.0 新規)。 7 milestone 連続 snippet validation streak (v1.23-v1.29)。 3 sub-Issue merge。

2/ 4 回繰返された bug — `pnpm -F {name} build` (build filter) と `pnpm publish --filter {name}` (publish filter) は `scripts.release` 内の別 2 半分。 contributor が新 package を片方だけに追加。 release は green で通る — build は artefact 生成、 publish は「言われた package」 が成功したので exit 0。 registry に新 package が届かない。 v1.14 payment / v1.25 perf-harness / v1.27 quality-metrics / v1.28 realtime が独立に hit。

3/ v1.29-1 release-smoke test axis (Issue #986, PR #989) — `tests/release-smoke/tests/release-script-filter.test.ts` (167 lines) を dynamic package discovery + 40 per-package `it.each` assert で新規 land。 `packages/*/package.json` を scan して `@kiwa-test/*` scope + `private: false` で filter、 各 publishable package が **両半分** に含まれることを assert。 milestone finisher **前** の fail-fast。 同 PR で 6 未登録 package (agent / ai-llm / component / mcp / search / streaming) を filter に land。

4/ v1.29-2 PostToolUse hook + `/issue-plan` checklist SSOT (Issue #987) — `hooks/post-tool-use/release-script-filter-guard.sh` proactive prevention。 Write / Edit が `packages/*/package.json` を触った際に root package.json release script filter 存在を scan、 いずれか半分欠落を warn 出力 (hard-deny せず)。 `/issue-plan` skill body に新 package 追加 checklist SSOT を embed、 実装前の Issue body 段階で不変条件が明示される。

5/ v1.29-3 release-invariants v0.1 + docs + publish (Issue #988) — 新 npm package `@kiwa-test/release-invariants` を 3 pure invariant checker + 1-shot aggregator で land。 `checkReleaseScriptFilter` (両半分 per package)、 `checkProvenanceFlagAbsence` (v1.14 で `pnpm publish` から撤去した `--provenance` が creep back しない不変条件)、 `checkGateScriptPackageCoverage` (`test:mutation` が全 publishable package を cover)、 `buildReleaseInvariantsSummary` (1-shot summary + `ok: boolean`)。 8 behavior test PASS。

6/ なぜ独立 npm package? 3 invariant は pure function で kiwa 固有 dependency なし。 任意の pnpm monorepo が `buildReleaseInvariantsSummary({ releaseScript, mutationGateScript, publishable })` を test runner or plain node script から呼べる。 SSOT を npm package として ship することで、 downstream user が kiwa 内部 code を copy せずに pattern を採用できる。

7/ 7 milestone 連続 snippet validation streak — `packages/release-invariants/tests/docs-tutorial-v1.29.test.ts` (8 test) が `docs/tutorials/55-release-script-filter-ssot.md` の全 code snippet を実 `@kiwa-test/release-invariants` v0.1 API で走査。 streak 継続 = v1.23 payment / v1.24 edge / v1.25 perf-harness / v1.26 orm / v1.27 quality-metrics / v1.28 realtime / v1.29 release-invariants。 docs drift を構造的遮断。

8/ docs — tutorial 55 (`release-script-filter-ssot.md` mock adapter + file adapter + 4 RED/GREEN behavior test の 15 分 walkthrough) + concept doc (`release-invariants.md` = 3 invariant catalog + systematic root cause pattern + 4 回 rediscovery ledger + 7 milestone snippet validation streak) + migration guide (`v1.28-to-v1.29.md` additive-only、 breaking change 0)。 VitePress sidebar 追記 + `/docs-publish-kiwa` gh-pages 反映。

9/ 18 milestone 連続完遂 — v1.11 → v1.12 → v1.13 → v1.14 → v1.15 → v1.16 → v1.17 → v1.18 → v1.19 → v1.20 → v1.21 → v1.22 → v1.23 → v1.24 → v1.25 → v1.26 → v1.27 → v1.28 → v1.29。 v1.11 以降全 milestone sub-Issue 完遂維持。 v1.29 は最軽量 sprint (3 sub-Issue、 4 layer 自動化、 1 新 npm package)。 Roadmap ... https://github.com/cardene777/kiwa#roadmap
