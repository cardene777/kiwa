# kiwa v1.63 x-thread (English)

## Tweet 1

kiwa v1.63 is out — Desktop deepening VII. **@kiwa-test/desktop v0.8** adds native binding availability probe + skip path, which/where CLI check + platform gate + 12 axis skip strategy + fidelity harness probe integration. Inherits v1.55-v1.62 4-PR rhythm (**10 milestones = 40 PRs same rhythm**), **systematic pattern 38th application**, **depth-8 pattern new candidate reached**.

## Tweet 2

probe layer = ProbeInput + ProbeResult + PlatformGate + NodePlatform SSOT, probeCliAvailable (which/where + DI) + shouldSkipAxis + platformGate + computeSkipMatrix. fidelity harness extended = runFidelityCheckWithProbe + SkippedPair integration, skipped pairs excluded from diffs, shape contract preserving absolutely maintained.

## Tweet 3

dogfood-desktop-probe-app new, 4-pattern workflow, 10 tests all pass. kiwa package count 47. **41-milestone consecutive snippet-validation streak** (v1.23-v1.63) achieved.

## Tweet 4

`pnpm add -D @kiwa-test/desktop@^0.8`. Migration: https://cardene777.github.io/kiwa/migrations/v1.62-to-v1.63

v1.64+ will implement real native binding invocation. Backward compat absolutely preserved — v0.1-v0.7 fully retained.

4 subs completed (v1.63-1 probe + fidelity integration / v1.63-2 dogfood new / v1.63-3 docs 41 streak / v1.63-4 publish).

#kiwa #desktop #probe #testing #vitest
