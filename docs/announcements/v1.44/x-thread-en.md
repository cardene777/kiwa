# kiwa v1.44 x-thread (English)

## Tweet 1 — hook

kiwa v1.44 released — Auth Passwordless UX III deepening lands.

@kiwa-test/auth v0.5.0 → v0.6.0 minor bump. 3 platform (chromium + webkit + firefox) × advanced Passwordless UX production semantics 8 axis = 24 fidelity grid.

**Pair 1 (Auth) achieves 3-stage extension** (v1.21 v0.4 → v1.22 v0.5 → v1.44 v0.6). Second pair after Search (v1.14→v1.15→v1.36) to reach depth 3.

## Tweet 2 — 8 axis Passwordless UX advanced III semantics

Device-bound-passkey (sync fabric + credential migration + credProps.rk) / Conditional-ui (autofill + mediation "conditional" + fallback + timeout) / Step-up-mfa (NIST SP 800-63B AAL ladder + trust cache) / Risk-based-auth (score aggregation + policy chain) / Auth-continuity (seamless reauth + refresh rotation + revocation window) / Cross-device-flow (caBLE + QR + BLE + tunnel + assertion) / Session-hijack-detect (fingerprint drift + geo anomaly + concurrent + cascade) / Auth-telemetry (attempt log + success rate + latency + abuse detection).

## Tweet 3 — pair 1 depth-3 record

Auth pair 3-stage extension complete. 4-stage records (v1.40 AI/LLM + v1.41 Payment + v1.42 Observability) + 5-milestone new-base cadence (v1.37 Security → v1.43 Edge/Serverless) + pair-deepening in-between milestones = kiwa's 2-tempo rhythm consolidated.

## Tweet 4 — snippet streak + npm publish

**22-milestone snippet validation streak** (v1.23-v1.44) achieved.

`pnpm add -D @kiwa-test/auth` gets v0.6.0. No breaking changes. Migration: https://cardene777.github.io/kiwa/migrations/v1.43-to-v1.44

6 sub-milestone complete (v1.44-1 auth v0.6 + 8 axis / v1.44-2 passwordless-ux dogfood / v1.44-3 step-up-mfa dogfood / v1.44-4 risk-based dogfood / v1.44-5 docs + 22 streak / v1.44-6 publish).

#kiwa #auth #webauthn #passkey #passwordless #testing #vitest
