# Migration guide — v2.16 → v2.17

`/spec-kit` skill 追加 = kiwa plugin marketplace 経由で spec-driven development skill 配布開始。 v2.16 で publish した `@kiwa-lab/spec-kit` npm package (programmable API) と 対で、 dialog flow による対話的仕様生成経路が 完成。 配布経路 pivot = skill は npm でなく kiwa GitHub repo 内 (`.claude/skills/spec-kit/`) が 自然。

```bash
# Claude Code plugin marketplace 経由で kiwa plugin install 済 user は
# /spec-kit --feature {name} で dialog 起動可能。
# 単発 install する場合:
pnpm add -D @kiwa-lab/spec-kit @kiwa-lab/lean
```

systematic pattern 59 度目、 62 milestone streak、 4 PR rhythm 16 milestone 目継続、 43 package 維持 (skill 追加、 npm package 変更なし)。
