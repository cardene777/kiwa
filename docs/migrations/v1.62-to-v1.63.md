# Migration guide — v1.62 → v1.63

v1.63 is **additive** — existing APIs stay。 v1.63 adds **Desktop 深化 VII (v0.8 native binding availability probe + skip 経路)** 単軸 milestone、 v1.55-v1.62 4 PR rhythm 継承 (v1.63 で **10 milestone 連続 = 40 PR 連続同 rhythm**)、 **systematic pattern 38 度目適用**、 **depth-8 pattern 新設 candidate 到達**、 41 milestone snippet streak 到達。

## 4 things v1.63 adds

- `@kiwa-test/desktop` v0.8 minor bump = probe.ts 新設 (ProbeInput + ProbeResult + PlatformGate + NodePlatform SSOT + probeCliAvailable + shouldSkipAxis + platformGate + computeSkipMatrix)、 fidelity-harness に runFidelityCheckWithProbe + SkippedPair 統合
- **縦深化 pair 第 14 の第 8 段 (depth-8 pattern 新設 candidate)**、 v1.62 depth-7 到達直後の v1.63 で depth-8 拡張
- dogfood-desktop-probe-app 新規 + tutorial 123 + concept doc `desktop-probe.md`
- **41 milestone 連続 snippet validation streak** (v1.23-v1.63) 達成、 systematic root cause pattern SSOT **38 度目適用**

## What breaks (nothing)

- 既存 46 package + 47 package 全部 API 変更 0
- v0.1-v0.7 の 12 axis / 48 method + adapter + fidelity + spawn stub + 実 spawn + real behavior runner 完全保持
- shape 契約 preserving 絶対維持 = 既存 fidelity-harness test 全継続 PASS

## Upgrade steps

```bash
pnpm add -D @kiwa-test/desktop@^0.8
```

### Pattern 1 — CLI probe

```ts
import { probeCliAvailable } from '@kiwa-test/desktop';

const result = await probeCliAvailable({ command: 'ffmpeg' });
// result.available === true if ffmpeg is installed
```

### Pattern 2 — Skip decision

```ts
import { shouldSkipAxis } from '@kiwa-test/desktop';

const decision = shouldSkipAxis('clipboard', 'linux');
// decision.skip === false on linux, true on other platforms
```

### Pattern 3 — Probe-aware fidelity check

```ts
import { runFidelityCheckWithProbe } from '@kiwa-test/desktop';

const { diffs, skippedPairs } = await runFidelityCheckWithProbe({});
console.log(`checked: ${diffs.length}, skipped: ${skippedPairs.length}`);
```

## 縦深化 pair pattern の深さ拡張

v1.63 で pair 14 (Desktop) の depth-7 (v0.7) → depth-8 (v0.8 probe layer) 拡張、 kiwa milestone 史上 depth-8 record 新設 candidate。

## 次に来るもの (v1.64+)

- 実 native binding 呼出 (probe availability 判定で 実 CLI 存在時のみ 呼出)
- 他 pair depth-5/6 拡張
- v2.0 milestone coverage 100% goal
