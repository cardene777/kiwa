# Migration guide — v1.55 → v1.56

v1.56 is **additive** — existing APIs stay。 v1.56 adds **Desktop 新規 base pair 第 14 導入** (42 package 到達、 v2.0 milestone desktop adapter goal 達成) 単軸 milestone。

## 4 things v1.56 adds

- `@kiwa-lab/desktop` v0.1 新規 package (**42 package 到達**)、 3 axis (Electron + Tauri + Webview) target-neutral state machine + 3 target (macos + windows + linux) × 3 axis = 9 row fidelity grid
- **縦深化 pair 第 14 新規 base pair 導入**、 v1.50 Mobile と対で v2.0 milestone 「desktop (Electron / Tauri) + mobile (React Native / Expo) adapters」 goal 達成
- dogfood 1 app (dogfood-desktop-electron-app) + 1 tutorial 116 + concept doc `desktop-testing-baseline.md`
- **34 milestone 連続 snippet validation streak** (v1.23-v1.56) 達成、 kiwa 史上最長記録更新継続、 systematic root cause pattern SSOT **31 度目適用**

## What breaks (nothing)

- 既存 41 package 全部 API 変更 0
- 新 base pair は完全 additive、 既存 code は無修正で v1.55 → v1.56 に upgrade 可能

## Upgrade steps

```bash
pnpm add -D @kiwa-lab/desktop@^0.1
```

### Pattern 1 — Electron lifecycle

```ts
import {
  createBrowserWindow,
  dispatchIpcMessage,
  quitElectronApp,
  startElectronApp,
} from '@kiwa-lab/desktop';

const s = startElectronApp({ target: 'macos', appId: 'com.example.app' });
createBrowserWindow(s, 'main');
dispatchIpcMessage(s, { channel: 'ping', payload: 'hi' });
quitElectronApp(s);
```

### Pattern 2 — Tauri invoke + event

```ts
import { startTauriApp, registerTauriCommand, invokeTauriCommand, emitTauriEvent } from '@kiwa-lab/desktop';

const s = startTauriApp({ target: 'windows', appName: 'myapp' });
registerTauriCommand(s, 'get_user');
invokeTauriCommand(s, { commandName: 'get_user', payload: '{"id":1}' });
emitTauriEvent(s, { eventName: 'user_updated', payload: '{"id":1}' });
```

### Pattern 3 — Webview preload + bridge

```ts
import { loadPreloadScript, bindContextBridge, postWebviewMessage, assertContextIsolation } from '@kiwa-lab/desktop';

const s = loadPreloadScript({ target: 'linux', webviewId: 'main' });
bindContextBridge(s, 'appAPI');
postWebviewMessage(s, { channel: 'ping', payload: 'hi' });
assertContextIsolation(s, true);
```

## 縦深化 pair pattern の広さ拡張

v1.56 で pair 13 → 14 拡張、 kiwa の縦深化戦略 SSOT に Desktop 領域を追加。 v1.50 Mobile new-base + v1.56 Desktop new-base で **v2.0 milestone 「desktop + mobile adapters」 goal 達成**、 kiwa の adapter 拡張戦略 SSOT 完成。

## 次に来るもの (v1.57+)

- Desktop 深化 (v0.1 → v0.2 advanced axis: Auto-updater / File system permissions / Notification / Menu bar 等)
- 他 pair 5 段拡張 (v1.40 AI/LLM / v1.41 Payment / v1.42 Observability の depth-4 record からの depth-5 拡張、 depth-5 pattern 2 例目 candidate)
- v2.0 milestone core deliverables 残り (multi-version Vitest matrix CI = 廃止済み CI 全面禁止 rule により削除、 coverage 100% milestone)
