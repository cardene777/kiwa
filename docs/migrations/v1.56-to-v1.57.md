# Migration guide — v1.56 → v1.57

v1.57 is **additive** — existing APIs stay。 v1.57 adds **Desktop 深化 I (v0.2 advanced 5 axis 追加)** 単軸 milestone、 v1.55/v1.56 4 PR rhythm 継承、 **systematic pattern 32 度目適用**。

## 4 things v1.57 adds

- `@kiwa/desktop` v0.2 minor bump = advanced 5 axis (Auto-updater + File-system permissions + Notification + Menu-bar + Tray-icon) 追加、 v0.1 3 axis に加えて 8 axis × 3 target = **24 spec fidelity grid** 構築
- **縦深化 pair 第 14 の第 2 段**、 v1.56 で新設した Desktop base pair の depth-2 拡張 (v0.1 → v0.2 advanced axis)、 v1.50-v1.55 Mobile 深化 rhythm と同軸
- dogfood-desktop-electron-app に v0.2 5 axis workflow test 追加 (11 test → 21 test) + 1 tutorial 117 + concept doc `desktop-advanced-axis.md`
- **35 milestone 連続 snippet validation streak** (v1.23-v1.57) 達成、 kiwa 史上最長記録更新継続、 systematic root cause pattern SSOT **32 度目適用** (v1.56 の 31 度目 = desktop v0.1 3 axis uniform を継承)

## What breaks (nothing)

- 既存 42 package 全部 API 変更 0
- `@kiwa/desktop` v0.1 3 axis (electron/tauri/webview) の 12 method / 12 event / 36 mapping は完全保持
- v0.2 で追加された 5 axis は additive、 既存 code は無修正で v0.1 → v0.2 に upgrade 可能

## Upgrade steps

```bash
pnpm add -D @kiwa/desktop@^0.2
```

### Pattern 1 — Auto-updater

```ts
import {
  applyDownloadedUpdate,
  recordUpdateDownloaded,
  scheduleRelaunch,
  startAutoUpdaterCheck,
} from '@kiwa/desktop';

const s = startAutoUpdaterCheck({ target: 'macos', channel: 'stable' });
recordUpdateDownloaded(s, { version: '1.2.3', bytes: 42_000_000 });
applyDownloadedUpdate(s);
scheduleRelaunch(s, 5_000);
```

### Pattern 2 — File-system permissions

```ts
import {
  grantFsPermission,
  logFsPermissionAudit,
  requestFsPermission,
  revokeFsPermission,
} from '@kiwa/desktop';

const s = requestFsPermission({ target: 'macos', path: '/Users/alice/Documents', scope: 'read-write' });
grantFsPermission(s, 'read');
grantFsPermission(s, 'write');
revokeFsPermission(s, 'read');
logFsPermissionAudit(s, 'user-revoke');
```

### Pattern 3 — Notification

```ts
import {
  dismissNotification,
  displayNotification,
  invokeNotificationAction,
  scheduleNotification,
} from '@kiwa/desktop';

const s = scheduleNotification({
  target: 'windows',
  notificationId: 'update-1',
  title: 'Update available',
  scheduledAtMs: 1_000,
});
displayNotification(s, 1_500);
invokeNotificationAction(s, 'view-details');
dismissNotification(s);
```

### Pattern 4 — Menu-bar

```ts
import { appendMenuBarItem, buildMenuBar, clickMenuBarItem, destroyMenuBar } from '@kiwa/desktop';

const s = buildMenuBar({ target: 'linux', menuId: 'main-menu' });
appendMenuBarItem(s, { id: 'file', label: 'File', accelerator: 'Cmd+F' });
clickMenuBarItem(s, 'file');
destroyMenuBar(s);
```

### Pattern 5 — Tray-icon

```ts
import { clickTrayIcon, createTrayIcon, removeTrayIcon, updateTrayTooltip } from '@kiwa/desktop';

const s = createTrayIcon({ target: 'macos', trayId: 'tray-1', iconPath: '/app/icon.png' });
updateTrayTooltip(s, 'Sync in progress');
clickTrayIcon(s);
removeTrayIcon(s);
```

## 縦深化 pair pattern の深さ拡張

v1.57 で pair 14 (Desktop) の depth-1 (v0.1) → depth-2 (v0.2 advanced axis) 拡張。 v1.50-v1.55 Mobile 深化で確立した depth-5 pattern (v0.1 semantics → v0.2 advanced → v0.3 real driver → v0.4 adapter → v0.5 stub → v0.6 real spawn) を Desktop pair に適用開始、 pair 14 の depth-2 到達。

## 次に来るもの (v1.58+)

- Desktop 深化 II (v0.3 real driver: @electron/test-utils + tauri-driver + WebDriver)
- Desktop 深化 III (v0.4 adapter layer: KIWA_DESKTOP_MODE env-gate + mock/real switching)
- 他 pair 深化 record (v1.40 AI/LLM / v1.41 Payment 等の depth-5 拡張、 depth-5 pattern 2 例目 candidate)
- v2.0 milestone coverage 100% goal
