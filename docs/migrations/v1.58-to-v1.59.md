# Migration guide — v1.58 → v1.59

v1.59 is **additive** — existing APIs stay。 v1.59 adds **Desktop 深化 III (v0.4 adapter layer + fidelity harness)** 単軸 milestone、 v1.55-v1.58 4 PR rhythm 継承 (v1.59 で **6 milestone 連続**)、 **systematic pattern 34 度目適用**、 **Mobile v1.53 rhythm 再現**、 **depth-4 record 5 例目到達**。

## 4 things v1.59 adds

- `@kiwa/desktop` v0.4 minor bump = adapter interface (AdapterInvocation + AdapterMode + AdapterResult + DesktopAdapter) + MOCK_ADAPTERS + REAL_ADAPTERS + makeMockAdapter / makeRealAdapter + runFidelityCheck + summarizeFidelity harness を追加、 v0.1 3 axis + v0.2 5 axis + v0.3 4 axis = **12 axis × mock/real = 24 adapter pair**、 3 target × 12 axis × 2 mode = **72 combination**、 3 target × 12 axis = **36 fidelity pair** (全 matched、 shape 契約 preserving)
- **縦深化 pair 第 14 の第 4 段**、 v1.56-v1.58 で構築した Desktop 12 axis semantics に adapter layer を pair 化、 Mobile v1.50-v1.53 (base → advanced II → advanced III → adapter) の 4 milestone rhythm を Desktop pair (v1.56-v1.59) で完全再現、 **depth-4 record 到達 = 5 例目** (Mobile depth-4 4 例目 + Desktop v1.59 5 例目)
- dogfood-desktop-adapter-app 新規 (72 combination workflow + 36 fidelity pair、 10 test) + 1 tutorial 119 + concept doc `desktop-adapter-layer.md`
- **37 milestone 連続 snippet validation streak** (v1.23-v1.59) 達成、 kiwa 史上最長記録更新継続、 systematic root cause pattern SSOT **34 度目適用** (v1.58 の 33 度目 = desktop v0.3 4 axis uniform を継承)

## What breaks (nothing)

- 既存 42 package + 43 package (dogfood-desktop-adapter-app 追加で 43) 全部 API 変更 0
- `@kiwa/desktop` v0.1 + v0.2 + v0.3 の 12 axis / 48 method / 48 event / 144 mapping は完全保持
- v0.4 で追加された adapter layer は additive、 既存 code は無修正で v0.3 → v0.4 に upgrade 可能

## Upgrade steps

```bash
pnpm add -D @kiwa/desktop@^0.4
```

### Pattern 1 — Adapter interface

```ts
import {
  MOCK_ADAPTERS,
  REAL_ADAPTERS,
  type AdapterInvocation,
  type AdapterResult,
} from '@kiwa/desktop';

const inv: AdapterInvocation = { scanId: 's', target: 'macos', mode: 'mock' };
const result: AdapterResult = await MOCK_ADAPTERS['clipboard'].scan(inv);
console.log(result.completed); // true
console.log(result.neutralEvents); // clipboard.written, clipboard.read, ...
```

### Pattern 2 — Fidelity harness

```ts
import { runFidelityCheck, summarizeFidelity } from '@kiwa/desktop';

const diffs = await runFidelityCheck({}); // 36 pair
const summary = summarizeFidelity(diffs);
console.log(summary.matchedRatio); // 1.0 (shape 契約 preserving 段階)
```

### Pattern 3 — Custom axes / targets subset

```ts
import { runFidelityCheck } from '@kiwa/desktop';

const diffs = await runFidelityCheck({
  axes: ['electron', 'clipboard', 'dark-mode'],
  targets: ['macos', 'linux'],
});
// 3 axis × 2 target = 6 pair
```

### Pattern 4 — Individual factory

```ts
import { makeMockAdapter, makeRealAdapter } from '@kiwa/desktop';

const mockElectron = makeMockAdapter('electron');
const realClipboard = makeRealAdapter('clipboard');
// 個別 adapter を factory 経由で生成
```

## 縦深化 pair pattern の深さ拡張

v1.59 で pair 14 (Desktop) の depth-3 (v0.3) → depth-4 (v0.4 adapter layer) 拡張。 Mobile v1.50-v1.53 (base → advanced II → advanced III → adapter) の 4 milestone rhythm を Desktop pair (v1.56-v1.59) で完全再現、 **depth-4 record 到達 = 5 例目**、 depth-4 pattern の 5 例安定化を実証 (Mobile depth-4 4 例 + Desktop depth-4 = 5 例)。

## 次に来るもの (v1.60+)

- Desktop 深化 IV (v0.5 spawn stub: Mobile v0.5 pattern 転用、 desktop 側 CLI-backed axis 抽出、 depth-5 pattern **2 例目 candidate**)
- Desktop 深化 V (v0.6 real spawn: Mobile v0.6 pattern 転用、 spawn-executor + env sanitize + timeout、 depth-6 pattern 新設 candidate)
- v0.4 real adapter を実 OS API 呼出 (electron-updater / SCStream / NSPasteboard 等) に置換 (v1.60+)
- v2.0 milestone coverage 100% goal
