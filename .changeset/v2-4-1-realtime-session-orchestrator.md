---
"@kiwa/realtime": minor
---

v2.4-1 @kiwa/realtime v2.1 = session-orchestrator 新設 (depth-5 pattern **5 例目発生** = systematic law 昇格 candidate)。

v1.13 realtime v0.1 + v1.28 realtime v0.2 の 5 axis に 加えて session-orchestrator (presence + broadcast + subscription + heartbeat + reconnect の 継続合成 layer) を追加。 5 state (connecting / subscribed / reconnecting / degraded / closed) + 8 event + 40 セル 遷移表 + heartbeat 3 回連続失敗 で degraded 降格 の 動的 QoS + soft-reject pattern (payment v2.1 と同型)。

Realtime pair v0.1 → v0.2 → v2.1 = 5 段深化到達 = **depth-5 pattern 5 例目発生** (Mobile + Desktop + quality-metrics + Payment + Realtime = 5 pair)、 「dominant pattern」 (4 例目) → **systematic law** 昇格 candidate 到達、 systematic pattern 47 度目適用 (continuous state machine variant Realtime 転用)。

16 behavior test (T-R-SO-001 〜 T-R-SO-016) で 全経路 cover、 shape 契約 preserving 絶対維持。
