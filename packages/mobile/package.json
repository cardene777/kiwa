{
  "name": "@kiwa-test/mobile",
  "version": "0.4.0",
  "description": "Mobile test harness for kiwa v0.4 — 11 axis (React Native + Expo + Metro + Navigation + Reanimated + AsyncStorage + SecureStorage + Fabric + TurboModules + Codegen + New Architecture) × mock/real adapter pair + fidelity harness (trace diff 一致検証)。 v0.1 (v1.50) 3 axis base + v0.2 (v1.51) advanced II 4 new axis + v0.3 (v1.52) advanced III 4 new axis に加えて v0.4 (v1.53) real driver adapter (11 mock + 11 real + fidelity harness) を追加、 backward compat 絶対維持で v0.1-v0.3 API 変更 0。 **pair 第 13 の 4 段目 (Phase 4、 4 段拡張達成 4 例目 depth-4 record)**。",
  "license": "MIT",
  "author": {
    "name": "cardene",
    "url": "https://github.com/cardene777"
  },
  "keywords": [
    "kiwa",
    "mobile",
    "react-native",
    "expo",
    "metro",
    "testing",
    "vitest"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cardene777/kiwa.git",
    "directory": "packages/mobile"
  },
  "homepage": "https://github.com/cardene777/kiwa#readme",
  "bugs": {
    "url": "https://github.com/cardene777/kiwa/issues"
  },
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      },
      "require": {
        "types": "./dist/index.d.cts",
        "default": "./dist/index.cjs"
      }
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsup",
    "test": "node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kiwa-test/core": "workspace:*"
  },
  "peerDependencies": {
    "vitest": "^2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsup": "^8.3.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
