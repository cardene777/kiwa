{
  "name": "@kiwa-test/mobile",
  "version": "0.3.0",
  "description": "Mobile test harness for kiwa v0.3 — 11 axis (React Native + Expo + Metro + Navigation + Reanimated + AsyncStorage + SecureStorage + Fabric + TurboModules + Codegen + New Architecture) unified as target-neutral state machine + real driver env-gate opt-in。 v0.1 (v1.50) 3 axis base + v0.2 (v1.51) advanced II 4 new axis に加えて v0.3 (v1.52) advanced III 4 new axis (React Native 0.76+ New Architecture: fabric concurrent renderer + turbo-modules typed native + codegen typed bridge + new-architecture async init) を追加、 backward compat 絶対維持で v0.1 + v0.2 API 変更 0。 **pair 第 13 の 3 段目 (Phase 3、 3 段拡張達成 5 例目 pair 深度 3 段記録)**。",
  "license": "MIT",
  "author": {
    "name": "cardene",
    "url": "https://github.com/cardene777"
  },
  "keywords": [
    "kiwa",
    "mobile",
    "react-native",
    "expo",
    "metro",
    "testing",
    "vitest"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cardene777/kiwa.git",
    "directory": "packages/mobile"
  },
  "homepage": "https://github.com/cardene777/kiwa#readme",
  "bugs": {
    "url": "https://github.com/cardene777/kiwa/issues"
  },
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      },
      "require": {
        "types": "./dist/index.d.cts",
        "default": "./dist/index.cjs"
      }
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsup",
    "test": "node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kiwa-test/core": "workspace:*"
  },
  "peerDependencies": {
    "vitest": "^2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsup": "^8.3.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
