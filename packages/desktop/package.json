{
  "name": "@kiwa-test/desktop",
  "version": "0.5.0",
  "description": "Desktop test harness for kiwa v0.5 — 12 axis semantics (v0.1 Electron + Tauri + Webview + v0.2 Auto-updater + FS permissions + Notification + Menu-bar + Tray-icon + v0.3 Screen recording + Global shortcut + Clipboard + Dark-mode) + v0.4 adapter interface + fidelity harness (24 adapter + 72 combination + 36 fidelity pair) + v0.5 spawn stub 契約層 (invokeDesktopCli + cliForAxis + buildSpawnInvocation + KIWA_DESKTOP_MODE env-gate + args 上限 32 + fail-closed、 12 axis から 8 CLI-backed axis 抽出 = electron-builder / electron-updater / ffmpeg / xclip / osascript / notify-send / defaults / reg) を pair layer 化。 Mobile v1.54 rhythm 再現、 v1.60 で kiwa 縦深化 pair 第 14 の第 5 段、 v1.55-v1.59 4 PR rhythm 継承 (7 milestone 連続)、 systematic pattern 35 度目適用 (uniform spawn stub)、 backward compat 絶対維持 (v0.1 + v0.2 + v0.3 + v0.4 API 変更 0)、 38 milestone snippet streak 到達、 depth-5 pattern 2 例目 candidate 到達 (Mobile depth-5 1 例目 + Desktop v1.60 2 例目 candidate、 v1.61+ v0.6 実 spawn で 2 例目確定予定)。",
  "license": "MIT",
  "author": {
    "name": "cardene",
    "url": "https://github.com/cardene777"
  },
  "keywords": [
    "kiwa",
    "desktop",
    "electron",
    "tauri",
    "webview",
    "testing",
    "vitest"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cardene777/kiwa.git",
    "directory": "packages/desktop"
  },
  "homepage": "https://github.com/cardene777/kiwa#readme",
  "bugs": {
    "url": "https://github.com/cardene777/kiwa/issues"
  },
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      },
      "require": {
        "types": "./dist/index.d.cts",
        "default": "./dist/index.cjs"
      }
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsup",
    "test": "node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kiwa-test/core": "workspace:*"
  },
  "peerDependencies": {
    "vitest": "^2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsup": "^8.3.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
