{
  "name": "@kiwa-test/desktop",
  "version": "0.9.0",
  "description": "Desktop test harness for kiwa v0.9 — 12 axis + adapter + fidelity + spawn stub + 実 spawn + real behavior runner + probe + skip + v0.9 実 native binding 呼出 (probeAndInvoke = probe + invoke 統合、 実 CLI 存在時のみ 実 spawn 呼出、 未 install 時は skip 経路で shape 契約 preserving) を pair layer 化。 native-invoke = probeAndInvoke + probeAndInvokeAll + InvokeStatus SSOT (invoked / cli-unavailable / axis-skipped / no-cli-mapping の 4 経路 return)、 shape 契約 preserving 絶対維持 = SpawnResult 構造無変更、 skip 時は spawnResult=null で明示。 v1.62 real behavior + v1.63 probe + v1.64 実 invoke の 3 milestone で構築した 3 layer separation の完全 pay off phase。 v1.64 で kiwa 縦深化 pair 第 14 の第 9 段 (depth-9 pattern 新設 candidate、 Desktop pair v0.1-v0.9)、 v1.55-v1.63 4 PR rhythm 継承 (11 milestone 連続 = 44 PR 連続同 rhythm)、 systematic pattern 39 度目適用 (uniform native invoke integration)、 backward compat 絶対維持 (v0.1-v0.8 API 変更 0)、 42 milestone snippet streak 到達。",
  "license": "MIT",
  "author": {
    "name": "cardene",
    "url": "https://github.com/cardene777"
  },
  "keywords": [
    "kiwa",
    "desktop",
    "electron",
    "tauri",
    "webview",
    "testing",
    "vitest"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cardene777/kiwa.git",
    "directory": "packages/desktop"
  },
  "homepage": "https://github.com/cardene777/kiwa#readme",
  "bugs": {
    "url": "https://github.com/cardene777/kiwa/issues"
  },
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      },
      "require": {
        "types": "./dist/index.d.cts",
        "default": "./dist/index.cjs"
      }
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsup",
    "test": "node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kiwa-test/core": "workspace:*"
  },
  "peerDependencies": {
    "vitest": "^2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsup": "^8.3.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
