{
  "name": "@kiwa-test/desktop",
  "version": "0.2.0",
  "description": "Desktop test harness for kiwa v0.2 — 8 axis (v0.1 Electron + Tauri + Webview に v0.2 で Auto-updater + File-system permissions + Notification + Menu-bar + Tray-icon の advanced 5 axis 追加) を target-neutral state machine で扱う。 3 target (macos + windows + linux) × 8 axis × 4 event = 96 dialect mapping、 fidelity grid 24 row 構築。 v1.57 で kiwa 縦深化 pair 第 14 の第 2 段、 v1.55/v1.56 4 PR rhythm 継承、 systematic pattern 32 度目適用 (uniform state machine + axis-neutral state)、 backward compat 絶対維持 (v0.1 API 変更 0)、 35 milestone snippet streak 到達。",
  "license": "MIT",
  "author": {
    "name": "cardene",
    "url": "https://github.com/cardene777"
  },
  "keywords": [
    "kiwa",
    "desktop",
    "electron",
    "tauri",
    "webview",
    "testing",
    "vitest"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cardene777/kiwa.git",
    "directory": "packages/desktop"
  },
  "homepage": "https://github.com/cardene777/kiwa#readme",
  "bugs": {
    "url": "https://github.com/cardene777/kiwa/issues"
  },
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      },
      "require": {
        "types": "./dist/index.d.cts",
        "default": "./dist/index.cjs"
      }
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsup",
    "test": "node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kiwa-test/core": "workspace:*"
  },
  "peerDependencies": {
    "vitest": "^2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsup": "^8.3.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
