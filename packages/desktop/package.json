{
  "name": "@kiwa-test/desktop",
  "version": "0.8.0",
  "description": "Desktop test harness for kiwa v0.8 — 12 axis + adapter + fidelity + spawn stub + 実 spawn + v0.7 real behavior runner + v0.8 native binding availability probe + skip 経路 (which/where CLI check + platform gate + 12 axis 別 skip strategy、 実 CLI 未 install 環境でも決定的 test 成立) を pair layer 化。 probe = probeCliAvailable + shouldSkipAxis + computeSkipMatrix + platformGate、 fidelity harness に runFidelityCheckWithProbe + SkippedPair 統合 (skip した pair は matched 判定から除外、 skippedPairs metadata で追跡)、 shape 契約 preserving 絶対維持 = neutralEvents + eventCount 一致、 v0.4 fidelity harness 設計思想の実運用継続。 v1.63 で kiwa 縦深化 pair 第 14 の第 8 段 (depth-8 pattern 新設 candidate、 Desktop pair v0.1-v0.8)、 v1.55-v1.62 4 PR rhythm 継承 (10 milestone 連続 = 40 PR 連続同 rhythm)、 systematic pattern 38 度目適用 (uniform probe layer)、 backward compat 絶対維持 (v0.1-v0.7 API 変更 0)、 41 milestone snippet streak 到達。",
  "license": "MIT",
  "author": {
    "name": "cardene",
    "url": "https://github.com/cardene777"
  },
  "keywords": [
    "kiwa",
    "desktop",
    "electron",
    "tauri",
    "webview",
    "testing",
    "vitest"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cardene777/kiwa.git",
    "directory": "packages/desktop"
  },
  "homepage": "https://github.com/cardene777/kiwa#readme",
  "bugs": {
    "url": "https://github.com/cardene777/kiwa/issues"
  },
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      },
      "require": {
        "types": "./dist/index.d.cts",
        "default": "./dist/index.cjs"
      }
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsup",
    "test": "node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kiwa-test/core": "workspace:*"
  },
  "peerDependencies": {
    "vitest": "^2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsup": "^8.3.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
