{
  "name": "@kiwa-test/desktop",
  "version": "0.6.0",
  "description": "Desktop test harness for kiwa v0.6 — 12 axis semantics + v0.4 adapter + fidelity harness + v0.5 spawn stub 契約層 (invokeDesktopCli + cliForAxis + buildSpawnInvocation) + v0.6 実 child_process.spawn 実行 (spawn-executor + per-command env allowlist 8 CLI × specific env + timeout 60s + buffer 上限 10MB + shell:false + detached:false + DI 経路 invokeDesktopCliWith + KIWA_DESKTOP_SPAWN=dry-run で v0.5 shape 契約復元) を pair layer 化。 shape 契約 preserving = SpawnResult 構造無変更、 stdout/stderr/exitCode/durationMs は 実 spawn からの実測値。 Mobile v1.55 rhythm 再現、 v1.61 で kiwa 縦深化 pair 第 14 の第 6 段、 v1.55-v1.60 4 PR rhythm 継承 (8 milestone 連続 = 32 PR 連続同 rhythm)、 systematic pattern 36 度目適用 (uniform spawn executor)、 backward compat 絶対維持 (v0.1 + v0.2 + v0.3 + v0.4 + v0.5 API 変更 0)、 39 milestone snippet streak 到達、 depth-5 pattern 2 例目確定 (Mobile depth-5 1 例目 + Desktop v1.60→v1.61 で 2 例目確定、 2 例安定化到達) + depth-6 pattern 新設 candidate (kiwa milestone 史上初 depth-6 record 到達 = pair 深度 6 段拡張 v0.1→v0.2→v0.3→v0.4→v0.5→v0.6)。",
  "license": "MIT",
  "author": {
    "name": "cardene",
    "url": "https://github.com/cardene777"
  },
  "keywords": [
    "kiwa",
    "desktop",
    "electron",
    "tauri",
    "webview",
    "testing",
    "vitest"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cardene777/kiwa.git",
    "directory": "packages/desktop"
  },
  "homepage": "https://github.com/cardene777/kiwa#readme",
  "bugs": {
    "url": "https://github.com/cardene777/kiwa/issues"
  },
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      },
      "require": {
        "types": "./dist/index.d.cts",
        "default": "./dist/index.cjs"
      }
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsup",
    "test": "node -e \"require('node:fs').rmSync('.vitest-dist',{recursive:true,force:true})\" && tsc -p tsconfig.vitest.json && vitest run .vitest-dist/tests --environment node",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kiwa-test/core": "workspace:*"
  },
  "peerDependencies": {
    "vitest": "^2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsup": "^8.3.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
